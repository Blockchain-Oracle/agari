import type { IndexQuery } from "./queries";

/** Lane 5b: `tape/markets`, `tape/fills`, `tape/actions` (proof-analytics.md §1). Placeholder from the S5 foundation: no paths yet. */
export function resolveTapeQuery(_path: readonly string[], _query: Record<string, string>): IndexQuery | null {
  return null;
}
