import type { IndexQuery } from "./queries";

/** Lane 5d: `proofs/:market` (proof-analytics.md §1). Placeholder from the S5 foundation: no paths yet. */
export function resolveProofQuery(_path: readonly string[], _query: Record<string, string>): IndexQuery | null {
  return null;
}
