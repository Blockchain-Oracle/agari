[package]
name = "agari-parlay"
version = "0.1.0"
description = "Masayume's MarketMakerVault on Solana: house-banked multi-leg tickets priced off the venue's own books"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib", "lib"]
name = "agari_parlay"

[features]
default = []
cpi = ["no-entrypoint"]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
idl-build = ["anchor-lang/idl-build", "anchor-spl/idl-build"]
anchor-debug = []
custom-heap = []
custom-panic = []

[dependencies]
anchor-lang = { workspace = true, features = ["event-cpi", "init-if-needed"] }
anchor-spl = { workspace = true }
bytemuck = { workspace = true }
agari-common = { path = "../../crates/agari-common" }
agari-events = { path = "../agari-events", features = ["cpi"] }

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(target_os, values("solana"))'] }

[dev-dependencies]
serde_json = "1"
