import { DeskRoot } from "~/features/desk/DeskRoot";

/** `/desk` (web/src/app/desk/page.tsx): your desk, or what a desk is and the shared one when you have none. */
export default function DeskRoute() {
  return <DeskRoot />;
}
