import { NotificationsScreen } from "~/features/alerts/NotificationsScreen";

/** `/notifications` — phone-only (S26.4): push settings, the Live Activity and the widget. */
export default function NotificationsRoute() {
  return <NotificationsScreen />;
}
