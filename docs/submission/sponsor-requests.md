# Sponsor requests

Logo use: PreStocks and Pyth both granted permission (2026-09-23); the marks live in `web/public/brand/sponsors/`
and `docs-site/public/brand/sponsors/`, taken from each sponsor's own site.

## Optional — to Pyth: pyth-indices access (the user decides whether to send)

We have wired Equity.Index.OPENAI/USD and Equity.Index.ANTHROPIC/USD valuation markets, plus a desk option that uses
the index as its price reference. Once our key is entitled to the pyth-indices group, the registration script accepts
the two valuation Series and they list at the next hourly probe, with no code change. Our trial key gets a 403 for that
group today. Is there a way to get it enabled for the hackathon?
