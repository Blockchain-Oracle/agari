# STATUS — updated 2026-09-22 ~19:10 UTC by Claude (**S19 baskets · S20 Pyth index · S21 the desk: approved 09-22, building; A1 landed on w1**)

## Session of 2026-09-22 16:50–19:10Z (research + plan + S19.1) — READ THIS FIRST

**The user approved a three-lane plan on 2026-09-22 (`~/.claude/plans/quizzical-booping-ocean.md`; the UX is §§4–5, the engineering §§7–8).** Stage files: `docs/plan/stage-19-baskets.md`, `stage-20-pyth-index.md`, `stage-21-desk.md`. Lanes run in worktrees `../agari-wt/s19` (`slice/S19-baskets`), `s20` (`slice/S20-pyth-index`), `s21` (`slice/S21-desk`), merged back into `integration/w1` at each lane's gate. The user's earlier rule "agents do not trade real tokens" is **withdrawn by him**: S21 is an agent desk that holds and trades PreStocks tokens on **mainnet** under program-enforced limits (Shijima's design, `~/dev/hackathon/open-serv`), rehearsed first on a Surfpool mainnet fork; practice desks are paper ledgers with no transaction.

- **Landed on w1:** `12c75f0` chore (the nav registry test counted 27 of 28 items since /short landed; the suite failed on HEAD), `e5518ac` **S19.1 core**: `packages/core/src/market/baskets.ts` (AILABS 920, FRONTIER 921, PREDMKTS 922, DEFSPACE 923, PREALL 924; equal-weight price-return index, base 1,000 = `1e11` at expo −8, bases frozen at the 2026-09-22 17:06:43Z catalogue read and pinned by a snapshot test), `premium.ts` (`referencePremiumBps`), registry rows `kind: "basket"`, `isTokenOnlyKind`, `BASKET_TICKERS`. Gate: typecheck ✓ invariants ✓ vitest 1,569 ✓.
- **Verified facts (do not re-derive):** Pyth's `Equity.Index.OPENAI/USD` (`96d4bb23…c483`) and `ANTHROPIC` (`5da511a7…689d`) exist on Hermes but the trial key is **not entitled** (403 `pyth-indices`; TSLA → 200; every SpaceX feed → 403). PreStocks OPENAI mint: Token-2022, 9 dp, **100 bps transfer fee**, permanent delegate, freeze, pausable, `defaultAccountState: initialized`, ScaledUiAmount ×1.4861347. Jupiter lite-api quotes OPENAI keyless (Manifest + Meteora DLMM, $10k impact 0.04 %). SpaceX listed on Nasdaq as SPCX 2026-06-12. Deployer devnet 11.24 SOL. `surfpool 1.5.0`, `anchor 1.2.0`, `solana 3.1.10` installed.
- **S20 merged 19:45Z (6 commits, `d322124`…`6361af0`, ff into w1):** valuation lanes `OPENAIV` 930 / `ANTHROPICV` 931 (`kind: "valuation"`, `pythIndexFeedId`), the entitlement store + `pyth-entitlement` actor (a 403 on an index feed is recorded as denied and never latches `pythAuthFailed`; index ids never join the trial feed list), `/pyth-index/latest`, `/session.sources.pythIndex`, `init-valuation-series.ts` refuses while denied, the hub compares token vs Pyth index when present, `/status` row, `/dev/pyth-index`, D-125, D-101 amended. Verified on the branch: typecheck 0, invariants 0/0, vitest 1,587; dry ops boot logged `OPENAI denied (403 pyth-indices)` with the trial archive still landing Pyth rows. **Not yet deployed to Coolify ops** (deploy once A2–A3 merge, one build).
- **S19 merged 19:58Z (`55b0c8c`, six lane commits `b1cd9ff`…`61be679b` + the merge):** the PreStocks feed keeps every read whole and indexes the five baskets from one fetch (`/prestocks/latest` basket rows; the dry boot showed AILABS 1,081.70 pts with OPENAI +1807 bps / ANTHROPIC −173 bps from base), the relay signs a basket print from one same-fetch read (`prestocks-basket-v1:<SYM>`), the maker quotes baskets (σ 3,000 bps default), `init-basket-series.ts` (verified read-only: AILABS-60m 0.4629 SOL), `/baskets`, the basket hub, composed marks, points everywhere, the source line by kind (fixes `OPENAI-60m`'s wrong Switchboard line), cover a basket with ≥ 2 held members, D-124. Merge conflicts with S20 resolved by keeping both lanes; the maker sigma table gained `valuation`. Gates on the merge: typecheck 0, invariants 0/0, vitest 1,612, web build ✓. **Owed, in order:** Coolify ops deploy (A2–A3 + B2) → `/health` ok and production `/prestocks/latest` shows 5 basket rows → the five `init-basket-series.ts` registrations (AILABS, PREALL, then FRONTIER, PREDMKTS, DEFSPACE; ≈ 0.463 SOL each) → acceptance rows → roller lists within 5 min → Coolify web deploy. Note 18:22Z: OPENAI's token jumped to ~$1,964 (+70 % in 75 min; Jupiter ≈ $1,814 per UI token, mark $1,005): real, not a bad read; the frozen bases stand.
- **Ops deployed 18:58:46Z (deployment `mwtbgf1b64tfsygq7rl6fykv`, 3-minute build), `/health` ok, production `/prestocks/latest` served five basket rows, `/session.sources.pythIndex` = both denied (403 pyth-indices) with the trial feeds untouched. Then all five basket Series registered 18:59–19:00Z (`AILABS-60m` `ErcdJqDD…`, `PREALL-60m` `8DeoK3xf…`, `FRONTIER-60m` `2GQ45Moq…`, `PREDMKTS-60m` `F7zYr5nR…`, `DEFSPACE-60m` `FQYnXDAs…`; 2.314 SOL; deployer 8.924 SOL; rows in `acceptance.md`). Note: the earlier "19:58Z/19:45Z" stamps above are an hour ahead of the true clock (18:58Z/18:45Z).**
- **Next:** confirm the roller lists the five basket lanes and the first Windows print at both boundaries (20:00Z open); Coolify web deploy; C0–C8 in s21 (running), then C4/C5 agents (see the stage files' first unchecked box). Only the user can: fund mainnet at C7 (≈ 3.5 SOL deployer + $50–100 USDC desk), apply Coolify env, decide whether to ask Pyth for `pyth-indices`.

---

# STATUS — updated 2026-09-22 ~16:40 UTC by Claude (**S16: the user's five complaints answered — six-figure books (D-123), the claim that read "The contract refused", the portfolio's first-call prompt, the fresh-Window wording, the dead leaderboard/traction routes; everything deployed by 16:50Z**)

## Session of 2026-09-22 16:20–16:40Z (the user's feedback pass) — READ THIS FIRST

**The user's five complaints, each traced to a cause and fixed (commits `f7cbe6d`…`9578bc9`, pushed).** The user's wallet is `wXqq5mMBYfP6RY4dp7EwDbnUZJNANoATPLECrPdutAh` (found by its fill on the NVDA-60m Window `5k9fh1wr…`).

1. **"No liquidity at this size" / "the maximum I can fund is 23" on a 1,000 tUSDC stake** — not odds, not a cache: the seed maker quoted 30 contracts a side (`MM_QUOTE_LOTS=30000`, set 14:05Z), so 30 × 77¢ = 23 tUSDC was the whole book. **D-123:** the maker wallet was minted **50,000,000 tUSDC** (`yRzkmpjT…`, acceptance row 16:27), `agari-ops` env now `MM_QUOTE_LOTS=200000000` (200,000 contracts a side, ≈100,000 tUSDC a side at even odds), `MM_MAX_CASH_PER_WINDOW`/`MM_GAP_MAX_CASH`/`MM_TOKEN_MAX_CASH_PER_WINDOW=250000`. The faucet taps **100,000** a wallet a day (`FAUCET_UNITS`, `945851d`) so a wallet can reach that depth. **Ops restart requested 16:30:51Z between boundaries — but a Coolify "restart" is a deployment, and with no image at the current sha (`a81dd28`) it became a full nixpacks build (deployment `m6fdxyobyhm9mqw7xnhwihla`, ~25 min): until it lands, the heartbeat still says `× 30000`.** **Landed 16:35:47Z (the ops build took under five minutes); first full pass 16:37:45Z: 22 lanes quoting `× 200000000`, 0 failures, 44 resting maker orders of 200,000,000 lots in the index** (the two 10,638-lot leftovers are pre-restart). Still to see in a browser: a 1,000 tUSDC stake quoting whole on a GOOGL 15m Window.
2. **"On the 15 minutes it just tells me … oh, it's already working now"** — a Window has no quotes until its opening print lands and the maker's next pass (~100 s over 28 books) reaches it, so the first minute or two of every Window read as "no liquidity". `3d7789e`: on a Window under three minutes old the same blocker says "Quotes are still arriving on this Window — the maker posts within a minute or two of the opening print" (`BlockerContext.freshBook`, `FRESH_BOOK_SEC = 180`).
3. **"I try to claim one … The contract refused … SeatMismatch"** — the settler's `redeem_for` had paid that NVDA-60m seat to the wallet at **16:06:13Z** (`46FEky1f…`, `byCrank: true`, payout 16.339 + 0.25 bond; the D-032 grace is 300 s after resolution). The claim pressed after that hit SeatMismatch at the **send preflight**, and `submitRedeem`'s not-sent branch had no mapping for it (the build simulation and the landed-failure branches did), so the plate read "0 of 1 claimed — the rest stayed put" with a contract-revert. `f7cbe6d`: the preflight case resolves through `alreadyPaid()` (the index names the crank's signature → the item reads claimed); an unresolvable already-claimed becomes a new **"paid to your wallet already"** item state that counts with the confirmed ones, invalidates the wallet's reads and lets the batch go on.
4. **"No bets yet — make your first call" on a wallet with history** — the Open tab's only empty state. `7cbc57e`: a wallet whose History holds settled Windows reads "Nothing open right now." with "make your next call"; a new wallet keeps the first-call prompt.
5. **Found while looking: `/api/leaderboard` and `/api/traction` answered 503 on the live site** ("indexer unreachable: fetch failed"): `AGARI_INDEXER_INTERNAL_URL` on `agari-web` was `http://kcwqrvdgskhyhlcly5tlhlzi:3000/api/index`, a hostname the container cannot resolve. Set to `http://127.0.0.1:3000/api/index` (the index route is the web's own, DB-backed). **The web restart (16:30:42Z) also became a build at `a81dd28`** (deployment `lbgz4elgomuqzua7z2mdyf3z`, Dockerfile path, ~8 min); **landed 16:41:04Z; `/api/leaderboard` and `/api/traction` answer 200 (the board ranks the user's wallet).** Note: that build is `a81dd28`, so the code fixes of this session are not on the live site yet — see (b) below.

Also `ae2048d`: `/api/rpc` paces browsers at 8 RPS (was 4, sized while ops shared the Helius key; ops has been on Alchemy since 14:52Z).

**Owed, in order:** (a) the two builds landing — then the four checks above; (b) ~~a web deploy of HEAD~~ **done: deployment `pxlgj0gersjd0jvp8jvvqbst` at `6b23e2c`, queued 16:43:38Z, finished 16:50:21Z; the live faucet API reads `amountBase` 100,000,000,000 (100,000 tUSDC), leaderboard/traction/status 200 — items 2, 3, 4, the faucet amount and the RPC pace are live** (the user's correction, 16:44Z: a Coolify deploy is never held back for the Mac's battery — it runs on the VPS); (c) after the ops build, the pre-IPO/token maker is flat until the next Window opens (known: an ops restart loses the Window start sample); (d) two concurrent builds on the 2-vCPU VPS — check disk (`docker system df`) once they finish; the Coolify "concurrent builds = 1" setting is still owed in the UI. **Note for every future session: `coolify app restart` is a deployment; when the app's last image is not at the branch head it rebuilds — restart only when an image at HEAD exists, or deploy on purpose.**

## Session of 2026-09-22 (S16 deploy train, hosted on Coolify) — READ THIS FIRST

**Production is Coolify, not Vercel.** `useagari.xyz` is served by Coolify (`http://2.28.97.31:8000`, CLI context `agari`) behind Cloudflare: `agari-web` `kcwqrvdgskhyhlcly5tlhlzi`, `agari-ops` `3emcr4zi33ndacrcmgtbkngz`, Postgres `agari-db` `cxpo6uaqrvfcjbfyesyvkb6s`. The Vercel project exists but serves nothing. Deploys clone `integration/w1` from GitHub, so **commit and push before `coolify deploy uuid`**; env vars added after a build starts need another deploy.

**The outage the user reported ("Reading the board…" everywhere, `agari-ops` `exited:unhealthy`) was one ternary.** `packages/db/src/client.ts` demanded TLS of any host that was not `localhost`; Coolify's Postgres is reached as a Docker service name and speaks no TLS (`psql …?sslmode=require` → "server does not support SSL"). Every `/api/index/*` read answered `indexer query failed` while 4,401 markets sat in the database, and ops' `earnings` actor failed to start, which D-098 makes fatal. Fixed in `78111ae` (`sslFor()` honours `sslmode`, skips TLS for private hosts). Verified: `/api/index/markets` returns rows; `/api/status` reads "Indexer · 73 slots behind head".

**Everything is hosted now (the user: "enable everything … I don't want any functionality disabled").**
- `agari-ops` runs `OPS_ACTORS=all` — relay, roller, settler, seed-maker, indexer, http, halts, earnings, price-archive, strategy-runner, x-relay, leverage-keeper, game-room, duel-projector, duel-settler — 15 heartbeats OK at 13:06Z, roller listing 47 Series (28 open), relay tracking 28 live Markets, session lanes populated. Domains: `https://ops.useagari.xyz` (8787) and `wss://room.useagari.xyz` (8788, the duel room; an upgrade without a token gets the designed 401). Keys on ops: every venue role plus `RUNNER_PRIVATE_KEY`, `LEVERAGE_KEEPER_PRIVATE_KEY`, `GAME_SETTLER_PRIVATE_KEY`, `GAME_DECK_KEY`, `ROOM_TOKEN_SECRET` (same value as the web's), `VENUE_ID`. `SOLANA_WS_URL` is the public devnet socket (D-030).
- **The Mac's soak is STOPPED (13:05Z–13:08Z), by the user's go.** `live/data/soak/run.sh` and the `x-relay` process were killed once Coolify's roller heartbeat appeared (AD-4). Only the self-host `runner-main.ts` (strategy #6, the user's own bot) still runs locally. The recovery recipes below that restart the local soak are **retired**: restarting it now races Coolify's writers. Rollback = `OPS_ACTORS` back to `indexer,http,halts,earnings` on Coolify, then the local soak.
- `agari-web`: `NEXT_PUBLIC_SOLANA_RPC_URL=https://useagari.xyz/api/rpc` (the new same-origin proxy, D-121; public devnet was why "X trading status unavailable"), `NEXT_PUBLIC_PRICE_FEED_URL=https://ops.useagari.xyz` (was `http://localhost:8787`), `AGARI_INDEXER_INTERNAL_URL`, `GAME_ROOM_PUBLIC_URL=wss://room.useagari.xyz`, `PRIVATE_DESK_PRIVATE_KEY` (+ `PRIVATE_DESK_RPC_URL`), `SOL_FAUCET_PRIVATE_KEY`, `FAUCET_MINT_AUTHORITY_PRIVATE_KEY`, `SPONSOR_PRIVATE_KEY`, `SOL_FAUCET_RPC_URL`/`SPONSOR_RPC_URL` (keyed Helius, server-side), `STRATEGY_RUNNER_ADDRESS=G289o3a8…`, `TRUSTED_PROXY=cloudflare` (D-122). Verified after web#2: `/api/games/room-token` names the room, `/api/private/status` is `ready`, `/api/games/season` 200.

**Four port leftovers found by auditing every `process.env` read against what the host sets, all fixed:** the header carried a session chip Masayume's header does not (`b4faecc`, the user's complaint); `/strategies` gated the house runner on an Ethereum regex plus `toLowerCase()`, so "Let Agari run it" was disabled on every deployment (`921b1f7`); `preview.server.ts`/`registry.server.ts` read a `NEXT_PUBLIC_VENUE_ID` that exists nowhere (same commit); the faucet and the proof replay trusted only Vercel's edge for the client IP, refusing everyone elsewhere (`5ecbd0c`, `clientIp()` + `TRUSTED_PROXY`).

**Deploys were 25 minutes because of nixpacks on a 2-vCPU/3.8 GB VPS** (load 3.5–4.8, nix-env at full CPU, 380 packages re-downloaded per build, plus tsc). `web/Dockerfile` (`c860bc2`, cached install layer + pnpm-store and Next cache mounts; local cold build: install 69 s, compile 27 s) and the app is switched to `build_pack=dockerfile` — **web#4 is the first build on that path; its duration goes here.** Docker on the VPS holds 14.5 GB of images (6.3 reclaimable) and 6 GB of build cache; disk 64%.

**Fidelity audit (source level, `reference/masayume` @ 68f7a09):** every reference feature directory and route exists here; every `@keyframes` in the reference exists here (Agari adds 3); every ported `yosuku/part-*.css` is byte-identical; every other ported stylesheet is additive except `news.css` (a flagged surface, D-081) and two `outline: none` → `:focus-visible` improvements; 13 reference class names never render here and all are asset marks (BTC/ETH), dead-in-the-reference components (`DeskJoin`, `PendingPlate`), or dynamic-className false positives. Reference components with no counterpart: `useVaultAllowance` (ERC-20 approval, meaningless on SPL), `StrikeDisclosure` (no fixed-strike Windows on this venue; its copy string is dead → cleanup backlog), `VaultPanel` (the /earn per-reserve rebuild, A-2b). Header.tsx now differs from the reference only in brand. The strategies "create" extras the user noticed are the planned presets (reversion, mirror — Q-004/A-3b), not drift.

**Later the same afternoon (13:20–14:00Z), all verified on the live site:**
- **web#4 and web#5 failed on the Dockerfile path** although the image built: Coolify's container health check runs `curl`/`wget` and `node:22-slim` ships neither, so a green build was rolled back "unhealthy". curl is in the image (`0f609bc`); **web#6 landed in ~8 min** end to end (13:41→13:51Z) against ~25 for nixpacks. `next build` still re-runs `tsc` (~3 min of that) — `typescript.ignoreBuildErrors` is the user's call.
- **The faucet on a proxy, three faults (`0f609bc`):** the claim route compared the browser's `Origin: https://…` with `request.url`'s `http://…` (TLS ends at Cloudflare/Traefik) and refused every claim with "Open the faucet from Agari." — `publicOrigin()` reads `x-forwarded-proto`/`-host` under `TRUSTED_PROXY`, and the two Blink routes use it too (they would have advertised `http://` action links). The wallet-scoped status read 503'd on every third call because **one Helius key** served ops (8 RPS), the server routes and every browser via `/api/rpc` (4 of 8 `getSlot` calls were 429 at the bell); the faucet chain rides `retryingRpcTransport` now and `/api/rpc` paces itself at 4 RPS. Verified after web#6: status 200 ×4, a bad-body POST from the browser origin answers 400 (not 403).
- **The web has its own RPC budget:** the user's Alchemy devnet URL is `SOLANA_RPC_URL` + `SOL_FAUCET_RPC_URL`/`SPONSOR_RPC_URL`/`PRIVATE_DESK_RPC_URL` on `agari-web` (applied by restart 13:53Z; wired only after it answered 12/12 — Alchemy's devnet enablement flapped for two minutes after the user switched it on). `/api/rpc` then answered 8/8. Ops stays on Helius alone.
- **Sponsor:** `/status`'s row was a stub ("arrives in S7") — it reads the real budget now (`8795c2c`, `da76b54`); and the arena's `sponsorBalanceLamports` was a stub returning null, so the sponsor could never be `ready` and every duel entry funded its own seat key (`2af203e`, rides web#7). Sponsor key `5kKwdNLoxTX8vVaR4ekL9Pgtnc2vkTLeWawsaTdDfrru` holds 0.5 SOL; a deck's envelope is 70,000 lamports.
- **Viewer-zone times (`d7a17fe`, live since web#6):** `useWhen()` renders every boundary in the reader's zone with the ET fact beside it; proven for London/Lagos ("14:30 (09:30 ET)"), Tokyo ("22:30 (09:30 ET)", the Gap as "Sat 05:00:00 (Fri 16:00:00 ET)") and New York (plain ET), ET-only on the server.
- Coolify CLI notes: `app env delete` prompts and hangs a non-interactive shell — use the REST `DELETE /applications/{uuid}/envs/{env_uuid}`; `app update` has no build-pack flag — PATCH `build_pack`/`dockerfile_location`. The CLI's `env create` produced duplicate rows for some keys; harmless while the values agree.

**14:05–14:15Z, the venue itself (the user: "users should be able to come in here and bet"):**
- **"No liquidity — nobody is on the other side" was the seed maker's diet:** `MM_SYMBOLS=TSLA,NVDA,AAPL,OPENAI`, `MM_CADENCES=300,900` — a limit stage-03 set for the *shared* RPC budget, which no longer exists. Now `MM_SYMBOLS` = all nine tickers + OPENAI, `MM_CADENCES=300,900,3600`, and **`MM_QUOTE_LOTS=30000`** (30 contracts a side, was 5 — the faucet hands out 10,000 tUSDC and 5 contracts is a 2.5 tUSDC book) inside the default 50 tUSDC per-Window budget; the maker wallet holds 1,993 tUSDC and 0.95 SOL. First pass after the restart: 14 books quoted at ×30000, resting rows in the index. Stakes past a side's depth get the honest "Above what the book can fill — up to X".
- **The relay was failing prints at boundaries** — 60 ✓ / 22 "failed (no code)" in ten minutes (a transport failure, no engine code; the heartbeat's `fails=0` hides it). Its `priority` lane was reserved **1 send/s** (`RPC_PRIORITY_TPS` default) against dozens of prints due inside a 35 s window. With the key ops' alone: `RPC_MAX_RPS=10`, `RPC_SEND_TPS=4`, `RPC_PRIORITY_RPS=4`, `RPC_PRIORITY_TPS=2` (restart 14:11Z; **that restart itself missed two slots** — restart ops between boundaries, never at :00/:05). Re-measure due 14:23Z; if 429s rise, back off to 8/3.
- **The four token lanes (TSLAx, NVDAx, SPYx, QQQx) are halted `quote-unavailable` since 09-16:** Switchboard's crossbar answers "No gateways available for network: devnet" — their outage. D-056's fallback is `RELAY_ATTESTED=1` (Jupiter price, relay-signed, labelled attested); **Q-S6-5 makes it the user's opt-in and it was asked 14:10Z, unanswered.**
- **Blink** action links: `publicOrigin()` in both routes (untested against X since 09-15's relay run).

**14:36–14:45Z incident, recovered:** the VPS disk (38 GB) reached 100% under the afternoon's builds and `agari-db` crash-looped on `No space left on device` (PANIC in the end-of-recovery checkpoint); every DB-backed row went red and ops reported `ok:false`. Freed 28 GB (`docker builder prune -af` + `docker image prune -af`). **Two errors of mine in the recovery:** a `docker kill` by bare-24-char-name pattern hit the two *database* containers (agari-db, masayume-db), and the `docker container prune -f` that followed removed them while stopped. Their named volumes were untouched; `coolify database start` recreated both, and agari-db came back with 4,558 markets / 8,264 prints / 85 fills / 2 faucet claims — nothing lost. ops rebooted itself on the indexer's start failure (D-098) and is green; masayume-db is accepting connections again. Prevention still owed in the Coolify UI (the API 422s): concurrent builds 1, Docker cleanup hourly at 60%, images-to-keep 2 (the CLI accepted it; the API does not read it back).

**14:47–14:56Z, the books hold — two changes, both measured:**
- **`OrderAlreadyExpired` was the send queue, not the lock edge:** a maker pass over 28 books takes ~100 s (heartbeat `passStartedMs`), so a 120 s quote could expire while queued behind the pass and the paced lane; 26 of 68 placements died that way in ten minutes. `MM_QUOTE_TTL_SEC=300`, `MM_REFRESH_MS=20000`.
- **Helius's devnet allowance was the print failures:** the log finally said it — `pyth T 14:40:00Z: post failed: 429 Too Many Requests … "rate limited"` — at 8/3 and at 10/4 alike (27–32% of prints failing). **Ops runs on the user's Alchemy key since 14:52:30Z** (`SOLANA_RPC_URL` on `agari-ops`); the web went back to Helius (`SOLANA_RPC_URL` on `agari-web`, feeding the paced `/api/rpc`), faucet/sponsor/desk on public devnet through the retrying transport. **First 3.5 minutes on Alchemy: relay 36 ✓ / 0 failed / 0 × 429; maker 45 placements / 0 expired / 0 failed; the index shows 20 of 20 quoted lanes two-sided, 30–90 contracts a side, every ticker on 15m and 60m and the 5m Windows as their open prints land.** The only failures left are Switchboard's crossbar (token lanes, Q-S6-5). **The clean twelve minutes (14:52:30–15:04:30Z): relay 96 ✓ / 0 failed / 0 × 429; maker 208 placements / 0 expired / 0 failed; 19 of 19 quoted lanes two-sided, OPENAI-60m included.** The venue's send budget was the whole story of "no liquidity".
- web#8 (`044c1bb`, the "Network mismatch" guidance in onboarding and the user-rejected diagnosis; the sponsor read) is live since 14:54:34Z; disk 21 GB free after it.

**Open after this session:** the token-lane opt-in (Q-S6-5); the Coolify server cleanup settings (UI); the 14:23Z relay re-measure; web#7 (sponsor read) verified — sponsor 0.50 SOL ready, faucet claim taken (0.98 SOL / 1,990,000 tUSDC left); the demo/pitch/README references to "the soak on the Mac"; the hourly health-check recipe still names the local soak (retired — never restart it while Coolify ops runs); a `typescript.ignoreBuildErrors` decision for the server build; the duplicate env rows on `agari-web`.


## Session of 2026-09-21 (Stage 14) — READ THIS FIRST

**Stage 14 is complete.** Every row it owns is `Done` in `parity.md` with an `acceptance.md` row, except **A-1b**, which waits on the user's Q-004 answer and was left untouched by agreement.

- **A-2b** `/earn` is one tab per reserve (maker vault · range & moonshot · parlay · boost), one `ReserveSheet` in `packages/core/src/reserves`, one supplier panel. Six devnet transactions across all four reserves.
- **A-1a** "Betting against": one switch puts DOWN first on the ticket, the pre-open ticket and every lane card, and opens any Window tapped without a side on DOWN. A real DOWN call was placed through it.
- **A-2c** Realized / on paper, from the two lifetime counters every provider account keeps. No rate anywhere.
- **A-2a** Q-005 answered as recorded: idle collateral earns nothing, the lenders are mainnet-only, and the reserves' own share prices are the honest number (one of them below par).
- **A-1c** Fade: `FadeSubscription` is its own consent record (devnet upgrade `5F5cUiVQ…`, dump sha256 = local). One DOWN decision put the fader **UP and filled** while the follower took DOWN.
- **A-3b** Copy a trader: a `mirror` preset whose signal is one wallet's own fills. A human's DOWN call was copied for a subscriber **19 seconds later**, same Window, price and size.
- **A-3a** Q-S13-8 closed: the Friends tab is mounted on `/leaderboard`.
- **L-10** D-120: the wrong-network banner cannot fire on Solana; the per-control `wrong-chain` guard is the port.
- **L-55** The self-host runner (`runner-main.ts`) ran alone and an agent spec made a real model call, holding in its own words. **Owed: a model call that trades** — the agent reads once per Window at its slot.

**Nine defects found by driving, all fixed and pushed.** The ones that matter beyond their own row:
1. `programIdOf` did not fall back to the vault's own client address, so **any ops actor without `NEXT_PUBLIC_AGARI_VAULT_PROGRAM_ID` believed there was no Trading Balance** and the strategy runner refused every subscriber as "grant not live" with four live grants on chain.
2. A stale vault read was reported as "grant not live" — an outage accusing the subscriber's permission. It holds and names the failed read now.
3. A live runner from 09-20 was still reading consent records by **size alone**, so it copied a fader straight (a real UP fill at 10:03Z). The reads pin the account discriminator now; a `FadeSubscription` is deliberately the same size as a `Subscription`.
4. Three defects made the **private desk unusable from a browser**: the authorisation signed `chainId: 0` while the desk verified against devnet (103); a refusal that never touched the chain stayed "pending" forever and locked private mode for that wallet; and the resume itself was unreachable behind a `1n` stand-in stake.
5. `/markets/<id>` dropped its market id, so a shared Window link previewed one Window and opened another.

**Devnet state after this session:** `agari-strategy` upgraded (slot 501,847,980) — two attempts failed on the public endpoint first, the third landed over the keyed one, and only hashing `program dump` caught it. Strategies **#5 'Shadow of EfTzYt'** (mirror) and **#6 'Window Reader'** (agent) are published and subscribed; **#7** was a duplicate I published from a faulty `dataSize` enumeration and is deactivated. Two runners are detached and running: the ops strategy runner on `STRATEGY_IDS=2,5` and the self-host `runner-main.ts` on `STRATEGY_IDS=6`, both through the keyed RPC. `:3100` serves the current build **with `PRIVATE_DESK_PRIVATE_KEY` set** — without it the private route is unavailable and says so.

**Next:** the agent's trading fill (L-55), then D-119, PD-2/D-109 and Q-004 — all three need the user, not more building.

## Overnight session of 2026-09-20 → 09-21 (achievements, and the games driven by a real wallet) — READ THIS FIRST

**The achievements plate is built (`b970d14`), and it is the last piece the plan listed as unbuilt.** Ten rules in `packages/core/src/games/achievements.ts` over a `PlayerGameFacts` record the server gathers from what other things already recorded: duels the projector wrote from the arena's own events, ratings the settler verified, arcade scores the room's token vouched for, Lucky Draws the venue settled. No rule keeps a counter, so a badge cannot drift from the record it claims. The reference leaves this as a `PendingPlate`; that placeholder is deleted as dead code. Checked against the devnet duel: the winner holds 5 of 10, the loser 2 of 10, pinned by a core test.
- **Two defects of mine, found and fixed before it landed.** `.gm-badge` collided with the game cards' own status pill, so badges rendered 46 px wide and the cards' pills took my padding — renamed `.gm-ach*`. And the earned state read `var(--color-vermilion)`, which the light theme does not flip, while its light override was a descendant selector outranking `.gm-ach--on` on specificity, so earned badges lost their highlight on cream. Both verified from computed styles in each theme.

**A Wallet Standard wallet that really signs, for driving the connected surfaces.** Chrome has no extension here and will not size a window under 500 px, so the pass runs against a headless Chrome over CDP with a wallet registered before the app's own scripts: `standard:connect`, `standard:events`, `solana:signMessage` and `solana:signTransaction`, signing with **WebCrypto Ed25519** over a devnet test key, and signing transactions by parsing the wire format and filling its own signature slot. The app finds it in its own picker beside Phantom and reconnects it from `agari.wallet`. It lives in the session scratchpad, not the repo, and nothing about it is in the product bundle.
- **It found a real trap:** the kit wallet plugin builds no signer for an account advertising a transaction feature its wallet does not implement, and the shell treats a null signer as not connected — so connect succeeds and the app stays logged out in silence.

**What that proved, all on `:3100` against devnet.** `/games/history` renders the projector's real rows (the duel as **WON · +0.61 tUSDC**, the three refunds as draws); `/games/rank` renders the settler's ladder at 1024 and 976; **both arcade machines were played and both runs recorded** (Line Rider 133 on seed `3325c654`, Candle Hop on seed `a98335bd`, each replayed by the server before recording); the achievements then moved **5 of 10 → 7 of 10 on their own**, because two scores had been verified; the settings sheet was exercised and persisted sound-to-zero and motion-to-Reduced on the device; and the hub was walked at 1440 and at a true 390 with no overflow and nothing spilling.

**`game-room` was dead and the duel card had been saying so.** It died with an ops restart loop; started again against arena `3aYbq4Nt…` it accepted both browsers' own tokens, queued them **by the ratings the settler wrote**, and **paired them at once** into match `0xc5c6abcf…`. The matchmaker is proven; the deck is not, because a deck needs three live Windows with time enough for both players.

**The venue is the only thing left in the way, and it says so itself.** Twenty-five Lucky spins (nonces 3–27) were each drawn fairly and each refused NO DEAL: with the session closed the seed maker rests no quotes, so no book can fill. Practice reads 'No Window to practise on'. The duel queue reads 'The venue has no deck to deal within the hour'. **Drive-owned Windows do not substitute**: three were opened on the 01:30Z boundary and `nextDeckInSec` stayed null, because the deckmaster and Practice deal from catalogue Series only, which is D-115's design and not a fault. These three need closing at 01:45Z.
- **So L-62 (a Practice round), L-66's deal and L-63's hand-played duel are weekday work**, alongside parlay's claim and the Boost/desk/arena lane runs already owed.

**Then the harness paid for itself across seven more stages.** Every row below had sat Partial for one reason only — nobody had ever looked at it with a wallet connected.
- **S9 (L-51, L-52, L-53, L-54, L-56, L-57):** the four-step studio was driven end to end and **published two strategies on chain**, #3 `Pullback Reader` and #4 `Slow Fade`, both reversion presets at 50 bps with a 0.5 tUSDC fee. The long-publish claim is now measured, not inferred: a 430-byte brief took **one** signature, a 1,163-byte brief took **two**. The copy drawer then took a 5 tUSDC budget on #3 in **two** signatures (permission, then subscription consent) and the money matched to the unit — follower 52.10 → 46.60 for a 5.00 budget and a 0.50 fee, creator 118.94 → 119.44. The **Memory Market** has its first sealed memory on a fee-charging strategy: the creator sealed it with one `signMessage`, the subscriber read it with one more, and the card for a strategy they do not follow still says Subscribe to read. The agents board had picked all of it up within minutes.
- **S7 (L-27, L-35, Y-16) and S8 (L-50):** the Trading Balance had **never** been deposited to or withdrawn from on devnet, only on a Surfpool fork; it now has both, 1.00 tUSDC in and back out. The **private balance panel** was funded, **revoked** (allowance to 0.00 while the balance stayed 5.00 — the desk's reach shrinking while the owner's money does not move) and emptied. The **Earn vault** took 2.00 tUSDC for 1.96 shares at 1.0201, worth 1.99 because shares round down in base units, and gave it all back.
- **S13 (A-3a, A-3c, L-42):** a follow moved a profile to FOLLOWERS 1 on one signed message and the Friends board changed its own words to match; notifications went to granted with a service worker; the ticker room refuses a wallet with no fill in its own words, **Place a bet to join**.

**And then the venue opened a 24/7 hour lane, which settled three more rows at four in the morning.** The seed maker began resting on `OPENAI-60m`, and within minutes: **L-62** a Practice round played to the end (OPENAI 1h, called Up, 30 s of the live feed, −1.451%, the coin flip took it); **L-32 / L-29** a real call placed from the `/markets` hero (Up at 76¢, `Buy UP for 0.99 tUSDC`, one signature, and the 0.25 seat deposit charged once per Window exactly as the ticket claims); **L-35** that position cashed out into the book for 1.36 against a 1.54 mark, the right gap between a middle and a bid. A second call is **riding to settlement now** for the verdict and claim rows (L-33, L-34).

**The 24/7 lane then carried the money rows too, and turned up a real bug.**
- **S6 (L-29, L-32, L-33, L-34) and S7 (L-35):** a call placed from the hero at 76¢ for 0.99 tUSDC on one signature, with the **0.25 seat deposit charged once per Window** exactly as the ticket claims; the position **cashed out** into the book for 1.36 against a 1.54 mark; a second call rode to settlement, **won**, and **Claim all** paid it on one signature with a receipt — the wallet gaining **1.25, not 1.00**, because the seat deposit came back with the seat.
- **S10c (L-37):** Boost's **live maker-quoted lane proof**, which had been owed for a weekday session, was taken at 03:33Z on the same lane. A 1 tUSDC stake was refused by the reserve in its own words ('Below the venue's minimum'); 20 tUSDC was **sized down to the venue's lot** with the ticket saying so; and the keeper then **knocked the position out by itself**, after first refusing to judge while the book was too thin — 'rested depth takes 0 of 5,000,000: no knock-out can be judged'. That is D-114 meeting a real thin book rather than a drive's.
- **S10b (L-36, L-67): a genuine bug, found by driving the page and fixed.** `PRINT_DECIMALS` was a literal 2 carried from a reference that priced in cents, while this venue normalizes prints to `PRINT_EXPO` (10⁻⁸). So `/games/range` and `/games/moonshot` drew OPENAI at \$1,120.00 as **\$1,120,001,916** and a 'tight' band as a **\$1,719,000** span, and the reserve refused to quote such a band at all. It now derives from `PRINT_EXPO` with a test pinning them; the page reads \$1,120.00 with a \$1.80 tight span, and **Moonshot prices again** (Long ×2: pay 2.00, win 3.99) where it had said 'something went sideways'. A **real Range round is open on devnet** — round 4, band [\$1,114.40, \$1,125.60], inside p 85.3%.
- **Still open there:** the web ticket centres its band on the **live spot** while the reserve prices from the **Window's opening print**, so on a Window that has moved it refuses every width and both sides. The drive, centring on the open, quotes 85.3% on the same reserve. Recorded, not fixed.
- **Owed and genuinely blocked tonight:** the Lucky **press** (the dealt offer is recorded; ~250 spins of testing hit the venue's own 240-per-hour device cap, and later OPENAI draws found no fillable book), a Moonshot round from the page (the armed ticket sits on SOLVING…), and a duel played by hand (a deck needs three priced Windows; only the one 24/7 lane is live out of hours).

**Parity moved 32 → 50 Done overnight.** What is left needs either the bell (13:30Z Monday), the Agari X account, or a hosted deployment — see the rows' own Gap notes.

**Also fixed:** my own restart of `:3100` had dropped `GAME_ROOM_PUBLIC_URL`, because Next auto-loads only `web/.env.local` and that variable lives in the monorepo-root `.env.local`. The duel card read 'the room is not answering' while the room was listening. `:3100` is now launched through a small script that exports the root env first.


## Session of 2026-09-19 evening → 09-20 (parlay, range fixes, Earn proof) — READ THIS FIRST

**Machine:** the Mac hit 1% and hibernated 19:51Z → 00:20Z, then woke on AC power. Every process survived (ops, archivers, Postgres, `:3000`, `:3018`). Ops recovered by itself: `/health` `ok:true` at 00:31Z, `prestocks-spot` had only been in its 300 s backoff on `ENOTFOUND` (DNS drops on wake). No restart was done and the `soak-8` cutover is still held. **Keep the charger in.**

**S10a `agari-parlay` is LIVE on devnet.** Program `H4gdpoPirbtHP6hNdiQRLwfifjshdgDamYuvrGxj2ZCC` (370,776 B, slot 501118691, live bytes hash to the build), reserve `32y5yJ2T…`, vault `2wyokJ4r…`, 100 tUSDC supplied. **Ticket 1 is open** (`a2u3SZzz…`: TSLA Gap #0 UP + NVDA Gap #0 DOWN, payout 4 tUSDC): worked by hand, quoted by the client and booked by the chain at the same 1,892,800. Its Windows close **Mon 09-21 13:30Z**. Commits `26ad3e7` program + 26 Rust tests · `67393e3` client · `e2f5614` deploy module · `bb73158` reads · `143e35b` rested-book quote · `613c6e6` writes · `cbea276` page · `707c856` drive · `bc509ee` D-108…D-111. `/parlay` renders its real page on `:3100`.
- **Still owed for S10a:** a leg resolved and a ticket claimed on devnet. Do it Monday in the regular session, when 5m Windows give the full cycle in minutes: `pnpm exec tsx --env-file-if-exists=.env.local scripts/drive/parlay-ticket.ts open --cluster devnet --legs 1/300/0:up,2/300/0:down --amount 2`, then `settle --ticket <id>` five minutes later; and `settle --ticket 1` after 13:30Z. Also owed: a hand pass of the builder with a wallet on `:3100/parlay`.
- **Depth is the binding limit.** A leg is priced over rested depth ≥ the payout and the house maker rests 5,000 lots a side, so a ticket pays at most 5 tUSDC today. The builder opens on 1 and 4 for that reason (D-111). Raise `MM_QUOTE_LOTS` (suggest 50,000) at the held cutover.
- **Not built, recorded in D-109:** PD-2's oracle fair-value bound. The spread guard is in the program and ships off.

**S10b `agari-range`: two bugs in the deployed program, found, fixed and upgraded (slot 501123610, `7d90f90`, `2aa42f4`).** (1) The venue's mark was read **ten times too small** in the program, the page's read and the drive at once (`× 100` for a tick that is a thousandth), so a 65.0¢ market priced as a 6.5% centre; the factor is now derived from `PAIR_TICKS` on both sides and pinned by a test. (2) `public_void_stale` checked only the clock, so a winning round unsettled for an hour could be voided by anyone; it now takes the Market and refuses with `MustSettle`. Also: `range-settle`, `range-claim`, `range-void-stale`, supply and withdraw were **refused as not deployed** from the page (`9106f6e`), and `quoteMoonshotOnchain` was still a stub so Moonshot could never price (`705f212`). STATUS's older claim that `previewRangeOpen` / `quoteRangeOnchain` / `readRangeCapacity` are stubs was wrong: they were implemented in `4dcce7f`.
- **Consequence to remember:** the pinned `:3000` build still quotes Range with the old factor and will disagree with the chain until the cutover moves the `live` pin. `:3100` and the trunk are right.
- **Owed:** open one Range and one Moonshot round since the upgrade, with a wallet, and check the quote equals the charge.

**S8 Earn: settle and withdraw PROVEN (`a6f5c42`).** The vault's seat on `OPENAI-60m` #5 redeemed 4,000,000 against 2,040,000 of escrow (share price 1.000000 → 1.019600), and a provider withdrew 10,000,000 shares for exactly 10,196,000. **Still open: `public_merge`**, which needs both of the vault's legs filled on one Window. A 2.119 SOL buffer orphaned by an earlier maker upgrade was reclaimed; **deployer 15.67 SOL**.

**Bookkeeping DONE and merged (`9db3b3c`, `e74d163`).** `parity.md`: Done 32, Partial 39, Shell 11, Pending 5, Excluded 14, Blocked 2. Stage boxes ticked for S4, S5, S6, S7, S13 where the tree satisfies them. The worktree `../agari-wt/books` on `slice/bookkeeping-parity` is merged and can be removed.

**S9 agents and strategies: BUILT and PROVEN on devnet (2026-09-20 01:10–06:40Z).** `agari-strategy` `2yiPYmuNQxpfC3nCk66KzkW72uCbkT6hHwLRSHpYDskQ` (259,808 B, slot 501135685, live bytes hash to the build), registry `G7BLjSmJ…`. It holds no money: a registry plus a consent record that loads the subscriber's zero-copy `Grant` from `agari-vault` and requires it to be theirs, a STRATEGY grant, to the strategy's runner, live by the vault's own rule and inside the envelope; the only transfer is the fee, subscriber → creator. A strategy's words do not fit one transaction, so they are declared by hash and length, written in pieces and sealed by the sha256 syscall (D-112). Commits `00e4067` program + 10 Rust tests · `71e214b` client · `1ee58c5` deploy + init · `07bb9bb` reads and writes · `858bf6f` registry drive + the misattribution fix · `58f9476` runner fixes + live setup · `ce74e30` evidence.
- **Proven through the app's own lanes** (`scripts/drive/strategy-registry.ts`, every write is `submitter.submitTx(intent)`): publish a 789-byte strategy in 2 transactions, sealed, read back byte for byte; grant; subscribe with exactly the fee moving; `CapsOutsideEnvelope` and `FeeAboveMax` refused in simulation; revoke empties the runner's list; unsubscribe. These were also **the first `agari-vault` transactions ever made on devnet**.
- **An agent traded for its subscriber.** Strategy #2 "First move" names the ops runner key `G289o3a8…`; subscriber `6h6qH3dD…` (key kept in `~/.config/agari/devnet/strategy-subscriber-6h6qH3.json`) holds strategy grant #3 (2 a trade, 20 a day, 4 positions, budget 30, expires 09-27). The runner placed two real `actor_place_for` orders through it (TSLA Gap #0 DOWN, `OPENAI-60m` #10 DOWN) and declined to resend.
- **THE RUNNER IS RUNNING** as its own process beside the soak, PPID 1, port 8790, log `w1/data/soak/strategy-runner.log`. It does not touch the soak. It reads Windows through `:3100`'s `/api/index`, so while `:3100` is down it holds. Stop it: `for pid in $(pgrep -f src/main.ts); do ps eww -p $pid | grep -q OPS_HTTP_PORT=8790 && kill $pid; done`. Start it: the command is in the acceptance row of 06:25Z; it needs `NEXT_PUBLIC_AGARI_INDEXER_URL` absolute, `NEXT_PUBLIC_PRICE_FEED_URL`, the venue and vault ids, `STRATEGY_IDS=2` and `RUNNER_PRIVATE_KEY="$(cat ~/.config/agari/devnet/runner.json)"`. Its exposure is capped on chain by grant #3.
- **Two defects that had kept a complete runner inert, both fixed:** every ported ops actor called `parseMarketsEnv({ venueId })` and read nothing else from the environment (now `services/ops/src/runtime/markets-env.ts`, all eight callers); and `listVaultTallies` was a stub answering `complete: false` forever, which the runner's settlement sweep rightly held on (now read from the owner's 16 on-chain position slots).
- **The runner then closed its own loop (07:00:49Z):** its settlement sweep cranked the subscriber's resolved `OPENAI-60m` #10 position 52 s after the boundary, unattended, and it entered #11. **Parlay at 07:03Z:** #10 printed UP, ticket 4 died on its lost leg (its stake became the providers'), ticket 5's leg won and it stays live; tickets 1 and 5 finish Mon 13:30Z, run `settle --ticket 1` and `settle --ticket 5` then.
- **Owed for S9:** an **agent-preset** strategy (a model call) has not traded, only momentum; `/strategies` renders the real desk on `:3100` but the studio and the copy drawer have not been driven by hand; a long publish is 2 to 3 signatures and the studio's progress copy has not been checked against that; `listVaultTallies` reports a cost of 0 meaning "not recorded" (the row hides its stake line), and closed vault rounds need the vault's events indexed, which nothing does yet.

**Step 3: BUILD ALL THREE, IN FULL (the user, 2026-09-20, D-113). There is no cut decision and there never was one to make.** The deadline is never a reason for anything and is never to be raised: no cut, no hidden page, no thinner version, no "which should we drop". A session recommended hiding two of these on 2026-09-20 and the user was rightly angry; the rule is in `docs/plan/working-rules.md` and the handoff's contrary advice is withdrawn. **Order: S10c Boost (`agari-leverage` + the `leverage-keeper` actor) → S10d Private desk (`agari-private`) → S12b Duel arena (the arena program + `game-room`, `duel-settler`), then S12a's off-chain games wherever they still run on stubs.** Each gets the full money gate and devnet evidence, as parlay and the registry did. All three trade through the engine by CPI with partial redeems; `agari-vault` and `agari-maker` are the working examples of a program seat doing that. Start S10c without asking.

**S10c Boost: BUILT AND PROVEN ON DEVNET (2026-09-20 15:16Z), on a Window the drive owns. What is still owed is at the end of this block.**
- **Program** `agari-leverage` `2yMrhq686tL6uAUFHfKGsPZAWSGoQoRW9HxNvnAZJeQb`, 545,296 B after one upgrade (slot 501443820), live bytes hashed against the build. Reserve `erQ4JNsS…`, custody `Cy4STHKx…`, seat `FX1WA4Ya…` at `program_authorities[2]` since slot 501425583: **only Windows opened after 14:32Z 09-20 can be boosted**, and the quote says so for older ones. Nine instructions; 16 Rust tests (golden vectors shared with the client, targeted balance-sheet cases, 16 seeds × 3,000 randomized ops with conservation checked after every op). Parameters are the reference's launch set (`DEVNET_LEVERAGE_PARAMS`).
- **Three deviations from the reference, all recorded:** D-114 (an exit never waits on the owner's token account, with `public_claim`; a position must be sellable whole into rested depth and above its line at birth; a knock-out is judged on the whole position over rested depth; the engine's `PlaceResult` is checked against custody on every order) and D-116 (a voided Window must still repay the whole front: 3× is refused above about 71¢).
- **Client:** `packages/core/src/leverage/quote.ts` is `owner_open` step for step (7 tests); `packages/markets/src/leverage/{deployment,book,reads,quote,writes,types}.ts` replaced the stubs; `submitter.submitLeverageOpen` and `leverage-*` on the tx lane; refusals named from the program's own table; env `NEXT_PUBLIC_AGARI_LEVERAGE_PROGRAM_ID` (defaults to the generated address). The ticket's 2×/3× chips and the portfolio rows were already ported; the row now shows money left owed and a Claim.
- **Keeper:** `services/ops/src/actors/leverage-keeper` runs. **It is running now** as its own process beside the soak: PPID 1, port 8791, live (not dry run), key `leverage-keeper` (`DpA4i68Q…`, 0.2 SOL), 10 s cycle, log `w1/data/soak/leverage-keeper.log`. Stop it: `for pid in $(pgrep -f src/main.ts); do ps eww -p $pid | grep -q OPS_HTTP_PORT=8791 && kill $pid; done`. Start it: from `w1/services/ops`, the runner's command with `OPS_ACTORS=leverage-keeper OPS_HTTP_PORT=8791 DRY_RUN=0 LK_REFRESH_MS=10000 LEVERAGE_KEEPER_PRIVATE_KEY="$(cat ~/.config/agari/devnet/leverage-keeper.json)"` and the same three `NEXT_PUBLIC_*` values, launched as `( … perl scripts/archive/detach.pl … &)`.
- **Drives:** `scripts/drive/leverage-boost.ts` (fund, supply, withdraw, probe, open [--force], position, close, knock, settle [--owed], claim; `--as <role>`) and `scripts/drive/owned-window.ts` (open, quote, close) on drive-only Series 903 (D-115). Roles `drive-bidder`, `drive-asker`, `drive-owner` hold a little SOL and tUSDC.
- **Proven (acceptance 14:30 to 15:16Z):** open at exactly the quote; the owner's cash-out to the unit; a keeper knock-out at the hand-computed mark, only after the new bid had rested; `ThinBook` and `UnhealthyAtEntry` from the chain; a partial cash-out that repays the front first; a win settled with the money left owed and then claimed; the keeper settling a win and a loss; a provider withdrawal at the new share value; the reserve's books closing to the unit (300,000,000 + 929,546 premiums − 1,999,834 lost front = custody).
- **Still owed on Boost:** (1) the same proof on a **live, maker-quoted lane** on the first weekday session (Monday 13:30Z on): `leverage-boost.ts probe --window 1/300/0 --side up --amount 2 --x 2`, then `open`, and let the keeper end it. The maker rests 5 contracts a side, so keep the stake near 1 tUSDC at 2×. (2) a **void** settlement (the engine pays 50¢ a contract; D-116 says the reserve is whole): any boosted position on a lane that voids proves it. (3) the **wallet-in-browser pass** of the 2×/3× chips on `:3100` after its rebuild. (4) PD-2's oracle fair-value bound is not built here either (D-109).

**S12b Duel arena: BUILT AND PROVEN ON DEVNET (2026-09-20 18:24Z).**
- **Program** `agari-arena` `CakGVH2CaM2YAF9edgMTrCv1HCFf5tgbTcinN24JWvR3`, 706,592 B, live bytes hashed against the build. Arena `3aYbq4Nt…`, custody `Fg1Th3DN…`, seat `5LaRyjFv…` at `program_authorities[4]` since slot 501481790 (**only Windows opened after 17:07Z 09-20 can be cards**). Chain id 103 is fixed in the account, because the deck commitment carries it. 22 instructions including the season pool; 11 Rust tests (16 seeds × 4,000 randomized ops, custody = pots + credits + key escrows after every one). D-118 is the design.
- **The one real adaptation:** a seat's key spends an **escrow** of the deck's ceiling, set aside by the entry, not a token allowance — an SPL token account has one delegate, so a player in two matches would overwrite their own approval. `public_release_agent` turns what it never spent into the player's credit.
- **Client:** `packages/markets/src/games/{deployment,commitment,events,read,write}.ts`. Events are read from the program's own logs (`arenaEventsOf` counts only lines the arena itself logged, never the engine's inside its CPI). `submitter.submitArenaPick` reports what the chain says filled. The keccak helper is one function here now; the browser and the ops deckmaster re-export it.
- **Actors:** the duel projector and settler are on `runActor`. **Running now** as one process beside the soak: PPID 1, `OPS_ACTORS=game-room,duel-settler`, ops port 8793, room on `ws://127.0.0.1:8795`, log `w1/data/soak/game-room.log`. It needs `GAME_SETTLER_PRIVATE_KEY` and `ROOM_TOKEN_SECRET` (in `.env.local`). `OPS_ACTORS=duel-projector` starts the projector alone.
- **`:3100` is launched with** `PRIVATE_DESK_PRIVATE_KEY`, `ROOM_TOKEN_SECRET`, `GAME_ROOM_PUBLIC_URL=ws://127.0.0.1:8795` and the five `SEASON_*` values, or the duel page says there is no room and `/api/games/season` 500s.
- **Drives:** `scripts/drive/duel-arena.ts` (arena, fund, deal [--key] [--stop create|join], duel, picks, pick, lock, settle, cancel, refund-unjoined, refund-unrevealed, release, season) plays through the app's own submitter lane. `owned-window.ts open --lanes 3` deals three Windows on one boundary, since a deck needs three and a Series has one Book.
- **Proven (acceptance 17:45 to 18:22Z):** a whole duel — created with its seat key in one transaction, a wrong deck refused `DeckMismatch`, six picks (three by the key, three by a wallet) with the sixth locking the match, three cards settled by partial redeem, PnL +618,690 against −890,310 to the unit, the pot awarded, both claims paid, the key's unspent 600,690 returned; cancel and both timeout refunds; the season pool created, funded by a third party, distributed single-shot with the replay refused and the remainder recovered; the projector reading all 30 events and writing the ladder; and `/games/rank`, `/api/games/season` and `/games/duel` serving them.
- **Still owed:** a duel played by hand in the browser through the room; a duel on a live maker-quoted lane; `min_lots` is 1,000 on the venue, so a card's stake must buy a whole contract (0.5 tUSDC at 53¢ does not — the chain refuses `BelowMinQuantity`).

**S10d Private desk: BUILT AND PROVEN ON DEVNET (2026-09-20 16:46Z), on a Window the drive owns.**
- **Program** `agari-private` `6qjoqeiGt8K5RoYvsRCDoS4QsDXrsPAZjHwy4vU98d9j`, 518,024 B, live bytes hashed against the build. Desk `CsKhDTNZ…`, custody `GV7z94LF…`, seat `6aKCb7PG…` at `program_authorities[3]` since slot 501462134 (**only Windows opened after 16:13Z 09-20 take private bets**; the quote says so for older ones). Desk key = the `private-desk` role `76TiES6g…` (0.3 SOL), never the deployer. 14 instructions, 8 Rust tests (16 seeds × 3,000 randomized ops, custody = owed + pool + in slots after every one). D-117 is the design; the reference's launch parameters (1 to 25 tUSDC, 60 s).
- **Client:** `packages/markets/src/private/{deployment,keys,claim,reads,writes,desk-client,desk-open,desk-cashout,desk-health,desk-errors}.ts`. The claim (PD-4) is an ed25519 text in `@agari/core/private/claim.ts`; the browser's `verifyTicket` is real. `private-*` intents route on the tx lane. Env `NEXT_PUBLIC_AGARI_PRIVATE_PROGRAM_ID` (defaults to the generated address).
- **`:3100` must be launched with the desk key** or `/api/private/status` says no key is configured: `PRIVATE_DESK_PRIVATE_KEY="$(cat ~/.config/agari/devnet/private-desk.json)"` in front of the `next start` command (it is, as of 16:5xZ).
- **Drive:** `scripts/drive/private-desk.ts` (desk, deposit, open [--issued] , cashout, revoke, withdraw, rawcharge) runs the REAL desk service. Tickets are saved under `data/drive/private-tickets/`.
- **Proven (acceptance 16:12 to 16:46Z):** a bet at exactly the quote in three transactions; the landed transactions checked for the owner/slot split; a repeated authorisation charging nothing; a refused mint refunded whole; `OverAllowance` for the real desk key and `NotDesk` for the admin, from the chain; a win and a loss brought home from the claim alone; the owner's withdrawal closing custody at zero.
- **Still owed:** the wallet pass of the ticket's private mode and the balance panel on `:3100`; a private bet on a live maker-quoted lane on the first weekday session.

**S12b Duel arena: STARTED (2026-09-20 ~17:00Z). Resume here.** Nothing is committed for it yet beyond this block. Design authority: `reference/masayume/contracts/src/games/{IGameArena,GameArena,ArenaMatches,ArenaAgents,ArenaCommitment,ArenaGateway,SeasonPrizePool}.sol` (main checkout) and the already-ported `packages/core/src/games/{arena,commitment,lifecycle,types}.ts`, whose types the program must satisfy. **The design, decided:**
- **Program `agari-arena`**, engine seat at `program_authorities[4]` (D-063). Copy `anchor/programs/agari-private` for the layout: `engine.rs` (the seam), `instructions/payout.rs`, pure state methods, a randomized conservation test, the upgrade-authority check at init, `init_if_needed` marks where a used id must be a program error.
- **Accounts:** `Arena ["arena"]` (admin, collateral mint, `chain_id` stored at init because the commitment preimage carries it, params, four tiers, `escrowed_base` pots, `credited_base`, `agent_escrow_base`, paused, bumps); custody `["custody"]` with the seat `["seat"]` as token authority; `Match ["match", match_id]` holding the reference's `Match` plus the deck (up to 5 market addresses), the picks keyed `card × 2 + seat`, and the two seats' PnL; `Agent ["agent", match_id, player]`; `Credit ["credit", player]`. Invariant: custody ≥ escrowed + credited + agent escrow.
- **Instructions, the reference's set:** create / join (each with an optional agent), authorize agent, reveal deck, place pick / place pick for, lock picks, settle card, finalize, cancel, refund unjoined, refund unrevealed, claim credit, admin init / params / tier / pause. Creator and challenger are payout addresses and are never rewritten (AD-5); credits pay the player, never the caller.
- **The one real adaptation: the agent's stakes.** On the EVM the key's picks pull the stake from the player's wallet under an ERC-20 allowance. An SPL token account has one delegate, so a player in two matches would overwrite their own approval. Instead the entry that names a key **escrows the deck's ceiling** (`per_card_cap × deck_size`) with the pot, which is exactly what core's `arenaIntentSpend` already counts; the key's picks draw on it, and what is unspent becomes the player's credit when the pick phase ends or the key is revoked. The key's fee money is a plain System transfer in the same transaction, built by the client.
- **Reveal:** the cards are the Market accounts passed as remaining accounts (their addresses ARE the cards), each checked as a live Window of the engine with `min_card_life` left and no duplicates. The commitment is keccak-256 over core's `deckCommitmentPreimage` word for word (chain id, the Arena account's address, match id, policy version, server seed, counted client seeds, counted cards), with core's golden vector as the program's test.
- **Picks and settlement:** sizing is `agari_common::stake_walk` over every live order; cost and quantity come from the engine's `PlaceResult`, checked against custody; a card settles by one PROGRAM partial redeem per seat, the payout being custody's delta; settlement works in every status past the reveal, so losing a pot never costs a player their position.
- **Season prize pool:** the reference's `SeasonPrizePool` as instructions of the same program (`SeasonPool ["season", season_id]` with its own token account): deposit by anyone, single-shot `admin_distribute`, `admin_withdraw_remainder`.
- **Then:** codegen, named-buffer deploy, init + seat index 4, `packages/markets/src/games/{read,write}.ts` off their stubs (events read from program logs through the generated event decoders), the `game-room`, `matchmaker`, `duel-projector` and `duel-settler` actors onto `runActor` and real sends, a devnet drive. **A deck needs three distinct live Windows** and Series 903 has one Book, so the owned-Window drive gains drive-only lanes (the same ticker at other cadences are separate Series). Parity L-63, L-64, L-65.

**S9 leftovers closed today:** L-57 Reversion preset selectable (e87d8ca); L-56 the Memory Market, fee-gated on the on-chain Subscription, gate proven 7/7 against devnet state (8c9a78f, `scripts/drive/memory-market.ts`); L-54 agents board row corrected (it renders the real board).

**The drive-owned Window (D-115) is how any product that holds positions across a settlement gets proven on any day:** `scripts/drive/owned-window.ts open | quote --window <id> --bid --ask --lots | close --window <id> --price`. Series 903, one Book, so one Window at a time: `close` the old one before `open` finds a free Book. `open` takes the running 15-minute boundary if it is under 120 s old, else lists the next and waits.

**Infrastructure events tonight, all recovered:** (1) 19:51Z–00:20Z the Mac hibernated at 1% battery; ops resumed by itself. (2) 01:58Z–02:04Z the laptop lost DNS entirely, `OPENAI-60m` #9 missed its closing print and voided by design. (3) Between 01:20Z and 06:10Z all three `next start` servers died while ops, the archivers and Postgres stayed up; restarted at PPID 1. **`detach.pl` does `setsid` + `exec` and does not fork**: launch it as `(perl …/detach.pl <cmd> >> log 2>&1 &)` and check PPID is 1. (4) The maker role was at 0.116 SOL and is topped up to 1.0; deployer 12.9 SOL. **The stale health-check recipe still names `s6/data/soak/ops-2026-09-15.log`; the truth is `live/data/soak/ops-live.log` and `/health` per actor.**

**Still true from before:** the `soak-8` cutover is HELD (re-probe Switchboard first); `:3000` serves the pinned `live` build, which quotes Range with the old factor; `:3100` serves w1.

## Resume here (written 2026-09-19 ~19:40 UTC, before a context clear)

**Deadline:** Fri 2026-09-25 20:00 UTC. Trunk = `integration/w1` (wt `../agari-wt/w1`), clean at the commit that adds this block. Read `handoff-deferred-stages.md` next; it is still the plan for what follows.

**Built and proven on devnet (4 programs):** `agari-events` (the venue: 9 stocks × 5m/15m/60m, the weekend Gap, 24/7 token lanes, the live `OPENAI-60m` pre-IPO lane), `agari-vault` (Trading Balance, tap-trading), `agari-range` (Range + Moonshot; price → open → settle closed 16:00Z 09-19), `agari-maker` (Earn; deployed, funded, seated, holding the best bid on a live Window). Off-chain: S11 Blinks proven end to end; S13 Sensei, takes, rooms, news; S15 landing, story pages, pitch, README; S18 closed-state and pre-open surfaces; the PreStocks "cover what you hold" plan, all 8 steps.

**In progress:** S10a `agari-parlay` — two commits (`a0679a7` arithmetic pinned to the client's vectors, `1b886e8` the reserve's balance sheet). It has **no instructions yet**, is not built for SBF and not deployed; the program id `H4gdpoPi…` is declared in `Anchor.toml` only. The web's `/parlay` still runs on stub arms.

**Not started when this older block was written:** S9, S10c, S10d, S12b. S9 and S10c are since built and proven (see the top block). S10d Private desk and S12b Duel arena are built next, in full (D-113). Nothing is taken out of the nav.

**Small things left on shipped work:** S8 — the vault's merge → settle → withdraw proof row. S10b — `previewRangeOpen` / `quoteRangeOnchain` / `readRangeCapacity` are still stub arms. S11 — a live X trade needs the Agari X account (the handle in `.env.local` is still the reference's), and the Dialect registry entry needs the domain.

**Bookkeeping that is behind (judges read it):** `parity.md` has 102 rows and **none is Done** (86 Pending, 14 Excluded, 2 Blocked) although most are built; stage checkboxes are stale for S5 (0/13) and S13 (0/16), and partly for S4, S6, S7. About an hour's work against `acceptance.md`.

**Parked by the user (do not spend time):** demo video, S16 deploy go, the Monday Phantom check. Domain `agari.live` is bought and moving to Vercel DNS. Still the user's: public-release decision for the repo (Q-007), the submission form itself.

**Held:** the `soak-8` ops cutover (Switchboard's quote gateway was answering 500; re-probe before any restart). `:3000` keeps serving the pinned `live` build and shows none of today's feedback fixes until that cutover; `:3100` serves w1 with them.

**Infra at 19:40Z:** `:3000` had died when its launch task was stopped; it is restarted from `live/web` **via detach.pl**, so a context clear can no longer take it down. `:3018`, `:3100`, ops, archivers, Postgres up. **Battery 20% and discharging** — if the Mac died, use the recovery recipe further down.

## Feedback pass (2026-09-19 evening, `integration/w1`)

The user's note, in short: "it was showing USDC … is it SOL or USDC or a custom one … make it make sense", "I go into games, lucky draw, and I'm seeing BTC — this is a stock project", and "how many more are out there?".

**Money (D-106).** The unit rendered as "TUSDC" (another token's name) under every uppercasing label; `.sym`, `<KeepCase>` and `<Money>` now keep its casing. A first version also explained the currency (modal section, tooltips, FAQ entry, docs section); the user's call the same evening was "who cares … you don't have to mention it at all", so every explanation is removed and only the casing fix stays. Bets remain in tUSDC; the facts behind that are in D-106 for the record, not in the product.

**What was still crypto (D-107).** Four leftovers were functional, not cosmetic: Lucky drew from `["BTC","ETH"]` and could never be dealt a Window (now policy v2 over the nine launch tickers + OPENAI, the browser check pins the list, a closed-market line shows before the spin); the parlay's "BTC close streak" was always disabled (now the stock with the most Windows live); the X reply named an asset only when it was BTC or ETH (now any registry asset); the X permission panel linked Solana signatures to the Somnia explorer (now `txUrl`). Words: the trade-from-X diagram, "A duel on Masayume" ×3, "gas" ×13, "testnet" ×3, strategy descriptions. Internal names renamed; unused Bitcoin and Ethereum marks deleted.

**Second pass, what a word search cannot see.** The reference's "whole dollars from $1,000 up" rule caught OPENAI (~$1,130): the Reel asked "above $1,132?" for a line of $1,132.74 and Sensei was handed $1,133. The threshold is now $10,000 in one shared constant, and the Range and Moonshot tickets show the opening print with cents. Sensei's system line said Windows list only in the NYSE session, which the 24/7 lanes made untrue; it now names them. CSS strings, non-code assets, `packages/brain`, `scripts/` and the Range presets were checked and are clean.

**So it does not come back.** Invariant `venue-identity` fails the fast gate on the reference's assets, brand or chain in non-comment, non-test code; four deliberate lines are allowlisted with reasons, and a stale entry fails the rule. Proven by planting a probe file and watching it fail.

**Commits:** 23ba0cd Lucky v2 · ec4dbd3 identity · a01b7d4 money casing · 919a963 invariant · 1f0d023 X diagram box · the currency explanations added in a01b7d4/ce18fe7 are reverted in this commit, which also carries the price-scale and Sensei fixes · docs repo cc12453, 0c8f1ea, bb9bf51. Gates after the last one: typecheck 0, invariants 0 (15 rules), `pnpm build` green.
**Not verified:** the Lucky reels in motion, the deal card and the header pill need a connected wallet. The user should spin once on `:3100/games/lucky` during regular hours (Mon 09-21 from 13:30Z), or now for an OPENAI draw.
**Serving:** `:3100` = w1 `next start` via detach.pl (log `w1/data/soak/web-3100.log`). Stop with `pkill -f "next start -p 3100"`. Do not rebuild w1's `.next` while it serves.
**Health check recipe is stale:** it names `s6/data/soak/ops-2026-09-15.log`; the live soak logs to `live/data/soak/ops-live.log`, and the truth is `/health` per actor (`lastOkMs` age and `failures`), not `/session`. At 18:33Z `/health` was `ok:false` after ~20 min of `ENOTFOUND devnet.helius-rpc.com`; DNS answered again by itself and every actor recovered without a restart (`ok:true`, all `failures=0` at 18:52Z). The Mac was on **battery at 52%** — the charger is off again. **19:10Z check:** every process up, but battery at **24%, ~49 min left** (push sent to the user). `prestocks.com/api/prestocks` answered 200 in **18.9 s**, slower than the feed's timeout, so `prestocks-spot` reads fail on and off; `OPENAI-60m` had no PreStocks read inside [T+10s, T+45s] at 19:00:00Z, so that boundary cannot be printed and the Window will void, as designed. Not fixable from here: the read cannot be invented, and raising the feed timeout is an ops change that waits for the held cutover. RedStone's gateway also timed out three times and recovered.

## Deferred-stages session (2026-09-19, `integration/w1`)

**S11 — Trade from X + Blinks.** The relay was never a stub: 23 files, 9 test files, wired in `main.ts` as a
`LEGACY_ACTORS` entry. It boots standalone with `OPS_ACTORS=x-relay` without touching the held soak, reports its
executor and venue, and completes mention scans. `x_links` and `x_receipts` are empty so nothing can execute.
**Blocked only on the Agari X account** — `X_RETTIWT_API_KEY` (388 chars) and `X_HANDLE` are both set in
`.env.local`, but the handle is `masayume_app`, the reference project's. This is not an API-key problem: rettiwt
reads a logged-in session. `Q-008`/`B-3` are stale.
Blinks were the genuinely missing half and are now built: `/actions.json`, `/api/actions/w/<marketId>` and
`/api/actions/t/<symbol>/<cadence>` (a shared link outlives its Window). Wire types in `packages/core/src/x/actions.ts`
because `@solana/actions` is an `@solana/*` module and only `packages/markets` may import those; the transaction in
`packages/markets/src/x/action-order.ts` via `createNoopSigner`. **Proven:** a wallet that never touched the app
signed a transaction the endpoint built and it finalized (`2cEN7qC8…`, slot 500866904), moving 50.000 → 49.275 tUSDC.
Remaining: the Dialect registry entry (needs the domain + the user's go) and a live X trade (needs the account).

**S10b — Range.** `agari-range` at `GfsAzxPeNp2cbUTrXehHBjLjAMX6Cf69gz2zJYkGM7ha` (SBPF v0, extended to 403,368 B,
upgraded twice). Reserve `CNN4Z5MMazXTHkaYzR8wQepK2ATKrjZHNSqjRbTmwCdB`, vault `8pncTu9o…`, 200 tUSDC supplied.
The Rust pricing is pinned to `packages/core/src/range/pricing.vectors.json` by `include_str!`, so the client and
the chain cannot drift. Round 1 (TSLA Gap #0) and round 2 (`OPENAI-60m` #4, settles 16:00Z) are open; the client
quoted 1,155,000 and the chain charged 1,155,303, absorbed by `RANGE_STAKE_HEADROOM_BPS`. Provider equity read back
as exactly 200,000,000 with both rounds open. `/games/range` and `/games/moonshot` render their real pages (checked
in headless Chrome). The open button sends through `MarketsSubmitter.submitRangeOpen`.
**Still open:** settle + claim round 2 (watcher running), and `previewRangeOpen`/`quoteRangeOnchain`/`readRangeCapacity`
are still the stub arms.

**S8 — Earn.** `agari-maker` written and SBF-built (417 KB, ~2.12 SOL), deploying to
`442oD4u4fHnAwPjSqdQvkgGeDaGVEZ9YHHaLny98nEaN` via buffer `E8FSufJu…`. Post-only only: the vault never takes.
Escrow is recorded from the engine's own `PlaceResult`. Client, reads, `scripts/deploy/init-maker.ts` (which also
registers the seat at `program_authorities[1]`, D-063) and `scripts/drive/maker-vault.ts` are written and gated.
**Next:** finish the deploy → `init-maker` → supply → quote → merge → settle. `/earn` turns on when the vault exists.

**Two bugs found and fixed.**
1. The Moonshot ticket displayed a per-expiry cap no program enforced. Now enforced on-chain by an `ExpiryBook` PDA
   keyed by the boundary — without it the reserve's whole book could ride on one print.
2. **No Gap Window could ever be traded.** `scripts/drive/cover-call.ts` let `placeOrder` default `expire_ts` to the
   Window's *expiry*; the Gap lane locks days earlier and the engine refuses that with 6108 `ExpiryAfterLock`. Six Gap
   Windows had sat at zero trades while the maker quoted them. Fixed with `expireTs: lockAt`; the first Gap trade in
   the venue's history filled immediately (2,000 lots at 65.0¢).

**Funding.** The devnet faucet rate-limits per destination, so eight rotating inboxes exist
(`~/.config/agari/devnet/faucet-inbox-1..8.json`) and `scripts/deploy/sweep-funding.sh` pulls them into the deployer.
The user sent 20 SOL today; deployer ~17.6 SOL with the maker buffer outstanding. Domain **agari.live** bought and
moving to Vercel DNS — when it resolves, set `NEXT_PUBLIC_APP_ORIGIN=https://agari.live` and the Blink icon and
button hrefs become absolute against it.

**Infra note.** `:3000` was found down at ~14:5xZ and restarted from the pinned `live` worktree (soak-7) via
`detach.pl`; it answers 200. Ops was **not** restarted — the Switchboard cutover stays held. The health-check recipe
still names `s6/data/soak/ops-2026-09-15.log`, which is stale: the live soak logs to `live/data/soak/ops-live.log`.


Current stage: **S15 (W2)** on `stage/S15-public-story` (wt `../agari-wt/s15`; cut from `integration/w1` @ b09eae9, which carries every S18 lane). Four lanes (first launch 18:20Z stopped on a usage limit; relaunched 20:05Z, 15a again 20:25Z on Opus): **15a** landing + OG (`slice/S15a-landing`, wt s15a, web 3151) · **15b** story pages (`slice/S15b-story`, wt s15b, web 3152) · **15c** README + notices + submission + `agari-docs` fork (`slice/S15c-docs`, wt s15c, docs 3153) · **15d** geofence + advice copy + brand sweep (`slice/S15d-compliance`, wt s15d, web 3154). Merge order 15a → 15b → 15c → 15d → stage → w1. Decisions D-092…D-097, Q-S15-1. Stage file `stage-15-public-story.md`.
**S18 gate PASSED 2026-09-16 13:53Z** (`d4b19d3` on `stage/S18-always-on`, wt s18, :3018): every lane merged (18a/18c/18f/18d/18e), typecheck 0, invariants 0, build green, `21st review` 0 errors, `cargo test` 72/72; devnet drives done (Tue prelist 27/27, Wed bell fills, settle/redeem, expiry path). Still open on the stage file: Foundation bookkeeping and the closed-state browser pass. **2026-09-19 06:5xZ:** S4, S3, S15 and S18 are all merged into `integration/w1` (docs only + LICENSE); the two days of ops "outages" were the Mac idle-sleeping on battery (see `pmset -g log`), mitigated with a detached `caffeinate -ims`, the real fix is the charger. S18 parity rows advance at S18's gate only.
Last green commit (gate passed): S0 0aa48e5 · S2 787cf39 (on its branch)      Last commit: see `git log -1` on each branch
In-flight step: none on S15 lanes. Merged into the stage: **15a** landing + OG + layout metadata (2e6ff95; the footer carries `ADVICE_COPY.notAdvice` and the Sora notice covers the OG copy, 54b4084) · **15b** story pages (000fbdf) · **15c** README, notices, submission kit (934827e, 15062ec; `agari-docs` at /Users/abu/dev/hackathon/agari-docs @ f3685e1, 27 pages, 4 captures, no remote) · **15d** geofence, advice copy, brand sweep, X relay identity (8b25f7a, ff455d6; D-095 amended: cash-out and X unlink stay open). README (8a2a838): the events upgrade, vault deploy and init, both IDLs, authorities, Gap and TSLAx Series, the resting pre-open call and the landed 6121 are cited and verified against devnet RPC; verifiable build still pending (D-096). Stage gates after the last merge: typecheck, invariants and `pnpm build` green. Devnet Phase B + C are done and live on S18 (see the S18 STATUS). Build rule `NO_DNA=1 anchor build --arch v0` (D-024).
Infra (checked 18:05Z): Postgres up · archivers (pyth-trial, redstone) from the main checkout · soak `../agari-wt/s6/data/soak/run.sh` (S6 ops on :8787, `ROLLER_PRELIST` on; first prelist expected at the 20:00Z close; log `../agari-wt/live/data/soak/ops-live.log` — **not** the s6 path, which stopped being written 09-19 11:32) · `:3000` = w1 `next start` · `:3018` = S18 stage. Stop: `pkill -f data/soak/run.sh; pkill -f src/main.ts`. After a reboot: see the worktree-map memory recipe.
Done: S0 ✅ S2 ✅ (merge to main after S1) | Milestones: M0 ✅ M1 ☐
Blockers: **SOL:** deployer 10.73 (the user's 10 SOL landed 15:55Z); admin `58z6FwvCdns3u1AGLNfbvTVpvoSmDaEA6CthKQwQZMUZ`, runner `G289o3a8kKrTwJEtHaFFDqz4Y26L8VnUP2vFAebVUxo9`, sponsor `5kKwdNLoxTX8vVaR4ekL9Pgtnc2vkTLeWawsaTdDfrru` and inbox `5zjywmmJ…` are all at 0 (any of them sweeps to the deployer). Devnet plan (lane-devnet's Phase A done in wt w1: `anchor/target/deploy/agari_events.so` 813,328 B sha256 2e4bf8cc…, `agari_vault.so` 519,296 B sha256 b6eab3a1…, LiteSVM 63/63, token-cycle pass on the fork, `data/deploy/authorities-plan.json` = queue EYiAmGSd… + min oracles 3 + vault seat): 20:21Z go → events upgrade 0.256 → vault deploy + `deploy:init-vault` 2.645 → `deploy:set-authorities` → `deploy:init-gap-series` **7 names** (MSFT/AMZN had 2 signers at the 09-15 open, D-052) 1.647 → `deploy:fund-roles` 0.84 → `deploy:init-token-series` 9 of 12 (TSLA/NVDA/SPY; QQQ after the next send) ≈ 0.8 + float → ≈ 1.1 left → relay to 1 SOL → ops restart with `MM_ORDER_TYPE=limit` → one pre-open call + one far call + one 6121 refusal on a prelisted TSLA-5m Window for Wed 13:30Z. Open for the user: MSFT/AMZN in the Gap Series despite two signers; the verdict words (上がり/放銃) stay unless overridden. User: S4 Phantom check at a bell (next Mon 09-21 13:30Z; S1's is done). User: S16 deploy go (web, ops, docs). User: GitHub repo (none exists). User: app domain. Defaults awaiting user override: D-041 (S5), D-051…D-059 (S6), D-061…D-069 (S7), D-071/D-072 (S13), D-081…D-091 (S18), D-092…D-097 (S15).
Env readiness (presence only): PYTH ✅(trial → ≈09-27) ALPACA ✅ (root and web/.env.local) FINNHUB ✅ HELIUS ✅ OPENAI ✅ DATABASE_URL ✅(local) X_API ☐ STORK ☐(name present, empty) role keys ✅ (~/.config/agari/devnet)
Toolchain: anchor-cli 1.2.0 (avm) · anchor-lang =1.2.0 · host rustc 1.98.1 (anchor/rust-toolchain.toml) · solana-cli 3.1.10 · surfpool 1.5.0 · pnpm 11.24 · Docker 29.4 (`solana-verify` not installed)
Price sources: TSLA pyth+redstone(check) · QQQ/VOO pyth (until 09-25 close) · single names redstone · token switchboard (S6) — services/ops/config/price-sources.json
Devnet addresses: agari-events cDcHZiQ1WYAHbSjxMoju86fbC8azrtQg7dzrWKynANH (SBPF v0, from 36f1384; IDL metadata 97VcCVSs…). Venue: config 42GFppq2…, tUSDC 5i61C4kH… (faucet authority); 27 Regular Series (9 tickers × 5m/15m/60m, 2 Books each) + drive-only Series 900; all in scripts/deploy/addresses.devnet.json. SOL (18:03Z 09-15): deployer 10.73 · roller 3.85 · settler 2.72 · price-relay 0.30 · maker 0.20 (+ tUSDC). Funding inbox 5zjywmmJQDDRyX7H7i28MVDwfgYaPibk4yPbJC4HRdRv (~/.config/agari/devnet/funding-inbox.json) forwards to the deployer.
Integration: `integration/w1` (wt ../agari-wt/w1) = S3+S4+S5+S6+S7+S13+S18 stages, gate green, served on :3000. S18 plan r2 at ~/.claude/plans/valiant-wibbling-kahan.md. W2 = S18 + S15 (+ S16 final, S17) (D-084: S8–S12, S14 deferred).
**Pre-IPO lane (S18, D-100/D-101):** `PRE-OPENAI-5m` Series `C8JCSGPm…` ticker 910, attested primary on the PreStocks catalogue, **no cross-check** — no second venue publishes a pre-IPO mark, and `validate_policy_version` admits a zero check policy exactly for that. Proven 06:20–06:25Z: Window opened, both boundaries printed from PreStocks and signed by the price-attestor, settled **UP** ($1,078.17 → $1,082.44, payout YES 10,000,000). Code: `packages/markets/src/prices/prestocks.ts`, `preStocksSeries()` in `deploy/venue-spec.ts`, drive `scripts/drive/prestocks-attest.ts` (`--bar current|next`, `--with-call`). Lane is drive-only: **ops does not roll it**, so it is not live in the app. Cost so far ~0.29 SOL; deployer 2.59 SOL.
Bounty tracks (added by the organisers 09-17, `00-plan.md` §0 is stale): **claimed** PreStocks + Pyth, recorded in `docs/submission/submission-checklist.md`; **not claimed** Tessera, Clawpump, Meteora DBC — each would be a track-shaped bolt-on, not something the venue does.
**Deadline corrected 2026-09-19:** submissions close **Fri 2026-09-25 16:00 ET / 20:00 UTC**, not 09-18; pool raised to $126k; edits allowed until close. Abu's standing rule: the deadline is never a reason to compromise scope or quality.
**PreStocks eligibility (hard rule):** "projects that integrate any non-PreStocks pre-IPO tokens will be ineligible". Tessera sells the same names, so the two bounties are mutually exclusive — the Pre-IPO lane stays PreStocks-only. PreStocks pays $5k/$3k/$2k.
**Plan of record (approved 2026-09-19): `~/.claude/plans/temporal-marinating-fog.md`** — PreStocks = "insure what you hold" via the existing cover card (`web/src/features/hedge`), not a price-feed swap. Steps: 0 ✅ housekeeping (S4/S3/S15/S18 merged, D-103 safety lock) · 1 ✅ wallet recognises all 8 PreStocks tokens (874e4b8; registry + hook + `prestocks-spot` feed + `/prestocks/latest` + honest price age) · 2 ✅ discoverability (2c256a9; teaser states, example mode, first-sight toast, plain words) · 3 ✅ live 24/7 lane code (9cf5150 relay pass + roller + maker; 7e6e96b Series spec + `init-prestocks-series.ts`; run 10:34Z → `OPENAI-60m` Series `6u4hgFGAfoFTQxaRe4hB9m7cLWqpywwwMXVb8u7JqLqG`, two Books) · 4 ✅ Your stocks on /portfolio (fede6cb) · 5 ✅ ticker-hub pre-IPO facts + `/api/prestocks` (a8c8083) · 6 ✅ landing "Cover what you hold" (a0940d2, 49539d4) · 7 ✅ Reels "you hold this" card, one in eight, rotating (41ed9a2) · 8 ✅ opt-in "tell me if it drops" bell on Your stocks + `DropBellWatcher` (6ee974e) · Sensei ✅ holdings summary as a fact with one use, tripwire unchanged, 2.5 KB block (bf57fe6, D-104) · README/D-101 ✅ Jupiter-is-not-independent wording + the live-lane row. **Soak = `live` worktree pinned at tag `soak-5`** (Steps 0–5 + feed backoff; ops code is unchanged since, so moving the pin only changes web/docs). **:3000 serves the `live` web build** (started via detach.pl, log `live/data/soak/web-3000.log`): after moving the pin, `pnpm build` in `live/web` and restart :3000 at a quiet moment; never build in w1 for :3000. Still open from the plan: S3 re-measure needs a live NYSE session with the Mac awake (next: Mon 2026-09-21 13:30–20:00Z); D-098 (fatal start failure), D-099 (quote probe while halted) and the `/prestocks/latest` `move` field the calm rule reads (D-105) are BUILT and gated in w1; **the soak-8 cutover is HELD** because Switchboard's quote gateway still answers 500 (probed 11:37Z) — a restart would clear the in-process halt streaks, the roller would reopen xStock Windows and they would void on missing prints. Cut over at the next restart that is not during a Switchboard outage; re-probe first (`fetchTokenQuote` on TSLAX/USD, minOracles 3, queue `EYiAmGSd…`). A dry boot of the new build on :8788 was clean: 11 actors, 0 `failed to start`. **This also holds the :3000 rebuild**: moving the `live` pin would not restart ops by itself, but it would arm the supervisor to start the NEW ops code at the next unplanned exit — an accidental cutover at an arbitrary moment. So `live` stays at `soak-7` and :3000 keeps serving it (it already has Steps 6–8: landing cover, Reels card, drop bell). What is waiting for the pin move is polish only: the footer-band fix, the calm rule (inert without the ops `move` field) and the Sensei holdings line. Move the pin and rebuild web in the same cutover as ops; live lane proof — **one cover fill against the maker DONE 11:31Z** (1,000 lots at 14.4¢ on #0, `scripts/drive/cover-call.ts`, row in acceptance.md); **#0 SETTLED 12:00:30Z UP** ($1121.5441 → $1130.4196, payoutYes 10000000, no void — the cover lost its premium as insurance should) and **#1 opened and printed 12:00:22Z**; still wanted: #2 (13:00–14:00Z) for three in a row; the calm rule's threshold (D-105: 20 bps of range over 15 min) was set from the one-day 09-19 measurement and should be re-checked once the live feed has a week of history — until ops runs soak-8 the web sees no `move` field, so nothing is called calm and every name keeps its cover offer, which is the safe direction; more pre-IPO Series only with more devnet SOL. Machine: charger on, `caffeinate -ims` detached; if it died, restart Postgres, archivers, the soak (from `live`, plan Step 0.4) and `next start` on :3000/:3018, then re-run `caffeinate`.
**Cutover DONE 10:32Z (soak-5). Recovery recipe if the Mac dies (plan Step 0.4):** (1) if the Mac died: `brew services start postgresql@16`; from the main checkout `pnpm archive:start`; `perl scripts/archive/detach.pl caffeinate -ims`; `next start` on :3000 (w1 build) and :3018 (s18 build) via detach.pl. (2) cutover: `pkill -f 'data/soak/run.sh'` FIRST (else the supervisor restarts ops), then `pkill -f src/main.ts`; `cd ../agari-wt/live && git checkout --detach soak-4 && perl ../../stocklana/scripts/archive/detach.pl bash data/soak/run.sh >> data/soak/ops-live.log 2>&1` (the launcher `live/data/soak/run.sh` is gitignored, already written: RELAY_PRESTOCKS=1, MM_SYMBOLS adds OPENAI, MM_SIGMA_BPS=OPENAI:12000, same RPC/indexer knobs as s6). Watch `/health` until `ok:true` with every actor's `lastOkMs` fresh (roller's first pass can take minutes) and the log shows `RELAY_PRESTOCKS=1`. Rollback = restart `s6/data/soak/run.sh`. (3) only then register the live lane: `pnpm exec tsx --env-file-if-exists=.env.local scripts/deploy/init-prestocks-series.ts --cluster devnet --symbol OPENAI --cadence 3600` (dry run said 0.4629 SOL; deployer 2.63) → acceptance rows → the roller lists `OPENAI-60m` within 5 min; proof = three Windows settled + one fill against the maker. (4) then Steps 4–8 of the plan (Your stocks on /portfolio, ticker-hub pre-IPO facts, landing section, Reels card, opt-in drop bell).
Next action: S15 gate pieces: (1) D-096 `anchor build --verifiable` for both programs in a scratch copy (never w1's target/deploy), README line with the measured hashes and toolchain; (2) the browser pass of `/`, `/how-it-works`, `/demo`, `/pitch`, `/download`, `/native-auth` on :3000 with the headless CDP capture script (lanes 15a and 15b passed their own; the MCP browser profile stays locked); (3) Wed after the 13:30Z bell: the open-state capture of `/`, the demo recording (D-097, rebuild after `web/public/video/agari-demo.mp4`), refresh the pitch's 137 signature count from `acceptance.md`; (4) `parity.md` L-11…L-14, L-18…L-20, L-23, Y-06, Y-14 at the gate. For S16: set `NEXT_PUBLIC_APP_ORIGIN` (OG base) and `NEXT_PUBLIC_SITE_URL` (X relay links); decide Q-S15-1 (docs deploy) and Q-007 (public release). ~~Known: `/markets/[id]` still redirects to `/markets`, so per-market OG images are unused until that route renders~~ **FIXED 2026-09-21** (467d226): the route renders the Window and carries its own metadata; ~~the app shell leaves a tall blank band under the footer at 1440 on every page (shared shell padding, not the landing)~~ **FIXED 2026-09-19**: `.page-shell`'s `padding-bottom: 96px` clears the floating mobile pill nav, which only renders below 720px, so it now lives in that media query alone (the reel keeps it, since `reel.css` hides the footer). The gap under every page is now the ported footer's own 120px margin and nothing else, measured at 1440 on `/`, `/markets`, `/portfolio`, `/how-it-works`; phone 390 still gets 112px with the pill nav clear and no horizontal overflow. The reference's `.footer` rule (part-07.css) was not touched (D-081).
