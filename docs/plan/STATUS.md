# STATUS — updated 2026-09-15 ~21:08 UTC by Claude (devnet Phase B + C done and merged 6b937c1; :3018 rebuilt; Wed bell drive scheduled)

Current stage: **S18 (W2)** on `stage/S18-always-on` @ b4884f9 (wt `../agari-wt/s18`; served on **:3018**): 18c ✅ · **18a ✅ merged** 0cf0c41 (S18a.5–7; browser pass at 390/768/1024/1440 both themes, DOM assertions pass; D-087 amended: header chip from 1024) · **18f ✅ merged** bd1dc22 (S18f.2–9: rest/cancel lanes, ScheduleTicket + PriceControl + ScheduledCall, ListedCard, RestingRows, `resting-filled`, settler bond-only crank, the schedule seam); S18f.10 ✅ merged 0be2caa (the closed hero stays the asset hero while a Listed Window is selected) · **18d ✅ merged** d665c05 (S18d.2–8: `21st generate` is locked on this account, so three hand-drafted directions in Agari's tokens were mocked locally in `data/18d-directions/`; the user picked **V1 Ledger + tweaks** at ~16:20Z; `NewsRow` grammar on /news, activity and the hub, take chip mark + cashtag, `21st review` zero errors; D-082 amended) · **18e ✅ merged** 59e645c (S18e.1–5, run Tue instead of Thu: every deferred route renders an honest state with a next action; `21st review web/src` 25 → 2 errors, the 2 left are `components/ui/tabs.tsx`/`toast.tsx` `focus-outline-none`, untouched by rule; one closed line via `SESSION_COPY.sessionClosedLine`; `agari` query keys; verdict words 上がり/放銃; six dead files removed). Remaining S18 boxes: devnet drives, browser pass, gate. Full vitest 1423/1423 on the stage. **S7d** (`Grant.market`, D-091, 7115) merged S7 262ef71 → w1 9adaf66 → S18 c30a92f. W1 stages S3 26a7f00 · S4 313f591 · S5 ffb1bfa · S6 2931695 · S7 e1cfe70 · S13 bf6eaff are all merged in `integration/w1` @ 39ffcf3 and in S18. 6a's roller prelist + maker knob (D-089/D-090) and 6b's program rule 6121 (D-088) are in S6 → w1 → S18; 6b's token-cycle drive is a8cc127 on `slice/S6b-token` (not yet merged to S6). S1 gate: user Phantom check. S2 ✅. Deadline: Fri 2026-09-18 20:00Z (16:00 ET).
Last green commit (gate passed): S0 0aa48e5 · S2 787cf39 (on its branch)      Last commit: see `git log -1` on each branch
In-flight step: lane agents 18a and 18f (their worktrees; merge order 18a → 18c ✅ → 18f → 18d → 18e into the stage, then `integration/w1`). Lanes add one append-only export line per own module to package index files (stage-owner approval). Build rule `NO_DNA=1 anchor build --arch v0` (D-024).
Machine rebooted ~14:27Z on 09-15; recovered 15:10–15:35Z: Postgres (`brew services` postgresql@16; a stale `postmaster.pid` from before the reboot had to be removed) · archivers `pnpm archive:start` from the **main checkout** (`data/archive/` lives there; Pyth backfill is 429-limited and retries) · soak `../agari-wt/s6/data/soak/run.sh` (**S6 stage** ops on :8787, detached via `scripts/archive/detach.pl`; log `data/soak/ops-2026-09-15.log`; `ROLLER_PRELIST` defaults on, so the first prelist is expected at the 20:00Z close — `roller-plan --at 2026-09-15T20:05:00Z` shows 21 Regular lanes prelisting and 6 waiting on a free Book; roller 3.85 SOL) · `:3000` = `integration/w1` production build (`next start`, log `../agari-wt/w1/data/web-3000.log`). Stop: `pkill -f data/soak/run.sh; pkill -f src/main.ts`.
Done: S0 ✅ S2 ✅ (merge to main after S1) | Milestones: M0 ✅ M1 ☐
Blockers: SOL: the user sent 10 SOL (deployer 10.73 at 15:55Z); the devnet lane (`lane-devnet`, wt w1) finished Phase A: events.so 813,328 B sha 2e4bf8cc… (6b's measured binary), vault.so 519,296 B, LiteSVM 63/63, token-cycle full pass on the fork (pre-open rests, 6121, print refusals), `drive:events` could not run on a fork of the live venue (roller collisions, not the binary; waived with the 63 suites), authorities payload = 3 fields (queue, min oracles 3, vault seat), w1 gained S7.11–13 deploy scripts (`deploy:set-authorities`, `deploy:init-vault`). SOL plan: upgrade 0.256 kept → vault 2.645 kept → Gap **7 names** (MSFT/AMZN had 2 signers at the 09-15 open, D-052) 1.647 → fund-roles 0.84 → token Series **9 of 12** (TSLA/NVDA/SPY; QQQ after the user's next send, ≈ 0.8 + float) → ≈ 1.1 left. It gets the go at 20:21Z for the events upgrade → vault deploy + init → authorities (vault seat + queue + min oracles 3) → 9 Gap Series → 12 token Series if SOL allows → relay to 1 SOL → ops restart with `MM_ORDER_TYPE=limit` → one pre-open call + one far call + one 6121 refusal on a prelisted TSLA-5m Window for Wed 13:30Z (a day earlier than the plan's Wed/Thu drive). The token Series need ≈ 5 more SOL: the user has three more inbox addresses (admin 58z6…, runner G289…, sponsor 5kKw…). User: news/take direction pick (after 18d generates). User: S16 deploy go. Defaults awaiting user override: D-041 (S5), D-051…D-059 (S6), D-061…D-069 (S7), D-071/D-072 (S13), D-081…D-091 (S18).
Env readiness (presence only): PYTH ✅(trial → ≈09-27) ALPACA ✅ (root and web/.env.local) FINNHUB ✅ HELIUS ✅ OPENAI ✅ DATABASE_URL ✅(local) X_API ☐ STORK ☐(name present, empty) role keys ✅ (~/.config/agari/devnet)
Toolchain: anchor-cli 1.2.0 (avm) · anchor-lang =1.2.0 · host rustc 1.98.1 (anchor/rust-toolchain.toml) · solana-cli 3.1.10 · surfpool 1.5.0 · pnpm 11.24
Price sources: TSLA pyth+redstone(check) · QQQ/VOO pyth (until 09-25 close) · single names redstone · token switchboard (S6) — services/ops/config/price-sources.json
Devnet addresses: agari-events cDcHZiQ1WYAHbSjxMoju86fbC8azrtQg7dzrWKynANH (SBPF v0, from 36f1384; IDL metadata 97VcCVSs…). Venue: config 42GFppq2…, tUSDC 5i61C4kH… (faucet authority); 27 Regular Series (9 tickers × 5m/15m/60m, 2 Books each) + drive-only Series 900; all in scripts/deploy/addresses.devnet.json. SOL (15:10Z 09-15): deployer 0.73 · roller 3.85 · settler 2.72 · price-relay 0.30 · maker 0.20 (+ tUSDC). Funding inbox 5zjywmmJQDDRyX7H7i28MVDwfgYaPibk4yPbJC4HRdRv (~/.config/agari/devnet/funding-inbox.json) forwards to the deployer.
Integration: `integration/w1` (wt ../agari-wt/w1) = S3+S4+S5+S6+S7+S13 stages, gate green, served on :3000. S18 plan r2 at ~/.claude/plans/valiant-wibbling-kahan.md; decisions D-081…D-091. W2 = S18 (D-084: S8–S12, S14 deferred).
Next action: **Switchboard still down at 09-17 13:45:29Z** (500; 31 h 55 m). No restart, per D-099; README softened on S15 (token row + limitation). **New gap:** `/markets` shows the token tabs with 0 Windows and no reason — the words paused/halted/unavailable appear nowhere on the page, though `/session` carries the halt reason per lane. Smallest fix is one copy line in the lane empty state (18a surface); not made, deadline day, needs the user's go. **`main` still blocked (S1–S4 gate rule).** S3 re-measured on the clean Wed session and **still fails two criteria**: indexer lag p95 13.32 s with 16.4% over 10 s (real measurement from `idx_txs`, since the logged `lag` field is frozen while the WebSocket is down; cause is `INDEXER_RPS=2` under 10,367 tx/session, **not** the WebSocket — my earlier attribution was wrong), and the RedStone 60 s archive bound at 94.9% (78/78 boundaries covered; four boundaries land one 60 s poll late). Everything else passes: 0 overlaps, 988 resolved + 2 voided with none left open, no actor failure. Fixes are ops tuning (raise `INDEXER_RPS` or batch the walk; shorten the archiver cadence or restate the bound). S4 still needs the user's Phantom check at the Thu bell. **Wed close ✅ (20:04–20:05Z):** 7/7 Gap Series listed their first Window (09-18 20:00Z → 09-21 13:30Z, the first Gap Windows on devnet) and 27/27 Regular Series prelisted Thursday's first Window; all 34 signatures confirmed, no actor failure. Balances: roller **2.138 SOL** (holding float for 7 Gap Windows + 27 prelists over the weekend — watch it), settler 1.664, deployer 2.875. **Open defect:** `recycle AMZN-15m #51` failed to send `public_release_book` for Book `BJSpqvN5…` with no later release, so that Book stays bound and out of the recycle pool; the lane continued on its second Book. Next: S3 gate re-measure 20:25Z; Thu 13:45Z third Switchboard probe; Thu bell for the S4 gate's wallet states (user). **Switchboard still down at 19:20:13Z** (500 in 4.07 s single, 0.20 s batched; 13 h 30 m out). No restart, per D-099; token lanes end the day paused with 0 Windows open, Regular lanes healthy all session. Third probe Thu 09-17 13:45Z after the bell; if it fails too, record the outage in the README and demo as upstream, not ours. **Switchboard still down at 16:50:20Z** (probe: 500 in 3.36 s, single and batched; 11 h 0 m since 05:50:16Z). No restart, per D-099. Token lanes stay paused, 0 Windows open; Regular lanes healthy. Next probe 19:20Z; if it passes, `pkill -f 'src/main.ts'` (supervisor relaunches with `MM_ORDER_TYPE=limit`) and check the next token Windows settle rather than void. **Parity after the S18 gate: nothing to advance.** S18 owns no ledger rows; L-17's Class is already `Adapted` and its Status belongs to S13's gate. The surfaces S18 built advance under S5 (L-40), S6 (L-29, L-32), S13 (L-17, L-43), S15 (Y-14) and S4 (L-08), citing S18's rows. Ledger unchanged: 86 Pending / 14 Excluded / 2 Blocked. **Expiry path proven 09-16 15:05Z** (drive `--only far --far-cents 5` on TSLA-5m #80, w1 6d32577 added the flags): rested 1,000 lots at 50 YES ticks while Listed (`4DWdejiP…`, PostOnlyRested, expire 14:51:30Z) → nothing crossed it → swept by `public_sweep_expired` at the **lock** 14:55:00Z (`5pgxWur5…`, not at the expiry instant) → #80 settled UP 14:57:07Z (`49Df39ya…`) → crank `redeem_for` 15:05Z (`2DNtx5uo…`) returned **300,000 base = 50,000 escrow + 250,000 bond**, payout 0, 0 fills, no second signature from the wallet. Every branch of D-088 now has devnet evidence; the S18 drives box has no open caveat. **S18 gate PASSED 2026-09-16 13:53Z** (d4b19d3): typecheck 0 · invariants 0 · build green and `:3018` restarted on the gate commit (the stale `/app/bet-screen.png` 500 is gone) · `21st review` 0 errors on the touched path · `cargo test` 72 passed / 0 failed incl. `events_preopen` · closed and open DOM assertions and the 2-min network counts in the browser-pass audit. **Drive evidence complete except one path:** rest-before-open → fill at the bell at the resting price → settle DOWN on the signed close with the RedStone check → crank redeem → ledger close are all on chain (rows through d4b19d3). The **expiry → sweep → credit** path is still unproven because today's far call filled; a follow-up drive is scheduled for **14:40Z** on a Listed Window ahead of the session clock, resting a far UP that nothing will cross. **Wed bell result (13:30Z, PASS):** both pre-open calls filled 23 s after the open, in the seat maker's first limit ask `5R2MfNsP…` (13:30:23Z, slot 499,313,376): `6sxySwvN…` 1,000 lots at **550** (its resting price) and `BATWbCtz…` 1,000 lots at **200**, both `remaining 0`, 67 s before the 13:31:30Z expiry. Open print (Pyth) `2waxKv6J…` 13:30:19Z; RedStone `checkOpen ✓` `DswMMChm…` 13:30:36Z. Rows committed. **Note for the 13:48Z settlement check:** the far call did NOT expire, so there is no sweep → credit → crank-redeem evidence from #64; look instead for the settler resolving #64 on the Pyth close with the RedStone check, then `redeem_for` or a claim on BOTH filled positions (UP at 550 and UP at 200). A second Window is needed for the expiry path. **Maker note:** its first post-open fair read 20 ticks before requoting at 330, which is why an ask at 50 crossed both bids; worth a look with the token-halt diagnosis. Open-state browser pass done (0b4b863): 6 captures, live price, Open chip, lanes incl. Gap and token, no LOADING; one defect, `:3018` serves a stale build that still requests the deleted `/app/bet-screen.png` (500) — the gate's rebuild clears it. **[CORRECTED 14:40Z — see the stage file's Findings and D-099; the Jupiter attribution below was wrong] Token lanes deadlocked since 05:50:59Z:** Switchboard's gateway has returned 500 (`fetchSignaturesConsensus`) since 05:50:16Z and still did at 14:36Z; 3 failed quotes halt an issuer, and the halt cannot clear because clearing needs a successful quote, a quote needs a due slot, and a paused lane opens no Windows. 529 min halted, 0 token Windows open. Do **not** restart ops during the outage (it would reopen Windows that void on missing prints and burn roller float); restart after Switchboard recovers. Token lanes 09-16: 54 resolved / 302 voided vs Regular 150 / 2. Superseded note follows: **Token lanes paused since 05:50:59Z (09-16, not a bell risk):** halt-watch flags all four xStock issuers `quote-unavailable`, so all 12 token lanes read `paused: halted (quote-unavailable)` and no token Window has rolled since 11:51:28Z (hence no venue tx for ~80 min and a frozen indexer cursor, which is correct: cursor 499,277,591 == the newest venue signature). Cause: the `xstock-spot` actor logs `Jupiter Price v3 failed: The operation was aborted due to timeout` repeatedly; ops boots with `JUPITER_API_KEY not set: polling keyless`. Not a provider or DNS fault: a probe at 13:14Z got 200 in 1.09 s from `lite-api.jup.ag` and 200 in 1.29 s from `api.jup.ag` for TSLAx (usdPrice 357.04). Regular lanes are unaffected throughout (Pyth TSLA 1 s old, session not halted, TSLA-5m #64 listed for 13:30Z). Diagnosis scheduled 14:20Z, after the bell and settlement checks; no ops change before the S18 gate and the S4 drive. **Devnet WebSocket churn (09-16, from ~05:52Z):** the public `wss://api.devnet.solana.com` log subscription drops continuously (130 drops in 10 min) and the indexer falls back to backfill-from-cursor, logging `reconnecting · walk …` instead of `connected · …`. It keeps up: cursor 499,147,556 → 499,196,047 over that window, txs 15,488 → 15,643, `lag 1.5 s`, `/health` `failures: 0` with `lastOk` seconds old. **No change before the deadline:** pointing `SOLANA_WS_URL` at the Helius devnet WS (run.sh line 5) would cut the churn but needs another supervisor relaunch, and the index is current as it is; revisit with D-098 after Friday. The pre-bell and bell checks treat `reconnecting` as healthy while the cursor advances and lag stays under 10 s. **Overnight incident 09-16 03:09–03:18Z (resolved):** the ops watchdog exited on a stuck seed-maker pass (`exiting: pass stuck over 10 min in seed-maker`, code 70) and the supervisor restarted ops at 03:14:24Z; on that boot the **indexer** failed to start with `write CONNECT_TIMEOUT localhost:5432` and stayed silent while every other actor logged normally (Postgres never went down: 12 h uptime, direct query fine). Killed `src/main.ts` at 03:17:28Z; the supervisor restarted ops and the indexer reconnected with 0.5–1.8 s lag. 46 lanes, TSLA-5m #64 still listed for the bell, both resting pre-open calls untouched. This is the second D-098 case (the first was the roller at 19:09Z), so the pre-bell check now also verifies the indexer is streaming and lanes = 46. S3 gate **not passed** (agent s3-gate, stage/S3-venue-ops bcfc7ec): passing are Series + rent, zero overlaps over 551 Windows (481 resolved, 70 MissingPrint voids, 0 open), `verify-index` over 10,607 txs with 0 gaps/dupes, sources match the config, TSLA cross-check 0.72 bps median / 3.03 max, 0 leftover `PriceUpdateV2`, and the post-09-25 dry run (TSLA on RedStone v2, QQQ/VOO paused). Failing are indexer lag (p50 1.5 s, p95 3.5 s, **max 26.5 s** against the 10 s bound) and the RedStone 60 s archive bound (553/553 rows, only 322 within 60 s), both traced to the 14:27Z reboot and the DNS outage, not venue logic; grid coverage was 548/990 Windows for the same reason. Re-measure over the clean Wed 13:30–20:00Z session is scheduled for **20:25Z**. D-098 records the ops bug behind it (an actor that fails to start does not exit ops, so the supervisor never restarts it; recorded, not built before the deadline). `main` still waits on the S3 and S4 gates. Token lane complete (09-15 22:23–22:28Z): the user sent 5 SOL to the funding inbox, swept to the deployer (3ovqG57E…), and NVDAx, SPYx and QQQx were registered at 5m/15m/60m with two Books each (9 Series, 18 Books, 4.166 SOL; 29 acceptance rows, w1 a6f0696 → S18 b53a4f1). The soak relists Series every 5 min and listed all 46 at 22:28Z with no restart; the first NVDAx/SPYx/QQQx Windows opened 22:30Z. The seed maker quotes only `MM_SYMBOLS=TSLA,NVDA,AAPL`, so SPYx and QQQx list without maker quotes (adding SPY,QQQ also quotes the QQQ Regular lanes and needs a supervisor relaunch). Deployer 2.878 SOL. **Devnet Phase B + C done** (lane-devnet-r2, w1 30275de, merged into S18 6b937c1, 245 acceptance rows): events upgraded in place (813,328 B, dump sha256 2e4bf8cc… = local), agari-vault deployed at 84puRVxG (519,296 B, dump b6eab3a1… = local) and initialised (VaultConfig FEhH7ACA, seat H767WASS), authorities = vault seat + Switchboard queue EYiAmGSd… + min oracles 3, 7 Gap Series (TSLA NVDA AAPL META GOOGL QQQ VOO; MSFT/AMZN left out, D-052), fund-roles (1.98 SOL: attestor and sponsor floats were missing from the plan), token Series TSLAx only (NVDA/SPY skipped for SOL), both IDLs (events 97VcCVSs, vault 54LqFBmc), relay 1 SOL; deployer 2.044 SOL. Soak relaunched 21:00Z (supervisor pid 54468) with `MM_ORDER_TYPE=limit`; 37 lanes; 27 Wed Windows still listed. Pre-open drive on TSLA-5m #64 (market 4vebwzyb…): UP 55¢ ×1,000 resting (36owo367), UP 20¢ ×1,000 resting (Q65BQ3ss), both expiring 13:31:30Z; IOC taker landed failed with 6121 (5mjWBsNe). Users' keys in the session scratchpad `devnet/`. :3018 rebuilt from 6b937c1 at 21:06Z (includes the /reels fix 0157529). Scheduled one-shots (this session): **13:12Z** pre-bell readiness, **13:33Z** fill check + rows + open-state browser pass, **13:48Z** settlement, sweep, credit and crank-redeem rows, then the S18 gate. Known: halt-watch logs `issuer TSLAx: TypeError` at boot (13 times on 09-15, recovers within a minute); the seed maker quotes TSLAx 24/7 now; a 15 s coordinator poll on the selected Listed Window (S17). S15 runs in parallel on `stage/S15-public-story` (see its STATUS).
