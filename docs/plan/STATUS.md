# STATUS — updated 2026-09-15 ~17:20 UTC by Claude (18a + 18f merged; 18d implementing V1; devnet Phase A done, go at 20:21Z)

Current stage: **S18 (W2)** on `stage/S18-always-on` @ b4884f9 (wt `../agari-wt/s18`; served on **:3018**): 18c ✅ · **18a ✅ merged** 0cf0c41 (S18a.5–7; browser pass at 390/768/1024/1440 both themes, DOM assertions pass; D-087 amended: header chip from 1024) · **18f ✅ merged** bd1dc22 (S18f.2–9: rest/cancel lanes, ScheduleTicket + PriceControl + ScheduledCall, ListedCard, RestingRows, `resting-filled`, settler bond-only crank, the schedule seam); S18f.10 in flight on its branch (the closed hero stays the asset hero while a Listed Window is selected — needed before tonight's prelist) · **18d** implementing on `slice/S18d-editorial` (wt s18d): `21st generate` is locked on this account (AI subscription), so three hand-drafted directions were mocked in Agari's tokens (`data/18d-directions/`, local); the user picked **V1 Ledger + tweaks** at ~17:05Z; S18d.4–7 in flight (news, activity + hub rows, take chip, docs) · 18e Thursday. **S7d** (`Grant.market`, D-091, 7115) merged S7 262ef71 → w1 9adaf66 → S18 c30a92f. W1 stages S3 26a7f00 · S4 313f591 · S5 ffb1bfa · S6 2931695 · S7 e1cfe70 · S13 bf6eaff are all merged in `integration/w1` @ 39ffcf3 and in S18. 6a's roller prelist + maker knob (D-089/D-090) and 6b's program rule 6121 (D-088) are in S6 → w1 → S18; 6b's token-cycle drive is a8cc127 on `slice/S6b-token` (not yet merged to S6). S1 gate: user Phantom check. S2 ✅. Deadline: Fri 2026-09-18 20:00Z (16:00 ET).
Last green commit (gate passed): S0 0aa48e5 · S2 787cf39 (on its branch)      Last commit: see `git log -1` on each branch
In-flight step: lane agents 18a and 18f (their worktrees; merge order 18a → 18c ✅ → 18f → 18d → 18e into the stage, then `integration/w1`). Lanes add one append-only export line per own module to package index files (stage-owner approval). Build rule `NO_DNA=1 anchor build --arch v0` (D-024).
Machine rebooted ~14:27Z on 09-15; recovered 15:10–15:35Z: Postgres (`brew services` postgresql@16; a stale `postmaster.pid` from before the reboot had to be removed) · archivers `pnpm archive:start` from the **main checkout** (`data/archive/` lives there; Pyth backfill is 429-limited and retries) · soak `../agari-wt/s6/data/soak/run.sh` (**S6 stage** ops on :8787, detached via `scripts/archive/detach.pl`; log `data/soak/ops-2026-09-15.log`; `ROLLER_PRELIST` defaults on, so the first prelist is expected at the 20:00Z close — `roller-plan --at 2026-09-15T20:05:00Z` shows 21 Regular lanes prelisting and 6 waiting on a free Book; roller 3.85 SOL) · `:3000` = `integration/w1` production build (`next start`, log `../agari-wt/w1/data/web-3000.log`). Stop: `pkill -f data/soak/run.sh; pkill -f src/main.ts`.
Done: S0 ✅ S2 ✅ (merge to main after S1) | Milestones: M0 ✅ M1 ☐
Blockers: SOL: the user sent 10 SOL (deployer 10.73 at 15:55Z); the devnet lane (`lane-devnet`, wt w1) finished Phase A: events.so 813,328 B sha 2e4bf8cc… (6b's measured binary), vault.so 519,296 B, LiteSVM 63/63, token-cycle full pass on the fork (pre-open rests, 6121, print refusals), `drive:events` could not run on a fork of the live venue (roller collisions, not the binary; waived with the 63 suites), authorities payload = 3 fields (queue, min oracles 3, vault seat), w1 gained S7.11–13 deploy scripts (`deploy:set-authorities`, `deploy:init-vault`). SOL plan: upgrade 0.256 kept → vault 2.645 kept → Gap **7 names** (MSFT/AMZN had 2 signers at the 09-15 open, D-052) 1.647 → fund-roles 0.84 → token Series **9 of 12** (TSLA/NVDA/SPY; QQQ after the user's next send, ≈ 0.8 + float) → ≈ 1.1 left. It gets the go at 20:21Z for the events upgrade → vault deploy + init → authorities (vault seat + queue + min oracles 3) → 9 Gap Series → 12 token Series if SOL allows → relay to 1 SOL → ops restart with `MM_ORDER_TYPE=limit` → one pre-open call + one far call + one 6121 refusal on a prelisted TSLA-5m Window for Wed 13:30Z (a day earlier than the plan's Wed/Thu drive). The token Series need ≈ 5 more SOL: the user has three more inbox addresses (admin 58z6…, runner G289…, sponsor 5kKw…). User: S1 Phantom check. User: news/take direction pick (after 18d generates). User: S16 deploy go. Defaults awaiting user override: D-041 (S5), D-051…D-059 (S6), D-061…D-069 (S7), D-071/D-072 (S13), D-081…D-091 (S18).
Env readiness (presence only): PYTH ✅(trial → ≈09-27) ALPACA ✅ (root and web/.env.local) FINNHUB ✅ HELIUS ✅ OPENAI ✅ DATABASE_URL ✅(local) X_API ☐ STORK ☐(name present, empty) role keys ✅ (~/.config/agari/devnet)
Toolchain: anchor-cli 1.2.0 (avm) · anchor-lang =1.2.0 · host rustc 1.98.1 (anchor/rust-toolchain.toml) · solana-cli 3.1.10 · surfpool 1.5.0 · pnpm 11.24
Price sources: TSLA pyth+redstone(check) · QQQ/VOO pyth (until 09-25 close) · single names redstone · token switchboard (S6) — services/ops/config/price-sources.json
Devnet addresses: agari-events cDcHZiQ1WYAHbSjxMoju86fbC8azrtQg7dzrWKynANH (SBPF v0, from 36f1384; IDL metadata 97VcCVSs…). Venue: config 42GFppq2…, tUSDC 5i61C4kH… (faucet authority); 27 Regular Series (9 tickers × 5m/15m/60m, 2 Books each) + drive-only Series 900; all in scripts/deploy/addresses.devnet.json. SOL (15:10Z 09-15): deployer 0.73 · roller 3.85 · settler 2.72 · price-relay 0.30 · maker 0.20 (+ tUSDC). Funding inbox 5zjywmmJQDDRyX7H7i28MVDwfgYaPibk4yPbJC4HRdRv (~/.config/agari/devnet/funding-inbox.json) forwards to the deployer.
Integration: `integration/w1` (wt ../agari-wt/w1) = S3+S4+S5+S6+S7+S13 stages, gate green, served on :3000. S18 plan r2 at ~/.claude/plans/valiant-wibbling-kahan.md; decisions D-081…D-091. W2 = S18 (D-084: S8–S12, S14 deferred).
Next action: merge 18f → S18 when S18f.9 reports (full gate, rebuild :3018); pick the 18d direction with the user; 20:06Z prelist rows; watch `../agari-wt/s6/data/soak/ops-2026-09-15.log` for `prelisting` after 20:00Z and record the prelist signatures in `acceptance.md`; then merge S18 → w1, rebuild :3000. 18d once `21st login` lands; 18e Thursday.
