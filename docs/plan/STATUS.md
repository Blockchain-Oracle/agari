# STATUS — updated 2026-09-20 ~06:45 UTC by Claude (**Four things landed overnight on `integration/w1`: `agari-parlay` live with tickets opened, resolved and refunded on devnet; two bugs in the deployed `agari-range` fixed and upgraded; Earn's merge, settle and withdraw all proven; and S9 built end to end, with `agari-strategy` live and the runner placing real orders for a subscriber.** Clean and pushed. A strategy runner process is running beside the soak on port 8790. NEXT = parlay's full win and claim Monday (`settle --ticket 1`, `--ticket 5` after 13:30Z, and a fresh 5m pair); a hand pass of `/parlay`, `/strategies`, Range and Moonshot with a wallet on `:3100`; the user's cut decision on S10c/S10d/S12b.)

## Session of 2026-09-19 evening → 09-20 (parlay, range fixes, Earn proof) — READ THIS FIRST

**Machine:** the Mac hit 1% and hibernated 19:51Z → 00:20Z, then woke on AC power. Every process survived (ops, archivers, Postgres, `:3000`, `:3018`). Ops recovered by itself: `/health` `ok:true` at 00:31Z, `prestocks-spot` had only been in its 300 s backoff on `ENOTFOUND` (DNS drops on wake). No restart was done and the `soak-8` cutover is still held. **Keep the charger in.**

**S10a `agari-parlay` is LIVE on devnet.** Program `H4gdpoPirbtHP6hNdiQRLwfifjshdgDamYuvrGxj2ZCC` (370,776 B, slot 501118691, live bytes hash to the build), reserve `32y5yJ2T…`, vault `2wyokJ4r…`, 100 tUSDC supplied. **Ticket 1 is open** (`a2u3SZzz…`: TSLA Gap #0 UP + NVDA Gap #0 DOWN, payout 4 tUSDC): worked by hand, quoted by the client and booked by the chain at the same 1,892,800. Its Windows close **Mon 09-21 13:30Z**. Commits `26ad3e7` program + 26 Rust tests · `67393e3` client · `e2f5614` deploy module · `bb73158` reads · `143e35b` rested-book quote · `613c6e6` writes · `cbea276` page · `707c856` drive · `bc509ee` D-108…D-111. `/parlay` renders its real page on `:3100`.
- **Still owed for S10a:** a leg resolved and a ticket claimed on devnet. Do it Monday in the regular session, when 5m Windows give the full cycle in minutes: `pnpm exec tsx --env-file-if-exists=.env.local scripts/drive/parlay-ticket.ts open --cluster devnet --legs 1/300/0:up,2/300/0:down --amount 2`, then `settle --ticket <id>` five minutes later; and `settle --ticket 1` after 13:30Z. Also owed: a hand pass of the builder with a wallet on `:3100/parlay`.
- **Depth is the binding limit.** A leg is priced over rested depth ≥ the payout and the house maker rests 5,000 lots a side, so a ticket pays at most 5 tUSDC today. The builder opens on 1 and 4 for that reason (D-111). Raise `MM_QUOTE_LOTS` (suggest 50,000) at the held cutover.
- **Not built, recorded in D-109:** PD-2's oracle fair-value bound. The spread guard is in the program and ships off.

**S10b `agari-range`: two bugs in the deployed program, found, fixed and upgraded (slot 501123610, `7d90f90`, `2aa42f4`).** (1) The venue's mark was read **ten times too small** in the program, the page's read and the drive at once (`× 100` for a tick that is a thousandth), so a 65.0¢ market priced as a 6.5% centre; the factor is now derived from `PAIR_TICKS` on both sides and pinned by a test. (2) `public_void_stale` checked only the clock, so a winning round unsettled for an hour could be voided by anyone; it now takes the Market and refuses with `MustSettle`. Also: `range-settle`, `range-claim`, `range-void-stale`, supply and withdraw were **refused as not deployed** from the page (`9106f6e`), and `quoteMoonshotOnchain` was still a stub so Moonshot could never price (`705f212`). STATUS's older claim that `previewRangeOpen` / `quoteRangeOnchain` / `readRangeCapacity` are stubs was wrong: they were implemented in `4dcce7f`.
- **Consequence to remember:** the pinned `:3000` build still quotes Range with the old factor and will disagree with the chain until the cutover moves the `live` pin. `:3100` and the trunk are right.
- **Owed:** open one Range and one Moonshot round since the upgrade, with a wallet, and check the quote equals the charge.

**S8 Earn: settle and withdraw PROVEN (`a6f5c42`).** The vault's seat on `OPENAI-60m` #5 redeemed 4,000,000 against 2,040,000 of escrow (share price 1.000000 → 1.019600), and a provider withdrew 10,000,000 shares for exactly 10,196,000. **Still open: `public_merge`**, which needs both of the vault's legs filled on one Window. A 2.119 SOL buffer orphaned by an earlier maker upgrade was reclaimed; **deployer 15.67 SOL**.

**Bookkeeping DONE and merged (`9db3b3c`, `e74d163`).** `parity.md`: Done 32, Partial 39, Shell 11, Pending 5, Excluded 14, Blocked 2. Stage boxes ticked for S4, S5, S6, S7, S13 where the tree satisfies them. The worktree `../agari-wt/books` on `slice/bookkeeping-parity` is merged and can be removed.

**S9 agents and strategies: BUILT and PROVEN on devnet (2026-09-20 01:10–06:40Z).** `agari-strategy` `2yiPYmuNQxpfC3nCk66KzkW72uCbkT6hHwLRSHpYDskQ` (259,808 B, slot 501135685, live bytes hash to the build), registry `G7BLjSmJ…`. It holds no money: a registry plus a consent record that loads the subscriber's zero-copy `Grant` from `agari-vault` and requires it to be theirs, a STRATEGY grant, to the strategy's runner, live by the vault's own rule and inside the envelope; the only transfer is the fee, subscriber → creator. A strategy's words do not fit one transaction, so they are declared by hash and length, written in pieces and sealed by the sha256 syscall (D-112). Commits `00e4067` program + 10 Rust tests · `71e214b` client · `1ee58c5` deploy + init · `07bb9bb` reads and writes · `858bf6f` registry drive + the misattribution fix · `58f9476` runner fixes + live setup · `ce74e30` evidence.
- **Proven through the app's own lanes** (`scripts/drive/strategy-registry.ts`, every write is `submitter.submitTx(intent)`): publish a 789-byte strategy in 2 transactions, sealed, read back byte for byte; grant; subscribe with exactly the fee moving; `CapsOutsideEnvelope` and `FeeAboveMax` refused in simulation; revoke empties the runner's list; unsubscribe. These were also **the first `agari-vault` transactions ever made on devnet**.
- **An agent traded for its subscriber.** Strategy #2 "First move" names the ops runner key `G289o3a8…`; subscriber `6h6qH3dD…` (key kept in `~/.config/agari/devnet/strategy-subscriber-6h6qH3.json`) holds strategy grant #3 (2 a trade, 20 a day, 4 positions, budget 30, expires 09-27). The runner placed two real `actor_place_for` orders through it (TSLA Gap #0 DOWN, `OPENAI-60m` #10 DOWN) and declined to resend.
- **THE RUNNER IS RUNNING** as its own process beside the soak, PPID 1, port 8790, log `w1/data/soak/strategy-runner.log`. It does not touch the soak. It reads Windows through `:3100`'s `/api/index`, so while `:3100` is down it holds. Stop it: `for pid in $(pgrep -f src/main.ts); do ps eww -p $pid | grep -q OPS_HTTP_PORT=8790 && kill $pid; done`. Start it: the command is in the acceptance row of 06:25Z; it needs `NEXT_PUBLIC_AGARI_INDEXER_URL` absolute, `NEXT_PUBLIC_PRICE_FEED_URL`, the venue and vault ids, `STRATEGY_IDS=2` and `RUNNER_PRIVATE_KEY="$(cat ~/.config/agari/devnet/runner.json)"`. Its exposure is capped on chain by grant #3.
- **Two defects that had kept a complete runner inert, both fixed:** every ported ops actor called `parseMarketsEnv({ venueId })` and read nothing else from the environment (now `services/ops/src/runtime/markets-env.ts`, all eight callers); and `listVaultTallies` was a stub answering `complete: false` forever, which the runner's settlement sweep rightly held on (now read from the owner's 16 on-chain position slots).
- **The runner then closed its own loop (07:00:49Z):** its settlement sweep cranked the subscriber's resolved `OPENAI-60m` #10 position 52 s after the boundary, unattended, and it entered #11. **Parlay at 07:03Z:** #10 printed UP, ticket 4 died on its lost leg (its stake became the providers'), ticket 5's leg won and it stays live; tickets 1 and 5 finish Mon 13:30Z, run `settle --ticket 1` and `settle --ticket 5` then.
- **Owed for S9:** an **agent-preset** strategy (a model call) has not traded, only momentum; `/strategies` renders the real desk on `:3100` but the studio and the copy drawer have not been driven by hand; a long publish is 2 to 3 signatures and the studio's progress copy has not been checked against that; `listVaultTallies` reports a cost of 0 meaning "not recorded" (the row hides its stake line), and closed vault rounds need the vault's events indexed, which nothing does yet.

**Step 3, what is left: S10c boost, S10d private desk, S12b duel arena. Needs the user's cut decision.** All three trade through the engine by CPI with partial redeems (reference: `leverage` 800 lines + a keeper actor, `private` 531 lines + a desk key and ed25519 claims, `games` 1,102 lines + a commitment scheme, season pools and two actors), each with the full money gate. Recommendation: S10c boost next if time allows, since its toggle sits on the main ticket where judges will see it; take `/private` and the duel arena out of the nav. Not decided.

**Infrastructure events tonight, all recovered:** (1) 19:51Z–00:20Z the Mac hibernated at 1% battery; ops resumed by itself. (2) 01:58Z–02:04Z the laptop lost DNS entirely, `OPENAI-60m` #9 missed its closing print and voided by design. (3) Between 01:20Z and 06:10Z all three `next start` servers died while ops, the archivers and Postgres stayed up; restarted at PPID 1. **`detach.pl` does `setsid` + `exec` and does not fork**: launch it as `(perl …/detach.pl <cmd> >> log 2>&1 &)` and check PPID is 1. (4) The maker role was at 0.116 SOL and is topped up to 1.0; deployer 12.9 SOL. **The stale health-check recipe still names `s6/data/soak/ops-2026-09-15.log`; the truth is `live/data/soak/ops-live.log` and `/health` per actor.**

**Still true from before:** the `soak-8` cutover is HELD (re-probe Switchboard first); `:3000` serves the pinned `live` build, which quotes Range with the old factor; `:3100` serves w1.

## Resume here (written 2026-09-19 ~19:40 UTC, before a context clear)

**Deadline:** Fri 2026-09-25 20:00 UTC. Trunk = `integration/w1` (wt `../agari-wt/w1`), clean at the commit that adds this block. Read `handoff-deferred-stages.md` next; it is still the plan for what follows.

**Built and proven on devnet (4 programs):** `agari-events` (the venue: 9 stocks × 5m/15m/60m, the weekend Gap, 24/7 token lanes, the live `OPENAI-60m` pre-IPO lane), `agari-vault` (Trading Balance, tap-trading), `agari-range` (Range + Moonshot; price → open → settle closed 16:00Z 09-19), `agari-maker` (Earn; deployed, funded, seated, holding the best bid on a live Window). Off-chain: S11 Blinks proven end to end; S13 Sensei, takes, rooms, news; S15 landing, story pages, pitch, README; S18 closed-state and pre-open surfaces; the PreStocks "cover what you hold" plan, all 8 steps.

**In progress:** S10a `agari-parlay` — two commits (`a0679a7` arithmetic pinned to the client's vectors, `1b886e8` the reserve's balance sheet). It has **no instructions yet**, is not built for SBF and not deployed; the program id `H4gdpoPi…` is declared in `Anchor.toml` only. The web's `/parlay` still runs on stub arms.

**Not started (each needs a new on-chain program):** S9 agents/strategies (`agari-strategy` + the `strategy-runner` actor), S10c boost/leverage (`agari-leverage` + keeper), S10d private desk (`agari-private`), S12b duel arena. The handoff's honest option still stands for any of these that will not be finished properly: take the page out of the nav rather than ship a "not live" state.

**Small things left on shipped work:** S8 — the vault's merge → settle → withdraw proof row. S10b — `previewRangeOpen` / `quoteRangeOnchain` / `readRangeCapacity` are still stub arms. S11 — a live X trade needs the Agari X account (the handle in `.env.local` is still the reference's), and the Dialect registry entry needs the domain.

**Bookkeeping that is behind (judges read it):** `parity.md` has 102 rows and **none is Done** (86 Pending, 14 Excluded, 2 Blocked) although most are built; stage checkboxes are stale for S5 (0/13) and S13 (0/16), and partly for S4, S6, S7. About an hour's work against `acceptance.md`.

**Parked by the user (do not spend time):** demo video, S16 deploy go, the Monday Phantom check. Domain `agari.live` is bought and moving to Vercel DNS. Still the user's: public-release decision for the repo (Q-007), the submission form itself.

**Held:** the `soak-8` ops cutover (Switchboard's quote gateway was answering 500; re-probe before any restart). `:3000` keeps serving the pinned `live` build and shows none of today's feedback fixes until that cutover; `:3100` serves w1 with them.

**Infra at 19:40Z:** `:3000` had died when its launch task was stopped; it is restarted from `live/web` **via detach.pl**, so a context clear can no longer take it down. `:3018`, `:3100`, ops, archivers, Postgres up. **Battery 20% and discharging** — if the Mac died, use the recovery recipe further down.

## Feedback pass (2026-09-19 evening, `integration/w1`)

The user's note, in short: "it was showing USDC … is it SOL or USDC or a custom one … make it make sense", "I go into games, lucky draw, and I'm seeing BTC — this is a stock project", and "how many more are out there?".

**Money (D-106).** The unit rendered as "TUSDC" (another token's name) under every uppercasing label; `.sym`, `<KeepCase>` and `<Money>` now keep its casing. A first version also explained the currency (modal section, tooltips, FAQ entry, docs section); the user's call the same evening was "who cares … you don't have to mention it at all", so every explanation is removed and only the casing fix stays. Bets remain in tUSDC; the facts behind that are in D-106 for the record, not in the product.

**What was still crypto (D-107).** Four leftovers were functional, not cosmetic: Lucky drew from `["BTC","ETH"]` and could never be dealt a Window (now policy v2 over the nine launch tickers + OPENAI, the browser check pins the list, a closed-market line shows before the spin); the parlay's "BTC close streak" was always disabled (now the stock with the most Windows live); the X reply named an asset only when it was BTC or ETH (now any registry asset); the X permission panel linked Solana signatures to the Somnia explorer (now `txUrl`). Words: the trade-from-X diagram, "A duel on Masayume" ×3, "gas" ×13, "testnet" ×3, strategy descriptions. Internal names renamed; unused Bitcoin and Ethereum marks deleted.

**Second pass, what a word search cannot see.** The reference's "whole dollars from $1,000 up" rule caught OPENAI (~$1,130): the Reel asked "above $1,132?" for a line of $1,132.74 and Sensei was handed $1,133. The threshold is now $10,000 in one shared constant, and the Range and Moonshot tickets show the opening print with cents. Sensei's system line said Windows list only in the NYSE session, which the 24/7 lanes made untrue; it now names them. CSS strings, non-code assets, `packages/brain`, `scripts/` and the Range presets were checked and are clean.

**So it does not come back.** Invariant `venue-identity` fails the fast gate on the reference's assets, brand or chain in non-comment, non-test code; four deliberate lines are allowlisted with reasons, and a stale entry fails the rule. Proven by planting a probe file and watching it fail.

**Commits:** 23ba0cd Lucky v2 · ec4dbd3 identity · a01b7d4 money casing · 919a963 invariant · 1f0d023 X diagram box · the currency explanations added in a01b7d4/ce18fe7 are reverted in this commit, which also carries the price-scale and Sensei fixes · docs repo cc12453, 0c8f1ea, bb9bf51. Gates after the last one: typecheck 0, invariants 0 (15 rules), `pnpm build` green.
**Not verified:** the Lucky reels in motion, the deal card and the header pill need a connected wallet. The user should spin once on `:3100/games/lucky` during regular hours (Mon 09-21 from 13:30Z), or now for an OPENAI draw.
**Serving:** `:3100` = w1 `next start` via detach.pl (log `w1/data/soak/web-3100.log`). Stop with `pkill -f "next start -p 3100"`. Do not rebuild w1's `.next` while it serves.
**Health check recipe is stale:** it names `s6/data/soak/ops-2026-09-15.log`; the live soak logs to `live/data/soak/ops-live.log`, and the truth is `/health` per actor (`lastOkMs` age and `failures`), not `/session`. At 18:33Z `/health` was `ok:false` after ~20 min of `ENOTFOUND devnet.helius-rpc.com`; DNS answered again by itself and every actor recovered without a restart (`ok:true`, all `failures=0` at 18:52Z). The Mac was on **battery at 52%** — the charger is off again. **19:10Z check:** every process up, but battery at **24%, ~49 min left** (push sent to the user). `prestocks.com/api/prestocks` answered 200 in **18.9 s**, slower than the feed's timeout, so `prestocks-spot` reads fail on and off; `OPENAI-60m` had no PreStocks read inside [T+10s, T+45s] at 19:00:00Z, so that boundary cannot be printed and the Window will void, as designed. Not fixable from here: the read cannot be invented, and raising the feed timeout is an ops change that waits for the held cutover. RedStone's gateway also timed out three times and recovered.

## Deferred-stages session (2026-09-19, `integration/w1`)

**S11 — Trade from X + Blinks.** The relay was never a stub: 23 files, 9 test files, wired in `main.ts` as a
`LEGACY_ACTORS` entry. It boots standalone with `OPS_ACTORS=x-relay` without touching the held soak, reports its
executor and venue, and completes mention scans. `x_links` and `x_receipts` are empty so nothing can execute.
**Blocked only on the Agari X account** — `X_RETTIWT_API_KEY` (388 chars) and `X_HANDLE` are both set in
`.env.local`, but the handle is `masayume_app`, the reference project's. This is not an API-key problem: rettiwt
reads a logged-in session. `Q-008`/`B-3` are stale.
Blinks were the genuinely missing half and are now built: `/actions.json`, `/api/actions/w/<marketId>` and
`/api/actions/t/<symbol>/<cadence>` (a shared link outlives its Window). Wire types in `packages/core/src/x/actions.ts`
because `@solana/actions` is an `@solana/*` module and only `packages/markets` may import those; the transaction in
`packages/markets/src/x/action-order.ts` via `createNoopSigner`. **Proven:** a wallet that never touched the app
signed a transaction the endpoint built and it finalized (`2cEN7qC8…`, slot 500866904), moving 50.000 → 49.275 tUSDC.
Remaining: the Dialect registry entry (needs the domain + the user's go) and a live X trade (needs the account).

**S10b — Range.** `agari-range` at `GfsAzxPeNp2cbUTrXehHBjLjAMX6Cf69gz2zJYkGM7ha` (SBPF v0, extended to 403,368 B,
upgraded twice). Reserve `CNN4Z5MMazXTHkaYzR8wQepK2ATKrjZHNSqjRbTmwCdB`, vault `8pncTu9o…`, 200 tUSDC supplied.
The Rust pricing is pinned to `packages/core/src/range/pricing.vectors.json` by `include_str!`, so the client and
the chain cannot drift. Round 1 (TSLA Gap #0) and round 2 (`OPENAI-60m` #4, settles 16:00Z) are open; the client
quoted 1,155,000 and the chain charged 1,155,303, absorbed by `RANGE_STAKE_HEADROOM_BPS`. Provider equity read back
as exactly 200,000,000 with both rounds open. `/games/range` and `/games/moonshot` render their real pages (checked
in headless Chrome). The open button sends through `MarketsSubmitter.submitRangeOpen`.
**Still open:** settle + claim round 2 (watcher running), and `previewRangeOpen`/`quoteRangeOnchain`/`readRangeCapacity`
are still the stub arms.

**S8 — Earn.** `agari-maker` written and SBF-built (417 KB, ~2.12 SOL), deploying to
`442oD4u4fHnAwPjSqdQvkgGeDaGVEZ9YHHaLny98nEaN` via buffer `E8FSufJu…`. Post-only only: the vault never takes.
Escrow is recorded from the engine's own `PlaceResult`. Client, reads, `scripts/deploy/init-maker.ts` (which also
registers the seat at `program_authorities[1]`, D-063) and `scripts/drive/maker-vault.ts` are written and gated.
**Next:** finish the deploy → `init-maker` → supply → quote → merge → settle. `/earn` turns on when the vault exists.

**Two bugs found and fixed.**
1. The Moonshot ticket displayed a per-expiry cap no program enforced. Now enforced on-chain by an `ExpiryBook` PDA
   keyed by the boundary — without it the reserve's whole book could ride on one print.
2. **No Gap Window could ever be traded.** `scripts/drive/cover-call.ts` let `placeOrder` default `expire_ts` to the
   Window's *expiry*; the Gap lane locks days earlier and the engine refuses that with 6108 `ExpiryAfterLock`. Six Gap
   Windows had sat at zero trades while the maker quoted them. Fixed with `expireTs: lockAt`; the first Gap trade in
   the venue's history filled immediately (2,000 lots at 65.0¢).

**Funding.** The devnet faucet rate-limits per destination, so eight rotating inboxes exist
(`~/.config/agari/devnet/faucet-inbox-1..8.json`) and `scripts/deploy/sweep-funding.sh` pulls them into the deployer.
The user sent 20 SOL today; deployer ~17.6 SOL with the maker buffer outstanding. Domain **agari.live** bought and
moving to Vercel DNS — when it resolves, set `NEXT_PUBLIC_APP_ORIGIN=https://agari.live` and the Blink icon and
button hrefs become absolute against it.

**Infra note.** `:3000` was found down at ~14:5xZ and restarted from the pinned `live` worktree (soak-7) via
`detach.pl`; it answers 200. Ops was **not** restarted — the Switchboard cutover stays held. The health-check recipe
still names `s6/data/soak/ops-2026-09-15.log`, which is stale: the live soak logs to `live/data/soak/ops-live.log`.


Current stage: **S15 (W2)** on `stage/S15-public-story` (wt `../agari-wt/s15`; cut from `integration/w1` @ b09eae9, which carries every S18 lane). Four lanes (first launch 18:20Z stopped on a usage limit; relaunched 20:05Z, 15a again 20:25Z on Opus): **15a** landing + OG (`slice/S15a-landing`, wt s15a, web 3151) · **15b** story pages (`slice/S15b-story`, wt s15b, web 3152) · **15c** README + notices + submission + `agari-docs` fork (`slice/S15c-docs`, wt s15c, docs 3153) · **15d** geofence + advice copy + brand sweep (`slice/S15d-compliance`, wt s15d, web 3154). Merge order 15a → 15b → 15c → 15d → stage → w1. Decisions D-092…D-097, Q-S15-1. Stage file `stage-15-public-story.md`.
**S18 gate PASSED 2026-09-16 13:53Z** (`d4b19d3` on `stage/S18-always-on`, wt s18, :3018): every lane merged (18a/18c/18f/18d/18e), typecheck 0, invariants 0, build green, `21st review` 0 errors, `cargo test` 72/72; devnet drives done (Tue prelist 27/27, Wed bell fills, settle/redeem, expiry path). Still open on the stage file: Foundation bookkeeping and the closed-state browser pass. **2026-09-19 06:5xZ:** S4, S3, S15 and S18 are all merged into `integration/w1` (docs only + LICENSE); the two days of ops "outages" were the Mac idle-sleeping on battery (see `pmset -g log`), mitigated with a detached `caffeinate -ims`, the real fix is the charger. S18 parity rows advance at S18's gate only.
Last green commit (gate passed): S0 0aa48e5 · S2 787cf39 (on its branch)      Last commit: see `git log -1` on each branch
In-flight step: none on S15 lanes. Merged into the stage: **15a** landing + OG + layout metadata (2e6ff95; the footer carries `ADVICE_COPY.notAdvice` and the Sora notice covers the OG copy, 54b4084) · **15b** story pages (000fbdf) · **15c** README, notices, submission kit (934827e, 15062ec; `agari-docs` at /Users/abu/dev/hackathon/agari-docs @ f3685e1, 27 pages, 4 captures, no remote) · **15d** geofence, advice copy, brand sweep, X relay identity (8b25f7a, ff455d6; D-095 amended: cash-out and X unlink stay open). README (8a2a838): the events upgrade, vault deploy and init, both IDLs, authorities, Gap and TSLAx Series, the resting pre-open call and the landed 6121 are cited and verified against devnet RPC; verifiable build still pending (D-096). Stage gates after the last merge: typecheck, invariants and `pnpm build` green. Devnet Phase B + C are done and live on S18 (see the S18 STATUS). Build rule `NO_DNA=1 anchor build --arch v0` (D-024).
Infra (checked 18:05Z): Postgres up · archivers (pyth-trial, redstone) from the main checkout · soak `../agari-wt/s6/data/soak/run.sh` (S6 ops on :8787, `ROLLER_PRELIST` on; first prelist expected at the 20:00Z close; log `data/soak/ops-2026-09-15.log`) · `:3000` = w1 `next start` · `:3018` = S18 stage. Stop: `pkill -f data/soak/run.sh; pkill -f src/main.ts`. After a reboot: see the worktree-map memory recipe.
Done: S0 ✅ S2 ✅ (merge to main after S1) | Milestones: M0 ✅ M1 ☐
Blockers: **SOL:** deployer 10.73 (the user's 10 SOL landed 15:55Z); admin `58z6FwvCdns3u1AGLNfbvTVpvoSmDaEA6CthKQwQZMUZ`, runner `G289o3a8kKrTwJEtHaFFDqz4Y26L8VnUP2vFAebVUxo9`, sponsor `5kKwdNLoxTX8vVaR4ekL9Pgtnc2vkTLeWawsaTdDfrru` and inbox `5zjywmmJ…` are all at 0 (any of them sweeps to the deployer). Devnet plan (lane-devnet's Phase A done in wt w1: `anchor/target/deploy/agari_events.so` 813,328 B sha256 2e4bf8cc…, `agari_vault.so` 519,296 B sha256 b6eab3a1…, LiteSVM 63/63, token-cycle pass on the fork, `data/deploy/authorities-plan.json` = queue EYiAmGSd… + min oracles 3 + vault seat): 20:21Z go → events upgrade 0.256 → vault deploy + `deploy:init-vault` 2.645 → `deploy:set-authorities` → `deploy:init-gap-series` **7 names** (MSFT/AMZN had 2 signers at the 09-15 open, D-052) 1.647 → `deploy:fund-roles` 0.84 → `deploy:init-token-series` 9 of 12 (TSLA/NVDA/SPY; QQQ after the next send) ≈ 0.8 + float → ≈ 1.1 left → relay to 1 SOL → ops restart with `MM_ORDER_TYPE=limit` → one pre-open call + one far call + one 6121 refusal on a prelisted TSLA-5m Window for Wed 13:30Z. Open for the user: MSFT/AMZN in the Gap Series despite two signers; the verdict words (上がり/放銃) stay unless overridden. User: S4 Phantom check at a bell (next Mon 09-21 13:30Z; S1's is done). User: S16 deploy go (web, ops, docs). User: GitHub repo (none exists). User: app domain. Defaults awaiting user override: D-041 (S5), D-051…D-059 (S6), D-061…D-069 (S7), D-071/D-072 (S13), D-081…D-091 (S18), D-092…D-097 (S15).
Env readiness (presence only): PYTH ✅(trial → ≈09-27) ALPACA ✅ (root and web/.env.local) FINNHUB ✅ HELIUS ✅ OPENAI ✅ DATABASE_URL ✅(local) X_API ☐ STORK ☐(name present, empty) role keys ✅ (~/.config/agari/devnet)
Toolchain: anchor-cli 1.2.0 (avm) · anchor-lang =1.2.0 · host rustc 1.98.1 (anchor/rust-toolchain.toml) · solana-cli 3.1.10 · surfpool 1.5.0 · pnpm 11.24 · Docker 29.4 (`solana-verify` not installed)
Price sources: TSLA pyth+redstone(check) · QQQ/VOO pyth (until 09-25 close) · single names redstone · token switchboard (S6) — services/ops/config/price-sources.json
Devnet addresses: agari-events cDcHZiQ1WYAHbSjxMoju86fbC8azrtQg7dzrWKynANH (SBPF v0, from 36f1384; IDL metadata 97VcCVSs…). Venue: config 42GFppq2…, tUSDC 5i61C4kH… (faucet authority); 27 Regular Series (9 tickers × 5m/15m/60m, 2 Books each) + drive-only Series 900; all in scripts/deploy/addresses.devnet.json. SOL (18:03Z 09-15): deployer 10.73 · roller 3.85 · settler 2.72 · price-relay 0.30 · maker 0.20 (+ tUSDC). Funding inbox 5zjywmmJQDDRyX7H7i28MVDwfgYaPibk4yPbJC4HRdRv (~/.config/agari/devnet/funding-inbox.json) forwards to the deployer.
Integration: `integration/w1` (wt ../agari-wt/w1) = S3+S4+S5+S6+S7+S13+S18 stages, gate green, served on :3000. S18 plan r2 at ~/.claude/plans/valiant-wibbling-kahan.md. W2 = S18 + S15 (+ S16 final, S17) (D-084: S8–S12, S14 deferred).
**Pre-IPO lane (S18, D-100/D-101):** `PRE-OPENAI-5m` Series `C8JCSGPm…` ticker 910, attested primary on the PreStocks catalogue, **no cross-check** — no second venue publishes a pre-IPO mark, and `validate_policy_version` admits a zero check policy exactly for that. Proven 06:20–06:25Z: Window opened, both boundaries printed from PreStocks and signed by the price-attestor, settled **UP** ($1,078.17 → $1,082.44, payout YES 10,000,000). Code: `packages/markets/src/prices/prestocks.ts`, `preStocksSeries()` in `deploy/venue-spec.ts`, drive `scripts/drive/prestocks-attest.ts` (`--bar current|next`, `--with-call`). Lane is drive-only: **ops does not roll it**, so it is not live in the app. Cost so far ~0.29 SOL; deployer 2.59 SOL.
Bounty tracks (added by the organisers 09-17, `00-plan.md` §0 is stale): **claimed** PreStocks + Pyth, recorded in `docs/submission/submission-checklist.md`; **not claimed** Tessera, Clawpump, Meteora DBC — each would be a track-shaped bolt-on, not something the venue does.
**Deadline corrected 2026-09-19:** submissions close **Fri 2026-09-25 16:00 ET / 20:00 UTC**, not 09-18; pool raised to $126k; edits allowed until close. Abu's standing rule: the deadline is never a reason to compromise scope or quality.
**PreStocks eligibility (hard rule):** "projects that integrate any non-PreStocks pre-IPO tokens will be ineligible". Tessera sells the same names, so the two bounties are mutually exclusive — the Pre-IPO lane stays PreStocks-only. PreStocks pays $5k/$3k/$2k.
**Plan of record (approved 2026-09-19): `~/.claude/plans/temporal-marinating-fog.md`** — PreStocks = "insure what you hold" via the existing cover card (`web/src/features/hedge`), not a price-feed swap. Steps: 0 ✅ housekeeping (S4/S3/S15/S18 merged, D-103 safety lock) · 1 ✅ wallet recognises all 8 PreStocks tokens (874e4b8; registry + hook + `prestocks-spot` feed + `/prestocks/latest` + honest price age) · 2 ✅ discoverability (2c256a9; teaser states, example mode, first-sight toast, plain words) · 3 ✅ live 24/7 lane code (9cf5150 relay pass + roller + maker; 7e6e96b Series spec + `init-prestocks-series.ts`; run 10:34Z → `OPENAI-60m` Series `6u4hgFGAfoFTQxaRe4hB9m7cLWqpywwwMXVb8u7JqLqG`, two Books) · 4 ✅ Your stocks on /portfolio (fede6cb) · 5 ✅ ticker-hub pre-IPO facts + `/api/prestocks` (a8c8083) · 6 ✅ landing "Cover what you hold" (a0940d2, 49539d4) · 7 ✅ Reels "you hold this" card, one in eight, rotating (41ed9a2) · 8 ✅ opt-in "tell me if it drops" bell on Your stocks + `DropBellWatcher` (6ee974e) · Sensei ✅ holdings summary as a fact with one use, tripwire unchanged, 2.5 KB block (bf57fe6, D-104) · README/D-101 ✅ Jupiter-is-not-independent wording + the live-lane row. **Soak = `live` worktree pinned at tag `soak-5`** (Steps 0–5 + feed backoff; ops code is unchanged since, so moving the pin only changes web/docs). **:3000 serves the `live` web build** (started via detach.pl, log `live/data/soak/web-3000.log`): after moving the pin, `pnpm build` in `live/web` and restart :3000 at a quiet moment; never build in w1 for :3000. Still open from the plan: S3 re-measure needs a live NYSE session with the Mac awake (next: Mon 2026-09-21 13:30–20:00Z); D-098 (fatal start failure), D-099 (quote probe while halted) and the `/prestocks/latest` `move` field the calm rule reads (D-105) are BUILT and gated in w1; **the soak-8 cutover is HELD** because Switchboard's quote gateway still answers 500 (probed 11:37Z) — a restart would clear the in-process halt streaks, the roller would reopen xStock Windows and they would void on missing prints. Cut over at the next restart that is not during a Switchboard outage; re-probe first (`fetchTokenQuote` on TSLAX/USD, minOracles 3, queue `EYiAmGSd…`). A dry boot of the new build on :8788 was clean: 11 actors, 0 `failed to start`. **This also holds the :3000 rebuild**: moving the `live` pin would not restart ops by itself, but it would arm the supervisor to start the NEW ops code at the next unplanned exit — an accidental cutover at an arbitrary moment. So `live` stays at `soak-7` and :3000 keeps serving it (it already has Steps 6–8: landing cover, Reels card, drop bell). What is waiting for the pin move is polish only: the footer-band fix, the calm rule (inert without the ops `move` field) and the Sensei holdings line. Move the pin and rebuild web in the same cutover as ops; live lane proof — **one cover fill against the maker DONE 11:31Z** (1,000 lots at 14.4¢ on #0, `scripts/drive/cover-call.ts`, row in acceptance.md); **#0 SETTLED 12:00:30Z UP** ($1121.5441 → $1130.4196, payoutYes 10000000, no void — the cover lost its premium as insurance should) and **#1 opened and printed 12:00:22Z**; still wanted: #2 (13:00–14:00Z) for three in a row; the calm rule's threshold (D-105: 20 bps of range over 15 min) was set from the one-day 09-19 measurement and should be re-checked once the live feed has a week of history — until ops runs soak-8 the web sees no `move` field, so nothing is called calm and every name keeps its cover offer, which is the safe direction; more pre-IPO Series only with more devnet SOL. Machine: charger on, `caffeinate -ims` detached; if it died, restart Postgres, archivers, the soak (from `live`, plan Step 0.4) and `next start` on :3000/:3018, then re-run `caffeinate`.
**Cutover DONE 10:32Z (soak-5). Recovery recipe if the Mac dies (plan Step 0.4):** (1) if the Mac died: `brew services start postgresql@16`; from the main checkout `pnpm archive:start`; `perl scripts/archive/detach.pl caffeinate -ims`; `next start` on :3000 (w1 build) and :3018 (s18 build) via detach.pl. (2) cutover: `pkill -f 'data/soak/run.sh'` FIRST (else the supervisor restarts ops), then `pkill -f src/main.ts`; `cd ../agari-wt/live && git checkout --detach soak-4 && perl ../../stocklana/scripts/archive/detach.pl bash data/soak/run.sh >> data/soak/ops-live.log 2>&1` (the launcher `live/data/soak/run.sh` is gitignored, already written: RELAY_PRESTOCKS=1, MM_SYMBOLS adds OPENAI, MM_SIGMA_BPS=OPENAI:12000, same RPC/indexer knobs as s6). Watch `/health` until `ok:true` with every actor's `lastOkMs` fresh (roller's first pass can take minutes) and the log shows `RELAY_PRESTOCKS=1`. Rollback = restart `s6/data/soak/run.sh`. (3) only then register the live lane: `pnpm exec tsx --env-file-if-exists=.env.local scripts/deploy/init-prestocks-series.ts --cluster devnet --symbol OPENAI --cadence 3600` (dry run said 0.4629 SOL; deployer 2.63) → acceptance rows → the roller lists `OPENAI-60m` within 5 min; proof = three Windows settled + one fill against the maker. (4) then Steps 4–8 of the plan (Your stocks on /portfolio, ticker-hub pre-IPO facts, landing section, Reels card, opt-in drop bell).
Next action: S15 gate pieces: (1) D-096 `anchor build --verifiable` for both programs in a scratch copy (never w1's target/deploy), README line with the measured hashes and toolchain; (2) the browser pass of `/`, `/how-it-works`, `/demo`, `/pitch`, `/download`, `/native-auth` on :3000 with the headless CDP capture script (lanes 15a and 15b passed their own; the MCP browser profile stays locked); (3) Wed after the 13:30Z bell: the open-state capture of `/`, the demo recording (D-097, rebuild after `web/public/video/agari-demo.mp4`), refresh the pitch's 137 signature count from `acceptance.md`; (4) `parity.md` L-11…L-14, L-18…L-20, L-23, Y-06, Y-14 at the gate. For S16: set `NEXT_PUBLIC_APP_ORIGIN` (OG base) and `NEXT_PUBLIC_SITE_URL` (X relay links); decide Q-S15-1 (docs deploy) and Q-007 (public release). Known: `/markets/[id]` still redirects to `/markets`, so per-market OG images are unused until that route renders; ~~the app shell leaves a tall blank band under the footer at 1440 on every page (shared shell padding, not the landing)~~ **FIXED 2026-09-19**: `.page-shell`'s `padding-bottom: 96px` clears the floating mobile pill nav, which only renders below 720px, so it now lives in that media query alone (the reel keeps it, since `reel.css` hides the footer). The gap under every page is now the ported footer's own 120px margin and nothing else, measured at 1440 on `/`, `/markets`, `/portfolio`, `/how-it-works`; phone 390 still gets 112px with the pill nav clear and no horizontal overflow. The reference's `.footer` rule (part-07.css) was not touched (D-081).
