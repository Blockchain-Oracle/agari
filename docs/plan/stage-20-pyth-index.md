# S20 — Pyth's pre-IPO valuation indices, switch-ready

**Goal:** wire Pyth's `Equity.Index.OPENAI/USD` (`96d4bb23a3db78fdb72b3a03ce80ead686096f324319166534d9a27c0519c483`) and `Equity.Index.ANTHROPIC/USD` (`5da511a7c68b17a3bc94380cab4756bc83ab87f86307af10ea58467a64b6689d`) — 24/7 feeds live since 2026-09-17 — into the venue so that a valuation lane per name settles on Pyth on-chain (the TSLA path, `public_record_print_pyth`), the OpenAI/Anthropic hubs compare the PreStocks token price with the Pyth index, and the S21 desk can read a max premium against an independent reference. Approved 2026-09-22 (plan §§3, 6, 7).

- **Entitlement fact (verified 2026-09-22):** the venue's trial key is entitled per feed (TSLA/QQQ/VOO → 200); every SPCX feed and both indices → 403 `requires group pyth-indices`; the public endpoint without a key → 401. So everything here is **gated on a live probe** and switches on with no deploy when a key answers 200. Whether to ask Pyth for `pyth-indices` is the user's call.
- **Hard rule:** index feed ids never join `sources.pythFeeds`, `spot-feed.ts`, `halt-watch/signals.ts` or `archive-pass.ts` unless entitled: one 403 latches `pythAuthFailed` (`boundary-cache.ts:84`) and stops TSLA/QQQ/VOO settlement and the spot stream.
- **Identity:** a valuation lane is its own ticker (`OPENAIV` 930, `ANTHROPICV` 931, `kind: "valuation"`, `valuationOf`) because the Series PDA is `(ticker, cadence, basis)` and `OPENAI/3600/token` is taken.
- **Decision:** D-125; D-101 amended ("no second venue publishes a pre-IPO mark" is no longer true). **Branch:** `../agari-wt/s20` on `slice/S20-pyth-index`.

## Steps

- [x] **B1 core** (`feat(S20.1/core)`): `Ticker.pythIndexFeedId: Hash32 | null` (OPENAI, ANTHROPIC), `kind` gains `"valuation"`, `valuationOf`, rows `OPENAIV`/`ANTHROPICV`, `isTokenOnlyKind` includes `valuation`, `VALUATION_TICKERS`, `pythIndexFeedOf`; tests (pre-IPO `pythFeedId` still null; index ids well-formed and distinct); label-only call sites learn the index feed (`proof/replay.ts:41–49`, `api/index/[...path]/queries-proof.ts:8`, `spot-sse.ts:54–58` and `queries-archive.ts:20` for the valuation symbol only). `sources.ts:37` and `policies.ts:84` untouched on purpose.
- [x] **B2 ops** (`feat(S20.2/ops)`): `price-sources.json` top-level `pythIndex` block (defaults `graceSec 60`, `maxConfBps 200`, `admissionSec 900`; tickers OPENAIV/ANTHROPICV, one open-ended pyth version; feed ids from the registry); `runtime/pyth-entitlement.ts` store (`entitled|denied|unknown`, hourly probe per feed with the Bearer key, records publish spacing and conf/price) in `VenueDeps`; actor `pyth-entitlement` (60 s tick; heartbeat detail = snapshot) started with `relay` or `roller`; `boundary-cache.ts` refuses an unentitled index feed before fetching; `archive-pass.ts` archives entitled index feeds only; roller `versions.ts` `highestCoveringVersion(…, usable)` + `PlanClock.pythUsable`, paused text `paused: no signed source (Pyth feed not entitled)`; `prices/pyth-index-spot.ts` (entitled feeds only) joined under the valuation symbol; `/pyth-index/latest` (`{entitlement, rows: {OPENAI: {indexE8, publishTimeSec, ageSec, fresh, tokenPriceE8, premiumBps}}}`); `/session.sources.pythIndex` from the live store; maker dispatch for `valuation`; tests.
- [ ] **B3 deploy** (`feat(S20.3/deploy)`): `policies.ts` `pythIndexPolicyVersions`, `venue-spec.ts` `pythValuationSeries` (token basis, 60 m, two Books, key `OPENAIV-60m`), `scripts/deploy/init-valuation-series.ts` — **probes first and refuses to register while the feed is denied** (no `--force`): no dead lane is ever shown.
- [ ] **B4 web** (`feat(S20.4/web)`): `/api/pyth-index` (30 s memo) + `usePythIndex.ts`; the OPENAI/ANTHROPIC hub stats: Token price · PreStocks mark · Pyth index (only when present) · Token vs mark · Token vs Pyth (only when present); `copy.ts` source variants; valuation lane hero source line; status row `pipelines.pythIndex` (good / warn "OPENAI, ANTHROPIC: not entitled" / bad) + session schema + `/dev/status` fixtures; Sensei one sentence; `/dev/basket` hub with and without the row. No licensing sentence on screen.
- [ ] **Docs**: D-125, D-101 amendment, README L89–104, submission checklist Pyth claim.

## Handoff

Read `docs/plan/STATUS.md` first. Gate = `pnpm typecheck && pnpm invariants && pnpm exec vitest run`; a dry ops boot with `PYTH_API_KEY` logs `pyth-entitlement: OPENAI denied (403 pyth-indices)`; `pnpm build` for B4. Never put an index feed under `tickers.<SYM>.pythFeedId`.
