# Local canonical URL; replace with the verified docs domain only when deployed.
NEXT_PUBLIC_DOCS_URL=http://localhost:3153
# Use the live app, or override with http://localhost:3000 while testing a local build.
NEXT_PUBLIC_APP_URL=https://useagari.xyz
