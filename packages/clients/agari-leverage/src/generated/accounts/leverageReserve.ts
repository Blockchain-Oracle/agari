/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getLeverageParamsDecoder,
  getLeverageParamsEncoder,
  getOpenSlotDecoder,
  getOpenSlotEncoder,
  type LeverageParams,
  type LeverageParamsArgs,
  type OpenSlot,
  type OpenSlotArgs,
} from "../types";

export const LEVERAGE_RESERVE_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([92, 253, 189, 113, 54, 69, 6, 2]);

export function getLeverageReserveDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    LEVERAGE_RESERVE_DISCRIMINATOR,
  );
}

export type LeverageReserve = {
  discriminator: ReadonlyUint8Array;
  admin: Address;
  collateralMint: Address;
  params: LeverageParams;
  outstandingBase: bigint;
  userOwedBase: bigint;
  supplyShares: bigint;
  nextPositionId: bigint;
  paused: boolean;
  bump: number;
  seatBump: number;
  custodyBump: number;
  /** Live positions, so a withdrawal can see an expired unsettled one without being handed any accounts. */
  open: Array<OpenSlot>;
};

export type LeverageReserveArgs = {
  admin: Address;
  collateralMint: Address;
  params: LeverageParamsArgs;
  outstandingBase: number | bigint;
  userOwedBase: number | bigint;
  supplyShares: number | bigint;
  nextPositionId: number | bigint;
  paused: boolean;
  bump: number;
  seatBump: number;
  custodyBump: number;
  /** Live positions, so a withdrawal can see an expired unsettled one without being handed any accounts. */
  open: Array<OpenSlotArgs>;
};

/** Gets the encoder for {@link LeverageReserveArgs} account data. */
export function getLeverageReserveEncoder(): FixedSizeEncoder<LeverageReserveArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["admin", getAddressEncoder()],
      ["collateralMint", getAddressEncoder()],
      ["params", getLeverageParamsEncoder()],
      ["outstandingBase", getU64Encoder()],
      ["userOwedBase", getU64Encoder()],
      ["supplyShares", getU64Encoder()],
      ["nextPositionId", getU64Encoder()],
      ["paused", getBooleanEncoder()],
      ["bump", getU8Encoder()],
      ["seatBump", getU8Encoder()],
      ["custodyBump", getU8Encoder()],
      ["open", getArrayEncoder(getOpenSlotEncoder(), { size: 128 })],
    ]),
    (value) => ({ ...value, discriminator: LEVERAGE_RESERVE_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link LeverageReserve} account data. */
export function getLeverageReserveDecoder(): FixedSizeDecoder<LeverageReserve> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["admin", getAddressDecoder()],
    ["collateralMint", getAddressDecoder()],
    ["params", getLeverageParamsDecoder()],
    ["outstandingBase", getU64Decoder()],
    ["userOwedBase", getU64Decoder()],
    ["supplyShares", getU64Decoder()],
    ["nextPositionId", getU64Decoder()],
    ["paused", getBooleanDecoder()],
    ["bump", getU8Decoder()],
    ["seatBump", getU8Decoder()],
    ["custodyBump", getU8Decoder()],
    ["open", getArrayDecoder(getOpenSlotDecoder(), { size: 128 })],
  ]);
}

/** Gets the codec for {@link LeverageReserve} account data. */
export function getLeverageReserveCodec(): FixedSizeCodec<
  LeverageReserveArgs,
  LeverageReserve
> {
  return combineCodec(getLeverageReserveEncoder(), getLeverageReserveDecoder());
}

export function decodeLeverageReserve<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<LeverageReserve, TAddress>;
export function decodeLeverageReserve<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<LeverageReserve, TAddress>;
export function decodeLeverageReserve<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
):
  Account<LeverageReserve, TAddress> | MaybeAccount<LeverageReserve, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getLeverageReserveDecoder(),
  );
}

export async function fetchLeverageReserve<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<LeverageReserve, TAddress>> {
  const maybeAccount = await fetchMaybeLeverageReserve(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeLeverageReserve<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<LeverageReserve, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeLeverageReserve(maybeAccount);
}

export async function fetchAllLeverageReserve(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<LeverageReserve>[]> {
  const maybeAccounts = await fetchAllMaybeLeverageReserve(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeLeverageReserve(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<LeverageReserve>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodeLeverageReserve(maybeAccount),
  );
}

export function getLeverageReserveSize(): number {
  return 2204;
}
