/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getPositionStatusDecoder,
  getPositionStatusEncoder,
  type PositionStatus,
  type PositionStatusArgs,
} from "../types";

export const POSITION_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  170, 188, 143, 228, 122, 64, 247, 208,
]);

export function getPositionDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(POSITION_DISCRIMINATOR);
}

export type Position = {
  discriminator: ReadonlyUint8Array;
  owner: Address;
  market: Address;
  positionId: bigint;
  status: PositionStatus;
  /** 0 Up (YES), 1 Down (NO). */
  outcome: number;
  leverageBps: number;
  openedAtSec: bigint;
  expirySec: bigint;
  exitedAtSec: bigint;
  /** Contracts the reserve still holds for this position, in the venue's lots. */
  lots: bigint;
  /** Base units one lot pays when its side lands, so a reader need not fetch the Series to size the position. */
  lotBase: bigint;
  /** What the owner put in, premium included: the most they can lose. */
  stakeBase: bigint;
  /** The reserve's outstanding claim, repaid first out of whatever the contracts fetch. */
  frontedBase: bigint;
  premiumBase: bigint;
  /** Collateral per whole contract paid at open, in the bought side's own terms. */
  entryPriceRaw: bigint;
  /** Cumulative: what the contracts fetched, what the reserve took back, what the owner was due. */
  proceedsBase: bigint;
  reclaimedBase: bigint;
  returnedBase: bigint;
  /**
   * The part of `returned_base` not yet paid out. An exit never waits on the owner's token account: when a
   * permissionless exit is not handed one, the money waits here for `public_claim`.
   */
  owedBase: bigint;
  bump: number;
};

export type PositionArgs = {
  owner: Address;
  market: Address;
  positionId: number | bigint;
  status: PositionStatusArgs;
  /** 0 Up (YES), 1 Down (NO). */
  outcome: number;
  leverageBps: number;
  openedAtSec: number | bigint;
  expirySec: number | bigint;
  exitedAtSec: number | bigint;
  /** Contracts the reserve still holds for this position, in the venue's lots. */
  lots: number | bigint;
  /** Base units one lot pays when its side lands, so a reader need not fetch the Series to size the position. */
  lotBase: number | bigint;
  /** What the owner put in, premium included: the most they can lose. */
  stakeBase: number | bigint;
  /** The reserve's outstanding claim, repaid first out of whatever the contracts fetch. */
  frontedBase: number | bigint;
  premiumBase: number | bigint;
  /** Collateral per whole contract paid at open, in the bought side's own terms. */
  entryPriceRaw: number | bigint;
  /** Cumulative: what the contracts fetched, what the reserve took back, what the owner was due. */
  proceedsBase: number | bigint;
  reclaimedBase: number | bigint;
  returnedBase: number | bigint;
  /**
   * The part of `returned_base` not yet paid out. An exit never waits on the owner's token account: when a
   * permissionless exit is not handed one, the money waits here for `public_claim`.
   */
  owedBase: number | bigint;
  bump: number;
};

/** Gets the encoder for {@link PositionArgs} account data. */
export function getPositionEncoder(): FixedSizeEncoder<PositionArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["owner", getAddressEncoder()],
      ["market", getAddressEncoder()],
      ["positionId", getU64Encoder()],
      ["status", getPositionStatusEncoder()],
      ["outcome", getU8Encoder()],
      ["leverageBps", getU32Encoder()],
      ["openedAtSec", getI64Encoder()],
      ["expirySec", getI64Encoder()],
      ["exitedAtSec", getI64Encoder()],
      ["lots", getU64Encoder()],
      ["lotBase", getU64Encoder()],
      ["stakeBase", getU64Encoder()],
      ["frontedBase", getU64Encoder()],
      ["premiumBase", getU64Encoder()],
      ["entryPriceRaw", getU64Encoder()],
      ["proceedsBase", getU64Encoder()],
      ["reclaimedBase", getU64Encoder()],
      ["returnedBase", getU64Encoder()],
      ["owedBase", getU64Encoder()],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: POSITION_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link Position} account data. */
export function getPositionDecoder(): FixedSizeDecoder<Position> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["owner", getAddressDecoder()],
    ["market", getAddressDecoder()],
    ["positionId", getU64Decoder()],
    ["status", getPositionStatusDecoder()],
    ["outcome", getU8Decoder()],
    ["leverageBps", getU32Decoder()],
    ["openedAtSec", getI64Decoder()],
    ["expirySec", getI64Decoder()],
    ["exitedAtSec", getI64Decoder()],
    ["lots", getU64Decoder()],
    ["lotBase", getU64Decoder()],
    ["stakeBase", getU64Decoder()],
    ["frontedBase", getU64Decoder()],
    ["premiumBase", getU64Decoder()],
    ["entryPriceRaw", getU64Decoder()],
    ["proceedsBase", getU64Decoder()],
    ["reclaimedBase", getU64Decoder()],
    ["returnedBase", getU64Decoder()],
    ["owedBase", getU64Decoder()],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link Position} account data. */
export function getPositionCodec(): FixedSizeCodec<PositionArgs, Position> {
  return combineCodec(getPositionEncoder(), getPositionDecoder());
}

export function decodePosition<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<Position, TAddress>;
export function decodePosition<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<Position, TAddress>;
export function decodePosition<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<Position, TAddress> | MaybeAccount<Position, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getPositionDecoder(),
  );
}

export async function fetchPosition<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Position, TAddress>> {
  const maybeAccount = await fetchMaybePosition(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybePosition<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Position, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodePosition(maybeAccount);
}

export async function fetchAllPosition(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Position>[]> {
  const maybeAccounts = await fetchAllMaybePosition(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybePosition(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Position>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodePosition(maybeAccount));
}

export function getPositionSize(): number {
  return 191;
}
