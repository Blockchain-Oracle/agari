/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findCustodyPda, findReservePda, findSeatPda } from "../pdas";
import { AGARI_LEVERAGE_PROGRAM_ADDRESS } from "../programs";

export const PUBLIC_CLAIM_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  57, 161, 227, 54, 107, 116, 1, 95,
]);

export function getPublicClaimDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PUBLIC_CLAIM_DISCRIMINATOR,
  );
}

export type PublicClaimInstruction<
  TProgram extends string = typeof AGARI_LEVERAGE_PROGRAM_ADDRESS,
  TAccountCaller extends string | AccountMeta<string> = string,
  TAccountReserve extends string | AccountMeta<string> = string,
  TAccountPosition extends string | AccountMeta<string> = string,
  TAccountCustody extends string | AccountMeta<string> = string,
  TAccountSeat extends string | AccountMeta<string> = string,
  TAccountOwnerToken extends string | AccountMeta<string> = string,
  TAccountCollateralMint extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCaller extends string
        ? ReadonlySignerAccount<TAccountCaller> &
            AccountSignerMeta<TAccountCaller>
        : TAccountCaller,
      TAccountReserve extends string
        ? WritableAccount<TAccountReserve>
        : TAccountReserve,
      TAccountPosition extends string
        ? WritableAccount<TAccountPosition>
        : TAccountPosition,
      TAccountCustody extends string
        ? WritableAccount<TAccountCustody>
        : TAccountCustody,
      TAccountSeat extends string
        ? ReadonlyAccount<TAccountSeat>
        : TAccountSeat,
      TAccountOwnerToken extends string
        ? WritableAccount<TAccountOwnerToken>
        : TAccountOwnerToken,
      TAccountCollateralMint extends string
        ? ReadonlyAccount<TAccountCollateralMint>
        : TAccountCollateralMint,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      ...TRemainingAccounts,
    ]
  >;

export type PublicClaimInstructionData = { discriminator: ReadonlyUint8Array };

export type PublicClaimInstructionDataArgs = {};

export function getPublicClaimInstructionDataEncoder(): FixedSizeEncoder<PublicClaimInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: PUBLIC_CLAIM_DISCRIMINATOR }),
  );
}

export function getPublicClaimInstructionDataDecoder(): FixedSizeDecoder<PublicClaimInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getPublicClaimInstructionDataCodec(): FixedSizeCodec<
  PublicClaimInstructionDataArgs,
  PublicClaimInstructionData
> {
  return combineCodec(
    getPublicClaimInstructionDataEncoder(),
    getPublicClaimInstructionDataDecoder(),
  );
}

export type PublicClaimAsyncInput<
  TAccountCaller extends string = string,
  TAccountReserve extends string = string,
  TAccountPosition extends string = string,
  TAccountCustody extends string = string,
  TAccountSeat extends string = string,
  TAccountOwnerToken extends string = string,
  TAccountCollateralMint extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  /** Permissionless to call; the money only ever goes to `position.owner` (AD-5). */
  caller: TransactionSigner<TAccountCaller>;
  reserve?: Address<TAccountReserve>;
  position: Address<TAccountPosition>;
  custody?: Address<TAccountCustody>;
  seat?: Address<TAccountSeat>;
  ownerToken: Address<TAccountOwnerToken>;
  collateralMint: Address<TAccountCollateralMint>;
  tokenProgram?: Address<TAccountTokenProgram>;
};

export async function getPublicClaimInstructionAsync<
  TAccountCaller extends string,
  TAccountReserve extends string,
  TAccountPosition extends string,
  TAccountCustody extends string,
  TAccountSeat extends string,
  TAccountOwnerToken extends string,
  TAccountCollateralMint extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof AGARI_LEVERAGE_PROGRAM_ADDRESS,
>(
  input: PublicClaimAsyncInput<
    TAccountCaller,
    TAccountReserve,
    TAccountPosition,
    TAccountCustody,
    TAccountSeat,
    TAccountOwnerToken,
    TAccountCollateralMint,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  PublicClaimInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountReserve,
    TAccountPosition,
    TAccountCustody,
    TAccountSeat,
    TAccountOwnerToken,
    TAccountCollateralMint,
    TAccountTokenProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGARI_LEVERAGE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: false },
    reserve: { value: input.reserve ?? null, isWritable: true },
    position: { value: input.position ?? null, isWritable: true },
    custody: { value: input.custody ?? null, isWritable: true },
    seat: { value: input.seat ?? null, isWritable: false },
    ownerToken: { value: input.ownerToken ?? null, isWritable: true },
    collateralMint: { value: input.collateralMint ?? null, isWritable: false },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.reserve.value) {
    accounts.reserve.value = await findReservePda({ programAddress });
  }
  if (!accounts.custody.value) {
    accounts.custody.value = await findCustodyPda({ programAddress });
  }
  if (!accounts.seat.value) {
    accounts.seat.value = await findSeatPda({ programAddress });
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("caller", accounts.caller),
      getAccountMeta("reserve", accounts.reserve),
      getAccountMeta("position", accounts.position),
      getAccountMeta("custody", accounts.custody),
      getAccountMeta("seat", accounts.seat),
      getAccountMeta("ownerToken", accounts.ownerToken),
      getAccountMeta("collateralMint", accounts.collateralMint),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
    ],
    data: getPublicClaimInstructionDataEncoder().encode({}),
    programAddress,
  } as PublicClaimInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountReserve,
    TAccountPosition,
    TAccountCustody,
    TAccountSeat,
    TAccountOwnerToken,
    TAccountCollateralMint,
    TAccountTokenProgram
  >);
}

export type PublicClaimInput<
  TAccountCaller extends string = string,
  TAccountReserve extends string = string,
  TAccountPosition extends string = string,
  TAccountCustody extends string = string,
  TAccountSeat extends string = string,
  TAccountOwnerToken extends string = string,
  TAccountCollateralMint extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  /** Permissionless to call; the money only ever goes to `position.owner` (AD-5). */
  caller: TransactionSigner<TAccountCaller>;
  reserve: Address<TAccountReserve>;
  position: Address<TAccountPosition>;
  custody: Address<TAccountCustody>;
  seat: Address<TAccountSeat>;
  ownerToken: Address<TAccountOwnerToken>;
  collateralMint: Address<TAccountCollateralMint>;
  tokenProgram?: Address<TAccountTokenProgram>;
};

export function getPublicClaimInstruction<
  TAccountCaller extends string,
  TAccountReserve extends string,
  TAccountPosition extends string,
  TAccountCustody extends string,
  TAccountSeat extends string,
  TAccountOwnerToken extends string,
  TAccountCollateralMint extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof AGARI_LEVERAGE_PROGRAM_ADDRESS,
>(
  input: PublicClaimInput<
    TAccountCaller,
    TAccountReserve,
    TAccountPosition,
    TAccountCustody,
    TAccountSeat,
    TAccountOwnerToken,
    TAccountCollateralMint,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): PublicClaimInstruction<
  TProgramAddress,
  TAccountCaller,
  TAccountReserve,
  TAccountPosition,
  TAccountCustody,
  TAccountSeat,
  TAccountOwnerToken,
  TAccountCollateralMint,
  TAccountTokenProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGARI_LEVERAGE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: false },
    reserve: { value: input.reserve ?? null, isWritable: true },
    position: { value: input.position ?? null, isWritable: true },
    custody: { value: input.custody ?? null, isWritable: true },
    seat: { value: input.seat ?? null, isWritable: false },
    ownerToken: { value: input.ownerToken ?? null, isWritable: true },
    collateralMint: { value: input.collateralMint ?? null, isWritable: false },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("caller", accounts.caller),
      getAccountMeta("reserve", accounts.reserve),
      getAccountMeta("position", accounts.position),
      getAccountMeta("custody", accounts.custody),
      getAccountMeta("seat", accounts.seat),
      getAccountMeta("ownerToken", accounts.ownerToken),
      getAccountMeta("collateralMint", accounts.collateralMint),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
    ],
    data: getPublicClaimInstructionDataEncoder().encode({}),
    programAddress,
  } as PublicClaimInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountReserve,
    TAccountPosition,
    TAccountCustody,
    TAccountSeat,
    TAccountOwnerToken,
    TAccountCollateralMint,
    TAccountTokenProgram
  >);
}

export type ParsedPublicClaimInstruction<
  TProgram extends string = typeof AGARI_LEVERAGE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Permissionless to call; the money only ever goes to `position.owner` (AD-5). */
    caller: TAccountMetas[0];
    reserve: TAccountMetas[1];
    position: TAccountMetas[2];
    custody: TAccountMetas[3];
    seat: TAccountMetas[4];
    ownerToken: TAccountMetas[5];
    collateralMint: TAccountMetas[6];
    tokenProgram: TAccountMetas[7];
  };
  data: PublicClaimInstructionData;
};

export function parsePublicClaimInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedPublicClaimInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 8) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 8,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      caller: getNextAccount(),
      reserve: getNextAccount(),
      position: getNextAccount(),
      custody: getNextAccount(),
      seat: getNextAccount(),
      ownerToken: getNextAccount(),
      collateralMint: getNextAccount(),
      tokenProgram: getNextAccount(),
    },
    data: getPublicClaimInstructionDataDecoder().decode(instruction.data),
  };
}
