/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findCustodyPda, findReservePda, findSeatPda } from "../pdas";
import { AGARI_LEVERAGE_PROGRAM_ADDRESS } from "../programs";
import {
  getLeverageParamsDecoder,
  getLeverageParamsEncoder,
  type LeverageParams,
  type LeverageParamsArgs,
} from "../types";

export const ADMIN_INIT_RESERVE_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([52, 121, 137, 255, 134, 216, 115, 8]);

export function getAdminInitReserveDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ADMIN_INIT_RESERVE_DISCRIMINATOR,
  );
}

export type AdminInitReserveInstruction<
  TProgram extends string = typeof AGARI_LEVERAGE_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountReserve extends string | AccountMeta<string> = string,
  TAccountSeat extends string | AccountMeta<string> = string,
  TAccountCustody extends string | AccountMeta<string> = string,
  TAccountEventsConfig extends string | AccountMeta<string> =
    "42GFppq2YX3LVet2mLEb6EKs1MzmUY38ds3F9VRrqvQ3",
  TAccountCollateralMint extends string | AccountMeta<string> = string,
  TAccountProgram extends string | AccountMeta<string> =
    "2yMrhq686tL6uAUFHfKGsPZAWSGoQoRW9HxNvnAZJeQb",
  TAccountProgramData extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? WritableSignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountReserve extends string
        ? WritableAccount<TAccountReserve>
        : TAccountReserve,
      TAccountSeat extends string
        ? ReadonlyAccount<TAccountSeat>
        : TAccountSeat,
      TAccountCustody extends string
        ? WritableAccount<TAccountCustody>
        : TAccountCustody,
      TAccountEventsConfig extends string
        ? ReadonlyAccount<TAccountEventsConfig>
        : TAccountEventsConfig,
      TAccountCollateralMint extends string
        ? ReadonlyAccount<TAccountCollateralMint>
        : TAccountCollateralMint,
      TAccountProgram extends string
        ? ReadonlyAccount<TAccountProgram>
        : TAccountProgram,
      TAccountProgramData extends string
        ? ReadonlyAccount<TAccountProgramData>
        : TAccountProgramData,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type AdminInitReserveInstructionData = {
  discriminator: ReadonlyUint8Array;
  params: LeverageParams;
};

export type AdminInitReserveInstructionDataArgs = {
  params: LeverageParamsArgs;
};

export function getAdminInitReserveInstructionDataEncoder(): FixedSizeEncoder<AdminInitReserveInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["params", getLeverageParamsEncoder()],
    ]),
    (value) => ({ ...value, discriminator: ADMIN_INIT_RESERVE_DISCRIMINATOR }),
  );
}

export function getAdminInitReserveInstructionDataDecoder(): FixedSizeDecoder<AdminInitReserveInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["params", getLeverageParamsDecoder()],
  ]);
}

export function getAdminInitReserveInstructionDataCodec(): FixedSizeCodec<
  AdminInitReserveInstructionDataArgs,
  AdminInitReserveInstructionData
> {
  return combineCodec(
    getAdminInitReserveInstructionDataEncoder(),
    getAdminInitReserveInstructionDataDecoder(),
  );
}

export type AdminInitReserveAsyncInput<
  TAccountAdmin extends string = string,
  TAccountReserve extends string = string,
  TAccountSeat extends string = string,
  TAccountCustody extends string = string,
  TAccountEventsConfig extends string = string,
  TAccountCollateralMint extends string = string,
  TAccountProgram extends string = string,
  TAccountProgramData extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  reserve?: Address<TAccountReserve>;
  seat?: Address<TAccountSeat>;
  /** Every unit the reserve holds. Its authority is the seat, because the engine moves it as the seat. */
  custody?: Address<TAccountCustody>;
  eventsConfig?: Address<TAccountEventsConfig>;
  collateralMint: Address<TAccountCollateralMint>;
  program?: Address<TAccountProgram>;
  programData: Address<TAccountProgramData>;
  tokenProgram?: Address<TAccountTokenProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
  params: AdminInitReserveInstructionDataArgs["params"];
};

export async function getAdminInitReserveInstructionAsync<
  TAccountAdmin extends string,
  TAccountReserve extends string,
  TAccountSeat extends string,
  TAccountCustody extends string,
  TAccountEventsConfig extends string,
  TAccountCollateralMint extends string,
  TAccountProgram extends string,
  TAccountProgramData extends string,
  TAccountTokenProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGARI_LEVERAGE_PROGRAM_ADDRESS,
>(
  input: AdminInitReserveAsyncInput<
    TAccountAdmin,
    TAccountReserve,
    TAccountSeat,
    TAccountCustody,
    TAccountEventsConfig,
    TAccountCollateralMint,
    TAccountProgram,
    TAccountProgramData,
    TAccountTokenProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  AdminInitReserveInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountReserve,
    TAccountSeat,
    TAccountCustody,
    TAccountEventsConfig,
    TAccountCollateralMint,
    TAccountProgram,
    TAccountProgramData,
    TAccountTokenProgram,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGARI_LEVERAGE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    reserve: { value: input.reserve ?? null, isWritable: true },
    seat: { value: input.seat ?? null, isWritable: false },
    custody: { value: input.custody ?? null, isWritable: true },
    eventsConfig: { value: input.eventsConfig ?? null, isWritable: false },
    collateralMint: { value: input.collateralMint ?? null, isWritable: false },
    program: { value: input.program ?? null, isWritable: false },
    programData: { value: input.programData ?? null, isWritable: false },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.reserve.value) {
    accounts.reserve.value = await findReservePda({ programAddress });
  }
  if (!accounts.seat.value) {
    accounts.seat.value = await findSeatPda({ programAddress });
  }
  if (!accounts.custody.value) {
    accounts.custody.value = await findCustodyPda({ programAddress });
  }
  if (!accounts.eventsConfig.value) {
    accounts.eventsConfig.value =
      "42GFppq2YX3LVet2mLEb6EKs1MzmUY38ds3F9VRrqvQ3" as Address<"42GFppq2YX3LVet2mLEb6EKs1MzmUY38ds3F9VRrqvQ3">;
  }
  if (!accounts.program.value) {
    accounts.program.value =
      "2yMrhq686tL6uAUFHfKGsPZAWSGoQoRW9HxNvnAZJeQb" as Address<"2yMrhq686tL6uAUFHfKGsPZAWSGoQoRW9HxNvnAZJeQb">;
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("reserve", accounts.reserve),
      getAccountMeta("seat", accounts.seat),
      getAccountMeta("custody", accounts.custody),
      getAccountMeta("eventsConfig", accounts.eventsConfig),
      getAccountMeta("collateralMint", accounts.collateralMint),
      getAccountMeta("program", accounts.program),
      getAccountMeta("programData", accounts.programData),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getAdminInitReserveInstructionDataEncoder().encode(
      args as AdminInitReserveInstructionDataArgs,
    ),
    programAddress,
  } as AdminInitReserveInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountReserve,
    TAccountSeat,
    TAccountCustody,
    TAccountEventsConfig,
    TAccountCollateralMint,
    TAccountProgram,
    TAccountProgramData,
    TAccountTokenProgram,
    TAccountSystemProgram
  >);
}

export type AdminInitReserveInput<
  TAccountAdmin extends string = string,
  TAccountReserve extends string = string,
  TAccountSeat extends string = string,
  TAccountCustody extends string = string,
  TAccountEventsConfig extends string = string,
  TAccountCollateralMint extends string = string,
  TAccountProgram extends string = string,
  TAccountProgramData extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  reserve: Address<TAccountReserve>;
  seat: Address<TAccountSeat>;
  /** Every unit the reserve holds. Its authority is the seat, because the engine moves it as the seat. */
  custody: Address<TAccountCustody>;
  eventsConfig?: Address<TAccountEventsConfig>;
  collateralMint: Address<TAccountCollateralMint>;
  program?: Address<TAccountProgram>;
  programData: Address<TAccountProgramData>;
  tokenProgram?: Address<TAccountTokenProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
  params: AdminInitReserveInstructionDataArgs["params"];
};

export function getAdminInitReserveInstruction<
  TAccountAdmin extends string,
  TAccountReserve extends string,
  TAccountSeat extends string,
  TAccountCustody extends string,
  TAccountEventsConfig extends string,
  TAccountCollateralMint extends string,
  TAccountProgram extends string,
  TAccountProgramData extends string,
  TAccountTokenProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGARI_LEVERAGE_PROGRAM_ADDRESS,
>(
  input: AdminInitReserveInput<
    TAccountAdmin,
    TAccountReserve,
    TAccountSeat,
    TAccountCustody,
    TAccountEventsConfig,
    TAccountCollateralMint,
    TAccountProgram,
    TAccountProgramData,
    TAccountTokenProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): AdminInitReserveInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountReserve,
  TAccountSeat,
  TAccountCustody,
  TAccountEventsConfig,
  TAccountCollateralMint,
  TAccountProgram,
  TAccountProgramData,
  TAccountTokenProgram,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGARI_LEVERAGE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    reserve: { value: input.reserve ?? null, isWritable: true },
    seat: { value: input.seat ?? null, isWritable: false },
    custody: { value: input.custody ?? null, isWritable: true },
    eventsConfig: { value: input.eventsConfig ?? null, isWritable: false },
    collateralMint: { value: input.collateralMint ?? null, isWritable: false },
    program: { value: input.program ?? null, isWritable: false },
    programData: { value: input.programData ?? null, isWritable: false },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.eventsConfig.value) {
    accounts.eventsConfig.value =
      "42GFppq2YX3LVet2mLEb6EKs1MzmUY38ds3F9VRrqvQ3" as Address<"42GFppq2YX3LVet2mLEb6EKs1MzmUY38ds3F9VRrqvQ3">;
  }
  if (!accounts.program.value) {
    accounts.program.value =
      "2yMrhq686tL6uAUFHfKGsPZAWSGoQoRW9HxNvnAZJeQb" as Address<"2yMrhq686tL6uAUFHfKGsPZAWSGoQoRW9HxNvnAZJeQb">;
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("reserve", accounts.reserve),
      getAccountMeta("seat", accounts.seat),
      getAccountMeta("custody", accounts.custody),
      getAccountMeta("eventsConfig", accounts.eventsConfig),
      getAccountMeta("collateralMint", accounts.collateralMint),
      getAccountMeta("program", accounts.program),
      getAccountMeta("programData", accounts.programData),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getAdminInitReserveInstructionDataEncoder().encode(
      args as AdminInitReserveInstructionDataArgs,
    ),
    programAddress,
  } as AdminInitReserveInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountReserve,
    TAccountSeat,
    TAccountCustody,
    TAccountEventsConfig,
    TAccountCollateralMint,
    TAccountProgram,
    TAccountProgramData,
    TAccountTokenProgram,
    TAccountSystemProgram
  >);
}

export type ParsedAdminInitReserveInstruction<
  TProgram extends string = typeof AGARI_LEVERAGE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    reserve: TAccountMetas[1];
    seat: TAccountMetas[2];
    /** Every unit the reserve holds. Its authority is the seat, because the engine moves it as the seat. */
    custody: TAccountMetas[3];
    eventsConfig: TAccountMetas[4];
    collateralMint: TAccountMetas[5];
    program: TAccountMetas[6];
    programData: TAccountMetas[7];
    tokenProgram: TAccountMetas[8];
    systemProgram: TAccountMetas[9];
  };
  data: AdminInitReserveInstructionData;
};

export function parseAdminInitReserveInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAdminInitReserveInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 10) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 10,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      admin: getNextAccount(),
      reserve: getNextAccount(),
      seat: getNextAccount(),
      custody: getNextAccount(),
      eventsConfig: getNextAccount(),
      collateralMint: getNextAccount(),
      program: getNextAccount(),
      programData: getNextAccount(),
      tokenProgram: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getAdminInitReserveInstructionDataDecoder().decode(instruction.data),
  };
}
