/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const ALLOWED_EVENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  106, 173, 214, 122, 135, 22, 173, 117,
]);

export function getAllowedEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ALLOWED_EVENT_DISCRIMINATOR,
  );
}

export type AllowedEvent = { owner: Address; allowanceBase: bigint };

export type AllowedEventArgs = {
  owner: Address;
  allowanceBase: number | bigint;
};

/** Gets the encoder for {@link AllowedEventArgs} event data. */
export function getAllowedEventEncoder(): FixedSizeEncoder<AllowedEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["owner", getAddressEncoder()],
      ["allowanceBase", getU64Encoder()],
    ]),
    [getConstantEncoder(ALLOWED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link AllowedEvent} event data. */
export function getAllowedEventDecoder(): FixedSizeDecoder<AllowedEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["owner", getAddressDecoder()],
      ["allowanceBase", getU64Decoder()],
    ]),
    [getConstantDecoder(ALLOWED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link AllowedEvent} event data. */
export function getAllowedEventCodec(): FixedSizeCodec<
  AllowedEventArgs,
  AllowedEvent
> {
  return combineCodec(getAllowedEventEncoder(), getAllowedEventDecoder());
}

/** Parses the provided bytes into {@link AllowedEvent} event data. */
export function parseAllowedEvent(
  data: ReadonlyUint8Array | Uint8Array,
): AllowedEvent {
  if (containsBytes(data, ALLOWED_EVENT_DISCRIMINATOR, 0)) {
    return getAllowedEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "AllowedEvent" event discriminators.',
  );
}
