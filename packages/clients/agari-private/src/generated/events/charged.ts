/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const CHARGED_EVENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  167, 170, 67, 179, 47, 88, 221, 132,
]);

export function getChargedEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CHARGED_EVENT_DISCRIMINATOR,
  );
}

export type ChargedEvent = {
  owner: Address;
  chargeKey: ReadonlyUint8Array;
  amountBase: bigint;
};

export type ChargedEventArgs = {
  owner: Address;
  chargeKey: ReadonlyUint8Array;
  amountBase: number | bigint;
};

/** Gets the encoder for {@link ChargedEventArgs} event data. */
export function getChargedEventEncoder(): FixedSizeEncoder<ChargedEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["owner", getAddressEncoder()],
      ["chargeKey", fixEncoderSize(getBytesEncoder(), 32)],
      ["amountBase", getU64Encoder()],
    ]),
    [getConstantEncoder(CHARGED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link ChargedEvent} event data. */
export function getChargedEventDecoder(): FixedSizeDecoder<ChargedEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["owner", getAddressDecoder()],
      ["chargeKey", fixDecoderSize(getBytesDecoder(), 32)],
      ["amountBase", getU64Decoder()],
    ]),
    [getConstantDecoder(CHARGED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link ChargedEvent} event data. */
export function getChargedEventCodec(): FixedSizeCodec<
  ChargedEventArgs,
  ChargedEvent
> {
  return combineCodec(getChargedEventEncoder(), getChargedEventDecoder());
}

/** Parses the provided bytes into {@link ChargedEvent} event data. */
export function parseChargedEvent(
  data: ReadonlyUint8Array | Uint8Array,
): ChargedEvent {
  if (containsBytes(data, CHARGED_EVENT_DISCRIMINATOR, 0)) {
    return getChargedEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "ChargedEvent" event discriminators.',
  );
}
