/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const CREDITED_EVENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  142, 248, 247, 7, 241, 250, 230, 55,
]);

export function getCreditedEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CREDITED_EVENT_DISCRIMINATOR,
  );
}

export type CreditedEvent = {
  owner: Address;
  creditKey: ReadonlyUint8Array;
  amountBase: bigint;
};

export type CreditedEventArgs = {
  owner: Address;
  creditKey: ReadonlyUint8Array;
  amountBase: number | bigint;
};

/** Gets the encoder for {@link CreditedEventArgs} event data. */
export function getCreditedEventEncoder(): FixedSizeEncoder<CreditedEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["owner", getAddressEncoder()],
      ["creditKey", fixEncoderSize(getBytesEncoder(), 32)],
      ["amountBase", getU64Encoder()],
    ]),
    [getConstantEncoder(CREDITED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link CreditedEvent} event data. */
export function getCreditedEventDecoder(): FixedSizeDecoder<CreditedEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["owner", getAddressDecoder()],
      ["creditKey", fixDecoderSize(getBytesDecoder(), 32)],
      ["amountBase", getU64Decoder()],
    ]),
    [getConstantDecoder(CREDITED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link CreditedEvent} event data. */
export function getCreditedEventCodec(): FixedSizeCodec<
  CreditedEventArgs,
  CreditedEvent
> {
  return combineCodec(getCreditedEventEncoder(), getCreditedEventDecoder());
}

/** Parses the provided bytes into {@link CreditedEvent} event data. */
export function parseCreditedEvent(
  data: ReadonlyUint8Array | Uint8Array,
): CreditedEvent {
  if (containsBytes(data, CREDITED_EVENT_DISCRIMINATOR, 0)) {
    return getCreditedEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "CreditedEvent" event discriminators.',
  );
}
