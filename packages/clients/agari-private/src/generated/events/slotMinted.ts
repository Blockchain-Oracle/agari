/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const SLOT_MINTED_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([153, 174, 230, 123, 24, 132, 130, 115]);

export function getSlotMintedEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SLOT_MINTED_EVENT_DISCRIMINATOR,
  );
}

export type SlotMintedEvent = {
  slotId: ReadonlyUint8Array;
  market: Address;
  outcome: number;
  lots: bigint;
  costBase: bigint;
};

export type SlotMintedEventArgs = {
  slotId: ReadonlyUint8Array;
  market: Address;
  outcome: number;
  lots: number | bigint;
  costBase: number | bigint;
};

/** Gets the encoder for {@link SlotMintedEventArgs} event data. */
export function getSlotMintedEventEncoder(): FixedSizeEncoder<SlotMintedEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["slotId", fixEncoderSize(getBytesEncoder(), 32)],
      ["market", getAddressEncoder()],
      ["outcome", getU8Encoder()],
      ["lots", getU64Encoder()],
      ["costBase", getU64Encoder()],
    ]),
    [getConstantEncoder(SLOT_MINTED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link SlotMintedEvent} event data. */
export function getSlotMintedEventDecoder(): FixedSizeDecoder<SlotMintedEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["slotId", fixDecoderSize(getBytesDecoder(), 32)],
      ["market", getAddressDecoder()],
      ["outcome", getU8Decoder()],
      ["lots", getU64Decoder()],
      ["costBase", getU64Decoder()],
    ]),
    [getConstantDecoder(SLOT_MINTED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link SlotMintedEvent} event data. */
export function getSlotMintedEventCodec(): FixedSizeCodec<
  SlotMintedEventArgs,
  SlotMintedEvent
> {
  return combineCodec(getSlotMintedEventEncoder(), getSlotMintedEventDecoder());
}

/** Parses the provided bytes into {@link SlotMintedEvent} event data. */
export function parseSlotMintedEvent(
  data: ReadonlyUint8Array | Uint8Array,
): SlotMintedEvent {
  if (containsBytes(data, SLOT_MINTED_EVENT_DISCRIMINATOR, 0)) {
    return getSlotMintedEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "SlotMintedEvent" event discriminators.',
  );
}
