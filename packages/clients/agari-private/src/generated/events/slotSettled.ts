/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const SLOT_SETTLED_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([252, 32, 33, 185, 200, 156, 247, 236]);

export function getSlotSettledEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SLOT_SETTLED_EVENT_DISCRIMINATOR,
  );
}

export type SlotSettledEvent = {
  slotId: ReadonlyUint8Array;
  market: Address;
  payoutBase: bigint;
  by: Address;
};

export type SlotSettledEventArgs = {
  slotId: ReadonlyUint8Array;
  market: Address;
  payoutBase: number | bigint;
  by: Address;
};

/** Gets the encoder for {@link SlotSettledEventArgs} event data. */
export function getSlotSettledEventEncoder(): FixedSizeEncoder<SlotSettledEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["slotId", fixEncoderSize(getBytesEncoder(), 32)],
      ["market", getAddressEncoder()],
      ["payoutBase", getU64Encoder()],
      ["by", getAddressEncoder()],
    ]),
    [getConstantEncoder(SLOT_SETTLED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link SlotSettledEvent} event data. */
export function getSlotSettledEventDecoder(): FixedSizeDecoder<SlotSettledEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["slotId", fixDecoderSize(getBytesDecoder(), 32)],
      ["market", getAddressDecoder()],
      ["payoutBase", getU64Decoder()],
      ["by", getAddressDecoder()],
    ]),
    [getConstantDecoder(SLOT_SETTLED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link SlotSettledEvent} event data. */
export function getSlotSettledEventCodec(): FixedSizeCodec<
  SlotSettledEventArgs,
  SlotSettledEvent
> {
  return combineCodec(
    getSlotSettledEventEncoder(),
    getSlotSettledEventDecoder(),
  );
}

/** Parses the provided bytes into {@link SlotSettledEvent} event data. */
export function parseSlotSettledEvent(
  data: ReadonlyUint8Array | Uint8Array,
): SlotSettledEvent {
  if (containsBytes(data, SLOT_SETTLED_EVENT_DISCRIMINATOR, 0)) {
    return getSlotSettledEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "SlotSettledEvent" event discriminators.',
  );
}
