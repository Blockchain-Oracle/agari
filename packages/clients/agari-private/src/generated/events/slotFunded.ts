/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const SLOT_FUNDED_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([82, 151, 146, 230, 100, 173, 242, 125]);

export function getSlotFundedEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SLOT_FUNDED_EVENT_DISCRIMINATOR,
  );
}

export type SlotFundedEvent = {
  slotId: ReadonlyUint8Array;
  amountBase: bigint;
};

export type SlotFundedEventArgs = {
  slotId: ReadonlyUint8Array;
  amountBase: number | bigint;
};

/** Gets the encoder for {@link SlotFundedEventArgs} event data. */
export function getSlotFundedEventEncoder(): FixedSizeEncoder<SlotFundedEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["slotId", fixEncoderSize(getBytesEncoder(), 32)],
      ["amountBase", getU64Encoder()],
    ]),
    [getConstantEncoder(SLOT_FUNDED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link SlotFundedEvent} event data. */
export function getSlotFundedEventDecoder(): FixedSizeDecoder<SlotFundedEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["slotId", fixDecoderSize(getBytesDecoder(), 32)],
      ["amountBase", getU64Decoder()],
    ]),
    [getConstantDecoder(SLOT_FUNDED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link SlotFundedEvent} event data. */
export function getSlotFundedEventCodec(): FixedSizeCodec<
  SlotFundedEventArgs,
  SlotFundedEvent
> {
  return combineCodec(getSlotFundedEventEncoder(), getSlotFundedEventDecoder());
}

/** Parses the provided bytes into {@link SlotFundedEvent} event data. */
export function parseSlotFundedEvent(
  data: ReadonlyUint8Array | Uint8Array,
): SlotFundedEvent {
  if (containsBytes(data, SLOT_FUNDED_EVENT_DISCRIMINATOR, 0)) {
    return getSlotFundedEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "SlotFundedEvent" event discriminators.',
  );
}
