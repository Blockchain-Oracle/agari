/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getPrivateParamsDecoder,
  getPrivateParamsEncoder,
  type PrivateParams,
  type PrivateParamsArgs,
} from "../types";

export const DESK_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  33, 28, 147, 6, 226, 158, 166, 73,
]);

export function getDeskDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(DESK_DISCRIMINATOR);
}

export type Desk = {
  discriminator: ReadonlyUint8Array;
  admin: Address;
  /** The one key that may charge, fund, mint, sweep and credit. It can never withdraw: only an owner can. */
  desk: Address;
  collateralMint: Address;
  params: PrivateParams;
  /** Money nobody has claimed yet: charged and not yet in a slot, or swept and not yet credited. */
  poolBase: bigint;
  /** The sum of every owner's balance. */
  owedBase: bigint;
  /** The sum of every slot's cash. */
  inSlotsBase: bigint;
  paused: boolean;
  bump: number;
  seatBump: number;
  custodyBump: number;
};

export type DeskArgs = {
  admin: Address;
  /** The one key that may charge, fund, mint, sweep and credit. It can never withdraw: only an owner can. */
  desk: Address;
  collateralMint: Address;
  params: PrivateParamsArgs;
  /** Money nobody has claimed yet: charged and not yet in a slot, or swept and not yet credited. */
  poolBase: number | bigint;
  /** The sum of every owner's balance. */
  owedBase: number | bigint;
  /** The sum of every slot's cash. */
  inSlotsBase: number | bigint;
  paused: boolean;
  bump: number;
  seatBump: number;
  custodyBump: number;
};

/** Gets the encoder for {@link DeskArgs} account data. */
export function getDeskEncoder(): FixedSizeEncoder<DeskArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["admin", getAddressEncoder()],
      ["desk", getAddressEncoder()],
      ["collateralMint", getAddressEncoder()],
      ["params", getPrivateParamsEncoder()],
      ["poolBase", getU64Encoder()],
      ["owedBase", getU64Encoder()],
      ["inSlotsBase", getU64Encoder()],
      ["paused", getBooleanEncoder()],
      ["bump", getU8Encoder()],
      ["seatBump", getU8Encoder()],
      ["custodyBump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: DESK_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link Desk} account data. */
export function getDeskDecoder(): FixedSizeDecoder<Desk> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["admin", getAddressDecoder()],
    ["desk", getAddressDecoder()],
    ["collateralMint", getAddressDecoder()],
    ["params", getPrivateParamsDecoder()],
    ["poolBase", getU64Decoder()],
    ["owedBase", getU64Decoder()],
    ["inSlotsBase", getU64Decoder()],
    ["paused", getBooleanDecoder()],
    ["bump", getU8Decoder()],
    ["seatBump", getU8Decoder()],
    ["custodyBump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link Desk} account data. */
export function getDeskCodec(): FixedSizeCodec<DeskArgs, Desk> {
  return combineCodec(getDeskEncoder(), getDeskDecoder());
}

export function decodeDesk<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<Desk, TAddress>;
export function decodeDesk<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<Desk, TAddress>;
export function decodeDesk<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<Desk, TAddress> | MaybeAccount<Desk, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getDeskDecoder(),
  );
}

export async function fetchDesk<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Desk, TAddress>> {
  const maybeAccount = await fetchMaybeDesk(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeDesk<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Desk, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeDesk(maybeAccount);
}

export async function fetchAllDesk(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Desk>[]> {
  const maybeAccounts = await fetchAllMaybeDesk(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeDesk(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Desk>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeDesk(maybeAccount));
}

export function getDeskSize(): number {
  return 152;
}
