/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findDeskAccountPda, findSlotPda } from "../pdas";
import { AGARI_PRIVATE_PROGRAM_ADDRESS } from "../programs";

export const DESK_SWEEP_SLOT_TO_POOL_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([98, 212, 147, 240, 148, 144, 156, 163]);

export function getDeskSweepSlotToPoolDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    DESK_SWEEP_SLOT_TO_POOL_DISCRIMINATOR,
  );
}

export type DeskSweepSlotToPoolInstruction<
  TProgram extends string = typeof AGARI_PRIVATE_PROGRAM_ADDRESS,
  TAccountDesk extends string | AccountMeta<string> = string,
  TAccountDeskAccount extends string | AccountMeta<string> = string,
  TAccountSlot extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountDesk extends string
        ? ReadonlySignerAccount<TAccountDesk> & AccountSignerMeta<TAccountDesk>
        : TAccountDesk,
      TAccountDeskAccount extends string
        ? WritableAccount<TAccountDeskAccount>
        : TAccountDeskAccount,
      TAccountSlot extends string
        ? WritableAccount<TAccountSlot>
        : TAccountSlot,
      ...TRemainingAccounts,
    ]
  >;

export type DeskSweepSlotToPoolInstructionData = {
  discriminator: ReadonlyUint8Array;
  slotId: ReadonlyUint8Array;
};

export type DeskSweepSlotToPoolInstructionDataArgs = {
  slotId: ReadonlyUint8Array;
};

export function getDeskSweepSlotToPoolInstructionDataEncoder(): FixedSizeEncoder<DeskSweepSlotToPoolInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["slotId", fixEncoderSize(getBytesEncoder(), 32)],
    ]),
    (value) => ({
      ...value,
      discriminator: DESK_SWEEP_SLOT_TO_POOL_DISCRIMINATOR,
    }),
  );
}

export function getDeskSweepSlotToPoolInstructionDataDecoder(): FixedSizeDecoder<DeskSweepSlotToPoolInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["slotId", fixDecoderSize(getBytesDecoder(), 32)],
  ]);
}

export function getDeskSweepSlotToPoolInstructionDataCodec(): FixedSizeCodec<
  DeskSweepSlotToPoolInstructionDataArgs,
  DeskSweepSlotToPoolInstructionData
> {
  return combineCodec(
    getDeskSweepSlotToPoolInstructionDataEncoder(),
    getDeskSweepSlotToPoolInstructionDataDecoder(),
  );
}

export type DeskSweepSlotToPoolAsyncInput<
  TAccountDesk extends string = string,
  TAccountDeskAccount extends string = string,
  TAccountSlot extends string = string,
> = {
  desk: TransactionSigner<TAccountDesk>;
  deskAccount?: Address<TAccountDeskAccount>;
  slot?: Address<TAccountSlot>;
  slotId: DeskSweepSlotToPoolInstructionDataArgs["slotId"];
};

export async function getDeskSweepSlotToPoolInstructionAsync<
  TAccountDesk extends string,
  TAccountDeskAccount extends string,
  TAccountSlot extends string,
  TProgramAddress extends Address = typeof AGARI_PRIVATE_PROGRAM_ADDRESS,
>(
  input: DeskSweepSlotToPoolAsyncInput<
    TAccountDesk,
    TAccountDeskAccount,
    TAccountSlot
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  DeskSweepSlotToPoolInstruction<
    TProgramAddress,
    TAccountDesk,
    TAccountDeskAccount,
    TAccountSlot
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGARI_PRIVATE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    desk: { value: input.desk ?? null, isWritable: false },
    deskAccount: { value: input.deskAccount ?? null, isWritable: true },
    slot: { value: input.slot ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.deskAccount.value) {
    accounts.deskAccount.value = await findDeskAccountPda({ programAddress });
  }
  if (!accounts.slot.value) {
    accounts.slot.value = await findSlotPda(
      { slotId: getNonNullResolvedInstructionInput("slotId", args.slotId) },
      { programAddress },
    );
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("desk", accounts.desk),
      getAccountMeta("deskAccount", accounts.deskAccount),
      getAccountMeta("slot", accounts.slot),
    ],
    data: getDeskSweepSlotToPoolInstructionDataEncoder().encode(
      args as DeskSweepSlotToPoolInstructionDataArgs,
    ),
    programAddress,
  } as DeskSweepSlotToPoolInstruction<
    TProgramAddress,
    TAccountDesk,
    TAccountDeskAccount,
    TAccountSlot
  >);
}

export type DeskSweepSlotToPoolInput<
  TAccountDesk extends string = string,
  TAccountDeskAccount extends string = string,
  TAccountSlot extends string = string,
> = {
  desk: TransactionSigner<TAccountDesk>;
  deskAccount: Address<TAccountDeskAccount>;
  slot: Address<TAccountSlot>;
  slotId: DeskSweepSlotToPoolInstructionDataArgs["slotId"];
};

export function getDeskSweepSlotToPoolInstruction<
  TAccountDesk extends string,
  TAccountDeskAccount extends string,
  TAccountSlot extends string,
  TProgramAddress extends Address = typeof AGARI_PRIVATE_PROGRAM_ADDRESS,
>(
  input: DeskSweepSlotToPoolInput<
    TAccountDesk,
    TAccountDeskAccount,
    TAccountSlot
  >,
  config?: { programAddress?: TProgramAddress },
): DeskSweepSlotToPoolInstruction<
  TProgramAddress,
  TAccountDesk,
  TAccountDeskAccount,
  TAccountSlot
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGARI_PRIVATE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    desk: { value: input.desk ?? null, isWritable: false },
    deskAccount: { value: input.deskAccount ?? null, isWritable: true },
    slot: { value: input.slot ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("desk", accounts.desk),
      getAccountMeta("deskAccount", accounts.deskAccount),
      getAccountMeta("slot", accounts.slot),
    ],
    data: getDeskSweepSlotToPoolInstructionDataEncoder().encode(
      args as DeskSweepSlotToPoolInstructionDataArgs,
    ),
    programAddress,
  } as DeskSweepSlotToPoolInstruction<
    TProgramAddress,
    TAccountDesk,
    TAccountDeskAccount,
    TAccountSlot
  >);
}

export type ParsedDeskSweepSlotToPoolInstruction<
  TProgram extends string = typeof AGARI_PRIVATE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    desk: TAccountMetas[0];
    deskAccount: TAccountMetas[1];
    slot: TAccountMetas[2];
  };
  data: DeskSweepSlotToPoolInstructionData;
};

export function parseDeskSweepSlotToPoolInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedDeskSweepSlotToPoolInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      desk: getNextAccount(),
      deskAccount: getNextAccount(),
      slot: getNextAccount(),
    },
    data: getDeskSweepSlotToPoolInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
