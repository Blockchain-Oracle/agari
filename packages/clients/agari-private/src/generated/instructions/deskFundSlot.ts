/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findDeskAccountPda, findSlotPda } from "../pdas";
import { AGARI_PRIVATE_PROGRAM_ADDRESS } from "../programs";

export const DESK_FUND_SLOT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  244, 51, 88, 25, 226, 40, 175, 176,
]);

export function getDeskFundSlotDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    DESK_FUND_SLOT_DISCRIMINATOR,
  );
}

export type DeskFundSlotInstruction<
  TProgram extends string = typeof AGARI_PRIVATE_PROGRAM_ADDRESS,
  TAccountDesk extends string | AccountMeta<string> = string,
  TAccountDeskAccount extends string | AccountMeta<string> = string,
  TAccountSlot extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountDesk extends string
        ? WritableSignerAccount<TAccountDesk> & AccountSignerMeta<TAccountDesk>
        : TAccountDesk,
      TAccountDeskAccount extends string
        ? WritableAccount<TAccountDeskAccount>
        : TAccountDeskAccount,
      TAccountSlot extends string
        ? WritableAccount<TAccountSlot>
        : TAccountSlot,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type DeskFundSlotInstructionData = {
  discriminator: ReadonlyUint8Array;
  slotId: ReadonlyUint8Array;
  amountBase: bigint;
};

export type DeskFundSlotInstructionDataArgs = {
  slotId: ReadonlyUint8Array;
  amountBase: number | bigint;
};

export function getDeskFundSlotInstructionDataEncoder(): FixedSizeEncoder<DeskFundSlotInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["slotId", fixEncoderSize(getBytesEncoder(), 32)],
      ["amountBase", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: DESK_FUND_SLOT_DISCRIMINATOR }),
  );
}

export function getDeskFundSlotInstructionDataDecoder(): FixedSizeDecoder<DeskFundSlotInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["slotId", fixDecoderSize(getBytesDecoder(), 32)],
    ["amountBase", getU64Decoder()],
  ]);
}

export function getDeskFundSlotInstructionDataCodec(): FixedSizeCodec<
  DeskFundSlotInstructionDataArgs,
  DeskFundSlotInstructionData
> {
  return combineCodec(
    getDeskFundSlotInstructionDataEncoder(),
    getDeskFundSlotInstructionDataDecoder(),
  );
}

export type DeskFundSlotAsyncInput<
  TAccountDesk extends string = string,
  TAccountDeskAccount extends string = string,
  TAccountSlot extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  desk: TransactionSigner<TAccountDesk>;
  deskAccount?: Address<TAccountDeskAccount>;
  slot?: Address<TAccountSlot>;
  systemProgram?: Address<TAccountSystemProgram>;
  slotId: DeskFundSlotInstructionDataArgs["slotId"];
  amountBase: DeskFundSlotInstructionDataArgs["amountBase"];
};

export async function getDeskFundSlotInstructionAsync<
  TAccountDesk extends string,
  TAccountDeskAccount extends string,
  TAccountSlot extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGARI_PRIVATE_PROGRAM_ADDRESS,
>(
  input: DeskFundSlotAsyncInput<
    TAccountDesk,
    TAccountDeskAccount,
    TAccountSlot,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  DeskFundSlotInstruction<
    TProgramAddress,
    TAccountDesk,
    TAccountDeskAccount,
    TAccountSlot,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGARI_PRIVATE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    desk: { value: input.desk ?? null, isWritable: true },
    deskAccount: { value: input.deskAccount ?? null, isWritable: true },
    slot: { value: input.slot ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.deskAccount.value) {
    accounts.deskAccount.value = await findDeskAccountPda({ programAddress });
  }
  if (!accounts.slot.value) {
    accounts.slot.value = await findSlotPda(
      { slotId: getNonNullResolvedInstructionInput("slotId", args.slotId) },
      { programAddress },
    );
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("desk", accounts.desk),
      getAccountMeta("deskAccount", accounts.deskAccount),
      getAccountMeta("slot", accounts.slot),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getDeskFundSlotInstructionDataEncoder().encode(
      args as DeskFundSlotInstructionDataArgs,
    ),
    programAddress,
  } as DeskFundSlotInstruction<
    TProgramAddress,
    TAccountDesk,
    TAccountDeskAccount,
    TAccountSlot,
    TAccountSystemProgram
  >);
}

export type DeskFundSlotInput<
  TAccountDesk extends string = string,
  TAccountDeskAccount extends string = string,
  TAccountSlot extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  desk: TransactionSigner<TAccountDesk>;
  deskAccount: Address<TAccountDeskAccount>;
  slot: Address<TAccountSlot>;
  systemProgram?: Address<TAccountSystemProgram>;
  slotId: DeskFundSlotInstructionDataArgs["slotId"];
  amountBase: DeskFundSlotInstructionDataArgs["amountBase"];
};

export function getDeskFundSlotInstruction<
  TAccountDesk extends string,
  TAccountDeskAccount extends string,
  TAccountSlot extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGARI_PRIVATE_PROGRAM_ADDRESS,
>(
  input: DeskFundSlotInput<
    TAccountDesk,
    TAccountDeskAccount,
    TAccountSlot,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): DeskFundSlotInstruction<
  TProgramAddress,
  TAccountDesk,
  TAccountDeskAccount,
  TAccountSlot,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGARI_PRIVATE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    desk: { value: input.desk ?? null, isWritable: true },
    deskAccount: { value: input.deskAccount ?? null, isWritable: true },
    slot: { value: input.slot ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("desk", accounts.desk),
      getAccountMeta("deskAccount", accounts.deskAccount),
      getAccountMeta("slot", accounts.slot),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getDeskFundSlotInstructionDataEncoder().encode(
      args as DeskFundSlotInstructionDataArgs,
    ),
    programAddress,
  } as DeskFundSlotInstruction<
    TProgramAddress,
    TAccountDesk,
    TAccountDeskAccount,
    TAccountSlot,
    TAccountSystemProgram
  >);
}

export type ParsedDeskFundSlotInstruction<
  TProgram extends string = typeof AGARI_PRIVATE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    desk: TAccountMetas[0];
    deskAccount: TAccountMetas[1];
    slot: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: DeskFundSlotInstructionData;
};

export function parseDeskFundSlotInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedDeskFundSlotInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      desk: getNextAccount(),
      deskAccount: getNextAccount(),
      slot: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getDeskFundSlotInstructionDataDecoder().decode(instruction.data),
  };
}
