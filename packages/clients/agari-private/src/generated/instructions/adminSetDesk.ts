/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findDeskAccountPda } from "../pdas";
import { AGARI_PRIVATE_PROGRAM_ADDRESS } from "../programs";

export const ADMIN_SET_DESK_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  123, 207, 38, 94, 103, 52, 211, 115,
]);

export function getAdminSetDeskDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ADMIN_SET_DESK_DISCRIMINATOR,
  );
}

export type AdminSetDeskInstruction<
  TProgram extends string = typeof AGARI_PRIVATE_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountDeskAccount extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? ReadonlySignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountDeskAccount extends string
        ? WritableAccount<TAccountDeskAccount>
        : TAccountDeskAccount,
      ...TRemainingAccounts,
    ]
  >;

export type AdminSetDeskInstructionData = {
  discriminator: ReadonlyUint8Array;
  desk: Address;
};

export type AdminSetDeskInstructionDataArgs = { desk: Address };

export function getAdminSetDeskInstructionDataEncoder(): FixedSizeEncoder<AdminSetDeskInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["desk", getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: ADMIN_SET_DESK_DISCRIMINATOR }),
  );
}

export function getAdminSetDeskInstructionDataDecoder(): FixedSizeDecoder<AdminSetDeskInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["desk", getAddressDecoder()],
  ]);
}

export function getAdminSetDeskInstructionDataCodec(): FixedSizeCodec<
  AdminSetDeskInstructionDataArgs,
  AdminSetDeskInstructionData
> {
  return combineCodec(
    getAdminSetDeskInstructionDataEncoder(),
    getAdminSetDeskInstructionDataDecoder(),
  );
}

export type AdminSetDeskAsyncInput<
  TAccountAdmin extends string = string,
  TAccountDeskAccount extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  deskAccount?: Address<TAccountDeskAccount>;
  desk: AdminSetDeskInstructionDataArgs["desk"];
};

export async function getAdminSetDeskInstructionAsync<
  TAccountAdmin extends string,
  TAccountDeskAccount extends string,
  TProgramAddress extends Address = typeof AGARI_PRIVATE_PROGRAM_ADDRESS,
>(
  input: AdminSetDeskAsyncInput<TAccountAdmin, TAccountDeskAccount>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  AdminSetDeskInstruction<TProgramAddress, TAccountAdmin, TAccountDeskAccount>
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGARI_PRIVATE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    deskAccount: { value: input.deskAccount ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.deskAccount.value) {
    accounts.deskAccount.value = await findDeskAccountPda({ programAddress });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("deskAccount", accounts.deskAccount),
    ],
    data: getAdminSetDeskInstructionDataEncoder().encode(
      args as AdminSetDeskInstructionDataArgs,
    ),
    programAddress,
  } as AdminSetDeskInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountDeskAccount
  >);
}

export type AdminSetDeskInput<
  TAccountAdmin extends string = string,
  TAccountDeskAccount extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  deskAccount: Address<TAccountDeskAccount>;
  desk: AdminSetDeskInstructionDataArgs["desk"];
};

export function getAdminSetDeskInstruction<
  TAccountAdmin extends string,
  TAccountDeskAccount extends string,
  TProgramAddress extends Address = typeof AGARI_PRIVATE_PROGRAM_ADDRESS,
>(
  input: AdminSetDeskInput<TAccountAdmin, TAccountDeskAccount>,
  config?: { programAddress?: TProgramAddress },
): AdminSetDeskInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountDeskAccount
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGARI_PRIVATE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    deskAccount: { value: input.deskAccount ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("deskAccount", accounts.deskAccount),
    ],
    data: getAdminSetDeskInstructionDataEncoder().encode(
      args as AdminSetDeskInstructionDataArgs,
    ),
    programAddress,
  } as AdminSetDeskInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountDeskAccount
  >);
}

export type ParsedAdminSetDeskInstruction<
  TProgram extends string = typeof AGARI_PRIVATE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    deskAccount: TAccountMetas[1];
  };
  data: AdminSetDeskInstructionData;
};

export function parseAdminSetDeskInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAdminSetDeskInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { admin: getNextAccount(), deskAccount: getNextAccount() },
    data: getAdminSetDeskInstructionDataDecoder().decode(instruction.data),
  };
}
