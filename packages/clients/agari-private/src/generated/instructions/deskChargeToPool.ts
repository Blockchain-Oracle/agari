/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findBudgetPda, findDeskAccountPda, findMarkPda } from "../pdas";
import { AGARI_PRIVATE_PROGRAM_ADDRESS } from "../programs";

export const DESK_CHARGE_TO_POOL_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([18, 246, 12, 141, 134, 121, 145, 148]);

export function getDeskChargeToPoolDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    DESK_CHARGE_TO_POOL_DISCRIMINATOR,
  );
}

export type DeskChargeToPoolInstruction<
  TProgram extends string = typeof AGARI_PRIVATE_PROGRAM_ADDRESS,
  TAccountDesk extends string | AccountMeta<string> = string,
  TAccountDeskAccount extends string | AccountMeta<string> = string,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountBudget extends string | AccountMeta<string> = string,
  TAccountMark extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountDesk extends string
        ? WritableSignerAccount<TAccountDesk> & AccountSignerMeta<TAccountDesk>
        : TAccountDesk,
      TAccountDeskAccount extends string
        ? WritableAccount<TAccountDeskAccount>
        : TAccountDeskAccount,
      TAccountOwner extends string
        ? ReadonlyAccount<TAccountOwner>
        : TAccountOwner,
      TAccountBudget extends string
        ? WritableAccount<TAccountBudget>
        : TAccountBudget,
      TAccountMark extends string
        ? WritableAccount<TAccountMark>
        : TAccountMark,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type DeskChargeToPoolInstructionData = {
  discriminator: ReadonlyUint8Array;
  amountBase: bigint;
  chargeKey: ReadonlyUint8Array;
};

export type DeskChargeToPoolInstructionDataArgs = {
  amountBase: number | bigint;
  chargeKey: ReadonlyUint8Array;
};

export function getDeskChargeToPoolInstructionDataEncoder(): FixedSizeEncoder<DeskChargeToPoolInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["amountBase", getU64Encoder()],
      ["chargeKey", fixEncoderSize(getBytesEncoder(), 32)],
    ]),
    (value) => ({ ...value, discriminator: DESK_CHARGE_TO_POOL_DISCRIMINATOR }),
  );
}

export function getDeskChargeToPoolInstructionDataDecoder(): FixedSizeDecoder<DeskChargeToPoolInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["amountBase", getU64Decoder()],
    ["chargeKey", fixDecoderSize(getBytesDecoder(), 32)],
  ]);
}

export function getDeskChargeToPoolInstructionDataCodec(): FixedSizeCodec<
  DeskChargeToPoolInstructionDataArgs,
  DeskChargeToPoolInstructionData
> {
  return combineCodec(
    getDeskChargeToPoolInstructionDataEncoder(),
    getDeskChargeToPoolInstructionDataDecoder(),
  );
}

export type DeskChargeToPoolAsyncInput<
  TAccountDesk extends string = string,
  TAccountDeskAccount extends string = string,
  TAccountOwner extends string = string,
  TAccountBudget extends string = string,
  TAccountMark extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  desk: TransactionSigner<TAccountDesk>;
  deskAccount?: Address<TAccountDeskAccount>;
  owner: Address<TAccountOwner>;
  budget?: Address<TAccountBudget>;
  /** One per key. `init_if_needed` so a used key is refused as `KeyUsed` and not as a system error. */
  mark?: Address<TAccountMark>;
  systemProgram?: Address<TAccountSystemProgram>;
  amountBase: DeskChargeToPoolInstructionDataArgs["amountBase"];
  chargeKey: DeskChargeToPoolInstructionDataArgs["chargeKey"];
};

export async function getDeskChargeToPoolInstructionAsync<
  TAccountDesk extends string,
  TAccountDeskAccount extends string,
  TAccountOwner extends string,
  TAccountBudget extends string,
  TAccountMark extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGARI_PRIVATE_PROGRAM_ADDRESS,
>(
  input: DeskChargeToPoolAsyncInput<
    TAccountDesk,
    TAccountDeskAccount,
    TAccountOwner,
    TAccountBudget,
    TAccountMark,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  DeskChargeToPoolInstruction<
    TProgramAddress,
    TAccountDesk,
    TAccountDeskAccount,
    TAccountOwner,
    TAccountBudget,
    TAccountMark,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGARI_PRIVATE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    desk: { value: input.desk ?? null, isWritable: true },
    deskAccount: { value: input.deskAccount ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: false },
    budget: { value: input.budget ?? null, isWritable: true },
    mark: { value: input.mark ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.deskAccount.value) {
    accounts.deskAccount.value = await findDeskAccountPda({ programAddress });
  }
  if (!accounts.budget.value) {
    accounts.budget.value = await findBudgetPda(
      {
        owner: getAddressFromResolvedInstructionAccount(
          "owner",
          accounts.owner.value,
        ),
      },
      { programAddress },
    );
  }
  if (!accounts.mark.value) {
    accounts.mark.value = await findMarkPda(
      {
        owner: getAddressFromResolvedInstructionAccount(
          "owner",
          accounts.owner.value,
        ),
        chargeKey: getNonNullResolvedInstructionInput(
          "chargeKey",
          args.chargeKey,
        ),
      },
      { programAddress },
    );
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("desk", accounts.desk),
      getAccountMeta("deskAccount", accounts.deskAccount),
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("budget", accounts.budget),
      getAccountMeta("mark", accounts.mark),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getDeskChargeToPoolInstructionDataEncoder().encode(
      args as DeskChargeToPoolInstructionDataArgs,
    ),
    programAddress,
  } as DeskChargeToPoolInstruction<
    TProgramAddress,
    TAccountDesk,
    TAccountDeskAccount,
    TAccountOwner,
    TAccountBudget,
    TAccountMark,
    TAccountSystemProgram
  >);
}

export type DeskChargeToPoolInput<
  TAccountDesk extends string = string,
  TAccountDeskAccount extends string = string,
  TAccountOwner extends string = string,
  TAccountBudget extends string = string,
  TAccountMark extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  desk: TransactionSigner<TAccountDesk>;
  deskAccount: Address<TAccountDeskAccount>;
  owner: Address<TAccountOwner>;
  budget: Address<TAccountBudget>;
  /** One per key. `init_if_needed` so a used key is refused as `KeyUsed` and not as a system error. */
  mark: Address<TAccountMark>;
  systemProgram?: Address<TAccountSystemProgram>;
  amountBase: DeskChargeToPoolInstructionDataArgs["amountBase"];
  chargeKey: DeskChargeToPoolInstructionDataArgs["chargeKey"];
};

export function getDeskChargeToPoolInstruction<
  TAccountDesk extends string,
  TAccountDeskAccount extends string,
  TAccountOwner extends string,
  TAccountBudget extends string,
  TAccountMark extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGARI_PRIVATE_PROGRAM_ADDRESS,
>(
  input: DeskChargeToPoolInput<
    TAccountDesk,
    TAccountDeskAccount,
    TAccountOwner,
    TAccountBudget,
    TAccountMark,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): DeskChargeToPoolInstruction<
  TProgramAddress,
  TAccountDesk,
  TAccountDeskAccount,
  TAccountOwner,
  TAccountBudget,
  TAccountMark,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGARI_PRIVATE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    desk: { value: input.desk ?? null, isWritable: true },
    deskAccount: { value: input.deskAccount ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: false },
    budget: { value: input.budget ?? null, isWritable: true },
    mark: { value: input.mark ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("desk", accounts.desk),
      getAccountMeta("deskAccount", accounts.deskAccount),
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("budget", accounts.budget),
      getAccountMeta("mark", accounts.mark),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getDeskChargeToPoolInstructionDataEncoder().encode(
      args as DeskChargeToPoolInstructionDataArgs,
    ),
    programAddress,
  } as DeskChargeToPoolInstruction<
    TProgramAddress,
    TAccountDesk,
    TAccountDeskAccount,
    TAccountOwner,
    TAccountBudget,
    TAccountMark,
    TAccountSystemProgram
  >);
}

export type ParsedDeskChargeToPoolInstruction<
  TProgram extends string = typeof AGARI_PRIVATE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    desk: TAccountMetas[0];
    deskAccount: TAccountMetas[1];
    owner: TAccountMetas[2];
    budget: TAccountMetas[3];
    /** One per key. `init_if_needed` so a used key is refused as `KeyUsed` and not as a system error. */
    mark: TAccountMetas[4];
    systemProgram: TAccountMetas[5];
  };
  data: DeskChargeToPoolInstructionData;
};

export function parseDeskChargeToPoolInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedDeskChargeToPoolInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 6,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      desk: getNextAccount(),
      deskAccount: getNextAccount(),
      owner: getNextAccount(),
      budget: getNextAccount(),
      mark: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getDeskChargeToPoolInstructionDataDecoder().decode(instruction.data),
  };
}
