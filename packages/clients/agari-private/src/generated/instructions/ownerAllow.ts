/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findBudgetPda } from "../pdas";
import { AGARI_PRIVATE_PROGRAM_ADDRESS } from "../programs";

export const OWNER_ALLOW_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  142, 130, 16, 78, 184, 79, 49, 160,
]);

export function getOwnerAllowDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(OWNER_ALLOW_DISCRIMINATOR);
}

export type OwnerAllowInstruction<
  TProgram extends string = typeof AGARI_PRIVATE_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountBudget extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountBudget extends string
        ? WritableAccount<TAccountBudget>
        : TAccountBudget,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type OwnerAllowInstructionData = {
  discriminator: ReadonlyUint8Array;
  allowanceBase: bigint;
};

export type OwnerAllowInstructionDataArgs = { allowanceBase: number | bigint };

export function getOwnerAllowInstructionDataEncoder(): FixedSizeEncoder<OwnerAllowInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["allowanceBase", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: OWNER_ALLOW_DISCRIMINATOR }),
  );
}

export function getOwnerAllowInstructionDataDecoder(): FixedSizeDecoder<OwnerAllowInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["allowanceBase", getU64Decoder()],
  ]);
}

export function getOwnerAllowInstructionDataCodec(): FixedSizeCodec<
  OwnerAllowInstructionDataArgs,
  OwnerAllowInstructionData
> {
  return combineCodec(
    getOwnerAllowInstructionDataEncoder(),
    getOwnerAllowInstructionDataDecoder(),
  );
}

export type OwnerAllowAsyncInput<
  TAccountOwner extends string = string,
  TAccountBudget extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  budget?: Address<TAccountBudget>;
  systemProgram?: Address<TAccountSystemProgram>;
  allowanceBase: OwnerAllowInstructionDataArgs["allowanceBase"];
};

export async function getOwnerAllowInstructionAsync<
  TAccountOwner extends string,
  TAccountBudget extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGARI_PRIVATE_PROGRAM_ADDRESS,
>(
  input: OwnerAllowAsyncInput<
    TAccountOwner,
    TAccountBudget,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  OwnerAllowInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountBudget,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGARI_PRIVATE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    budget: { value: input.budget ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.budget.value) {
    accounts.budget.value = await findBudgetPda(
      {
        owner: getAddressFromResolvedInstructionAccount(
          "owner",
          accounts.owner.value,
        ),
      },
      { programAddress },
    );
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("budget", accounts.budget),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getOwnerAllowInstructionDataEncoder().encode(
      args as OwnerAllowInstructionDataArgs,
    ),
    programAddress,
  } as OwnerAllowInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountBudget,
    TAccountSystemProgram
  >);
}

export type OwnerAllowInput<
  TAccountOwner extends string = string,
  TAccountBudget extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  budget: Address<TAccountBudget>;
  systemProgram?: Address<TAccountSystemProgram>;
  allowanceBase: OwnerAllowInstructionDataArgs["allowanceBase"];
};

export function getOwnerAllowInstruction<
  TAccountOwner extends string,
  TAccountBudget extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGARI_PRIVATE_PROGRAM_ADDRESS,
>(
  input: OwnerAllowInput<TAccountOwner, TAccountBudget, TAccountSystemProgram>,
  config?: { programAddress?: TProgramAddress },
): OwnerAllowInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountBudget,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGARI_PRIVATE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    budget: { value: input.budget ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("budget", accounts.budget),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getOwnerAllowInstructionDataEncoder().encode(
      args as OwnerAllowInstructionDataArgs,
    ),
    programAddress,
  } as OwnerAllowInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountBudget,
    TAccountSystemProgram
  >);
}

export type ParsedOwnerAllowInstruction<
  TProgram extends string = typeof AGARI_PRIVATE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    budget: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: OwnerAllowInstructionData;
};

export function parseOwnerAllowInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedOwnerAllowInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      owner: getNextAccount(),
      budget: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getOwnerAllowInstructionDataDecoder().decode(instruction.data),
  };
}
