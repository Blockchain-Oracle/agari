/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const ROUND_SETTLED_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([249, 225, 66, 54, 157, 200, 234, 222]);

export function getRoundSettledEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ROUND_SETTLED_EVENT_DISCRIMINATOR,
  );
}

export type RoundSettledEvent = {
  reserve: Address;
  round: Address;
  roundId: bigint;
  closingPrint: bigint;
  won: boolean;
  payoutBase: bigint;
};

export type RoundSettledEventArgs = {
  reserve: Address;
  round: Address;
  roundId: number | bigint;
  closingPrint: number | bigint;
  won: boolean;
  payoutBase: number | bigint;
};

/** Gets the encoder for {@link RoundSettledEventArgs} event data. */
export function getRoundSettledEventEncoder(): FixedSizeEncoder<RoundSettledEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["reserve", getAddressEncoder()],
      ["round", getAddressEncoder()],
      ["roundId", getU64Encoder()],
      ["closingPrint", getI64Encoder()],
      ["won", getBooleanEncoder()],
      ["payoutBase", getU64Encoder()],
    ]),
    [getConstantEncoder(ROUND_SETTLED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link RoundSettledEvent} event data. */
export function getRoundSettledEventDecoder(): FixedSizeDecoder<RoundSettledEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["reserve", getAddressDecoder()],
      ["round", getAddressDecoder()],
      ["roundId", getU64Decoder()],
      ["closingPrint", getI64Decoder()],
      ["won", getBooleanDecoder()],
      ["payoutBase", getU64Decoder()],
    ]),
    [getConstantDecoder(ROUND_SETTLED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link RoundSettledEvent} event data. */
export function getRoundSettledEventCodec(): FixedSizeCodec<
  RoundSettledEventArgs,
  RoundSettledEvent
> {
  return combineCodec(
    getRoundSettledEventEncoder(),
    getRoundSettledEventDecoder(),
  );
}

/** Parses the provided bytes into {@link RoundSettledEvent} event data. */
export function parseRoundSettledEvent(
  data: ReadonlyUint8Array | Uint8Array,
): RoundSettledEvent {
  if (containsBytes(data, ROUND_SETTLED_EVENT_DISCRIMINATOR, 0)) {
    return getRoundSettledEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "RoundSettledEvent" event discriminators.',
  );
}
