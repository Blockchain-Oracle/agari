/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const ROUND_CLAIMED_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([127, 74, 89, 65, 99, 237, 183, 67]);

export function getRoundClaimedEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ROUND_CLAIMED_EVENT_DISCRIMINATOR,
  );
}

export type RoundClaimedEvent = {
  reserve: Address;
  round: Address;
  roundId: bigint;
  owner: Address;
  payoutBase: bigint;
};

export type RoundClaimedEventArgs = {
  reserve: Address;
  round: Address;
  roundId: number | bigint;
  owner: Address;
  payoutBase: number | bigint;
};

/** Gets the encoder for {@link RoundClaimedEventArgs} event data. */
export function getRoundClaimedEventEncoder(): FixedSizeEncoder<RoundClaimedEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["reserve", getAddressEncoder()],
      ["round", getAddressEncoder()],
      ["roundId", getU64Encoder()],
      ["owner", getAddressEncoder()],
      ["payoutBase", getU64Encoder()],
    ]),
    [getConstantEncoder(ROUND_CLAIMED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link RoundClaimedEvent} event data. */
export function getRoundClaimedEventDecoder(): FixedSizeDecoder<RoundClaimedEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["reserve", getAddressDecoder()],
      ["round", getAddressDecoder()],
      ["roundId", getU64Decoder()],
      ["owner", getAddressDecoder()],
      ["payoutBase", getU64Decoder()],
    ]),
    [getConstantDecoder(ROUND_CLAIMED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link RoundClaimedEvent} event data. */
export function getRoundClaimedEventCodec(): FixedSizeCodec<
  RoundClaimedEventArgs,
  RoundClaimedEvent
> {
  return combineCodec(
    getRoundClaimedEventEncoder(),
    getRoundClaimedEventDecoder(),
  );
}

/** Parses the provided bytes into {@link RoundClaimedEvent} event data. */
export function parseRoundClaimedEvent(
  data: ReadonlyUint8Array | Uint8Array,
): RoundClaimedEvent {
  if (containsBytes(data, ROUND_CLAIMED_EVENT_DISCRIMINATOR, 0)) {
    return getRoundClaimedEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "RoundClaimedEvent" event discriminators.',
  );
}
