/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
} from "@solana/kit";

/** The reserve's tunables, mirrored by `RangeParams` in `packages/core/src/range/types.ts`. */
export type RangeParams = {
  /** The reserve's edge over fair value, in basis points. */
  marginBps: number;
  /** The most of the reserve's equity that may back open rounds at once. */
  maxExposureBps: number;
  /** The venue mark must sit inside this band, × 1e6: a book with no trades yet prices nothing. */
  minCenterQE6: number;
  maxCenterQE6: number;
  /** Refuse a round the reserve would barely be paid for, or one it is nearly certain to lose. */
  minProbRaw: bigint;
  maxProbRaw: bigint;
  /** A Window with less than this left is not worth pricing; one further out than `max_horizon_sec` is guesswork. */
  minTimeLeftSec: number;
  maxHorizonSec: number;
  /** A venue mark older than this is not a mark. Measured from the Window's last trade. */
  staleAfterSec: number;
  /** The largest payout one round may promise. */
  maxPayoutCapBase: bigint;
  /** The house's per-√second volatility for the asset, × 1e8. */
  sigmaE8: bigint;
  /** The most provider capital that may come due at any one boundary. */
  maxExpiryLockedBase: bigint;
};

export type RangeParamsArgs = {
  /** The reserve's edge over fair value, in basis points. */
  marginBps: number;
  /** The most of the reserve's equity that may back open rounds at once. */
  maxExposureBps: number;
  /** The venue mark must sit inside this band, × 1e6: a book with no trades yet prices nothing. */
  minCenterQE6: number;
  maxCenterQE6: number;
  /** Refuse a round the reserve would barely be paid for, or one it is nearly certain to lose. */
  minProbRaw: number | bigint;
  maxProbRaw: number | bigint;
  /** A Window with less than this left is not worth pricing; one further out than `max_horizon_sec` is guesswork. */
  minTimeLeftSec: number;
  maxHorizonSec: number;
  /** A venue mark older than this is not a mark. Measured from the Window's last trade. */
  staleAfterSec: number;
  /** The largest payout one round may promise. */
  maxPayoutCapBase: number | bigint;
  /** The house's per-√second volatility for the asset, × 1e8. */
  sigmaE8: number | bigint;
  /** The most provider capital that may come due at any one boundary. */
  maxExpiryLockedBase: number | bigint;
};

export function getRangeParamsEncoder(): FixedSizeEncoder<RangeParamsArgs> {
  return getStructEncoder([
    ["marginBps", getU16Encoder()],
    ["maxExposureBps", getU16Encoder()],
    ["minCenterQE6", getU32Encoder()],
    ["maxCenterQE6", getU32Encoder()],
    ["minProbRaw", getU64Encoder()],
    ["maxProbRaw", getU64Encoder()],
    ["minTimeLeftSec", getU32Encoder()],
    ["maxHorizonSec", getU32Encoder()],
    ["staleAfterSec", getU32Encoder()],
    ["maxPayoutCapBase", getU64Encoder()],
    ["sigmaE8", getU64Encoder()],
    ["maxExpiryLockedBase", getU64Encoder()],
  ]);
}

export function getRangeParamsDecoder(): FixedSizeDecoder<RangeParams> {
  return getStructDecoder([
    ["marginBps", getU16Decoder()],
    ["maxExposureBps", getU16Decoder()],
    ["minCenterQE6", getU32Decoder()],
    ["maxCenterQE6", getU32Decoder()],
    ["minProbRaw", getU64Decoder()],
    ["maxProbRaw", getU64Decoder()],
    ["minTimeLeftSec", getU32Decoder()],
    ["maxHorizonSec", getU32Decoder()],
    ["staleAfterSec", getU32Decoder()],
    ["maxPayoutCapBase", getU64Decoder()],
    ["sigmaE8", getU64Decoder()],
    ["maxExpiryLockedBase", getU64Decoder()],
  ]);
}

export function getRangeParamsCodec(): FixedSizeCodec<
  RangeParamsArgs,
  RangeParams
> {
  return combineCodec(getRangeParamsEncoder(), getRangeParamsDecoder());
}
