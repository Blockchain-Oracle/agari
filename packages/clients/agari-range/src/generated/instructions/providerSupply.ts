/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findPositionPda, findReservePda, findVaultPda } from "../pdas";
import { AGARI_RANGE_PROGRAM_ADDRESS } from "../programs";

export const PROVIDER_SUPPLY_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array(
  [73, 69, 72, 253, 228, 71, 82, 86],
);

export function getProviderSupplyDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PROVIDER_SUPPLY_DISCRIMINATOR,
  );
}

export type ProviderSupplyInstruction<
  TProgram extends string = typeof AGARI_RANGE_PROGRAM_ADDRESS,
  TAccountProvider extends string | AccountMeta<string> = string,
  TAccountPosition extends string | AccountMeta<string> = string,
  TAccountReserve extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountProviderToken extends string | AccountMeta<string> = string,
  TAccountCollateralMint extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountProvider extends string
        ? WritableSignerAccount<TAccountProvider> &
            AccountSignerMeta<TAccountProvider>
        : TAccountProvider,
      TAccountPosition extends string
        ? WritableAccount<TAccountPosition>
        : TAccountPosition,
      TAccountReserve extends string
        ? WritableAccount<TAccountReserve>
        : TAccountReserve,
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      TAccountProviderToken extends string
        ? WritableAccount<TAccountProviderToken>
        : TAccountProviderToken,
      TAccountCollateralMint extends string
        ? ReadonlyAccount<TAccountCollateralMint>
        : TAccountCollateralMint,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ProviderSupplyInstructionData = {
  discriminator: ReadonlyUint8Array;
  amountBase: bigint;
};

export type ProviderSupplyInstructionDataArgs = { amountBase: number | bigint };

export function getProviderSupplyInstructionDataEncoder(): FixedSizeEncoder<ProviderSupplyInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["amountBase", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: PROVIDER_SUPPLY_DISCRIMINATOR }),
  );
}

export function getProviderSupplyInstructionDataDecoder(): FixedSizeDecoder<ProviderSupplyInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["amountBase", getU64Decoder()],
  ]);
}

export function getProviderSupplyInstructionDataCodec(): FixedSizeCodec<
  ProviderSupplyInstructionDataArgs,
  ProviderSupplyInstructionData
> {
  return combineCodec(
    getProviderSupplyInstructionDataEncoder(),
    getProviderSupplyInstructionDataDecoder(),
  );
}

export type ProviderSupplyAsyncInput<
  TAccountProvider extends string = string,
  TAccountPosition extends string = string,
  TAccountReserve extends string = string,
  TAccountVault extends string = string,
  TAccountProviderToken extends string = string,
  TAccountCollateralMint extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  provider: TransactionSigner<TAccountProvider>;
  /** This wallet's own share balance. Created on first supply; never writable by anyone else. */
  position?: Address<TAccountPosition>;
  reserve?: Address<TAccountReserve>;
  vault?: Address<TAccountVault>;
  providerToken: Address<TAccountProviderToken>;
  collateralMint: Address<TAccountCollateralMint>;
  tokenProgram?: Address<TAccountTokenProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
  amountBase: ProviderSupplyInstructionDataArgs["amountBase"];
};

export async function getProviderSupplyInstructionAsync<
  TAccountProvider extends string,
  TAccountPosition extends string,
  TAccountReserve extends string,
  TAccountVault extends string,
  TAccountProviderToken extends string,
  TAccountCollateralMint extends string,
  TAccountTokenProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGARI_RANGE_PROGRAM_ADDRESS,
>(
  input: ProviderSupplyAsyncInput<
    TAccountProvider,
    TAccountPosition,
    TAccountReserve,
    TAccountVault,
    TAccountProviderToken,
    TAccountCollateralMint,
    TAccountTokenProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ProviderSupplyInstruction<
    TProgramAddress,
    TAccountProvider,
    TAccountPosition,
    TAccountReserve,
    TAccountVault,
    TAccountProviderToken,
    TAccountCollateralMint,
    TAccountTokenProgram,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? AGARI_RANGE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    provider: { value: input.provider ?? null, isWritable: true },
    position: { value: input.position ?? null, isWritable: true },
    reserve: { value: input.reserve ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    providerToken: { value: input.providerToken ?? null, isWritable: true },
    collateralMint: { value: input.collateralMint ?? null, isWritable: false },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.position.value) {
    accounts.position.value = await findPositionPda(
      {
        provider: getAddressFromResolvedInstructionAccount(
          "provider",
          accounts.provider.value,
        ),
      },
      { programAddress },
    );
  }
  if (!accounts.reserve.value) {
    accounts.reserve.value = await findReservePda({ programAddress });
  }
  if (!accounts.vault.value) {
    accounts.vault.value = await findVaultPda({ programAddress });
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("provider", accounts.provider),
      getAccountMeta("position", accounts.position),
      getAccountMeta("reserve", accounts.reserve),
      getAccountMeta("vault", accounts.vault),
      getAccountMeta("providerToken", accounts.providerToken),
      getAccountMeta("collateralMint", accounts.collateralMint),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getProviderSupplyInstructionDataEncoder().encode(
      args as ProviderSupplyInstructionDataArgs,
    ),
    programAddress,
  } as ProviderSupplyInstruction<
    TProgramAddress,
    TAccountProvider,
    TAccountPosition,
    TAccountReserve,
    TAccountVault,
    TAccountProviderToken,
    TAccountCollateralMint,
    TAccountTokenProgram,
    TAccountSystemProgram
  >);
}

export type ProviderSupplyInput<
  TAccountProvider extends string = string,
  TAccountPosition extends string = string,
  TAccountReserve extends string = string,
  TAccountVault extends string = string,
  TAccountProviderToken extends string = string,
  TAccountCollateralMint extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  provider: TransactionSigner<TAccountProvider>;
  /** This wallet's own share balance. Created on first supply; never writable by anyone else. */
  position: Address<TAccountPosition>;
  reserve: Address<TAccountReserve>;
  vault: Address<TAccountVault>;
  providerToken: Address<TAccountProviderToken>;
  collateralMint: Address<TAccountCollateralMint>;
  tokenProgram?: Address<TAccountTokenProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
  amountBase: ProviderSupplyInstructionDataArgs["amountBase"];
};

export function getProviderSupplyInstruction<
  TAccountProvider extends string,
  TAccountPosition extends string,
  TAccountReserve extends string,
  TAccountVault extends string,
  TAccountProviderToken extends string,
  TAccountCollateralMint extends string,
  TAccountTokenProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGARI_RANGE_PROGRAM_ADDRESS,
>(
  input: ProviderSupplyInput<
    TAccountProvider,
    TAccountPosition,
    TAccountReserve,
    TAccountVault,
    TAccountProviderToken,
    TAccountCollateralMint,
    TAccountTokenProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): ProviderSupplyInstruction<
  TProgramAddress,
  TAccountProvider,
  TAccountPosition,
  TAccountReserve,
  TAccountVault,
  TAccountProviderToken,
  TAccountCollateralMint,
  TAccountTokenProgram,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? AGARI_RANGE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    provider: { value: input.provider ?? null, isWritable: true },
    position: { value: input.position ?? null, isWritable: true },
    reserve: { value: input.reserve ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    providerToken: { value: input.providerToken ?? null, isWritable: true },
    collateralMint: { value: input.collateralMint ?? null, isWritable: false },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("provider", accounts.provider),
      getAccountMeta("position", accounts.position),
      getAccountMeta("reserve", accounts.reserve),
      getAccountMeta("vault", accounts.vault),
      getAccountMeta("providerToken", accounts.providerToken),
      getAccountMeta("collateralMint", accounts.collateralMint),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getProviderSupplyInstructionDataEncoder().encode(
      args as ProviderSupplyInstructionDataArgs,
    ),
    programAddress,
  } as ProviderSupplyInstruction<
    TProgramAddress,
    TAccountProvider,
    TAccountPosition,
    TAccountReserve,
    TAccountVault,
    TAccountProviderToken,
    TAccountCollateralMint,
    TAccountTokenProgram,
    TAccountSystemProgram
  >);
}

export type ParsedProviderSupplyInstruction<
  TProgram extends string = typeof AGARI_RANGE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    provider: TAccountMetas[0];
    /** This wallet's own share balance. Created on first supply; never writable by anyone else. */
    position: TAccountMetas[1];
    reserve: TAccountMetas[2];
    vault: TAccountMetas[3];
    providerToken: TAccountMetas[4];
    collateralMint: TAccountMetas[5];
    tokenProgram: TAccountMetas[6];
    systemProgram: TAccountMetas[7];
  };
  data: ProviderSupplyInstructionData;
};

export function parseProviderSupplyInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedProviderSupplyInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 8) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 8,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      provider: getNextAccount(),
      position: getNextAccount(),
      reserve: getNextAccount(),
      vault: getNextAccount(),
      providerToken: getNextAccount(),
      collateralMint: getNextAccount(),
      tokenProgram: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getProviderSupplyInstructionDataDecoder().decode(instruction.data),
  };
}
