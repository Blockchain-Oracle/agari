/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findReservePda } from "../pdas";
import { AGARI_RANGE_PROGRAM_ADDRESS } from "../programs";

export const PUBLIC_VOID_STALE_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([151, 10, 111, 181, 112, 44, 51, 61]);

export function getPublicVoidStaleDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PUBLIC_VOID_STALE_DISCRIMINATOR,
  );
}

export type PublicVoidStaleInstruction<
  TProgram extends string = typeof AGARI_RANGE_PROGRAM_ADDRESS,
  TAccountCranker extends string | AccountMeta<string> = string,
  TAccountReserve extends string | AccountMeta<string> = string,
  TAccountRound extends string | AccountMeta<string> = string,
  TAccountExpiryBook extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCranker extends string
        ? ReadonlySignerAccount<TAccountCranker> &
            AccountSignerMeta<TAccountCranker>
        : TAccountCranker,
      TAccountReserve extends string
        ? WritableAccount<TAccountReserve>
        : TAccountReserve,
      TAccountRound extends string
        ? WritableAccount<TAccountRound>
        : TAccountRound,
      TAccountExpiryBook extends string
        ? WritableAccount<TAccountExpiryBook>
        : TAccountExpiryBook,
      ...TRemainingAccounts,
    ]
  >;

export type PublicVoidStaleInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type PublicVoidStaleInstructionDataArgs = {};

export function getPublicVoidStaleInstructionDataEncoder(): FixedSizeEncoder<PublicVoidStaleInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: PUBLIC_VOID_STALE_DISCRIMINATOR }),
  );
}

export function getPublicVoidStaleInstructionDataDecoder(): FixedSizeDecoder<PublicVoidStaleInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getPublicVoidStaleInstructionDataCodec(): FixedSizeCodec<
  PublicVoidStaleInstructionDataArgs,
  PublicVoidStaleInstructionData
> {
  return combineCodec(
    getPublicVoidStaleInstructionDataEncoder(),
    getPublicVoidStaleInstructionDataDecoder(),
  );
}

export type PublicVoidStaleAsyncInput<
  TAccountCranker extends string = string,
  TAccountReserve extends string = string,
  TAccountRound extends string = string,
  TAccountExpiryBook extends string = string,
> = {
  cranker: TransactionSigner<TAccountCranker>;
  reserve?: Address<TAccountReserve>;
  round: Address<TAccountRound>;
  /** The boundary book this round was counted into at open; released here. */
  expiryBook: Address<TAccountExpiryBook>;
};

export async function getPublicVoidStaleInstructionAsync<
  TAccountCranker extends string,
  TAccountReserve extends string,
  TAccountRound extends string,
  TAccountExpiryBook extends string,
  TProgramAddress extends Address = typeof AGARI_RANGE_PROGRAM_ADDRESS,
>(
  input: PublicVoidStaleAsyncInput<
    TAccountCranker,
    TAccountReserve,
    TAccountRound,
    TAccountExpiryBook
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  PublicVoidStaleInstruction<
    TProgramAddress,
    TAccountCranker,
    TAccountReserve,
    TAccountRound,
    TAccountExpiryBook
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? AGARI_RANGE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    cranker: { value: input.cranker ?? null, isWritable: false },
    reserve: { value: input.reserve ?? null, isWritable: true },
    round: { value: input.round ?? null, isWritable: true },
    expiryBook: { value: input.expiryBook ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.reserve.value) {
    accounts.reserve.value = await findReservePda({ programAddress });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("cranker", accounts.cranker),
      getAccountMeta("reserve", accounts.reserve),
      getAccountMeta("round", accounts.round),
      getAccountMeta("expiryBook", accounts.expiryBook),
    ],
    data: getPublicVoidStaleInstructionDataEncoder().encode({}),
    programAddress,
  } as PublicVoidStaleInstruction<
    TProgramAddress,
    TAccountCranker,
    TAccountReserve,
    TAccountRound,
    TAccountExpiryBook
  >);
}

export type PublicVoidStaleInput<
  TAccountCranker extends string = string,
  TAccountReserve extends string = string,
  TAccountRound extends string = string,
  TAccountExpiryBook extends string = string,
> = {
  cranker: TransactionSigner<TAccountCranker>;
  reserve: Address<TAccountReserve>;
  round: Address<TAccountRound>;
  /** The boundary book this round was counted into at open; released here. */
  expiryBook: Address<TAccountExpiryBook>;
};

export function getPublicVoidStaleInstruction<
  TAccountCranker extends string,
  TAccountReserve extends string,
  TAccountRound extends string,
  TAccountExpiryBook extends string,
  TProgramAddress extends Address = typeof AGARI_RANGE_PROGRAM_ADDRESS,
>(
  input: PublicVoidStaleInput<
    TAccountCranker,
    TAccountReserve,
    TAccountRound,
    TAccountExpiryBook
  >,
  config?: { programAddress?: TProgramAddress },
): PublicVoidStaleInstruction<
  TProgramAddress,
  TAccountCranker,
  TAccountReserve,
  TAccountRound,
  TAccountExpiryBook
> {
  // Program address.
  const programAddress = config?.programAddress ?? AGARI_RANGE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    cranker: { value: input.cranker ?? null, isWritable: false },
    reserve: { value: input.reserve ?? null, isWritable: true },
    round: { value: input.round ?? null, isWritable: true },
    expiryBook: { value: input.expiryBook ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("cranker", accounts.cranker),
      getAccountMeta("reserve", accounts.reserve),
      getAccountMeta("round", accounts.round),
      getAccountMeta("expiryBook", accounts.expiryBook),
    ],
    data: getPublicVoidStaleInstructionDataEncoder().encode({}),
    programAddress,
  } as PublicVoidStaleInstruction<
    TProgramAddress,
    TAccountCranker,
    TAccountReserve,
    TAccountRound,
    TAccountExpiryBook
  >);
}

export type ParsedPublicVoidStaleInstruction<
  TProgram extends string = typeof AGARI_RANGE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    cranker: TAccountMetas[0];
    reserve: TAccountMetas[1];
    round: TAccountMetas[2];
    /** The boundary book this round was counted into at open; released here. */
    expiryBook: TAccountMetas[3];
  };
  data: PublicVoidStaleInstructionData;
};

export function parsePublicVoidStaleInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedPublicVoidStaleInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      cranker: getNextAccount(),
      reserve: getNextAccount(),
      round: getNextAccount(),
      expiryBook: getNextAccount(),
    },
    data: getPublicVoidStaleInstructionDataDecoder().decode(instruction.data),
  };
}
