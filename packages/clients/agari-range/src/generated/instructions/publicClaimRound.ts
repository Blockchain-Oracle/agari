/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findReservePda, findVaultPda } from "../pdas";
import { AGARI_RANGE_PROGRAM_ADDRESS } from "../programs";

export const PUBLIC_CLAIM_ROUND_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([148, 131, 23, 77, 194, 104, 89, 55]);

export function getPublicClaimRoundDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PUBLIC_CLAIM_ROUND_DISCRIMINATOR,
  );
}

export type PublicClaimRoundInstruction<
  TProgram extends string = typeof AGARI_RANGE_PROGRAM_ADDRESS,
  TAccountCranker extends string | AccountMeta<string> = string,
  TAccountReserve extends string | AccountMeta<string> = string,
  TAccountRound extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountOwnerToken extends string | AccountMeta<string> = string,
  TAccountCollateralMint extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCranker extends string
        ? ReadonlySignerAccount<TAccountCranker> &
            AccountSignerMeta<TAccountCranker>
        : TAccountCranker,
      TAccountReserve extends string
        ? WritableAccount<TAccountReserve>
        : TAccountReserve,
      TAccountRound extends string
        ? WritableAccount<TAccountRound>
        : TAccountRound,
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      TAccountOwnerToken extends string
        ? WritableAccount<TAccountOwnerToken>
        : TAccountOwnerToken,
      TAccountCollateralMint extends string
        ? ReadonlyAccount<TAccountCollateralMint>
        : TAccountCollateralMint,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      ...TRemainingAccounts,
    ]
  >;

export type PublicClaimRoundInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type PublicClaimRoundInstructionDataArgs = {};

export function getPublicClaimRoundInstructionDataEncoder(): FixedSizeEncoder<PublicClaimRoundInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: PUBLIC_CLAIM_ROUND_DISCRIMINATOR }),
  );
}

export function getPublicClaimRoundInstructionDataDecoder(): FixedSizeDecoder<PublicClaimRoundInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getPublicClaimRoundInstructionDataCodec(): FixedSizeCodec<
  PublicClaimRoundInstructionDataArgs,
  PublicClaimRoundInstructionData
> {
  return combineCodec(
    getPublicClaimRoundInstructionDataEncoder(),
    getPublicClaimRoundInstructionDataDecoder(),
  );
}

export type PublicClaimRoundAsyncInput<
  TAccountCranker extends string = string,
  TAccountReserve extends string = string,
  TAccountRound extends string = string,
  TAccountVault extends string = string,
  TAccountOwnerToken extends string = string,
  TAccountCollateralMint extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  /** Permissionless to call; the money only ever goes to `round.owner` (AD-5). */
  cranker: TransactionSigner<TAccountCranker>;
  reserve?: Address<TAccountReserve>;
  round: Address<TAccountRound>;
  vault?: Address<TAccountVault>;
  ownerToken: Address<TAccountOwnerToken>;
  collateralMint: Address<TAccountCollateralMint>;
  tokenProgram?: Address<TAccountTokenProgram>;
};

export async function getPublicClaimRoundInstructionAsync<
  TAccountCranker extends string,
  TAccountReserve extends string,
  TAccountRound extends string,
  TAccountVault extends string,
  TAccountOwnerToken extends string,
  TAccountCollateralMint extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof AGARI_RANGE_PROGRAM_ADDRESS,
>(
  input: PublicClaimRoundAsyncInput<
    TAccountCranker,
    TAccountReserve,
    TAccountRound,
    TAccountVault,
    TAccountOwnerToken,
    TAccountCollateralMint,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  PublicClaimRoundInstruction<
    TProgramAddress,
    TAccountCranker,
    TAccountReserve,
    TAccountRound,
    TAccountVault,
    TAccountOwnerToken,
    TAccountCollateralMint,
    TAccountTokenProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? AGARI_RANGE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    cranker: { value: input.cranker ?? null, isWritable: false },
    reserve: { value: input.reserve ?? null, isWritable: true },
    round: { value: input.round ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    ownerToken: { value: input.ownerToken ?? null, isWritable: true },
    collateralMint: { value: input.collateralMint ?? null, isWritable: false },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.reserve.value) {
    accounts.reserve.value = await findReservePda({ programAddress });
  }
  if (!accounts.vault.value) {
    accounts.vault.value = await findVaultPda({ programAddress });
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("cranker", accounts.cranker),
      getAccountMeta("reserve", accounts.reserve),
      getAccountMeta("round", accounts.round),
      getAccountMeta("vault", accounts.vault),
      getAccountMeta("ownerToken", accounts.ownerToken),
      getAccountMeta("collateralMint", accounts.collateralMint),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
    ],
    data: getPublicClaimRoundInstructionDataEncoder().encode({}),
    programAddress,
  } as PublicClaimRoundInstruction<
    TProgramAddress,
    TAccountCranker,
    TAccountReserve,
    TAccountRound,
    TAccountVault,
    TAccountOwnerToken,
    TAccountCollateralMint,
    TAccountTokenProgram
  >);
}

export type PublicClaimRoundInput<
  TAccountCranker extends string = string,
  TAccountReserve extends string = string,
  TAccountRound extends string = string,
  TAccountVault extends string = string,
  TAccountOwnerToken extends string = string,
  TAccountCollateralMint extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  /** Permissionless to call; the money only ever goes to `round.owner` (AD-5). */
  cranker: TransactionSigner<TAccountCranker>;
  reserve: Address<TAccountReserve>;
  round: Address<TAccountRound>;
  vault: Address<TAccountVault>;
  ownerToken: Address<TAccountOwnerToken>;
  collateralMint: Address<TAccountCollateralMint>;
  tokenProgram?: Address<TAccountTokenProgram>;
};

export function getPublicClaimRoundInstruction<
  TAccountCranker extends string,
  TAccountReserve extends string,
  TAccountRound extends string,
  TAccountVault extends string,
  TAccountOwnerToken extends string,
  TAccountCollateralMint extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof AGARI_RANGE_PROGRAM_ADDRESS,
>(
  input: PublicClaimRoundInput<
    TAccountCranker,
    TAccountReserve,
    TAccountRound,
    TAccountVault,
    TAccountOwnerToken,
    TAccountCollateralMint,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): PublicClaimRoundInstruction<
  TProgramAddress,
  TAccountCranker,
  TAccountReserve,
  TAccountRound,
  TAccountVault,
  TAccountOwnerToken,
  TAccountCollateralMint,
  TAccountTokenProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? AGARI_RANGE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    cranker: { value: input.cranker ?? null, isWritable: false },
    reserve: { value: input.reserve ?? null, isWritable: true },
    round: { value: input.round ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    ownerToken: { value: input.ownerToken ?? null, isWritable: true },
    collateralMint: { value: input.collateralMint ?? null, isWritable: false },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("cranker", accounts.cranker),
      getAccountMeta("reserve", accounts.reserve),
      getAccountMeta("round", accounts.round),
      getAccountMeta("vault", accounts.vault),
      getAccountMeta("ownerToken", accounts.ownerToken),
      getAccountMeta("collateralMint", accounts.collateralMint),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
    ],
    data: getPublicClaimRoundInstructionDataEncoder().encode({}),
    programAddress,
  } as PublicClaimRoundInstruction<
    TProgramAddress,
    TAccountCranker,
    TAccountReserve,
    TAccountRound,
    TAccountVault,
    TAccountOwnerToken,
    TAccountCollateralMint,
    TAccountTokenProgram
  >);
}

export type ParsedPublicClaimRoundInstruction<
  TProgram extends string = typeof AGARI_RANGE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Permissionless to call; the money only ever goes to `round.owner` (AD-5). */
    cranker: TAccountMetas[0];
    reserve: TAccountMetas[1];
    round: TAccountMetas[2];
    vault: TAccountMetas[3];
    ownerToken: TAccountMetas[4];
    collateralMint: TAccountMetas[5];
    tokenProgram: TAccountMetas[6];
  };
  data: PublicClaimRoundInstructionData;
};

export function parsePublicClaimRoundInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedPublicClaimRoundInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 7) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 7,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      cranker: getNextAccount(),
      reserve: getNextAccount(),
      round: getNextAccount(),
      vault: getNextAccount(),
      ownerToken: getNextAccount(),
      collateralMint: getNextAccount(),
      tokenProgram: getNextAccount(),
    },
    data: getPublicClaimRoundInstructionDataDecoder().decode(instruction.data),
  };
}
