/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findReservePda } from "../pdas";
import { AGARI_RANGE_PROGRAM_ADDRESS } from "../programs";

export const PUBLIC_SETTLE_ROUND_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([34, 133, 73, 228, 42, 8, 72, 177]);

export function getPublicSettleRoundDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PUBLIC_SETTLE_ROUND_DISCRIMINATOR,
  );
}

export type PublicSettleRoundInstruction<
  TProgram extends string = typeof AGARI_RANGE_PROGRAM_ADDRESS,
  TAccountCranker extends string | AccountMeta<string> = string,
  TAccountReserve extends string | AccountMeta<string> = string,
  TAccountRound extends string | AccountMeta<string> = string,
  TAccountExpiryBook extends string | AccountMeta<string> = string,
  TAccountMarket extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCranker extends string
        ? ReadonlySignerAccount<TAccountCranker> &
            AccountSignerMeta<TAccountCranker>
        : TAccountCranker,
      TAccountReserve extends string
        ? WritableAccount<TAccountReserve>
        : TAccountReserve,
      TAccountRound extends string
        ? WritableAccount<TAccountRound>
        : TAccountRound,
      TAccountExpiryBook extends string
        ? WritableAccount<TAccountExpiryBook>
        : TAccountExpiryBook,
      TAccountMarket extends string
        ? ReadonlyAccount<TAccountMarket>
        : TAccountMarket,
      ...TRemainingAccounts,
    ]
  >;

export type PublicSettleRoundInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type PublicSettleRoundInstructionDataArgs = {};

export function getPublicSettleRoundInstructionDataEncoder(): FixedSizeEncoder<PublicSettleRoundInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: PUBLIC_SETTLE_ROUND_DISCRIMINATOR }),
  );
}

export function getPublicSettleRoundInstructionDataDecoder(): FixedSizeDecoder<PublicSettleRoundInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getPublicSettleRoundInstructionDataCodec(): FixedSizeCodec<
  PublicSettleRoundInstructionDataArgs,
  PublicSettleRoundInstructionData
> {
  return combineCodec(
    getPublicSettleRoundInstructionDataEncoder(),
    getPublicSettleRoundInstructionDataDecoder(),
  );
}

export type PublicSettleRoundAsyncInput<
  TAccountCranker extends string = string,
  TAccountReserve extends string = string,
  TAccountRound extends string = string,
  TAccountExpiryBook extends string = string,
  TAccountMarket extends string = string,
> = {
  /** Permissionless: settling decides a round, it never moves money to the caller. */
  cranker: TransactionSigner<TAccountCranker>;
  reserve?: Address<TAccountReserve>;
  round: Address<TAccountRound>;
  /** The boundary book this round was counted into at open; released here. */
  expiryBook: Address<TAccountExpiryBook>;
  market: Address<TAccountMarket>;
};

export async function getPublicSettleRoundInstructionAsync<
  TAccountCranker extends string,
  TAccountReserve extends string,
  TAccountRound extends string,
  TAccountExpiryBook extends string,
  TAccountMarket extends string,
  TProgramAddress extends Address = typeof AGARI_RANGE_PROGRAM_ADDRESS,
>(
  input: PublicSettleRoundAsyncInput<
    TAccountCranker,
    TAccountReserve,
    TAccountRound,
    TAccountExpiryBook,
    TAccountMarket
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  PublicSettleRoundInstruction<
    TProgramAddress,
    TAccountCranker,
    TAccountReserve,
    TAccountRound,
    TAccountExpiryBook,
    TAccountMarket
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? AGARI_RANGE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    cranker: { value: input.cranker ?? null, isWritable: false },
    reserve: { value: input.reserve ?? null, isWritable: true },
    round: { value: input.round ?? null, isWritable: true },
    expiryBook: { value: input.expiryBook ?? null, isWritable: true },
    market: { value: input.market ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.reserve.value) {
    accounts.reserve.value = await findReservePda({ programAddress });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("cranker", accounts.cranker),
      getAccountMeta("reserve", accounts.reserve),
      getAccountMeta("round", accounts.round),
      getAccountMeta("expiryBook", accounts.expiryBook),
      getAccountMeta("market", accounts.market),
    ],
    data: getPublicSettleRoundInstructionDataEncoder().encode({}),
    programAddress,
  } as PublicSettleRoundInstruction<
    TProgramAddress,
    TAccountCranker,
    TAccountReserve,
    TAccountRound,
    TAccountExpiryBook,
    TAccountMarket
  >);
}

export type PublicSettleRoundInput<
  TAccountCranker extends string = string,
  TAccountReserve extends string = string,
  TAccountRound extends string = string,
  TAccountExpiryBook extends string = string,
  TAccountMarket extends string = string,
> = {
  /** Permissionless: settling decides a round, it never moves money to the caller. */
  cranker: TransactionSigner<TAccountCranker>;
  reserve: Address<TAccountReserve>;
  round: Address<TAccountRound>;
  /** The boundary book this round was counted into at open; released here. */
  expiryBook: Address<TAccountExpiryBook>;
  market: Address<TAccountMarket>;
};

export function getPublicSettleRoundInstruction<
  TAccountCranker extends string,
  TAccountReserve extends string,
  TAccountRound extends string,
  TAccountExpiryBook extends string,
  TAccountMarket extends string,
  TProgramAddress extends Address = typeof AGARI_RANGE_PROGRAM_ADDRESS,
>(
  input: PublicSettleRoundInput<
    TAccountCranker,
    TAccountReserve,
    TAccountRound,
    TAccountExpiryBook,
    TAccountMarket
  >,
  config?: { programAddress?: TProgramAddress },
): PublicSettleRoundInstruction<
  TProgramAddress,
  TAccountCranker,
  TAccountReserve,
  TAccountRound,
  TAccountExpiryBook,
  TAccountMarket
> {
  // Program address.
  const programAddress = config?.programAddress ?? AGARI_RANGE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    cranker: { value: input.cranker ?? null, isWritable: false },
    reserve: { value: input.reserve ?? null, isWritable: true },
    round: { value: input.round ?? null, isWritable: true },
    expiryBook: { value: input.expiryBook ?? null, isWritable: true },
    market: { value: input.market ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("cranker", accounts.cranker),
      getAccountMeta("reserve", accounts.reserve),
      getAccountMeta("round", accounts.round),
      getAccountMeta("expiryBook", accounts.expiryBook),
      getAccountMeta("market", accounts.market),
    ],
    data: getPublicSettleRoundInstructionDataEncoder().encode({}),
    programAddress,
  } as PublicSettleRoundInstruction<
    TProgramAddress,
    TAccountCranker,
    TAccountReserve,
    TAccountRound,
    TAccountExpiryBook,
    TAccountMarket
  >);
}

export type ParsedPublicSettleRoundInstruction<
  TProgram extends string = typeof AGARI_RANGE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Permissionless: settling decides a round, it never moves money to the caller. */
    cranker: TAccountMetas[0];
    reserve: TAccountMetas[1];
    round: TAccountMetas[2];
    /** The boundary book this round was counted into at open; released here. */
    expiryBook: TAccountMetas[3];
    market: TAccountMetas[4];
  };
  data: PublicSettleRoundInstructionData;
};

export function parsePublicSettleRoundInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedPublicSettleRoundInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      cranker: getNextAccount(),
      reserve: getNextAccount(),
      round: getNextAccount(),
      expiryBook: getNextAccount(),
      market: getNextAccount(),
    },
    data: getPublicSettleRoundInstructionDataDecoder().decode(instruction.data),
  };
}
