/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const DESK_REF_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  220, 57, 10, 187, 69, 186, 31, 15,
]);

export function getDeskRefDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(DESK_REF_DISCRIMINATOR);
}

export type DeskRef = {
  discriminator: ReadonlyUint8Array;
  mint: Address;
  /** Pyth `Equity.Index.<NAME>/USD` feed id; zero until the admin sets it. */
  pythFeedId: ReadonlyUint8Array;
  /** PreStocks `tokenPrice` per UI token × 10^8. */
  tokenPriceE8: bigint;
  /** PreStocks `markPrice` (the SPV's valuation per token) × 10^8. */
  markPriceE8: bigint;
  /** The mint's ScaledUiAmount multiplier × 10^12, floored, as the attestor read it. */
  multiplierE12: bigint;
  /** When the attestor fetched the catalogue; 0 = never posted. */
  fetchedAtSec: bigint;
  postedBy: Address;
  bump: number;
  pad: ReadonlyUint8Array;
  reserved: ReadonlyUint8Array;
};

export type DeskRefArgs = {
  mint: Address;
  /** Pyth `Equity.Index.<NAME>/USD` feed id; zero until the admin sets it. */
  pythFeedId: ReadonlyUint8Array;
  /** PreStocks `tokenPrice` per UI token × 10^8. */
  tokenPriceE8: number | bigint;
  /** PreStocks `markPrice` (the SPV's valuation per token) × 10^8. */
  markPriceE8: number | bigint;
  /** The mint's ScaledUiAmount multiplier × 10^12, floored, as the attestor read it. */
  multiplierE12: number | bigint;
  /** When the attestor fetched the catalogue; 0 = never posted. */
  fetchedAtSec: number | bigint;
  postedBy: Address;
  bump: number;
  pad: ReadonlyUint8Array;
  reserved: ReadonlyUint8Array;
};

/** Gets the encoder for {@link DeskRefArgs} account data. */
export function getDeskRefEncoder(): FixedSizeEncoder<DeskRefArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["mint", getAddressEncoder()],
      ["pythFeedId", fixEncoderSize(getBytesEncoder(), 32)],
      ["tokenPriceE8", getU64Encoder()],
      ["markPriceE8", getU64Encoder()],
      ["multiplierE12", getU64Encoder()],
      ["fetchedAtSec", getI64Encoder()],
      ["postedBy", getAddressEncoder()],
      ["bump", getU8Encoder()],
      ["pad", fixEncoderSize(getBytesEncoder(), 7)],
      ["reserved", fixEncoderSize(getBytesEncoder(), 32)],
    ]),
    (value) => ({ ...value, discriminator: DESK_REF_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link DeskRef} account data. */
export function getDeskRefDecoder(): FixedSizeDecoder<DeskRef> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["mint", getAddressDecoder()],
    ["pythFeedId", fixDecoderSize(getBytesDecoder(), 32)],
    ["tokenPriceE8", getU64Decoder()],
    ["markPriceE8", getU64Decoder()],
    ["multiplierE12", getU64Decoder()],
    ["fetchedAtSec", getI64Decoder()],
    ["postedBy", getAddressDecoder()],
    ["bump", getU8Decoder()],
    ["pad", fixDecoderSize(getBytesDecoder(), 7)],
    ["reserved", fixDecoderSize(getBytesDecoder(), 32)],
  ]);
}

/** Gets the codec for {@link DeskRef} account data. */
export function getDeskRefCodec(): FixedSizeCodec<DeskRefArgs, DeskRef> {
  return combineCodec(getDeskRefEncoder(), getDeskRefDecoder());
}

export function decodeDeskRef<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<DeskRef, TAddress>;
export function decodeDeskRef<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<DeskRef, TAddress>;
export function decodeDeskRef<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<DeskRef, TAddress> | MaybeAccount<DeskRef, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getDeskRefDecoder(),
  );
}

export async function fetchDeskRef<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<DeskRef, TAddress>> {
  const maybeAccount = await fetchMaybeDeskRef(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeDeskRef<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<DeskRef, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeDeskRef(maybeAccount);
}

export async function fetchAllDeskRef(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<DeskRef>[]> {
  const maybeAccounts = await fetchAllMaybeDeskRef(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeDeskRef(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<DeskRef>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeDeskRef(maybeAccount));
}

export function getDeskRefSize(): number {
  return 176;
}
