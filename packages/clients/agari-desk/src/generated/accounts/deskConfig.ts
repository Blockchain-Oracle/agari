/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const DESK_CONFIG_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  50, 232, 76, 9, 116, 204, 140, 153,
]);

export function getDeskConfigDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(DESK_CONFIG_DISCRIMINATOR);
}

export type DeskConfig = {
  discriminator: ReadonlyUint8Array;
  /** The program upgrade authority at init. */
  admin: Address;
  /** The collateral: USDC on mainnet (SPL Token, 6 dp); a test mint on LiteSVM. */
  usdcMint: Address;
  /** The router every operator swap goes through: Jupiter v6 on mainnet; the stub in tests. */
  swapProgram: Address;
  /** ed25519 keys allowed to sign reference messages. */
  attestors: Array<Address>;
  /** Core `CLUSTER_ID` (101 / 103 / 104), bound into reference messages. */
  clusterTag: number;
  bump: number;
  pad: ReadonlyUint8Array;
  reserved: ReadonlyUint8Array;
};

export type DeskConfigArgs = {
  /** The program upgrade authority at init. */
  admin: Address;
  /** The collateral: USDC on mainnet (SPL Token, 6 dp); a test mint on LiteSVM. */
  usdcMint: Address;
  /** The router every operator swap goes through: Jupiter v6 on mainnet; the stub in tests. */
  swapProgram: Address;
  /** ed25519 keys allowed to sign reference messages. */
  attestors: Array<Address>;
  /** Core `CLUSTER_ID` (101 / 103 / 104), bound into reference messages. */
  clusterTag: number;
  bump: number;
  pad: ReadonlyUint8Array;
  reserved: ReadonlyUint8Array;
};

/** Gets the encoder for {@link DeskConfigArgs} account data. */
export function getDeskConfigEncoder(): FixedSizeEncoder<DeskConfigArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["admin", getAddressEncoder()],
      ["usdcMint", getAddressEncoder()],
      ["swapProgram", getAddressEncoder()],
      ["attestors", getArrayEncoder(getAddressEncoder(), { size: 4 })],
      ["clusterTag", getU8Encoder()],
      ["bump", getU8Encoder()],
      ["pad", fixEncoderSize(getBytesEncoder(), 6)],
      ["reserved", fixEncoderSize(getBytesEncoder(), 64)],
    ]),
    (value) => ({ ...value, discriminator: DESK_CONFIG_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link DeskConfig} account data. */
export function getDeskConfigDecoder(): FixedSizeDecoder<DeskConfig> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["admin", getAddressDecoder()],
    ["usdcMint", getAddressDecoder()],
    ["swapProgram", getAddressDecoder()],
    ["attestors", getArrayDecoder(getAddressDecoder(), { size: 4 })],
    ["clusterTag", getU8Decoder()],
    ["bump", getU8Decoder()],
    ["pad", fixDecoderSize(getBytesDecoder(), 6)],
    ["reserved", fixDecoderSize(getBytesDecoder(), 64)],
  ]);
}

/** Gets the codec for {@link DeskConfig} account data. */
export function getDeskConfigCodec(): FixedSizeCodec<
  DeskConfigArgs,
  DeskConfig
> {
  return combineCodec(getDeskConfigEncoder(), getDeskConfigDecoder());
}

export function decodeDeskConfig<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<DeskConfig, TAddress>;
export function decodeDeskConfig<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<DeskConfig, TAddress>;
export function decodeDeskConfig<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<DeskConfig, TAddress> | MaybeAccount<DeskConfig, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getDeskConfigDecoder(),
  );
}

export async function fetchDeskConfig<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<DeskConfig, TAddress>> {
  const maybeAccount = await fetchMaybeDeskConfig(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeDeskConfig<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<DeskConfig, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeDeskConfig(maybeAccount);
}

export async function fetchAllDeskConfig(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<DeskConfig>[]> {
  const maybeAccounts = await fetchAllMaybeDeskConfig(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeDeskConfig(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<DeskConfig>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeDeskConfig(maybeAccount));
}

export function getDeskConfigSize(): number {
  return 304;
}
