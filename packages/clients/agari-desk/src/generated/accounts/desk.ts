/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getDeskTokenDecoder,
  getDeskTokenEncoder,
  type DeskToken,
  type DeskTokenArgs,
} from "../types";

export const DESK_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  33, 28, 147, 6, 226, 158, 166, 73,
]);

export function getDeskDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(DESK_DISCRIMINATOR);
}

export type Desk = {
  discriminator: ReadonlyUint8Array;
  /** Set once, never changes. */
  owner: Address;
  /** The default key means revoked. */
  operator: Address;
  /** `sha256(head ‖ seq ‖ decision_hash)`: a hash chain over the record, from 32 zero bytes. */
  head: ReadonlyUint8Array;
  /** +1 on every sealed action, gap free. */
  seq: bigint;
  /** USDC E6. */
  perActionCap: bigint;
  dailyCap: bigint;
  spentInWindow: bigint;
  /** The fixed 24 h spending window starts here (Shijima `Desk.sol` `_spend`). */
  windowStartSec: bigint;
  tokens: Array<DeskToken>;
  /** A buy must not pay more than this above its reference. */
  maxPremiumBps: number;
  /** 0 practice (checkpoints only), 1 ask first, 2 on its own. 1 and 2 are the same to the program. */
  mode: number;
  paused: number;
  /** 1 = every buy must carry a fully verified Pyth `Equity.Index` update and is measured against it. */
  requirePythIndex: number;
  bump: number;
  tokenCount: number;
  pad0: number;
  reserved: ReadonlyUint8Array;
};

export type DeskArgs = {
  /** Set once, never changes. */
  owner: Address;
  /** The default key means revoked. */
  operator: Address;
  /** `sha256(head ‖ seq ‖ decision_hash)`: a hash chain over the record, from 32 zero bytes. */
  head: ReadonlyUint8Array;
  /** +1 on every sealed action, gap free. */
  seq: number | bigint;
  /** USDC E6. */
  perActionCap: number | bigint;
  dailyCap: number | bigint;
  spentInWindow: number | bigint;
  /** The fixed 24 h spending window starts here (Shijima `Desk.sol` `_spend`). */
  windowStartSec: number | bigint;
  tokens: Array<DeskTokenArgs>;
  /** A buy must not pay more than this above its reference. */
  maxPremiumBps: number;
  /** 0 practice (checkpoints only), 1 ask first, 2 on its own. 1 and 2 are the same to the program. */
  mode: number;
  paused: number;
  /** 1 = every buy must carry a fully verified Pyth `Equity.Index` update and is measured against it. */
  requirePythIndex: number;
  bump: number;
  tokenCount: number;
  pad0: number;
  reserved: ReadonlyUint8Array;
};

/** Gets the encoder for {@link DeskArgs} account data. */
export function getDeskEncoder(): FixedSizeEncoder<DeskArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["owner", getAddressEncoder()],
      ["operator", getAddressEncoder()],
      ["head", fixEncoderSize(getBytesEncoder(), 32)],
      ["seq", getU64Encoder()],
      ["perActionCap", getU64Encoder()],
      ["dailyCap", getU64Encoder()],
      ["spentInWindow", getU64Encoder()],
      ["windowStartSec", getI64Encoder()],
      ["tokens", getArrayEncoder(getDeskTokenEncoder(), { size: 8 })],
      ["maxPremiumBps", getU16Encoder()],
      ["mode", getU8Encoder()],
      ["paused", getU8Encoder()],
      ["requirePythIndex", getU8Encoder()],
      ["bump", getU8Encoder()],
      ["tokenCount", getU8Encoder()],
      ["pad0", getU8Encoder()],
      ["reserved", fixEncoderSize(getBytesEncoder(), 64)],
    ]),
    (value) => ({ ...value, discriminator: DESK_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link Desk} account data. */
export function getDeskDecoder(): FixedSizeDecoder<Desk> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["owner", getAddressDecoder()],
    ["operator", getAddressDecoder()],
    ["head", fixDecoderSize(getBytesDecoder(), 32)],
    ["seq", getU64Decoder()],
    ["perActionCap", getU64Decoder()],
    ["dailyCap", getU64Decoder()],
    ["spentInWindow", getU64Decoder()],
    ["windowStartSec", getI64Decoder()],
    ["tokens", getArrayDecoder(getDeskTokenDecoder(), { size: 8 })],
    ["maxPremiumBps", getU16Decoder()],
    ["mode", getU8Decoder()],
    ["paused", getU8Decoder()],
    ["requirePythIndex", getU8Decoder()],
    ["bump", getU8Decoder()],
    ["tokenCount", getU8Decoder()],
    ["pad0", getU8Decoder()],
    ["reserved", fixDecoderSize(getBytesDecoder(), 64)],
  ]);
}

/** Gets the codec for {@link Desk} account data. */
export function getDeskCodec(): FixedSizeCodec<DeskArgs, Desk> {
  return combineCodec(getDeskEncoder(), getDeskDecoder());
}

export function decodeDesk<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<Desk, TAddress>;
export function decodeDesk<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<Desk, TAddress>;
export function decodeDesk<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<Desk, TAddress> | MaybeAccount<Desk, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getDeskDecoder(),
  );
}

export async function fetchDesk<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Desk, TAddress>> {
  const maybeAccount = await fetchMaybeDesk(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeDesk<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Desk, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeDesk(maybeAccount);
}

export async function fetchAllDesk(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Desk>[]> {
  const maybeAccounts = await fetchAllMaybeDesk(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeDesk(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Desk>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeDesk(maybeAccount));
}

export function getDeskSize(): number {
  return 536;
}
