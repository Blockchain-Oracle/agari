/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findDeskRefPda } from "../pdas";
import { AGARI_DESK_PROGRAM_ADDRESS } from "../programs";

export const PUBLIC_INIT_REFERENCE_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([243, 129, 113, 113, 139, 138, 225, 15]);

export function getPublicInitReferenceDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PUBLIC_INIT_REFERENCE_DISCRIMINATOR,
  );
}

export type PublicInitReferenceInstruction<
  TProgram extends string = typeof AGARI_DESK_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountMint extends string | AccountMeta<string> = string,
  TAccountDeskRef extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TAccountEventAuthority extends string | AccountMeta<string> = string,
  TAccountProgram extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountMint extends string
        ? ReadonlyAccount<TAccountMint>
        : TAccountMint,
      TAccountDeskRef extends string
        ? WritableAccount<TAccountDeskRef>
        : TAccountDeskRef,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      TAccountEventAuthority extends string
        ? ReadonlyAccount<TAccountEventAuthority>
        : TAccountEventAuthority,
      TAccountProgram extends string
        ? ReadonlyAccount<TAccountProgram>
        : TAccountProgram,
      ...TRemainingAccounts,
    ]
  >;

export type PublicInitReferenceInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type PublicInitReferenceInstructionDataArgs = {};

export function getPublicInitReferenceInstructionDataEncoder(): FixedSizeEncoder<PublicInitReferenceInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: PUBLIC_INIT_REFERENCE_DISCRIMINATOR,
    }),
  );
}

export function getPublicInitReferenceInstructionDataDecoder(): FixedSizeDecoder<PublicInitReferenceInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getPublicInitReferenceInstructionDataCodec(): FixedSizeCodec<
  PublicInitReferenceInstructionDataArgs,
  PublicInitReferenceInstructionData
> {
  return combineCodec(
    getPublicInitReferenceInstructionDataEncoder(),
    getPublicInitReferenceInstructionDataDecoder(),
  );
}

export type PublicInitReferenceAsyncInput<
  TAccountPayer extends string = string,
  TAccountMint extends string = string,
  TAccountDeskRef extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountEventAuthority extends string = string,
  TAccountProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  /** A PreStocks name: Token-2022, 9 decimals, checked in the handler. */
  mint: Address<TAccountMint>;
  deskRef?: Address<TAccountDeskRef>;
  systemProgram?: Address<TAccountSystemProgram>;
  eventAuthority: Address<TAccountEventAuthority>;
  program: Address<TAccountProgram>;
};

export async function getPublicInitReferenceInstructionAsync<
  TAccountPayer extends string,
  TAccountMint extends string,
  TAccountDeskRef extends string,
  TAccountSystemProgram extends string,
  TAccountEventAuthority extends string,
  TAccountProgram extends string,
  TProgramAddress extends Address = typeof AGARI_DESK_PROGRAM_ADDRESS,
>(
  input: PublicInitReferenceAsyncInput<
    TAccountPayer,
    TAccountMint,
    TAccountDeskRef,
    TAccountSystemProgram,
    TAccountEventAuthority,
    TAccountProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  PublicInitReferenceInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountMint,
    TAccountDeskRef,
    TAccountSystemProgram,
    TAccountEventAuthority,
    TAccountProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? AGARI_DESK_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    mint: { value: input.mint ?? null, isWritable: false },
    deskRef: { value: input.deskRef ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    program: { value: input.program ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.deskRef.value) {
    accounts.deskRef.value = await findDeskRefPda(
      {
        mint: getAddressFromResolvedInstructionAccount(
          "mint",
          accounts.mint.value,
        ),
      },
      { programAddress },
    );
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("mint", accounts.mint),
      getAccountMeta("deskRef", accounts.deskRef),
      getAccountMeta("systemProgram", accounts.systemProgram),
      getAccountMeta("eventAuthority", accounts.eventAuthority),
      getAccountMeta("program", accounts.program),
    ],
    data: getPublicInitReferenceInstructionDataEncoder().encode({}),
    programAddress,
  } as PublicInitReferenceInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountMint,
    TAccountDeskRef,
    TAccountSystemProgram,
    TAccountEventAuthority,
    TAccountProgram
  >);
}

export type PublicInitReferenceInput<
  TAccountPayer extends string = string,
  TAccountMint extends string = string,
  TAccountDeskRef extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountEventAuthority extends string = string,
  TAccountProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  /** A PreStocks name: Token-2022, 9 decimals, checked in the handler. */
  mint: Address<TAccountMint>;
  deskRef: Address<TAccountDeskRef>;
  systemProgram?: Address<TAccountSystemProgram>;
  eventAuthority: Address<TAccountEventAuthority>;
  program: Address<TAccountProgram>;
};

export function getPublicInitReferenceInstruction<
  TAccountPayer extends string,
  TAccountMint extends string,
  TAccountDeskRef extends string,
  TAccountSystemProgram extends string,
  TAccountEventAuthority extends string,
  TAccountProgram extends string,
  TProgramAddress extends Address = typeof AGARI_DESK_PROGRAM_ADDRESS,
>(
  input: PublicInitReferenceInput<
    TAccountPayer,
    TAccountMint,
    TAccountDeskRef,
    TAccountSystemProgram,
    TAccountEventAuthority,
    TAccountProgram
  >,
  config?: { programAddress?: TProgramAddress },
): PublicInitReferenceInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountMint,
  TAccountDeskRef,
  TAccountSystemProgram,
  TAccountEventAuthority,
  TAccountProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? AGARI_DESK_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    mint: { value: input.mint ?? null, isWritable: false },
    deskRef: { value: input.deskRef ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    program: { value: input.program ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("mint", accounts.mint),
      getAccountMeta("deskRef", accounts.deskRef),
      getAccountMeta("systemProgram", accounts.systemProgram),
      getAccountMeta("eventAuthority", accounts.eventAuthority),
      getAccountMeta("program", accounts.program),
    ],
    data: getPublicInitReferenceInstructionDataEncoder().encode({}),
    programAddress,
  } as PublicInitReferenceInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountMint,
    TAccountDeskRef,
    TAccountSystemProgram,
    TAccountEventAuthority,
    TAccountProgram
  >);
}

export type ParsedPublicInitReferenceInstruction<
  TProgram extends string = typeof AGARI_DESK_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    payer: TAccountMetas[0];
    /** A PreStocks name: Token-2022, 9 decimals, checked in the handler. */
    mint: TAccountMetas[1];
    deskRef: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
    eventAuthority: TAccountMetas[4];
    program: TAccountMetas[5];
  };
  data: PublicInitReferenceInstructionData;
};

export function parsePublicInitReferenceInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedPublicInitReferenceInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 6,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      mint: getNextAccount(),
      deskRef: getNextAccount(),
      systemProgram: getNextAccount(),
      eventAuthority: getNextAccount(),
      program: getNextAccount(),
    },
    data: getPublicInitReferenceInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
