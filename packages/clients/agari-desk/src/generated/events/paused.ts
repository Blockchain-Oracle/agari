/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const PAUSED_EVENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  172, 248, 5, 253, 49, 255, 255, 232,
]);

export function getPausedEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PAUSED_EVENT_DISCRIMINATOR,
  );
}

export type PausedEvent = { owner: Address; by: Address };

export type PausedEventArgs = PausedEvent;

/** Gets the encoder for {@link PausedEventArgs} event data. */
export function getPausedEventEncoder(): FixedSizeEncoder<PausedEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["owner", getAddressEncoder()],
      ["by", getAddressEncoder()],
    ]),
    [getConstantEncoder(PAUSED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link PausedEvent} event data. */
export function getPausedEventDecoder(): FixedSizeDecoder<PausedEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["owner", getAddressDecoder()],
      ["by", getAddressDecoder()],
    ]),
    [getConstantDecoder(PAUSED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link PausedEvent} event data. */
export function getPausedEventCodec(): FixedSizeCodec<
  PausedEventArgs,
  PausedEvent
> {
  return combineCodec(getPausedEventEncoder(), getPausedEventDecoder());
}

/** Parses the provided bytes into {@link PausedEvent} event data. */
export function parsePausedEvent(
  data: ReadonlyUint8Array | Uint8Array,
): PausedEvent {
  if (containsBytes(data, PAUSED_EVENT_DISCRIMINATOR, 0)) {
    return getPausedEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "PausedEvent" event discriminators.',
  );
}
