/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const CHECKPOINT_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([61, 240, 32, 73, 165, 69, 208, 74]);

export function getCheckpointEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CHECKPOINT_EVENT_DISCRIMINATOR,
  );
}

export type CheckpointEvent = {
  owner: Address;
  seq: bigint;
  decisionHash: ReadonlyUint8Array;
  head: ReadonlyUint8Array;
};

export type CheckpointEventArgs = {
  owner: Address;
  seq: number | bigint;
  decisionHash: ReadonlyUint8Array;
  head: ReadonlyUint8Array;
};

/** Gets the encoder for {@link CheckpointEventArgs} event data. */
export function getCheckpointEventEncoder(): FixedSizeEncoder<CheckpointEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["owner", getAddressEncoder()],
      ["seq", getU64Encoder()],
      ["decisionHash", fixEncoderSize(getBytesEncoder(), 32)],
      ["head", fixEncoderSize(getBytesEncoder(), 32)],
    ]),
    [getConstantEncoder(CHECKPOINT_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link CheckpointEvent} event data. */
export function getCheckpointEventDecoder(): FixedSizeDecoder<CheckpointEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["owner", getAddressDecoder()],
      ["seq", getU64Decoder()],
      ["decisionHash", fixDecoderSize(getBytesDecoder(), 32)],
      ["head", fixDecoderSize(getBytesDecoder(), 32)],
    ]),
    [getConstantDecoder(CHECKPOINT_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link CheckpointEvent} event data. */
export function getCheckpointEventCodec(): FixedSizeCodec<
  CheckpointEventArgs,
  CheckpointEvent
> {
  return combineCodec(getCheckpointEventEncoder(), getCheckpointEventDecoder());
}

/** Parses the provided bytes into {@link CheckpointEvent} event data. */
export function parseCheckpointEvent(
  data: ReadonlyUint8Array | Uint8Array,
): CheckpointEvent {
  if (containsBytes(data, CHECKPOINT_EVENT_DISCRIMINATOR, 0)) {
    return getCheckpointEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "CheckpointEvent" event discriminators.',
  );
}
