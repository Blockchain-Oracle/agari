/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getPositionSlotDecoder,
  getPositionSlotEncoder,
  type PositionSlot,
  type PositionSlotArgs,
} from "../types";

export const VAULT_ACCOUNT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  230, 251, 241, 83, 139, 202, 93, 28,
]);

export function getVaultAccountDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    VAULT_ACCOUNT_DISCRIMINATOR,
  );
}

export type VaultAccount = {
  discriminator: ReadonlyUint8Array;
  owner: Address;
  /** Free Trading Balance (base units). */
  available: bigint;
  /** The private bucket (`moveToPrivate`). */
  privateAvailable: bigint;
  totalDeposited: bigint;
  totalWithdrawn: bigint;
  /** The live grant id per kind (SESSION, EXECUTOR, STRATEGY); 0 = none. */
  activeGrants: Array<bigint>;
  custodyBump: number;
  bump: number;
  /** Slots whose `market` is set. */
  slotsUsed: number;
  pad: ReadonlyUint8Array;
  reserved: ReadonlyUint8Array;
  positions: Array<PositionSlot>;
};

export type VaultAccountArgs = {
  owner: Address;
  /** Free Trading Balance (base units). */
  available: number | bigint;
  /** The private bucket (`moveToPrivate`). */
  privateAvailable: number | bigint;
  totalDeposited: number | bigint;
  totalWithdrawn: number | bigint;
  /** The live grant id per kind (SESSION, EXECUTOR, STRATEGY); 0 = none. */
  activeGrants: Array<number | bigint>;
  custodyBump: number;
  bump: number;
  /** Slots whose `market` is set. */
  slotsUsed: number;
  pad: ReadonlyUint8Array;
  reserved: ReadonlyUint8Array;
  positions: Array<PositionSlotArgs>;
};

/** Gets the encoder for {@link VaultAccountArgs} account data. */
export function getVaultAccountEncoder(): FixedSizeEncoder<VaultAccountArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["owner", getAddressEncoder()],
      ["available", getU64Encoder()],
      ["privateAvailable", getU64Encoder()],
      ["totalDeposited", getU64Encoder()],
      ["totalWithdrawn", getU64Encoder()],
      ["activeGrants", getArrayEncoder(getU64Encoder(), { size: 3 })],
      ["custodyBump", getU8Encoder()],
      ["bump", getU8Encoder()],
      ["slotsUsed", getU16Encoder()],
      ["pad", fixEncoderSize(getBytesEncoder(), 4)],
      ["reserved", fixEncoderSize(getBytesEncoder(), 32)],
      ["positions", getArrayEncoder(getPositionSlotEncoder(), { size: 16 })],
    ]),
    (value) => ({ ...value, discriminator: VAULT_ACCOUNT_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link VaultAccount} account data. */
export function getVaultAccountDecoder(): FixedSizeDecoder<VaultAccount> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["owner", getAddressDecoder()],
    ["available", getU64Decoder()],
    ["privateAvailable", getU64Decoder()],
    ["totalDeposited", getU64Decoder()],
    ["totalWithdrawn", getU64Decoder()],
    ["activeGrants", getArrayDecoder(getU64Decoder(), { size: 3 })],
    ["custodyBump", getU8Decoder()],
    ["bump", getU8Decoder()],
    ["slotsUsed", getU16Decoder()],
    ["pad", fixDecoderSize(getBytesDecoder(), 4)],
    ["reserved", fixDecoderSize(getBytesDecoder(), 32)],
    ["positions", getArrayDecoder(getPositionSlotDecoder(), { size: 16 })],
  ]);
}

/** Gets the codec for {@link VaultAccount} account data. */
export function getVaultAccountCodec(): FixedSizeCodec<
  VaultAccountArgs,
  VaultAccount
> {
  return combineCodec(getVaultAccountEncoder(), getVaultAccountDecoder());
}

export function decodeVaultAccount<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<VaultAccount, TAddress>;
export function decodeVaultAccount<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<VaultAccount, TAddress>;
export function decodeVaultAccount<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<VaultAccount, TAddress> | MaybeAccount<VaultAccount, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getVaultAccountDecoder(),
  );
}

export async function fetchVaultAccount<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<VaultAccount, TAddress>> {
  const maybeAccount = await fetchMaybeVaultAccount(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeVaultAccount<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<VaultAccount, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeVaultAccount(maybeAccount);
}

export async function fetchAllVaultAccount(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<VaultAccount>[]> {
  const maybeAccounts = await fetchAllMaybeVaultAccount(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeVaultAccount(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<VaultAccount>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeVaultAccount(maybeAccount));
}

export function getVaultAccountSize(): number {
  return 1160;
}
