/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const VAULT_CONFIG_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  99, 86, 43, 216, 184, 102, 119, 77,
]);

export function getVaultConfigDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    VAULT_CONFIG_DISCRIMINATOR,
  );
}

export type VaultConfig = {
  discriminator: ReadonlyUint8Array;
  /** The program upgrade authority at init. */
  admin: Address;
  /** agari-events `GlobalConfig`. */
  eventsConfig: Address;
  /** = `events_config.collateral_mint` at init. */
  collateralMint: Address;
  /** The `["seat"]` PDA: engine authority and custody owner. */
  seat: Address;
  /** The id the next grant must take; starts at 1 (0 = attended). */
  nextGrantId: bigint;
  seatBump: number;
  bump: number;
  pad: ReadonlyUint8Array;
  reserved: ReadonlyUint8Array;
};

export type VaultConfigArgs = {
  /** The program upgrade authority at init. */
  admin: Address;
  /** agari-events `GlobalConfig`. */
  eventsConfig: Address;
  /** = `events_config.collateral_mint` at init. */
  collateralMint: Address;
  /** The `["seat"]` PDA: engine authority and custody owner. */
  seat: Address;
  /** The id the next grant must take; starts at 1 (0 = attended). */
  nextGrantId: number | bigint;
  seatBump: number;
  bump: number;
  pad: ReadonlyUint8Array;
  reserved: ReadonlyUint8Array;
};

/** Gets the encoder for {@link VaultConfigArgs} account data. */
export function getVaultConfigEncoder(): FixedSizeEncoder<VaultConfigArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["admin", getAddressEncoder()],
      ["eventsConfig", getAddressEncoder()],
      ["collateralMint", getAddressEncoder()],
      ["seat", getAddressEncoder()],
      ["nextGrantId", getU64Encoder()],
      ["seatBump", getU8Encoder()],
      ["bump", getU8Encoder()],
      ["pad", fixEncoderSize(getBytesEncoder(), 6)],
      ["reserved", fixEncoderSize(getBytesEncoder(), 64)],
    ]),
    (value) => ({ ...value, discriminator: VAULT_CONFIG_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link VaultConfig} account data. */
export function getVaultConfigDecoder(): FixedSizeDecoder<VaultConfig> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["admin", getAddressDecoder()],
    ["eventsConfig", getAddressDecoder()],
    ["collateralMint", getAddressDecoder()],
    ["seat", getAddressDecoder()],
    ["nextGrantId", getU64Decoder()],
    ["seatBump", getU8Decoder()],
    ["bump", getU8Decoder()],
    ["pad", fixDecoderSize(getBytesDecoder(), 6)],
    ["reserved", fixDecoderSize(getBytesDecoder(), 64)],
  ]);
}

/** Gets the codec for {@link VaultConfig} account data. */
export function getVaultConfigCodec(): FixedSizeCodec<
  VaultConfigArgs,
  VaultConfig
> {
  return combineCodec(getVaultConfigEncoder(), getVaultConfigDecoder());
}

export function decodeVaultConfig<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<VaultConfig, TAddress>;
export function decodeVaultConfig<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<VaultConfig, TAddress>;
export function decodeVaultConfig<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<VaultConfig, TAddress> | MaybeAccount<VaultConfig, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getVaultConfigDecoder(),
  );
}

export async function fetchVaultConfig<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<VaultConfig, TAddress>> {
  const maybeAccount = await fetchMaybeVaultConfig(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeVaultConfig<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<VaultConfig, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeVaultConfig(maybeAccount);
}

export async function fetchAllVaultConfig(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<VaultConfig>[]> {
  const maybeAccounts = await fetchAllMaybeVaultConfig(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeVaultConfig(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<VaultConfig>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeVaultConfig(maybeAccount));
}

export function getVaultConfigSize(): number {
  return 216;
}
