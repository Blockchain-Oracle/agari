/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const GRANT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  161, 166, 11, 205, 204, 135, 205, 54,
]);

export function getGrantDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(GRANT_DISCRIMINATOR);
}

export type Grant = {
  discriminator: ReadonlyUint8Array;
  owner: Address;
  actor: Address;
  /** Global and never reused (`VaultConfig.next_grant_id`). */
  grantId: bigint;
  /** Live while `now ≤ expires_at_sec`. */
  expiresAtSec: bigint;
  /** UTC day (`⌊unix_ts / 86,400⌋`) that `spent_today` belongs to. */
  spentDay: bigint;
  spentToday: bigint;
  /** What the actor may still spend; proceeds never flow back here. */
  budget: bigint;
  maxStakePerTrade: bigint;
  maxDailySpend: bigint;
  maxOpenPositions: number;
  openPositions: number;
  /** Dearest own-side price in ticks; 0 = no cap. */
  maxPriceTicks: number;
  /** SESSION 0, EXECUTOR 1, STRATEGY 2. */
  kind: number;
  /** 0 live, 1 revoked. */
  revoked: number;
  bump: number;
  pad: ReadonlyUint8Array;
  reserved: ReadonlyUint8Array;
};

export type GrantArgs = {
  owner: Address;
  actor: Address;
  /** Global and never reused (`VaultConfig.next_grant_id`). */
  grantId: number | bigint;
  /** Live while `now ≤ expires_at_sec`. */
  expiresAtSec: number | bigint;
  /** UTC day (`⌊unix_ts / 86,400⌋`) that `spent_today` belongs to. */
  spentDay: number | bigint;
  spentToday: number | bigint;
  /** What the actor may still spend; proceeds never flow back here. */
  budget: number | bigint;
  maxStakePerTrade: number | bigint;
  maxDailySpend: number | bigint;
  maxOpenPositions: number;
  openPositions: number;
  /** Dearest own-side price in ticks; 0 = no cap. */
  maxPriceTicks: number;
  /** SESSION 0, EXECUTOR 1, STRATEGY 2. */
  kind: number;
  /** 0 live, 1 revoked. */
  revoked: number;
  bump: number;
  pad: ReadonlyUint8Array;
  reserved: ReadonlyUint8Array;
};

/** Gets the encoder for {@link GrantArgs} account data. */
export function getGrantEncoder(): FixedSizeEncoder<GrantArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["owner", getAddressEncoder()],
      ["actor", getAddressEncoder()],
      ["grantId", getU64Encoder()],
      ["expiresAtSec", getI64Encoder()],
      ["spentDay", getU64Encoder()],
      ["spentToday", getU64Encoder()],
      ["budget", getU64Encoder()],
      ["maxStakePerTrade", getU64Encoder()],
      ["maxDailySpend", getU64Encoder()],
      ["maxOpenPositions", getU32Encoder()],
      ["openPositions", getU32Encoder()],
      ["maxPriceTicks", getU16Encoder()],
      ["kind", getU8Encoder()],
      ["revoked", getU8Encoder()],
      ["bump", getU8Encoder()],
      ["pad", fixEncoderSize(getBytesEncoder(), 3)],
      ["reserved", fixEncoderSize(getBytesEncoder(), 32)],
    ]),
    (value) => ({ ...value, discriminator: GRANT_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link Grant} account data. */
export function getGrantDecoder(): FixedSizeDecoder<Grant> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["owner", getAddressDecoder()],
    ["actor", getAddressDecoder()],
    ["grantId", getU64Decoder()],
    ["expiresAtSec", getI64Decoder()],
    ["spentDay", getU64Decoder()],
    ["spentToday", getU64Decoder()],
    ["budget", getU64Decoder()],
    ["maxStakePerTrade", getU64Decoder()],
    ["maxDailySpend", getU64Decoder()],
    ["maxOpenPositions", getU32Decoder()],
    ["openPositions", getU32Decoder()],
    ["maxPriceTicks", getU16Decoder()],
    ["kind", getU8Decoder()],
    ["revoked", getU8Decoder()],
    ["bump", getU8Decoder()],
    ["pad", fixDecoderSize(getBytesDecoder(), 3)],
    ["reserved", fixDecoderSize(getBytesDecoder(), 32)],
  ]);
}

/** Gets the codec for {@link Grant} account data. */
export function getGrantCodec(): FixedSizeCodec<GrantArgs, Grant> {
  return combineCodec(getGrantEncoder(), getGrantDecoder());
}

export function decodeGrant<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<Grant, TAddress>;
export function decodeGrant<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<Grant, TAddress>;
export function decodeGrant<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<Grant, TAddress> | MaybeAccount<Grant, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getGrantDecoder(),
  );
}

export async function fetchGrant<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Grant, TAddress>> {
  const maybeAccount = await fetchMaybeGrant(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeGrant<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Grant, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeGrant(maybeAccount);
}

export async function fetchAllGrant(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Grant>[]> {
  const maybeAccounts = await fetchAllMaybeGrant(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeGrant(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Grant>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeGrant(maybeAccount));
}

export function getGrantSize(): number {
  return 176;
}
