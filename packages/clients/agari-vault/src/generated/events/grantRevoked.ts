/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const GRANT_REVOKED_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([192, 60, 253, 12, 110, 158, 200, 30]);

export function getGrantRevokedEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    GRANT_REVOKED_EVENT_DISCRIMINATOR,
  );
}

export type GrantRevokedEvent = {
  grantId: bigint;
  owner: Address;
  returned: bigint;
};

export type GrantRevokedEventArgs = {
  grantId: number | bigint;
  owner: Address;
  returned: number | bigint;
};

/** Gets the encoder for {@link GrantRevokedEventArgs} event data. */
export function getGrantRevokedEventEncoder(): FixedSizeEncoder<GrantRevokedEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["grantId", getU64Encoder()],
      ["owner", getAddressEncoder()],
      ["returned", getU64Encoder()],
    ]),
    [getConstantEncoder(GRANT_REVOKED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link GrantRevokedEvent} event data. */
export function getGrantRevokedEventDecoder(): FixedSizeDecoder<GrantRevokedEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["grantId", getU64Decoder()],
      ["owner", getAddressDecoder()],
      ["returned", getU64Decoder()],
    ]),
    [getConstantDecoder(GRANT_REVOKED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link GrantRevokedEvent} event data. */
export function getGrantRevokedEventCodec(): FixedSizeCodec<
  GrantRevokedEventArgs,
  GrantRevokedEvent
> {
  return combineCodec(
    getGrantRevokedEventEncoder(),
    getGrantRevokedEventDecoder(),
  );
}

/** Parses the provided bytes into {@link GrantRevokedEvent} event data. */
export function parseGrantRevokedEvent(
  data: ReadonlyUint8Array | Uint8Array,
): GrantRevokedEvent {
  if (containsBytes(data, GRANT_REVOKED_EVENT_DISCRIMINATOR, 0)) {
    return getGrantRevokedEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "GrantRevokedEvent" event discriminators.',
  );
}
