/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const MERGED_EVENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  57, 129, 241, 73, 156, 41, 126, 251,
]);

export function getMergedEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    MERGED_EVENT_DISCRIMINATOR,
  );
}

export type MergedEvent = {
  vault: Address;
  market: Address;
  lots: bigint;
  mergedBase: bigint;
};

export type MergedEventArgs = {
  vault: Address;
  market: Address;
  lots: number | bigint;
  mergedBase: number | bigint;
};

/** Gets the encoder for {@link MergedEventArgs} event data. */
export function getMergedEventEncoder(): FixedSizeEncoder<MergedEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["vault", getAddressEncoder()],
      ["market", getAddressEncoder()],
      ["lots", getU64Encoder()],
      ["mergedBase", getU64Encoder()],
    ]),
    [getConstantEncoder(MERGED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link MergedEvent} event data. */
export function getMergedEventDecoder(): FixedSizeDecoder<MergedEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["vault", getAddressDecoder()],
      ["market", getAddressDecoder()],
      ["lots", getU64Decoder()],
      ["mergedBase", getU64Decoder()],
    ]),
    [getConstantDecoder(MERGED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link MergedEvent} event data. */
export function getMergedEventCodec(): FixedSizeCodec<
  MergedEventArgs,
  MergedEvent
> {
  return combineCodec(getMergedEventEncoder(), getMergedEventDecoder());
}

/** Parses the provided bytes into {@link MergedEvent} event data. */
export function parseMergedEvent(
  data: ReadonlyUint8Array | Uint8Array,
): MergedEvent {
  if (containsBytes(data, MERGED_EVENT_DISCRIMINATOR, 0)) {
    return getMergedEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "MergedEvent" event discriminators.',
  );
}
