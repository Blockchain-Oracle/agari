/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const PULLED_EVENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  65, 228, 151, 236, 209, 242, 90, 236,
]);

export function getPulledEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PULLED_EVENT_DISCRIMINATOR,
  );
}

export type PulledEvent = {
  vault: Address;
  market: Address;
  escrowBackBase: bigint;
};

export type PulledEventArgs = {
  vault: Address;
  market: Address;
  escrowBackBase: number | bigint;
};

/** Gets the encoder for {@link PulledEventArgs} event data. */
export function getPulledEventEncoder(): FixedSizeEncoder<PulledEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["vault", getAddressEncoder()],
      ["market", getAddressEncoder()],
      ["escrowBackBase", getU64Encoder()],
    ]),
    [getConstantEncoder(PULLED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link PulledEvent} event data. */
export function getPulledEventDecoder(): FixedSizeDecoder<PulledEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["vault", getAddressDecoder()],
      ["market", getAddressDecoder()],
      ["escrowBackBase", getU64Decoder()],
    ]),
    [getConstantDecoder(PULLED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link PulledEvent} event data. */
export function getPulledEventCodec(): FixedSizeCodec<
  PulledEventArgs,
  PulledEvent
> {
  return combineCodec(getPulledEventEncoder(), getPulledEventDecoder());
}

/** Parses the provided bytes into {@link PulledEvent} event data. */
export function parsePulledEvent(
  data: ReadonlyUint8Array | Uint8Array,
): PulledEvent {
  if (containsBytes(data, PULLED_EVENT_DISCRIMINATOR, 0)) {
    return getPulledEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "PulledEvent" event discriminators.',
  );
}
