/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
} from "@solana/kit";

/**
 * What the vault will and will not do with its providers' money, mirrored by `MakerParams` in
 * `packages/core/src/maker/types.ts`.
 */
export type MakerParams = {
  /** The most of the vault's value that may sit in the venue at once. */
  maxExposureBps: number;
  /** A quote tighter than this is not a quote, it is a gift. */
  minSpreadTicks: number;
  /** The band the vault will quote inside; outside it the edge stops covering the rounding. */
  minPriceTicks: number;
  maxPriceTicks: number;
  /** Per-quote and per-Window ceilings, so one Window cannot take the book. */
  maxQuantityLots: bigint;
  maxWindowDeployedBase: bigint;
  maxOpenWindows: number;
  /** A Window with less than this left is not worth quoting into. */
  minTimeLeftSec: number;
};

export type MakerParamsArgs = {
  /** The most of the vault's value that may sit in the venue at once. */
  maxExposureBps: number;
  /** A quote tighter than this is not a quote, it is a gift. */
  minSpreadTicks: number;
  /** The band the vault will quote inside; outside it the edge stops covering the rounding. */
  minPriceTicks: number;
  maxPriceTicks: number;
  /** Per-quote and per-Window ceilings, so one Window cannot take the book. */
  maxQuantityLots: number | bigint;
  maxWindowDeployedBase: number | bigint;
  maxOpenWindows: number;
  /** A Window with less than this left is not worth quoting into. */
  minTimeLeftSec: number;
};

export function getMakerParamsEncoder(): FixedSizeEncoder<MakerParamsArgs> {
  return getStructEncoder([
    ["maxExposureBps", getU16Encoder()],
    ["minSpreadTicks", getU16Encoder()],
    ["minPriceTicks", getU16Encoder()],
    ["maxPriceTicks", getU16Encoder()],
    ["maxQuantityLots", getU64Encoder()],
    ["maxWindowDeployedBase", getU64Encoder()],
    ["maxOpenWindows", getU16Encoder()],
    ["minTimeLeftSec", getU32Encoder()],
  ]);
}

export function getMakerParamsDecoder(): FixedSizeDecoder<MakerParams> {
  return getStructDecoder([
    ["maxExposureBps", getU16Decoder()],
    ["minSpreadTicks", getU16Decoder()],
    ["minPriceTicks", getU16Decoder()],
    ["maxPriceTicks", getU16Decoder()],
    ["maxQuantityLots", getU64Decoder()],
    ["maxWindowDeployedBase", getU64Decoder()],
    ["maxOpenWindows", getU16Decoder()],
    ["minTimeLeftSec", getU32Decoder()],
  ]);
}

export function getMakerParamsCodec(): FixedSizeCodec<
  MakerParamsArgs,
  MakerParams
> {
  return combineCodec(getMakerParamsEncoder(), getMakerParamsDecoder());
}
