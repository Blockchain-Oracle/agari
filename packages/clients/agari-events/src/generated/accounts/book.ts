/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getLevelDecoder,
  getLevelEncoder,
  type Level,
  type LevelArgs,
} from "../types";

export const BOOK_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  121, 34, 121, 35, 91, 62, 85, 222,
]);

export function getBookDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(BOOK_DISCRIMINATOR);
}

export type Book = {
  discriminator: ReadonlyUint8Array;
  /** `Pubkey::default()` = free. */
  market: Address;
  series: Address;
  /** Never reset, so `(node, seq)` handles stay ABA-safe across recycling. */
  nextSeq: bigint;
  generation: number;
  capacity: number;
  orderCount: number;
  freeHead: number;
  /** Nodes `≥ high_water` have never been used. */
  highWater: number;
  pad0: number;
  /** Bit `p` set iff bid level `p` is non-empty. */
  bidBits: Array<bigint>;
  askBits: Array<bigint>;
  reserved: ReadonlyUint8Array;
  /** Indexed by price ticks; index 0 unused. */
  bids: Array<Level>;
  asks: Array<Level>;
};

export type BookArgs = {
  /** `Pubkey::default()` = free. */
  market: Address;
  series: Address;
  /** Never reset, so `(node, seq)` handles stay ABA-safe across recycling. */
  nextSeq: number | bigint;
  generation: number;
  capacity: number;
  orderCount: number;
  freeHead: number;
  /** Nodes `≥ high_water` have never been used. */
  highWater: number;
  pad0: number;
  /** Bit `p` set iff bid level `p` is non-empty. */
  bidBits: Array<number | bigint>;
  askBits: Array<number | bigint>;
  reserved: ReadonlyUint8Array;
  /** Indexed by price ticks; index 0 unused. */
  bids: Array<LevelArgs>;
  asks: Array<LevelArgs>;
};

/** Gets the encoder for {@link BookArgs} account data. */
export function getBookEncoder(): FixedSizeEncoder<BookArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["market", getAddressEncoder()],
      ["series", getAddressEncoder()],
      ["nextSeq", getU64Encoder()],
      ["generation", getU32Encoder()],
      ["capacity", getU32Encoder()],
      ["orderCount", getU32Encoder()],
      ["freeHead", getU32Encoder()],
      ["highWater", getU32Encoder()],
      ["pad0", getU32Encoder()],
      ["bidBits", getArrayEncoder(getU64Encoder(), { size: 16 })],
      ["askBits", getArrayEncoder(getU64Encoder(), { size: 16 })],
      ["reserved", fixEncoderSize(getBytesEncoder(), 32)],
      ["bids", getArrayEncoder(getLevelEncoder(), { size: 1000 })],
      ["asks", getArrayEncoder(getLevelEncoder(), { size: 1000 })],
    ]),
    (value) => ({ ...value, discriminator: BOOK_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link Book} account data. */
export function getBookDecoder(): FixedSizeDecoder<Book> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["market", getAddressDecoder()],
    ["series", getAddressDecoder()],
    ["nextSeq", getU64Decoder()],
    ["generation", getU32Decoder()],
    ["capacity", getU32Decoder()],
    ["orderCount", getU32Decoder()],
    ["freeHead", getU32Decoder()],
    ["highWater", getU32Decoder()],
    ["pad0", getU32Decoder()],
    ["bidBits", getArrayDecoder(getU64Decoder(), { size: 16 })],
    ["askBits", getArrayDecoder(getU64Decoder(), { size: 16 })],
    ["reserved", fixDecoderSize(getBytesDecoder(), 32)],
    ["bids", getArrayDecoder(getLevelDecoder(), { size: 1000 })],
    ["asks", getArrayDecoder(getLevelDecoder(), { size: 1000 })],
  ]);
}

/** Gets the codec for {@link Book} account data. */
export function getBookCodec(): FixedSizeCodec<BookArgs, Book> {
  return combineCodec(getBookEncoder(), getBookDecoder());
}

export function decodeBook<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<Book, TAddress>;
export function decodeBook<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<Book, TAddress>;
export function decodeBook<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<Book, TAddress> | MaybeAccount<Book, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getBookDecoder(),
  );
}

export async function fetchBook<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Book, TAddress>> {
  const maybeAccount = await fetchMaybeBook(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeBook<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Book, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeBook(maybeAccount);
}

export async function fetchAllBook(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Book>[]> {
  const maybeAccounts = await fetchAllMaybeBook(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeBook(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Book>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeBook(maybeAccount));
}

export function getBookSize(): number {
  return 32392;
}
