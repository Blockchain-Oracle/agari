/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const GLOBAL_CONFIG_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  149, 8, 156, 202, 160, 252, 176, 217,
]);

export function getGlobalConfigDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    GLOBAL_CONFIG_DISCRIMINATOR,
  );
}

export type GlobalConfig = {
  discriminator: ReadonlyUint8Array;
  /** Must equal the program upgrade authority at init. */
  admin: Address;
  /** SPL Token mint; Token-2022 is rejected. */
  collateralMint: Address;
  tokenProgram: Address;
  /** Collateral token account that receives donated residue at ledger close. */
  treasury: Address;
  rollers: Array<Address>;
  /** ed25519 keys allowed to sign attested prints. */
  attestors: Array<Address>;
  /** Product seat PDAs; index `i` owns Ledger seat `i` in every Window. */
  programAuthorities: Array<Address>;
  switchboardQueue: Address;
  /** 20-byte EVM addresses from RedStone's adapter config (D-002). */
  redstoneSigners: Array<ReadonlyUint8Array>;
  redstoneSignerCount: number;
  /** Liveness threshold after `strict_sec`. */
  redstoneThreshold: number;
  switchboardMinOracles: number;
  mode: number;
  collateralDecimals: number;
  /** Core `CLUSTER_ID` (101 / 103 / 104), bound into attested messages. */
  clusterTag: number;
  bump: number;
  pad0: number;
  resultRetentionSec: number;
  reserved: ReadonlyUint8Array;
};

export type GlobalConfigArgs = {
  /** Must equal the program upgrade authority at init. */
  admin: Address;
  /** SPL Token mint; Token-2022 is rejected. */
  collateralMint: Address;
  tokenProgram: Address;
  /** Collateral token account that receives donated residue at ledger close. */
  treasury: Address;
  rollers: Array<Address>;
  /** ed25519 keys allowed to sign attested prints. */
  attestors: Array<Address>;
  /** Product seat PDAs; index `i` owns Ledger seat `i` in every Window. */
  programAuthorities: Array<Address>;
  switchboardQueue: Address;
  /** 20-byte EVM addresses from RedStone's adapter config (D-002). */
  redstoneSigners: Array<ReadonlyUint8Array>;
  redstoneSignerCount: number;
  /** Liveness threshold after `strict_sec`. */
  redstoneThreshold: number;
  switchboardMinOracles: number;
  mode: number;
  collateralDecimals: number;
  /** Core `CLUSTER_ID` (101 / 103 / 104), bound into attested messages. */
  clusterTag: number;
  bump: number;
  pad0: number;
  resultRetentionSec: number;
  reserved: ReadonlyUint8Array;
};

/** Gets the encoder for {@link GlobalConfigArgs} account data. */
export function getGlobalConfigEncoder(): FixedSizeEncoder<GlobalConfigArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["admin", getAddressEncoder()],
      ["collateralMint", getAddressEncoder()],
      ["tokenProgram", getAddressEncoder()],
      ["treasury", getAddressEncoder()],
      ["rollers", getArrayEncoder(getAddressEncoder(), { size: 4 })],
      ["attestors", getArrayEncoder(getAddressEncoder(), { size: 4 })],
      ["programAuthorities", getArrayEncoder(getAddressEncoder(), { size: 8 })],
      ["switchboardQueue", getAddressEncoder()],
      [
        "redstoneSigners",
        getArrayEncoder(fixEncoderSize(getBytesEncoder(), 20), { size: 5 }),
      ],
      ["redstoneSignerCount", getU8Encoder()],
      ["redstoneThreshold", getU8Encoder()],
      ["switchboardMinOracles", getU8Encoder()],
      ["mode", getU8Encoder()],
      ["collateralDecimals", getU8Encoder()],
      ["clusterTag", getU8Encoder()],
      ["bump", getU8Encoder()],
      ["pad0", getU8Encoder()],
      ["resultRetentionSec", getU32Encoder()],
      ["reserved", fixEncoderSize(getBytesEncoder(), 64)],
    ]),
    (value) => ({ ...value, discriminator: GLOBAL_CONFIG_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link GlobalConfig} account data. */
export function getGlobalConfigDecoder(): FixedSizeDecoder<GlobalConfig> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["admin", getAddressDecoder()],
    ["collateralMint", getAddressDecoder()],
    ["tokenProgram", getAddressDecoder()],
    ["treasury", getAddressDecoder()],
    ["rollers", getArrayDecoder(getAddressDecoder(), { size: 4 })],
    ["attestors", getArrayDecoder(getAddressDecoder(), { size: 4 })],
    ["programAuthorities", getArrayDecoder(getAddressDecoder(), { size: 8 })],
    ["switchboardQueue", getAddressDecoder()],
    [
      "redstoneSigners",
      getArrayDecoder(fixDecoderSize(getBytesDecoder(), 20), { size: 5 }),
    ],
    ["redstoneSignerCount", getU8Decoder()],
    ["redstoneThreshold", getU8Decoder()],
    ["switchboardMinOracles", getU8Decoder()],
    ["mode", getU8Decoder()],
    ["collateralDecimals", getU8Decoder()],
    ["clusterTag", getU8Decoder()],
    ["bump", getU8Decoder()],
    ["pad0", getU8Decoder()],
    ["resultRetentionSec", getU32Decoder()],
    ["reserved", fixDecoderSize(getBytesDecoder(), 64)],
  ]);
}

/** Gets the codec for {@link GlobalConfig} account data. */
export function getGlobalConfigCodec(): FixedSizeCodec<
  GlobalConfigArgs,
  GlobalConfig
> {
  return combineCodec(getGlobalConfigEncoder(), getGlobalConfigDecoder());
}

export function decodeGlobalConfig<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<GlobalConfig, TAddress>;
export function decodeGlobalConfig<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<GlobalConfig, TAddress>;
export function decodeGlobalConfig<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<GlobalConfig, TAddress> | MaybeAccount<GlobalConfig, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getGlobalConfigDecoder(),
  );
}

export async function fetchGlobalConfig<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<GlobalConfig, TAddress>> {
  const maybeAccount = await fetchMaybeGlobalConfig(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeGlobalConfig<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<GlobalConfig, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeGlobalConfig(maybeAccount);
}

export async function fetchAllGlobalConfig(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<GlobalConfig>[]> {
  const maybeAccounts = await fetchAllMaybeGlobalConfig(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeGlobalConfig(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<GlobalConfig>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeGlobalConfig(maybeAccount));
}

export function getGlobalConfigSize(): number {
  return 856;
}
