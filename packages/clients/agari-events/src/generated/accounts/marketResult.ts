/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getPrintDecoder,
  getPrintEncoder,
  type Print,
  type PrintArgs,
} from "../types";

export const MARKET_RESULT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  228, 116, 192, 234, 125, 54, 69, 142,
]);

export function getMarketResultDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    MARKET_RESULT_DISCRIMINATOR,
  );
}

export type MarketResult = {
  discriminator: ReadonlyUint8Array;
  market: Address;
  series: Address;
  /** The settler or voider who paid this account's rent, refunded at close. */
  rentPayer: Address;
  open: Print;
  close: Print;
  checkOpen: Print;
  checkClose: Print;
  resolvedTs: bigint;
  payoutYes: number;
  payoutNo: number;
  policyVersion: number;
  voidReason: number;
  /** bool: settled on the primary source alone because the check prints missed their window. */
  singleSource: number;
  winner: number;
  bump: number;
  pad0: ReadonlyUint8Array;
  reserved: ReadonlyUint8Array;
};

export type MarketResultArgs = {
  market: Address;
  series: Address;
  /** The settler or voider who paid this account's rent, refunded at close. */
  rentPayer: Address;
  open: PrintArgs;
  close: PrintArgs;
  checkOpen: PrintArgs;
  checkClose: PrintArgs;
  resolvedTs: number | bigint;
  payoutYes: number;
  payoutNo: number;
  policyVersion: number;
  voidReason: number;
  /** bool: settled on the primary source alone because the check prints missed their window. */
  singleSource: number;
  winner: number;
  bump: number;
  pad0: ReadonlyUint8Array;
  reserved: ReadonlyUint8Array;
};

/** Gets the encoder for {@link MarketResultArgs} account data. */
export function getMarketResultEncoder(): FixedSizeEncoder<MarketResultArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["market", getAddressEncoder()],
      ["series", getAddressEncoder()],
      ["rentPayer", getAddressEncoder()],
      ["open", getPrintEncoder()],
      ["close", getPrintEncoder()],
      ["checkOpen", getPrintEncoder()],
      ["checkClose", getPrintEncoder()],
      ["resolvedTs", getI64Encoder()],
      ["payoutYes", getU32Encoder()],
      ["payoutNo", getU32Encoder()],
      ["policyVersion", getU8Encoder()],
      ["voidReason", getU8Encoder()],
      ["singleSource", getU8Encoder()],
      ["winner", getU8Encoder()],
      ["bump", getU8Encoder()],
      ["pad0", fixEncoderSize(getBytesEncoder(), 3)],
      ["reserved", fixEncoderSize(getBytesEncoder(), 32)],
    ]),
    (value) => ({ ...value, discriminator: MARKET_RESULT_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link MarketResult} account data. */
export function getMarketResultDecoder(): FixedSizeDecoder<MarketResult> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["market", getAddressDecoder()],
    ["series", getAddressDecoder()],
    ["rentPayer", getAddressDecoder()],
    ["open", getPrintDecoder()],
    ["close", getPrintDecoder()],
    ["checkOpen", getPrintDecoder()],
    ["checkClose", getPrintDecoder()],
    ["resolvedTs", getI64Decoder()],
    ["payoutYes", getU32Decoder()],
    ["payoutNo", getU32Decoder()],
    ["policyVersion", getU8Decoder()],
    ["voidReason", getU8Decoder()],
    ["singleSource", getU8Decoder()],
    ["winner", getU8Decoder()],
    ["bump", getU8Decoder()],
    ["pad0", fixDecoderSize(getBytesDecoder(), 3)],
    ["reserved", fixDecoderSize(getBytesDecoder(), 32)],
  ]);
}

/** Gets the codec for {@link MarketResult} account data. */
export function getMarketResultCodec(): FixedSizeCodec<
  MarketResultArgs,
  MarketResult
> {
  return combineCodec(getMarketResultEncoder(), getMarketResultDecoder());
}

export function decodeMarketResult<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<MarketResult, TAddress>;
export function decodeMarketResult<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<MarketResult, TAddress>;
export function decodeMarketResult<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<MarketResult, TAddress> | MaybeAccount<MarketResult, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getMarketResultDecoder(),
  );
}

export async function fetchMarketResult<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<MarketResult, TAddress>> {
  const maybeAccount = await fetchMaybeMarketResult(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeMarketResult<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<MarketResult, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeMarketResult(maybeAccount);
}

export async function fetchAllMarketResult(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<MarketResult>[]> {
  const maybeAccounts = await fetchAllMaybeMarketResult(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeMarketResult(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<MarketResult>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeMarketResult(maybeAccount));
}

export function getMarketResultSize(): number {
  return 256;
}
