/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findConfigPda } from "../pdas";
import { AGARI_EVENTS_PROGRAM_ADDRESS } from "../programs";

export const PUBLIC_RECORD_PRINT_SWITCHBOARD_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([173, 209, 187, 79, 234, 0, 150, 224]);

export function getPublicRecordPrintSwitchboardDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PUBLIC_RECORD_PRINT_SWITCHBOARD_DISCRIMINATOR,
  );
}

export type PublicRecordPrintSwitchboardInstruction<
  TProgram extends string = typeof AGARI_EVENTS_PROGRAM_ADDRESS,
  TAccountRecorder extends string | AccountMeta<string> = string,
  TAccountSeries extends string | AccountMeta<string> = string,
  TAccountMarket extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountQueue extends string | AccountMeta<string> = string,
  TAccountSlothashes extends string | AccountMeta<string> =
    "SysvarS1otHashes111111111111111111111111111",
  TAccountInstructions extends string | AccountMeta<string> =
    "Sysvar1nstructions1111111111111111111111111",
  TAccountPrevMarket extends string | AccountMeta<string> = string,
  TAccountEventAuthority extends string | AccountMeta<string> = string,
  TAccountProgram extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountRecorder extends string
        ? ReadonlySignerAccount<TAccountRecorder> &
            AccountSignerMeta<TAccountRecorder>
        : TAccountRecorder,
      TAccountSeries extends string
        ? ReadonlyAccount<TAccountSeries>
        : TAccountSeries,
      TAccountMarket extends string
        ? WritableAccount<TAccountMarket>
        : TAccountMarket,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountQueue extends string
        ? ReadonlyAccount<TAccountQueue>
        : TAccountQueue,
      TAccountSlothashes extends string
        ? ReadonlyAccount<TAccountSlothashes>
        : TAccountSlothashes,
      TAccountInstructions extends string
        ? ReadonlyAccount<TAccountInstructions>
        : TAccountInstructions,
      TAccountPrevMarket extends string
        ? ReadonlyAccount<TAccountPrevMarket>
        : TAccountPrevMarket,
      TAccountEventAuthority extends string
        ? ReadonlyAccount<TAccountEventAuthority>
        : TAccountEventAuthority,
      TAccountProgram extends string
        ? ReadonlyAccount<TAccountProgram>
        : TAccountProgram,
      ...TRemainingAccounts,
    ]
  >;

export type PublicRecordPrintSwitchboardInstructionData = {
  discriminator: ReadonlyUint8Array;
  which: number;
};

export type PublicRecordPrintSwitchboardInstructionDataArgs = { which: number };

export function getPublicRecordPrintSwitchboardInstructionDataEncoder(): FixedSizeEncoder<PublicRecordPrintSwitchboardInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["which", getU8Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: PUBLIC_RECORD_PRINT_SWITCHBOARD_DISCRIMINATOR,
    }),
  );
}

export function getPublicRecordPrintSwitchboardInstructionDataDecoder(): FixedSizeDecoder<PublicRecordPrintSwitchboardInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["which", getU8Decoder()],
  ]);
}

export function getPublicRecordPrintSwitchboardInstructionDataCodec(): FixedSizeCodec<
  PublicRecordPrintSwitchboardInstructionDataArgs,
  PublicRecordPrintSwitchboardInstructionData
> {
  return combineCodec(
    getPublicRecordPrintSwitchboardInstructionDataEncoder(),
    getPublicRecordPrintSwitchboardInstructionDataDecoder(),
  );
}

export type PublicRecordPrintSwitchboardAsyncInput<
  TAccountRecorder extends string = string,
  TAccountSeries extends string = string,
  TAccountMarket extends string = string,
  TAccountConfig extends string = string,
  TAccountQueue extends string = string,
  TAccountSlothashes extends string = string,
  TAccountInstructions extends string = string,
  TAccountPrevMarket extends string = string,
  TAccountEventAuthority extends string = string,
  TAccountProgram extends string = string,
> = {
  /** Until `T + 40` this must be a `config.attestors` key; afterwards any signer records (the fee payer signs anyway). */
  recorder: TransactionSigner<TAccountRecorder>;
  series: Address<TAccountSeries>;
  market: Address<TAccountMarket>;
  config?: Address<TAccountConfig>;
  /**
   * The Switchboard queue whose ed25519 oracle signing keys must have signed the quote.
   * checked in the handler (the crate's verifier checks only its size).
   */
  queue: Address<TAccountQueue>;
  slothashes?: Address<TAccountSlothashes>;
  instructions?: Address<TAccountInstructions>;
  /**
   * Window `index − 1` of this Series, required for an Open print past index 0: an Open whose previous Close is in
   * must be copied, never printed.
   */
  prevMarket?: Address<TAccountPrevMarket>;
  eventAuthority: Address<TAccountEventAuthority>;
  program: Address<TAccountProgram>;
  which: PublicRecordPrintSwitchboardInstructionDataArgs["which"];
};

export async function getPublicRecordPrintSwitchboardInstructionAsync<
  TAccountRecorder extends string,
  TAccountSeries extends string,
  TAccountMarket extends string,
  TAccountConfig extends string,
  TAccountQueue extends string,
  TAccountSlothashes extends string,
  TAccountInstructions extends string,
  TAccountPrevMarket extends string,
  TAccountEventAuthority extends string,
  TAccountProgram extends string,
  TProgramAddress extends Address = typeof AGARI_EVENTS_PROGRAM_ADDRESS,
>(
  input: PublicRecordPrintSwitchboardAsyncInput<
    TAccountRecorder,
    TAccountSeries,
    TAccountMarket,
    TAccountConfig,
    TAccountQueue,
    TAccountSlothashes,
    TAccountInstructions,
    TAccountPrevMarket,
    TAccountEventAuthority,
    TAccountProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  PublicRecordPrintSwitchboardInstruction<
    TProgramAddress,
    TAccountRecorder,
    TAccountSeries,
    TAccountMarket,
    TAccountConfig,
    TAccountQueue,
    TAccountSlothashes,
    TAccountInstructions,
    TAccountPrevMarket,
    TAccountEventAuthority,
    TAccountProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? AGARI_EVENTS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    recorder: { value: input.recorder ?? null, isWritable: false },
    series: { value: input.series ?? null, isWritable: false },
    market: { value: input.market ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    queue: { value: input.queue ?? null, isWritable: false },
    slothashes: { value: input.slothashes ?? null, isWritable: false },
    instructions: { value: input.instructions ?? null, isWritable: false },
    prevMarket: { value: input.prevMarket ?? null, isWritable: false },
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    program: { value: input.program ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda({ programAddress });
  }
  if (!accounts.slothashes.value) {
    accounts.slothashes.value =
      "SysvarS1otHashes111111111111111111111111111" as Address<"SysvarS1otHashes111111111111111111111111111">;
  }
  if (!accounts.instructions.value) {
    accounts.instructions.value =
      "Sysvar1nstructions1111111111111111111111111" as Address<"Sysvar1nstructions1111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("recorder", accounts.recorder),
      getAccountMeta("series", accounts.series),
      getAccountMeta("market", accounts.market),
      getAccountMeta("config", accounts.config),
      getAccountMeta("queue", accounts.queue),
      getAccountMeta("slothashes", accounts.slothashes),
      getAccountMeta("instructions", accounts.instructions),
      getAccountMeta("prevMarket", accounts.prevMarket),
      getAccountMeta("eventAuthority", accounts.eventAuthority),
      getAccountMeta("program", accounts.program),
    ],
    data: getPublicRecordPrintSwitchboardInstructionDataEncoder().encode(
      args as PublicRecordPrintSwitchboardInstructionDataArgs,
    ),
    programAddress,
  } as PublicRecordPrintSwitchboardInstruction<
    TProgramAddress,
    TAccountRecorder,
    TAccountSeries,
    TAccountMarket,
    TAccountConfig,
    TAccountQueue,
    TAccountSlothashes,
    TAccountInstructions,
    TAccountPrevMarket,
    TAccountEventAuthority,
    TAccountProgram
  >);
}

export type PublicRecordPrintSwitchboardInput<
  TAccountRecorder extends string = string,
  TAccountSeries extends string = string,
  TAccountMarket extends string = string,
  TAccountConfig extends string = string,
  TAccountQueue extends string = string,
  TAccountSlothashes extends string = string,
  TAccountInstructions extends string = string,
  TAccountPrevMarket extends string = string,
  TAccountEventAuthority extends string = string,
  TAccountProgram extends string = string,
> = {
  /** Until `T + 40` this must be a `config.attestors` key; afterwards any signer records (the fee payer signs anyway). */
  recorder: TransactionSigner<TAccountRecorder>;
  series: Address<TAccountSeries>;
  market: Address<TAccountMarket>;
  config: Address<TAccountConfig>;
  /**
   * The Switchboard queue whose ed25519 oracle signing keys must have signed the quote.
   * checked in the handler (the crate's verifier checks only its size).
   */
  queue: Address<TAccountQueue>;
  slothashes?: Address<TAccountSlothashes>;
  instructions?: Address<TAccountInstructions>;
  /**
   * Window `index − 1` of this Series, required for an Open print past index 0: an Open whose previous Close is in
   * must be copied, never printed.
   */
  prevMarket?: Address<TAccountPrevMarket>;
  eventAuthority: Address<TAccountEventAuthority>;
  program: Address<TAccountProgram>;
  which: PublicRecordPrintSwitchboardInstructionDataArgs["which"];
};

export function getPublicRecordPrintSwitchboardInstruction<
  TAccountRecorder extends string,
  TAccountSeries extends string,
  TAccountMarket extends string,
  TAccountConfig extends string,
  TAccountQueue extends string,
  TAccountSlothashes extends string,
  TAccountInstructions extends string,
  TAccountPrevMarket extends string,
  TAccountEventAuthority extends string,
  TAccountProgram extends string,
  TProgramAddress extends Address = typeof AGARI_EVENTS_PROGRAM_ADDRESS,
>(
  input: PublicRecordPrintSwitchboardInput<
    TAccountRecorder,
    TAccountSeries,
    TAccountMarket,
    TAccountConfig,
    TAccountQueue,
    TAccountSlothashes,
    TAccountInstructions,
    TAccountPrevMarket,
    TAccountEventAuthority,
    TAccountProgram
  >,
  config?: { programAddress?: TProgramAddress },
): PublicRecordPrintSwitchboardInstruction<
  TProgramAddress,
  TAccountRecorder,
  TAccountSeries,
  TAccountMarket,
  TAccountConfig,
  TAccountQueue,
  TAccountSlothashes,
  TAccountInstructions,
  TAccountPrevMarket,
  TAccountEventAuthority,
  TAccountProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? AGARI_EVENTS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    recorder: { value: input.recorder ?? null, isWritable: false },
    series: { value: input.series ?? null, isWritable: false },
    market: { value: input.market ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    queue: { value: input.queue ?? null, isWritable: false },
    slothashes: { value: input.slothashes ?? null, isWritable: false },
    instructions: { value: input.instructions ?? null, isWritable: false },
    prevMarket: { value: input.prevMarket ?? null, isWritable: false },
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    program: { value: input.program ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.slothashes.value) {
    accounts.slothashes.value =
      "SysvarS1otHashes111111111111111111111111111" as Address<"SysvarS1otHashes111111111111111111111111111">;
  }
  if (!accounts.instructions.value) {
    accounts.instructions.value =
      "Sysvar1nstructions1111111111111111111111111" as Address<"Sysvar1nstructions1111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("recorder", accounts.recorder),
      getAccountMeta("series", accounts.series),
      getAccountMeta("market", accounts.market),
      getAccountMeta("config", accounts.config),
      getAccountMeta("queue", accounts.queue),
      getAccountMeta("slothashes", accounts.slothashes),
      getAccountMeta("instructions", accounts.instructions),
      getAccountMeta("prevMarket", accounts.prevMarket),
      getAccountMeta("eventAuthority", accounts.eventAuthority),
      getAccountMeta("program", accounts.program),
    ],
    data: getPublicRecordPrintSwitchboardInstructionDataEncoder().encode(
      args as PublicRecordPrintSwitchboardInstructionDataArgs,
    ),
    programAddress,
  } as PublicRecordPrintSwitchboardInstruction<
    TProgramAddress,
    TAccountRecorder,
    TAccountSeries,
    TAccountMarket,
    TAccountConfig,
    TAccountQueue,
    TAccountSlothashes,
    TAccountInstructions,
    TAccountPrevMarket,
    TAccountEventAuthority,
    TAccountProgram
  >);
}

export type ParsedPublicRecordPrintSwitchboardInstruction<
  TProgram extends string = typeof AGARI_EVENTS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Until `T + 40` this must be a `config.attestors` key; afterwards any signer records (the fee payer signs anyway). */
    recorder: TAccountMetas[0];
    series: TAccountMetas[1];
    market: TAccountMetas[2];
    config: TAccountMetas[3];
    /**
     * The Switchboard queue whose ed25519 oracle signing keys must have signed the quote.
     * checked in the handler (the crate's verifier checks only its size).
     */
    queue: TAccountMetas[4];
    slothashes: TAccountMetas[5];
    instructions: TAccountMetas[6];
    /**
     * Window `index − 1` of this Series, required for an Open print past index 0: an Open whose previous Close is in
     * must be copied, never printed.
     */
    prevMarket?: TAccountMetas[7] | undefined;
    eventAuthority: TAccountMetas[8];
    program: TAccountMetas[9];
  };
  data: PublicRecordPrintSwitchboardInstructionData;
};

export function parsePublicRecordPrintSwitchboardInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedPublicRecordPrintSwitchboardInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 10) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 10,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount();
    return accountMeta.address === AGARI_EVENTS_PROGRAM_ADDRESS
      ? undefined
      : accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      recorder: getNextAccount(),
      series: getNextAccount(),
      market: getNextAccount(),
      config: getNextAccount(),
      queue: getNextAccount(),
      slothashes: getNextAccount(),
      instructions: getNextAccount(),
      prevMarket: getNextOptionalAccount(),
      eventAuthority: getNextAccount(),
      program: getNextAccount(),
    },
    data: getPublicRecordPrintSwitchboardInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
