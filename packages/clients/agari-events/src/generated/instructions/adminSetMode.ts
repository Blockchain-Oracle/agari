/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findConfigPda } from "../pdas";
import { AGARI_EVENTS_PROGRAM_ADDRESS } from "../programs";

export const ADMIN_SET_MODE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  96, 81, 2, 27, 171, 84, 196, 90,
]);

export function getAdminSetModeDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ADMIN_SET_MODE_DISCRIMINATOR,
  );
}

export type AdminSetModeInstruction<
  TProgram extends string = typeof AGARI_EVENTS_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? ReadonlySignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountConfig extends string
        ? WritableAccount<TAccountConfig>
        : TAccountConfig,
      ...TRemainingAccounts,
    ]
  >;

export type AdminSetModeInstructionData = {
  discriminator: ReadonlyUint8Array;
  mode: number;
};

export type AdminSetModeInstructionDataArgs = { mode: number };

export function getAdminSetModeInstructionDataEncoder(): FixedSizeEncoder<AdminSetModeInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["mode", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: ADMIN_SET_MODE_DISCRIMINATOR }),
  );
}

export function getAdminSetModeInstructionDataDecoder(): FixedSizeDecoder<AdminSetModeInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["mode", getU8Decoder()],
  ]);
}

export function getAdminSetModeInstructionDataCodec(): FixedSizeCodec<
  AdminSetModeInstructionDataArgs,
  AdminSetModeInstructionData
> {
  return combineCodec(
    getAdminSetModeInstructionDataEncoder(),
    getAdminSetModeInstructionDataDecoder(),
  );
}

export type AdminSetModeAsyncInput<
  TAccountAdmin extends string = string,
  TAccountConfig extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  config?: Address<TAccountConfig>;
  mode: AdminSetModeInstructionDataArgs["mode"];
};

export async function getAdminSetModeInstructionAsync<
  TAccountAdmin extends string,
  TAccountConfig extends string,
  TProgramAddress extends Address = typeof AGARI_EVENTS_PROGRAM_ADDRESS,
>(
  input: AdminSetModeAsyncInput<TAccountAdmin, TAccountConfig>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  AdminSetModeInstruction<TProgramAddress, TAccountAdmin, TAccountConfig>
> {
  // Program address.
  const programAddress = config?.programAddress ?? AGARI_EVENTS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda({ programAddress });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("config", accounts.config),
    ],
    data: getAdminSetModeInstructionDataEncoder().encode(
      args as AdminSetModeInstructionDataArgs,
    ),
    programAddress,
  } as AdminSetModeInstruction<TProgramAddress, TAccountAdmin, TAccountConfig>);
}

export type AdminSetModeInput<
  TAccountAdmin extends string = string,
  TAccountConfig extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  config: Address<TAccountConfig>;
  mode: AdminSetModeInstructionDataArgs["mode"];
};

export function getAdminSetModeInstruction<
  TAccountAdmin extends string,
  TAccountConfig extends string,
  TProgramAddress extends Address = typeof AGARI_EVENTS_PROGRAM_ADDRESS,
>(
  input: AdminSetModeInput<TAccountAdmin, TAccountConfig>,
  config?: { programAddress?: TProgramAddress },
): AdminSetModeInstruction<TProgramAddress, TAccountAdmin, TAccountConfig> {
  // Program address.
  const programAddress = config?.programAddress ?? AGARI_EVENTS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("config", accounts.config),
    ],
    data: getAdminSetModeInstructionDataEncoder().encode(
      args as AdminSetModeInstructionDataArgs,
    ),
    programAddress,
  } as AdminSetModeInstruction<TProgramAddress, TAccountAdmin, TAccountConfig>);
}

export type ParsedAdminSetModeInstruction<
  TProgram extends string = typeof AGARI_EVENTS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    config: TAccountMetas[1];
  };
  data: AdminSetModeInstructionData;
};

export function parseAdminSetModeInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAdminSetModeInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { admin: getNextAccount(), config: getNextAccount() },
    data: getAdminSetModeInstructionDataDecoder().decode(instruction.data),
  };
}
