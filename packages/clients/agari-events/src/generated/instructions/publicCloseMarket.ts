/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findConfigPda, findResultPda } from "../pdas";
import { AGARI_EVENTS_PROGRAM_ADDRESS } from "../programs";

export const PUBLIC_CLOSE_MARKET_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([74, 69, 255, 93, 35, 22, 214, 91]);

export function getPublicCloseMarketDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PUBLIC_CLOSE_MARKET_DISCRIMINATOR,
  );
}

export type PublicCloseMarketInstruction<
  TProgram extends string = typeof AGARI_EVENTS_PROGRAM_ADDRESS,
  TAccountMarket extends string | AccountMeta<string> = string,
  TAccountResult extends string | AccountMeta<string> = string,
  TAccountMarketRentPayer extends string | AccountMeta<string> = string,
  TAccountResultRentPayer extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountEventAuthority extends string | AccountMeta<string> = string,
  TAccountProgram extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountMarket extends string
        ? WritableAccount<TAccountMarket>
        : TAccountMarket,
      TAccountResult extends string
        ? WritableAccount<TAccountResult>
        : TAccountResult,
      TAccountMarketRentPayer extends string
        ? WritableAccount<TAccountMarketRentPayer>
        : TAccountMarketRentPayer,
      TAccountResultRentPayer extends string
        ? WritableAccount<TAccountResultRentPayer>
        : TAccountResultRentPayer,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountEventAuthority extends string
        ? ReadonlyAccount<TAccountEventAuthority>
        : TAccountEventAuthority,
      TAccountProgram extends string
        ? ReadonlyAccount<TAccountProgram>
        : TAccountProgram,
      ...TRemainingAccounts,
    ]
  >;

export type PublicCloseMarketInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type PublicCloseMarketInstructionDataArgs = {};

export function getPublicCloseMarketInstructionDataEncoder(): FixedSizeEncoder<PublicCloseMarketInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: PUBLIC_CLOSE_MARKET_DISCRIMINATOR }),
  );
}

export function getPublicCloseMarketInstructionDataDecoder(): FixedSizeDecoder<PublicCloseMarketInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getPublicCloseMarketInstructionDataCodec(): FixedSizeCodec<
  PublicCloseMarketInstructionDataArgs,
  PublicCloseMarketInstructionData
> {
  return combineCodec(
    getPublicCloseMarketInstructionDataEncoder(),
    getPublicCloseMarketInstructionDataDecoder(),
  );
}

export type PublicCloseMarketAsyncInput<
  TAccountMarket extends string = string,
  TAccountResult extends string = string,
  TAccountMarketRentPayer extends string = string,
  TAccountResultRentPayer extends string = string,
  TAccountConfig extends string = string,
  TAccountEventAuthority extends string = string,
  TAccountProgram extends string = string,
> = {
  market: Address<TAccountMarket>;
  result?: Address<TAccountResult>;
  marketRentPayer: Address<TAccountMarketRentPayer>;
  resultRentPayer: Address<TAccountResultRentPayer>;
  config?: Address<TAccountConfig>;
  eventAuthority: Address<TAccountEventAuthority>;
  program: Address<TAccountProgram>;
};

export async function getPublicCloseMarketInstructionAsync<
  TAccountMarket extends string,
  TAccountResult extends string,
  TAccountMarketRentPayer extends string,
  TAccountResultRentPayer extends string,
  TAccountConfig extends string,
  TAccountEventAuthority extends string,
  TAccountProgram extends string,
  TProgramAddress extends Address = typeof AGARI_EVENTS_PROGRAM_ADDRESS,
>(
  input: PublicCloseMarketAsyncInput<
    TAccountMarket,
    TAccountResult,
    TAccountMarketRentPayer,
    TAccountResultRentPayer,
    TAccountConfig,
    TAccountEventAuthority,
    TAccountProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  PublicCloseMarketInstruction<
    TProgramAddress,
    TAccountMarket,
    TAccountResult,
    TAccountMarketRentPayer,
    TAccountResultRentPayer,
    TAccountConfig,
    TAccountEventAuthority,
    TAccountProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? AGARI_EVENTS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    market: { value: input.market ?? null, isWritable: true },
    result: { value: input.result ?? null, isWritable: true },
    marketRentPayer: { value: input.marketRentPayer ?? null, isWritable: true },
    resultRentPayer: { value: input.resultRentPayer ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    program: { value: input.program ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.result.value) {
    accounts.result.value = await findResultPda(
      {
        market: getAddressFromResolvedInstructionAccount(
          "market",
          accounts.market.value,
        ),
      },
      { programAddress },
    );
  }
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda({ programAddress });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("market", accounts.market),
      getAccountMeta("result", accounts.result),
      getAccountMeta("marketRentPayer", accounts.marketRentPayer),
      getAccountMeta("resultRentPayer", accounts.resultRentPayer),
      getAccountMeta("config", accounts.config),
      getAccountMeta("eventAuthority", accounts.eventAuthority),
      getAccountMeta("program", accounts.program),
    ],
    data: getPublicCloseMarketInstructionDataEncoder().encode({}),
    programAddress,
  } as PublicCloseMarketInstruction<
    TProgramAddress,
    TAccountMarket,
    TAccountResult,
    TAccountMarketRentPayer,
    TAccountResultRentPayer,
    TAccountConfig,
    TAccountEventAuthority,
    TAccountProgram
  >);
}

export type PublicCloseMarketInput<
  TAccountMarket extends string = string,
  TAccountResult extends string = string,
  TAccountMarketRentPayer extends string = string,
  TAccountResultRentPayer extends string = string,
  TAccountConfig extends string = string,
  TAccountEventAuthority extends string = string,
  TAccountProgram extends string = string,
> = {
  market: Address<TAccountMarket>;
  result: Address<TAccountResult>;
  marketRentPayer: Address<TAccountMarketRentPayer>;
  resultRentPayer: Address<TAccountResultRentPayer>;
  config: Address<TAccountConfig>;
  eventAuthority: Address<TAccountEventAuthority>;
  program: Address<TAccountProgram>;
};

export function getPublicCloseMarketInstruction<
  TAccountMarket extends string,
  TAccountResult extends string,
  TAccountMarketRentPayer extends string,
  TAccountResultRentPayer extends string,
  TAccountConfig extends string,
  TAccountEventAuthority extends string,
  TAccountProgram extends string,
  TProgramAddress extends Address = typeof AGARI_EVENTS_PROGRAM_ADDRESS,
>(
  input: PublicCloseMarketInput<
    TAccountMarket,
    TAccountResult,
    TAccountMarketRentPayer,
    TAccountResultRentPayer,
    TAccountConfig,
    TAccountEventAuthority,
    TAccountProgram
  >,
  config?: { programAddress?: TProgramAddress },
): PublicCloseMarketInstruction<
  TProgramAddress,
  TAccountMarket,
  TAccountResult,
  TAccountMarketRentPayer,
  TAccountResultRentPayer,
  TAccountConfig,
  TAccountEventAuthority,
  TAccountProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? AGARI_EVENTS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    market: { value: input.market ?? null, isWritable: true },
    result: { value: input.result ?? null, isWritable: true },
    marketRentPayer: { value: input.marketRentPayer ?? null, isWritable: true },
    resultRentPayer: { value: input.resultRentPayer ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    program: { value: input.program ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("market", accounts.market),
      getAccountMeta("result", accounts.result),
      getAccountMeta("marketRentPayer", accounts.marketRentPayer),
      getAccountMeta("resultRentPayer", accounts.resultRentPayer),
      getAccountMeta("config", accounts.config),
      getAccountMeta("eventAuthority", accounts.eventAuthority),
      getAccountMeta("program", accounts.program),
    ],
    data: getPublicCloseMarketInstructionDataEncoder().encode({}),
    programAddress,
  } as PublicCloseMarketInstruction<
    TProgramAddress,
    TAccountMarket,
    TAccountResult,
    TAccountMarketRentPayer,
    TAccountResultRentPayer,
    TAccountConfig,
    TAccountEventAuthority,
    TAccountProgram
  >);
}

export type ParsedPublicCloseMarketInstruction<
  TProgram extends string = typeof AGARI_EVENTS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    market: TAccountMetas[0];
    result: TAccountMetas[1];
    marketRentPayer: TAccountMetas[2];
    resultRentPayer: TAccountMetas[3];
    config: TAccountMetas[4];
    eventAuthority: TAccountMetas[5];
    program: TAccountMetas[6];
  };
  data: PublicCloseMarketInstructionData;
};

export function parsePublicCloseMarketInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedPublicCloseMarketInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 7) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 7,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      market: getNextAccount(),
      result: getNextAccount(),
      marketRentPayer: getNextAccount(),
      resultRentPayer: getNextAccount(),
      config: getNextAccount(),
      eventAuthority: getNextAccount(),
      program: getNextAccount(),
    },
    data: getPublicCloseMarketInstructionDataDecoder().decode(instruction.data),
  };
}
