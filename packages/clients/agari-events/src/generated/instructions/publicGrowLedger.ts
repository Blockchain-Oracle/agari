/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findConfigPda } from "../pdas";
import { AGARI_EVENTS_PROGRAM_ADDRESS } from "../programs";

export const PUBLIC_GROW_LEDGER_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([69, 159, 222, 168, 61, 132, 65, 7]);

export function getPublicGrowLedgerDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PUBLIC_GROW_LEDGER_DISCRIMINATOR,
  );
}

export type PublicGrowLedgerInstruction<
  TProgram extends string = typeof AGARI_EVENTS_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountMarket extends string | AccountMeta<string> = string,
  TAccountLedger extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TAccountEventAuthority extends string | AccountMeta<string> = string,
  TAccountProgram extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountMarket extends string
        ? WritableAccount<TAccountMarket>
        : TAccountMarket,
      TAccountLedger extends string
        ? WritableAccount<TAccountLedger>
        : TAccountLedger,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      TAccountEventAuthority extends string
        ? ReadonlyAccount<TAccountEventAuthority>
        : TAccountEventAuthority,
      TAccountProgram extends string
        ? ReadonlyAccount<TAccountProgram>
        : TAccountProgram,
      ...TRemainingAccounts,
    ]
  >;

export type PublicGrowLedgerInstructionData = {
  discriminator: ReadonlyUint8Array;
  extraSeats: number;
};

export type PublicGrowLedgerInstructionDataArgs = { extraSeats: number };

export function getPublicGrowLedgerInstructionDataEncoder(): FixedSizeEncoder<PublicGrowLedgerInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["extraSeats", getU16Encoder()],
    ]),
    (value) => ({ ...value, discriminator: PUBLIC_GROW_LEDGER_DISCRIMINATOR }),
  );
}

export function getPublicGrowLedgerInstructionDataDecoder(): FixedSizeDecoder<PublicGrowLedgerInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["extraSeats", getU16Decoder()],
  ]);
}

export function getPublicGrowLedgerInstructionDataCodec(): FixedSizeCodec<
  PublicGrowLedgerInstructionDataArgs,
  PublicGrowLedgerInstructionData
> {
  return combineCodec(
    getPublicGrowLedgerInstructionDataEncoder(),
    getPublicGrowLedgerInstructionDataDecoder(),
  );
}

export type PublicGrowLedgerAsyncInput<
  TAccountPayer extends string = string,
  TAccountConfig extends string = string,
  TAccountMarket extends string = string,
  TAccountLedger extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountEventAuthority extends string = string,
  TAccountProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  config?: Address<TAccountConfig>;
  market: Address<TAccountMarket>;
  ledger: Address<TAccountLedger>;
  systemProgram?: Address<TAccountSystemProgram>;
  eventAuthority: Address<TAccountEventAuthority>;
  program: Address<TAccountProgram>;
  extraSeats: PublicGrowLedgerInstructionDataArgs["extraSeats"];
};

export async function getPublicGrowLedgerInstructionAsync<
  TAccountPayer extends string,
  TAccountConfig extends string,
  TAccountMarket extends string,
  TAccountLedger extends string,
  TAccountSystemProgram extends string,
  TAccountEventAuthority extends string,
  TAccountProgram extends string,
  TProgramAddress extends Address = typeof AGARI_EVENTS_PROGRAM_ADDRESS,
>(
  input: PublicGrowLedgerAsyncInput<
    TAccountPayer,
    TAccountConfig,
    TAccountMarket,
    TAccountLedger,
    TAccountSystemProgram,
    TAccountEventAuthority,
    TAccountProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  PublicGrowLedgerInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountConfig,
    TAccountMarket,
    TAccountLedger,
    TAccountSystemProgram,
    TAccountEventAuthority,
    TAccountProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? AGARI_EVENTS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    market: { value: input.market ?? null, isWritable: true },
    ledger: { value: input.ledger ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    program: { value: input.program ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda({ programAddress });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("config", accounts.config),
      getAccountMeta("market", accounts.market),
      getAccountMeta("ledger", accounts.ledger),
      getAccountMeta("systemProgram", accounts.systemProgram),
      getAccountMeta("eventAuthority", accounts.eventAuthority),
      getAccountMeta("program", accounts.program),
    ],
    data: getPublicGrowLedgerInstructionDataEncoder().encode(
      args as PublicGrowLedgerInstructionDataArgs,
    ),
    programAddress,
  } as PublicGrowLedgerInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountConfig,
    TAccountMarket,
    TAccountLedger,
    TAccountSystemProgram,
    TAccountEventAuthority,
    TAccountProgram
  >);
}

export type PublicGrowLedgerInput<
  TAccountPayer extends string = string,
  TAccountConfig extends string = string,
  TAccountMarket extends string = string,
  TAccountLedger extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountEventAuthority extends string = string,
  TAccountProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  config: Address<TAccountConfig>;
  market: Address<TAccountMarket>;
  ledger: Address<TAccountLedger>;
  systemProgram?: Address<TAccountSystemProgram>;
  eventAuthority: Address<TAccountEventAuthority>;
  program: Address<TAccountProgram>;
  extraSeats: PublicGrowLedgerInstructionDataArgs["extraSeats"];
};

export function getPublicGrowLedgerInstruction<
  TAccountPayer extends string,
  TAccountConfig extends string,
  TAccountMarket extends string,
  TAccountLedger extends string,
  TAccountSystemProgram extends string,
  TAccountEventAuthority extends string,
  TAccountProgram extends string,
  TProgramAddress extends Address = typeof AGARI_EVENTS_PROGRAM_ADDRESS,
>(
  input: PublicGrowLedgerInput<
    TAccountPayer,
    TAccountConfig,
    TAccountMarket,
    TAccountLedger,
    TAccountSystemProgram,
    TAccountEventAuthority,
    TAccountProgram
  >,
  config?: { programAddress?: TProgramAddress },
): PublicGrowLedgerInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountConfig,
  TAccountMarket,
  TAccountLedger,
  TAccountSystemProgram,
  TAccountEventAuthority,
  TAccountProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? AGARI_EVENTS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    market: { value: input.market ?? null, isWritable: true },
    ledger: { value: input.ledger ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    program: { value: input.program ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("config", accounts.config),
      getAccountMeta("market", accounts.market),
      getAccountMeta("ledger", accounts.ledger),
      getAccountMeta("systemProgram", accounts.systemProgram),
      getAccountMeta("eventAuthority", accounts.eventAuthority),
      getAccountMeta("program", accounts.program),
    ],
    data: getPublicGrowLedgerInstructionDataEncoder().encode(
      args as PublicGrowLedgerInstructionDataArgs,
    ),
    programAddress,
  } as PublicGrowLedgerInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountConfig,
    TAccountMarket,
    TAccountLedger,
    TAccountSystemProgram,
    TAccountEventAuthority,
    TAccountProgram
  >);
}

export type ParsedPublicGrowLedgerInstruction<
  TProgram extends string = typeof AGARI_EVENTS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    payer: TAccountMetas[0];
    config: TAccountMetas[1];
    market: TAccountMetas[2];
    ledger: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
    eventAuthority: TAccountMetas[5];
    program: TAccountMetas[6];
  };
  data: PublicGrowLedgerInstructionData;
};

export function parsePublicGrowLedgerInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedPublicGrowLedgerInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 7) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 7,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      config: getNextAccount(),
      market: getNextAccount(),
      ledger: getNextAccount(),
      systemProgram: getNextAccount(),
      eventAuthority: getNextAccount(),
      program: getNextAccount(),
    },
    data: getPublicGrowLedgerInstructionDataDecoder().decode(instruction.data),
  };
}
