/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findConfigPda } from "../pdas";
import { AGARI_EVENTS_PROGRAM_ADDRESS } from "../programs";

export const ADMIN_ADD_BOOK_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  157, 176, 72, 15, 62, 96, 92, 110,
]);

export function getAdminAddBookDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ADMIN_ADD_BOOK_DISCRIMINATOR,
  );
}

export type AdminAddBookInstruction<
  TProgram extends string = typeof AGARI_EVENTS_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountSeries extends string | AccountMeta<string> = string,
  TAccountBook extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? ReadonlySignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountSeries extends string
        ? WritableAccount<TAccountSeries>
        : TAccountSeries,
      TAccountBook extends string
        ? WritableAccount<TAccountBook>
        : TAccountBook,
      ...TRemainingAccounts,
    ]
  >;

export type AdminAddBookInstructionData = {
  discriminator: ReadonlyUint8Array;
  capacity: number;
};

export type AdminAddBookInstructionDataArgs = { capacity: number };

export function getAdminAddBookInstructionDataEncoder(): FixedSizeEncoder<AdminAddBookInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["capacity", getU16Encoder()],
    ]),
    (value) => ({ ...value, discriminator: ADMIN_ADD_BOOK_DISCRIMINATOR }),
  );
}

export function getAdminAddBookInstructionDataDecoder(): FixedSizeDecoder<AdminAddBookInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["capacity", getU16Decoder()],
  ]);
}

export function getAdminAddBookInstructionDataCodec(): FixedSizeCodec<
  AdminAddBookInstructionDataArgs,
  AdminAddBookInstructionData
> {
  return combineCodec(
    getAdminAddBookInstructionDataEncoder(),
    getAdminAddBookInstructionDataDecoder(),
  );
}

export type AdminAddBookAsyncInput<
  TAccountAdmin extends string = string,
  TAccountConfig extends string = string,
  TAccountSeries extends string = string,
  TAccountBook extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  config?: Address<TAccountConfig>;
  series: Address<TAccountSeries>;
  /** Created earlier in the same transaction by System `createAccount` (owner = this program). */
  book: Address<TAccountBook>;
  capacity: AdminAddBookInstructionDataArgs["capacity"];
};

export async function getAdminAddBookInstructionAsync<
  TAccountAdmin extends string,
  TAccountConfig extends string,
  TAccountSeries extends string,
  TAccountBook extends string,
  TProgramAddress extends Address = typeof AGARI_EVENTS_PROGRAM_ADDRESS,
>(
  input: AdminAddBookAsyncInput<
    TAccountAdmin,
    TAccountConfig,
    TAccountSeries,
    TAccountBook
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  AdminAddBookInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig,
    TAccountSeries,
    TAccountBook
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? AGARI_EVENTS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: false },
    series: { value: input.series ?? null, isWritable: true },
    book: { value: input.book ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda({ programAddress });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("config", accounts.config),
      getAccountMeta("series", accounts.series),
      getAccountMeta("book", accounts.book),
    ],
    data: getAdminAddBookInstructionDataEncoder().encode(
      args as AdminAddBookInstructionDataArgs,
    ),
    programAddress,
  } as AdminAddBookInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig,
    TAccountSeries,
    TAccountBook
  >);
}

export type AdminAddBookInput<
  TAccountAdmin extends string = string,
  TAccountConfig extends string = string,
  TAccountSeries extends string = string,
  TAccountBook extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  config: Address<TAccountConfig>;
  series: Address<TAccountSeries>;
  /** Created earlier in the same transaction by System `createAccount` (owner = this program). */
  book: Address<TAccountBook>;
  capacity: AdminAddBookInstructionDataArgs["capacity"];
};

export function getAdminAddBookInstruction<
  TAccountAdmin extends string,
  TAccountConfig extends string,
  TAccountSeries extends string,
  TAccountBook extends string,
  TProgramAddress extends Address = typeof AGARI_EVENTS_PROGRAM_ADDRESS,
>(
  input: AdminAddBookInput<
    TAccountAdmin,
    TAccountConfig,
    TAccountSeries,
    TAccountBook
  >,
  config?: { programAddress?: TProgramAddress },
): AdminAddBookInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountConfig,
  TAccountSeries,
  TAccountBook
> {
  // Program address.
  const programAddress = config?.programAddress ?? AGARI_EVENTS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: false },
    series: { value: input.series ?? null, isWritable: true },
    book: { value: input.book ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("config", accounts.config),
      getAccountMeta("series", accounts.series),
      getAccountMeta("book", accounts.book),
    ],
    data: getAdminAddBookInstructionDataEncoder().encode(
      args as AdminAddBookInstructionDataArgs,
    ),
    programAddress,
  } as AdminAddBookInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig,
    TAccountSeries,
    TAccountBook
  >);
}

export type ParsedAdminAddBookInstruction<
  TProgram extends string = typeof AGARI_EVENTS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    config: TAccountMetas[1];
    series: TAccountMetas[2];
    /** Created earlier in the same transaction by System `createAccount` (owner = this program). */
    book: TAccountMetas[3];
  };
  data: AdminAddBookInstructionData;
};

export function parseAdminAddBookInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAdminAddBookInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      admin: getNextAccount(),
      config: getNextAccount(),
      series: getNextAccount(),
      book: getNextAccount(),
    },
    data: getAdminAddBookInstructionDataDecoder().decode(instruction.data),
  };
}
