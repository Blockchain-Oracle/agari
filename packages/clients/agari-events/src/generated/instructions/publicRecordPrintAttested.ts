/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getI32Decoder,
  getI32Encoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findConfigPda } from "../pdas";
import { AGARI_EVENTS_PROGRAM_ADDRESS } from "../programs";

export const PUBLIC_RECORD_PRINT_ATTESTED_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([110, 110, 63, 161, 118, 80, 129, 116]);

export function getPublicRecordPrintAttestedDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PUBLIC_RECORD_PRINT_ATTESTED_DISCRIMINATOR,
  );
}

export type PublicRecordPrintAttestedInstruction<
  TProgram extends string = typeof AGARI_EVENTS_PROGRAM_ADDRESS,
  TAccountSeries extends string | AccountMeta<string> = string,
  TAccountMarket extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountInstructions extends string | AccountMeta<string> =
    "Sysvar1nstructions1111111111111111111111111",
  TAccountEventAuthority extends string | AccountMeta<string> = string,
  TAccountProgram extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSeries extends string
        ? ReadonlyAccount<TAccountSeries>
        : TAccountSeries,
      TAccountMarket extends string
        ? WritableAccount<TAccountMarket>
        : TAccountMarket,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountInstructions extends string
        ? ReadonlyAccount<TAccountInstructions>
        : TAccountInstructions,
      TAccountEventAuthority extends string
        ? ReadonlyAccount<TAccountEventAuthority>
        : TAccountEventAuthority,
      TAccountProgram extends string
        ? ReadonlyAccount<TAccountProgram>
        : TAccountProgram,
      ...TRemainingAccounts,
    ]
  >;

export type PublicRecordPrintAttestedInstructionData = {
  discriminator: ReadonlyUint8Array;
  which: number;
  price: bigint;
  expo: number;
  barStartTs: bigint;
  fetchedAtTs: bigint;
};

export type PublicRecordPrintAttestedInstructionDataArgs = {
  which: number;
  price: number | bigint;
  expo: number;
  barStartTs: number | bigint;
  fetchedAtTs: number | bigint;
};

export function getPublicRecordPrintAttestedInstructionDataEncoder(): FixedSizeEncoder<PublicRecordPrintAttestedInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["which", getU8Encoder()],
      ["price", getI64Encoder()],
      ["expo", getI32Encoder()],
      ["barStartTs", getI64Encoder()],
      ["fetchedAtTs", getI64Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: PUBLIC_RECORD_PRINT_ATTESTED_DISCRIMINATOR,
    }),
  );
}

export function getPublicRecordPrintAttestedInstructionDataDecoder(): FixedSizeDecoder<PublicRecordPrintAttestedInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["which", getU8Decoder()],
    ["price", getI64Decoder()],
    ["expo", getI32Decoder()],
    ["barStartTs", getI64Decoder()],
    ["fetchedAtTs", getI64Decoder()],
  ]);
}

export function getPublicRecordPrintAttestedInstructionDataCodec(): FixedSizeCodec<
  PublicRecordPrintAttestedInstructionDataArgs,
  PublicRecordPrintAttestedInstructionData
> {
  return combineCodec(
    getPublicRecordPrintAttestedInstructionDataEncoder(),
    getPublicRecordPrintAttestedInstructionDataDecoder(),
  );
}

export type PublicRecordPrintAttestedAsyncInput<
  TAccountSeries extends string = string,
  TAccountMarket extends string = string,
  TAccountConfig extends string = string,
  TAccountInstructions extends string = string,
  TAccountEventAuthority extends string = string,
  TAccountProgram extends string = string,
> = {
  series: Address<TAccountSeries>;
  market: Address<TAccountMarket>;
  config?: Address<TAccountConfig>;
  instructions?: Address<TAccountInstructions>;
  eventAuthority: Address<TAccountEventAuthority>;
  program: Address<TAccountProgram>;
  which: PublicRecordPrintAttestedInstructionDataArgs["which"];
  price: PublicRecordPrintAttestedInstructionDataArgs["price"];
  expo: PublicRecordPrintAttestedInstructionDataArgs["expo"];
  barStartTs: PublicRecordPrintAttestedInstructionDataArgs["barStartTs"];
  fetchedAtTs: PublicRecordPrintAttestedInstructionDataArgs["fetchedAtTs"];
};

export async function getPublicRecordPrintAttestedInstructionAsync<
  TAccountSeries extends string,
  TAccountMarket extends string,
  TAccountConfig extends string,
  TAccountInstructions extends string,
  TAccountEventAuthority extends string,
  TAccountProgram extends string,
  TProgramAddress extends Address = typeof AGARI_EVENTS_PROGRAM_ADDRESS,
>(
  input: PublicRecordPrintAttestedAsyncInput<
    TAccountSeries,
    TAccountMarket,
    TAccountConfig,
    TAccountInstructions,
    TAccountEventAuthority,
    TAccountProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  PublicRecordPrintAttestedInstruction<
    TProgramAddress,
    TAccountSeries,
    TAccountMarket,
    TAccountConfig,
    TAccountInstructions,
    TAccountEventAuthority,
    TAccountProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? AGARI_EVENTS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    series: { value: input.series ?? null, isWritable: false },
    market: { value: input.market ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    instructions: { value: input.instructions ?? null, isWritable: false },
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    program: { value: input.program ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda({ programAddress });
  }
  if (!accounts.instructions.value) {
    accounts.instructions.value =
      "Sysvar1nstructions1111111111111111111111111" as Address<"Sysvar1nstructions1111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("series", accounts.series),
      getAccountMeta("market", accounts.market),
      getAccountMeta("config", accounts.config),
      getAccountMeta("instructions", accounts.instructions),
      getAccountMeta("eventAuthority", accounts.eventAuthority),
      getAccountMeta("program", accounts.program),
    ],
    data: getPublicRecordPrintAttestedInstructionDataEncoder().encode(
      args as PublicRecordPrintAttestedInstructionDataArgs,
    ),
    programAddress,
  } as PublicRecordPrintAttestedInstruction<
    TProgramAddress,
    TAccountSeries,
    TAccountMarket,
    TAccountConfig,
    TAccountInstructions,
    TAccountEventAuthority,
    TAccountProgram
  >);
}

export type PublicRecordPrintAttestedInput<
  TAccountSeries extends string = string,
  TAccountMarket extends string = string,
  TAccountConfig extends string = string,
  TAccountInstructions extends string = string,
  TAccountEventAuthority extends string = string,
  TAccountProgram extends string = string,
> = {
  series: Address<TAccountSeries>;
  market: Address<TAccountMarket>;
  config: Address<TAccountConfig>;
  instructions?: Address<TAccountInstructions>;
  eventAuthority: Address<TAccountEventAuthority>;
  program: Address<TAccountProgram>;
  which: PublicRecordPrintAttestedInstructionDataArgs["which"];
  price: PublicRecordPrintAttestedInstructionDataArgs["price"];
  expo: PublicRecordPrintAttestedInstructionDataArgs["expo"];
  barStartTs: PublicRecordPrintAttestedInstructionDataArgs["barStartTs"];
  fetchedAtTs: PublicRecordPrintAttestedInstructionDataArgs["fetchedAtTs"];
};

export function getPublicRecordPrintAttestedInstruction<
  TAccountSeries extends string,
  TAccountMarket extends string,
  TAccountConfig extends string,
  TAccountInstructions extends string,
  TAccountEventAuthority extends string,
  TAccountProgram extends string,
  TProgramAddress extends Address = typeof AGARI_EVENTS_PROGRAM_ADDRESS,
>(
  input: PublicRecordPrintAttestedInput<
    TAccountSeries,
    TAccountMarket,
    TAccountConfig,
    TAccountInstructions,
    TAccountEventAuthority,
    TAccountProgram
  >,
  config?: { programAddress?: TProgramAddress },
): PublicRecordPrintAttestedInstruction<
  TProgramAddress,
  TAccountSeries,
  TAccountMarket,
  TAccountConfig,
  TAccountInstructions,
  TAccountEventAuthority,
  TAccountProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? AGARI_EVENTS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    series: { value: input.series ?? null, isWritable: false },
    market: { value: input.market ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    instructions: { value: input.instructions ?? null, isWritable: false },
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    program: { value: input.program ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.instructions.value) {
    accounts.instructions.value =
      "Sysvar1nstructions1111111111111111111111111" as Address<"Sysvar1nstructions1111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("series", accounts.series),
      getAccountMeta("market", accounts.market),
      getAccountMeta("config", accounts.config),
      getAccountMeta("instructions", accounts.instructions),
      getAccountMeta("eventAuthority", accounts.eventAuthority),
      getAccountMeta("program", accounts.program),
    ],
    data: getPublicRecordPrintAttestedInstructionDataEncoder().encode(
      args as PublicRecordPrintAttestedInstructionDataArgs,
    ),
    programAddress,
  } as PublicRecordPrintAttestedInstruction<
    TProgramAddress,
    TAccountSeries,
    TAccountMarket,
    TAccountConfig,
    TAccountInstructions,
    TAccountEventAuthority,
    TAccountProgram
  >);
}

export type ParsedPublicRecordPrintAttestedInstruction<
  TProgram extends string = typeof AGARI_EVENTS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    series: TAccountMetas[0];
    market: TAccountMetas[1];
    config: TAccountMetas[2];
    instructions: TAccountMetas[3];
    eventAuthority: TAccountMetas[4];
    program: TAccountMetas[5];
  };
  data: PublicRecordPrintAttestedInstructionData;
};

export function parsePublicRecordPrintAttestedInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedPublicRecordPrintAttestedInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 6,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      series: getNextAccount(),
      market: getNextAccount(),
      config: getNextAccount(),
      instructions: getNextAccount(),
      eventAuthority: getNextAccount(),
      program: getNextAccount(),
    },
    data: getPublicRecordPrintAttestedInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
