/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findConfigPda } from "../pdas";
import { AGARI_EVENTS_PROGRAM_ADDRESS } from "../programs";

export const PUBLIC_RECORD_PRINT_REDSTONE_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([10, 182, 113, 45, 107, 225, 191, 95]);

export function getPublicRecordPrintRedstoneDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PUBLIC_RECORD_PRINT_REDSTONE_DISCRIMINATOR,
  );
}

export type PublicRecordPrintRedstoneInstruction<
  TProgram extends string = typeof AGARI_EVENTS_PROGRAM_ADDRESS,
  TAccountSeries extends string | AccountMeta<string> = string,
  TAccountMarket extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountEventAuthority extends string | AccountMeta<string> = string,
  TAccountProgram extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSeries extends string
        ? ReadonlyAccount<TAccountSeries>
        : TAccountSeries,
      TAccountMarket extends string
        ? WritableAccount<TAccountMarket>
        : TAccountMarket,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountEventAuthority extends string
        ? ReadonlyAccount<TAccountEventAuthority>
        : TAccountEventAuthority,
      TAccountProgram extends string
        ? ReadonlyAccount<TAccountProgram>
        : TAccountProgram,
      ...TRemainingAccounts,
    ]
  >;

export type PublicRecordPrintRedstoneInstructionData = {
  discriminator: ReadonlyUint8Array;
  which: number;
  payload: ReadonlyUint8Array;
};

export type PublicRecordPrintRedstoneInstructionDataArgs = {
  which: number;
  payload: ReadonlyUint8Array;
};

export function getPublicRecordPrintRedstoneInstructionDataEncoder(): Encoder<PublicRecordPrintRedstoneInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["which", getU8Encoder()],
      ["payload", addEncoderSizePrefix(getBytesEncoder(), getU32Encoder())],
    ]),
    (value) => ({
      ...value,
      discriminator: PUBLIC_RECORD_PRINT_REDSTONE_DISCRIMINATOR,
    }),
  );
}

export function getPublicRecordPrintRedstoneInstructionDataDecoder(): Decoder<PublicRecordPrintRedstoneInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["which", getU8Decoder()],
    ["payload", addDecoderSizePrefix(getBytesDecoder(), getU32Decoder())],
  ]);
}

export function getPublicRecordPrintRedstoneInstructionDataCodec(): Codec<
  PublicRecordPrintRedstoneInstructionDataArgs,
  PublicRecordPrintRedstoneInstructionData
> {
  return combineCodec(
    getPublicRecordPrintRedstoneInstructionDataEncoder(),
    getPublicRecordPrintRedstoneInstructionDataDecoder(),
  );
}

export type PublicRecordPrintRedstoneAsyncInput<
  TAccountSeries extends string = string,
  TAccountMarket extends string = string,
  TAccountConfig extends string = string,
  TAccountEventAuthority extends string = string,
  TAccountProgram extends string = string,
> = {
  series: Address<TAccountSeries>;
  market: Address<TAccountMarket>;
  config?: Address<TAccountConfig>;
  eventAuthority: Address<TAccountEventAuthority>;
  program: Address<TAccountProgram>;
  which: PublicRecordPrintRedstoneInstructionDataArgs["which"];
  payload: PublicRecordPrintRedstoneInstructionDataArgs["payload"];
};

export async function getPublicRecordPrintRedstoneInstructionAsync<
  TAccountSeries extends string,
  TAccountMarket extends string,
  TAccountConfig extends string,
  TAccountEventAuthority extends string,
  TAccountProgram extends string,
  TProgramAddress extends Address = typeof AGARI_EVENTS_PROGRAM_ADDRESS,
>(
  input: PublicRecordPrintRedstoneAsyncInput<
    TAccountSeries,
    TAccountMarket,
    TAccountConfig,
    TAccountEventAuthority,
    TAccountProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  PublicRecordPrintRedstoneInstruction<
    TProgramAddress,
    TAccountSeries,
    TAccountMarket,
    TAccountConfig,
    TAccountEventAuthority,
    TAccountProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? AGARI_EVENTS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    series: { value: input.series ?? null, isWritable: false },
    market: { value: input.market ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    program: { value: input.program ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda({ programAddress });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("series", accounts.series),
      getAccountMeta("market", accounts.market),
      getAccountMeta("config", accounts.config),
      getAccountMeta("eventAuthority", accounts.eventAuthority),
      getAccountMeta("program", accounts.program),
    ],
    data: getPublicRecordPrintRedstoneInstructionDataEncoder().encode(
      args as PublicRecordPrintRedstoneInstructionDataArgs,
    ),
    programAddress,
  } as PublicRecordPrintRedstoneInstruction<
    TProgramAddress,
    TAccountSeries,
    TAccountMarket,
    TAccountConfig,
    TAccountEventAuthority,
    TAccountProgram
  >);
}

export type PublicRecordPrintRedstoneInput<
  TAccountSeries extends string = string,
  TAccountMarket extends string = string,
  TAccountConfig extends string = string,
  TAccountEventAuthority extends string = string,
  TAccountProgram extends string = string,
> = {
  series: Address<TAccountSeries>;
  market: Address<TAccountMarket>;
  config: Address<TAccountConfig>;
  eventAuthority: Address<TAccountEventAuthority>;
  program: Address<TAccountProgram>;
  which: PublicRecordPrintRedstoneInstructionDataArgs["which"];
  payload: PublicRecordPrintRedstoneInstructionDataArgs["payload"];
};

export function getPublicRecordPrintRedstoneInstruction<
  TAccountSeries extends string,
  TAccountMarket extends string,
  TAccountConfig extends string,
  TAccountEventAuthority extends string,
  TAccountProgram extends string,
  TProgramAddress extends Address = typeof AGARI_EVENTS_PROGRAM_ADDRESS,
>(
  input: PublicRecordPrintRedstoneInput<
    TAccountSeries,
    TAccountMarket,
    TAccountConfig,
    TAccountEventAuthority,
    TAccountProgram
  >,
  config?: { programAddress?: TProgramAddress },
): PublicRecordPrintRedstoneInstruction<
  TProgramAddress,
  TAccountSeries,
  TAccountMarket,
  TAccountConfig,
  TAccountEventAuthority,
  TAccountProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? AGARI_EVENTS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    series: { value: input.series ?? null, isWritable: false },
    market: { value: input.market ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    program: { value: input.program ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("series", accounts.series),
      getAccountMeta("market", accounts.market),
      getAccountMeta("config", accounts.config),
      getAccountMeta("eventAuthority", accounts.eventAuthority),
      getAccountMeta("program", accounts.program),
    ],
    data: getPublicRecordPrintRedstoneInstructionDataEncoder().encode(
      args as PublicRecordPrintRedstoneInstructionDataArgs,
    ),
    programAddress,
  } as PublicRecordPrintRedstoneInstruction<
    TProgramAddress,
    TAccountSeries,
    TAccountMarket,
    TAccountConfig,
    TAccountEventAuthority,
    TAccountProgram
  >);
}

export type ParsedPublicRecordPrintRedstoneInstruction<
  TProgram extends string = typeof AGARI_EVENTS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    series: TAccountMetas[0];
    market: TAccountMetas[1];
    config: TAccountMetas[2];
    eventAuthority: TAccountMetas[3];
    program: TAccountMetas[4];
  };
  data: PublicRecordPrintRedstoneInstructionData;
};

export function parsePublicRecordPrintRedstoneInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedPublicRecordPrintRedstoneInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      series: getNextAccount(),
      market: getNextAccount(),
      config: getNextAccount(),
      eventAuthority: getNextAccount(),
      program: getNextAccount(),
    },
    data: getPublicRecordPrintRedstoneInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
