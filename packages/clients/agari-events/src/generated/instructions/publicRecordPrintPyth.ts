/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { AGARI_EVENTS_PROGRAM_ADDRESS } from "../programs";

export const PUBLIC_RECORD_PRINT_PYTH_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([73, 110, 24, 19, 191, 215, 79, 237]);

export function getPublicRecordPrintPythDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PUBLIC_RECORD_PRINT_PYTH_DISCRIMINATOR,
  );
}

export type PublicRecordPrintPythInstruction<
  TProgram extends string = typeof AGARI_EVENTS_PROGRAM_ADDRESS,
  TAccountSeries extends string | AccountMeta<string> = string,
  TAccountMarket extends string | AccountMeta<string> = string,
  TAccountPriceUpdate extends string | AccountMeta<string> = string,
  TAccountEventAuthority extends string | AccountMeta<string> = string,
  TAccountProgram extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSeries extends string
        ? ReadonlyAccount<TAccountSeries>
        : TAccountSeries,
      TAccountMarket extends string
        ? WritableAccount<TAccountMarket>
        : TAccountMarket,
      TAccountPriceUpdate extends string
        ? ReadonlyAccount<TAccountPriceUpdate>
        : TAccountPriceUpdate,
      TAccountEventAuthority extends string
        ? ReadonlyAccount<TAccountEventAuthority>
        : TAccountEventAuthority,
      TAccountProgram extends string
        ? ReadonlyAccount<TAccountProgram>
        : TAccountProgram,
      ...TRemainingAccounts,
    ]
  >;

export type PublicRecordPrintPythInstructionData = {
  discriminator: ReadonlyUint8Array;
  which: number;
};

export type PublicRecordPrintPythInstructionDataArgs = { which: number };

export function getPublicRecordPrintPythInstructionDataEncoder(): FixedSizeEncoder<PublicRecordPrintPythInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["which", getU8Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: PUBLIC_RECORD_PRINT_PYTH_DISCRIMINATOR,
    }),
  );
}

export function getPublicRecordPrintPythInstructionDataDecoder(): FixedSizeDecoder<PublicRecordPrintPythInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["which", getU8Decoder()],
  ]);
}

export function getPublicRecordPrintPythInstructionDataCodec(): FixedSizeCodec<
  PublicRecordPrintPythInstructionDataArgs,
  PublicRecordPrintPythInstructionData
> {
  return combineCodec(
    getPublicRecordPrintPythInstructionDataEncoder(),
    getPublicRecordPrintPythInstructionDataDecoder(),
  );
}

export type PublicRecordPrintPythInput<
  TAccountSeries extends string = string,
  TAccountMarket extends string = string,
  TAccountPriceUpdate extends string = string,
  TAccountEventAuthority extends string = string,
  TAccountProgram extends string = string,
> = {
  series: Address<TAccountSeries>;
  market: Address<TAccountMarket>;
  /** Anchor's owner check pins the receiver compiled into `pyth-solana-receiver-sdk` (default feature, D-021). */
  priceUpdate: Address<TAccountPriceUpdate>;
  eventAuthority: Address<TAccountEventAuthority>;
  program: Address<TAccountProgram>;
  which: PublicRecordPrintPythInstructionDataArgs["which"];
};

export function getPublicRecordPrintPythInstruction<
  TAccountSeries extends string,
  TAccountMarket extends string,
  TAccountPriceUpdate extends string,
  TAccountEventAuthority extends string,
  TAccountProgram extends string,
  TProgramAddress extends Address = typeof AGARI_EVENTS_PROGRAM_ADDRESS,
>(
  input: PublicRecordPrintPythInput<
    TAccountSeries,
    TAccountMarket,
    TAccountPriceUpdate,
    TAccountEventAuthority,
    TAccountProgram
  >,
  config?: { programAddress?: TProgramAddress },
): PublicRecordPrintPythInstruction<
  TProgramAddress,
  TAccountSeries,
  TAccountMarket,
  TAccountPriceUpdate,
  TAccountEventAuthority,
  TAccountProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? AGARI_EVENTS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    series: { value: input.series ?? null, isWritable: false },
    market: { value: input.market ?? null, isWritable: true },
    priceUpdate: { value: input.priceUpdate ?? null, isWritable: false },
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    program: { value: input.program ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("series", accounts.series),
      getAccountMeta("market", accounts.market),
      getAccountMeta("priceUpdate", accounts.priceUpdate),
      getAccountMeta("eventAuthority", accounts.eventAuthority),
      getAccountMeta("program", accounts.program),
    ],
    data: getPublicRecordPrintPythInstructionDataEncoder().encode(
      args as PublicRecordPrintPythInstructionDataArgs,
    ),
    programAddress,
  } as PublicRecordPrintPythInstruction<
    TProgramAddress,
    TAccountSeries,
    TAccountMarket,
    TAccountPriceUpdate,
    TAccountEventAuthority,
    TAccountProgram
  >);
}

export type ParsedPublicRecordPrintPythInstruction<
  TProgram extends string = typeof AGARI_EVENTS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    series: TAccountMetas[0];
    market: TAccountMetas[1];
    /** Anchor's owner check pins the receiver compiled into `pyth-solana-receiver-sdk` (default feature, D-021). */
    priceUpdate: TAccountMetas[2];
    eventAuthority: TAccountMetas[3];
    program: TAccountMetas[4];
  };
  data: PublicRecordPrintPythInstructionData;
};

export function parsePublicRecordPrintPythInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedPublicRecordPrintPythInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      series: getNextAccount(),
      market: getNextAccount(),
      priceUpdate: getNextAccount(),
      eventAuthority: getNextAccount(),
      program: getNextAccount(),
    },
    data: getPublicRecordPrintPythInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
