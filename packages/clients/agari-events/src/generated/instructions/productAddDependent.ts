/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findConfigPda } from "../pdas";
import { AGARI_EVENTS_PROGRAM_ADDRESS } from "../programs";

export const PRODUCT_ADD_DEPENDENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([167, 111, 216, 189, 10, 125, 173, 87]);

export function getProductAddDependentDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PRODUCT_ADD_DEPENDENT_DISCRIMINATOR,
  );
}

export type ProductAddDependentInstruction<
  TProgram extends string = typeof AGARI_EVENTS_PROGRAM_ADDRESS,
  TAccountProgramAuthority extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountMarket extends string | AccountMeta<string> = string,
  TAccountEventAuthority extends string | AccountMeta<string> = string,
  TAccountProgram extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountProgramAuthority extends string
        ? ReadonlySignerAccount<TAccountProgramAuthority> &
            AccountSignerMeta<TAccountProgramAuthority>
        : TAccountProgramAuthority,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountMarket extends string
        ? WritableAccount<TAccountMarket>
        : TAccountMarket,
      TAccountEventAuthority extends string
        ? ReadonlyAccount<TAccountEventAuthority>
        : TAccountEventAuthority,
      TAccountProgram extends string
        ? ReadonlyAccount<TAccountProgram>
        : TAccountProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ProductAddDependentInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type ProductAddDependentInstructionDataArgs = {};

export function getProductAddDependentInstructionDataEncoder(): FixedSizeEncoder<ProductAddDependentInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: PRODUCT_ADD_DEPENDENT_DISCRIMINATOR,
    }),
  );
}

export function getProductAddDependentInstructionDataDecoder(): FixedSizeDecoder<ProductAddDependentInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getProductAddDependentInstructionDataCodec(): FixedSizeCodec<
  ProductAddDependentInstructionDataArgs,
  ProductAddDependentInstructionData
> {
  return combineCodec(
    getProductAddDependentInstructionDataEncoder(),
    getProductAddDependentInstructionDataDecoder(),
  );
}

export type ProductAddDependentAsyncInput<
  TAccountProgramAuthority extends string = string,
  TAccountConfig extends string = string,
  TAccountMarket extends string = string,
  TAccountEventAuthority extends string = string,
  TAccountProgram extends string = string,
> = {
  /** A product's seat PDA listed in `config.program_authorities`. */
  programAuthority: TransactionSigner<TAccountProgramAuthority>;
  config?: Address<TAccountConfig>;
  market: Address<TAccountMarket>;
  eventAuthority: Address<TAccountEventAuthority>;
  program: Address<TAccountProgram>;
};

export async function getProductAddDependentInstructionAsync<
  TAccountProgramAuthority extends string,
  TAccountConfig extends string,
  TAccountMarket extends string,
  TAccountEventAuthority extends string,
  TAccountProgram extends string,
  TProgramAddress extends Address = typeof AGARI_EVENTS_PROGRAM_ADDRESS,
>(
  input: ProductAddDependentAsyncInput<
    TAccountProgramAuthority,
    TAccountConfig,
    TAccountMarket,
    TAccountEventAuthority,
    TAccountProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ProductAddDependentInstruction<
    TProgramAddress,
    TAccountProgramAuthority,
    TAccountConfig,
    TAccountMarket,
    TAccountEventAuthority,
    TAccountProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? AGARI_EVENTS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    programAuthority: {
      value: input.programAuthority ?? null,
      isWritable: false,
    },
    config: { value: input.config ?? null, isWritable: false },
    market: { value: input.market ?? null, isWritable: true },
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    program: { value: input.program ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda({ programAddress });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("programAuthority", accounts.programAuthority),
      getAccountMeta("config", accounts.config),
      getAccountMeta("market", accounts.market),
      getAccountMeta("eventAuthority", accounts.eventAuthority),
      getAccountMeta("program", accounts.program),
    ],
    data: getProductAddDependentInstructionDataEncoder().encode({}),
    programAddress,
  } as ProductAddDependentInstruction<
    TProgramAddress,
    TAccountProgramAuthority,
    TAccountConfig,
    TAccountMarket,
    TAccountEventAuthority,
    TAccountProgram
  >);
}

export type ProductAddDependentInput<
  TAccountProgramAuthority extends string = string,
  TAccountConfig extends string = string,
  TAccountMarket extends string = string,
  TAccountEventAuthority extends string = string,
  TAccountProgram extends string = string,
> = {
  /** A product's seat PDA listed in `config.program_authorities`. */
  programAuthority: TransactionSigner<TAccountProgramAuthority>;
  config: Address<TAccountConfig>;
  market: Address<TAccountMarket>;
  eventAuthority: Address<TAccountEventAuthority>;
  program: Address<TAccountProgram>;
};

export function getProductAddDependentInstruction<
  TAccountProgramAuthority extends string,
  TAccountConfig extends string,
  TAccountMarket extends string,
  TAccountEventAuthority extends string,
  TAccountProgram extends string,
  TProgramAddress extends Address = typeof AGARI_EVENTS_PROGRAM_ADDRESS,
>(
  input: ProductAddDependentInput<
    TAccountProgramAuthority,
    TAccountConfig,
    TAccountMarket,
    TAccountEventAuthority,
    TAccountProgram
  >,
  config?: { programAddress?: TProgramAddress },
): ProductAddDependentInstruction<
  TProgramAddress,
  TAccountProgramAuthority,
  TAccountConfig,
  TAccountMarket,
  TAccountEventAuthority,
  TAccountProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? AGARI_EVENTS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    programAuthority: {
      value: input.programAuthority ?? null,
      isWritable: false,
    },
    config: { value: input.config ?? null, isWritable: false },
    market: { value: input.market ?? null, isWritable: true },
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    program: { value: input.program ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("programAuthority", accounts.programAuthority),
      getAccountMeta("config", accounts.config),
      getAccountMeta("market", accounts.market),
      getAccountMeta("eventAuthority", accounts.eventAuthority),
      getAccountMeta("program", accounts.program),
    ],
    data: getProductAddDependentInstructionDataEncoder().encode({}),
    programAddress,
  } as ProductAddDependentInstruction<
    TProgramAddress,
    TAccountProgramAuthority,
    TAccountConfig,
    TAccountMarket,
    TAccountEventAuthority,
    TAccountProgram
  >);
}

export type ParsedProductAddDependentInstruction<
  TProgram extends string = typeof AGARI_EVENTS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** A product's seat PDA listed in `config.program_authorities`. */
    programAuthority: TAccountMetas[0];
    config: TAccountMetas[1];
    market: TAccountMetas[2];
    eventAuthority: TAccountMetas[3];
    program: TAccountMetas[4];
  };
  data: ProductAddDependentInstructionData;
};

export function parseProductAddDependentInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedProductAddDependentInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      programAuthority: getNextAccount(),
      config: getNextAccount(),
      market: getNextAccount(),
      eventAuthority: getNextAccount(),
      program: getNextAccount(),
    },
    data: getProductAddDependentInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
