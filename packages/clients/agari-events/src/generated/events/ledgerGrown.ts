/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU64Decoder,
  getU64Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const LEDGER_GROWN_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([175, 106, 135, 18, 182, 203, 165, 191]);

export function getLedgerGrownEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    LEDGER_GROWN_EVENT_DISCRIMINATOR,
  );
}

export type LedgerGrownEvent = {
  market: Address;
  seq: bigint;
  payer: Address;
  capacity: number;
};

export type LedgerGrownEventArgs = {
  market: Address;
  seq: number | bigint;
  payer: Address;
  capacity: number;
};

/** Gets the encoder for {@link LedgerGrownEventArgs} event data. */
export function getLedgerGrownEventEncoder(): FixedSizeEncoder<LedgerGrownEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["market", getAddressEncoder()],
      ["seq", getU64Encoder()],
      ["payer", getAddressEncoder()],
      ["capacity", getU16Encoder()],
    ]),
    [getConstantEncoder(LEDGER_GROWN_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link LedgerGrownEvent} event data. */
export function getLedgerGrownEventDecoder(): FixedSizeDecoder<LedgerGrownEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["market", getAddressDecoder()],
      ["seq", getU64Decoder()],
      ["payer", getAddressDecoder()],
      ["capacity", getU16Decoder()],
    ]),
    [getConstantDecoder(LEDGER_GROWN_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link LedgerGrownEvent} event data. */
export function getLedgerGrownEventCodec(): FixedSizeCodec<
  LedgerGrownEventArgs,
  LedgerGrownEvent
> {
  return combineCodec(
    getLedgerGrownEventEncoder(),
    getLedgerGrownEventDecoder(),
  );
}

/** Parses the provided bytes into {@link LedgerGrownEvent} event data. */
export function parseLedgerGrownEvent(
  data: ReadonlyUint8Array | Uint8Array,
): LedgerGrownEvent {
  if (containsBytes(data, LEDGER_GROWN_EVENT_DISCRIMINATOR, 0)) {
    return getLedgerGrownEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "LedgerGrownEvent" event discriminators.',
  );
}
