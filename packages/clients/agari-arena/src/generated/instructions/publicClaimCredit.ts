/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import {
  findArenaPda,
  findCreditPda,
  findCustodyPda,
  findSeatPda,
} from "../pdas";
import { AGARI_ARENA_PROGRAM_ADDRESS } from "../programs";

export const PUBLIC_CLAIM_CREDIT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([182, 132, 40, 250, 49, 80, 254, 71]);

export function getPublicClaimCreditDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PUBLIC_CLAIM_CREDIT_DISCRIMINATOR,
  );
}

export type PublicClaimCreditInstruction<
  TProgram extends string = typeof AGARI_ARENA_PROGRAM_ADDRESS,
  TAccountCaller extends string | AccountMeta<string> = string,
  TAccountArena extends string | AccountMeta<string> = string,
  TAccountPlayer extends string | AccountMeta<string> = string,
  TAccountCredit extends string | AccountMeta<string> = string,
  TAccountCustody extends string | AccountMeta<string> = string,
  TAccountSeat extends string | AccountMeta<string> = string,
  TAccountPlayerToken extends string | AccountMeta<string> = string,
  TAccountCollateralMint extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCaller extends string
        ? ReadonlySignerAccount<TAccountCaller> &
            AccountSignerMeta<TAccountCaller>
        : TAccountCaller,
      TAccountArena extends string
        ? WritableAccount<TAccountArena>
        : TAccountArena,
      TAccountPlayer extends string
        ? ReadonlyAccount<TAccountPlayer>
        : TAccountPlayer,
      TAccountCredit extends string
        ? WritableAccount<TAccountCredit>
        : TAccountCredit,
      TAccountCustody extends string
        ? WritableAccount<TAccountCustody>
        : TAccountCustody,
      TAccountSeat extends string
        ? ReadonlyAccount<TAccountSeat>
        : TAccountSeat,
      TAccountPlayerToken extends string
        ? WritableAccount<TAccountPlayerToken>
        : TAccountPlayerToken,
      TAccountCollateralMint extends string
        ? ReadonlyAccount<TAccountCollateralMint>
        : TAccountCollateralMint,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      ...TRemainingAccounts,
    ]
  >;

export type PublicClaimCreditInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type PublicClaimCreditInstructionDataArgs = {};

export function getPublicClaimCreditInstructionDataEncoder(): FixedSizeEncoder<PublicClaimCreditInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: PUBLIC_CLAIM_CREDIT_DISCRIMINATOR }),
  );
}

export function getPublicClaimCreditInstructionDataDecoder(): FixedSizeDecoder<PublicClaimCreditInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getPublicClaimCreditInstructionDataCodec(): FixedSizeCodec<
  PublicClaimCreditInstructionDataArgs,
  PublicClaimCreditInstructionData
> {
  return combineCodec(
    getPublicClaimCreditInstructionDataEncoder(),
    getPublicClaimCreditInstructionDataDecoder(),
  );
}

export type PublicClaimCreditAsyncInput<
  TAccountCaller extends string = string,
  TAccountArena extends string = string,
  TAccountPlayer extends string = string,
  TAccountCredit extends string = string,
  TAccountCustody extends string = string,
  TAccountSeat extends string = string,
  TAccountPlayerToken extends string = string,
  TAccountCollateralMint extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  /** Permissionless: a relayer or a friend can crank it without becoming the beneficiary. */
  caller: TransactionSigner<TAccountCaller>;
  arena?: Address<TAccountArena>;
  player: Address<TAccountPlayer>;
  credit?: Address<TAccountCredit>;
  custody?: Address<TAccountCustody>;
  seat?: Address<TAccountSeat>;
  /** The player's own token account, and nobody else's (AD-5). */
  playerToken: Address<TAccountPlayerToken>;
  collateralMint: Address<TAccountCollateralMint>;
  tokenProgram?: Address<TAccountTokenProgram>;
};

export async function getPublicClaimCreditInstructionAsync<
  TAccountCaller extends string,
  TAccountArena extends string,
  TAccountPlayer extends string,
  TAccountCredit extends string,
  TAccountCustody extends string,
  TAccountSeat extends string,
  TAccountPlayerToken extends string,
  TAccountCollateralMint extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof AGARI_ARENA_PROGRAM_ADDRESS,
>(
  input: PublicClaimCreditAsyncInput<
    TAccountCaller,
    TAccountArena,
    TAccountPlayer,
    TAccountCredit,
    TAccountCustody,
    TAccountSeat,
    TAccountPlayerToken,
    TAccountCollateralMint,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  PublicClaimCreditInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountArena,
    TAccountPlayer,
    TAccountCredit,
    TAccountCustody,
    TAccountSeat,
    TAccountPlayerToken,
    TAccountCollateralMint,
    TAccountTokenProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? AGARI_ARENA_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: false },
    arena: { value: input.arena ?? null, isWritable: true },
    player: { value: input.player ?? null, isWritable: false },
    credit: { value: input.credit ?? null, isWritable: true },
    custody: { value: input.custody ?? null, isWritable: true },
    seat: { value: input.seat ?? null, isWritable: false },
    playerToken: { value: input.playerToken ?? null, isWritable: true },
    collateralMint: { value: input.collateralMint ?? null, isWritable: false },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.arena.value) {
    accounts.arena.value = await findArenaPda({ programAddress });
  }
  if (!accounts.credit.value) {
    accounts.credit.value = await findCreditPda(
      {
        player: getAddressFromResolvedInstructionAccount(
          "player",
          accounts.player.value,
        ),
      },
      { programAddress },
    );
  }
  if (!accounts.custody.value) {
    accounts.custody.value = await findCustodyPda({ programAddress });
  }
  if (!accounts.seat.value) {
    accounts.seat.value = await findSeatPda({ programAddress });
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("caller", accounts.caller),
      getAccountMeta("arena", accounts.arena),
      getAccountMeta("player", accounts.player),
      getAccountMeta("credit", accounts.credit),
      getAccountMeta("custody", accounts.custody),
      getAccountMeta("seat", accounts.seat),
      getAccountMeta("playerToken", accounts.playerToken),
      getAccountMeta("collateralMint", accounts.collateralMint),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
    ],
    data: getPublicClaimCreditInstructionDataEncoder().encode({}),
    programAddress,
  } as PublicClaimCreditInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountArena,
    TAccountPlayer,
    TAccountCredit,
    TAccountCustody,
    TAccountSeat,
    TAccountPlayerToken,
    TAccountCollateralMint,
    TAccountTokenProgram
  >);
}

export type PublicClaimCreditInput<
  TAccountCaller extends string = string,
  TAccountArena extends string = string,
  TAccountPlayer extends string = string,
  TAccountCredit extends string = string,
  TAccountCustody extends string = string,
  TAccountSeat extends string = string,
  TAccountPlayerToken extends string = string,
  TAccountCollateralMint extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  /** Permissionless: a relayer or a friend can crank it without becoming the beneficiary. */
  caller: TransactionSigner<TAccountCaller>;
  arena: Address<TAccountArena>;
  player: Address<TAccountPlayer>;
  credit: Address<TAccountCredit>;
  custody: Address<TAccountCustody>;
  seat: Address<TAccountSeat>;
  /** The player's own token account, and nobody else's (AD-5). */
  playerToken: Address<TAccountPlayerToken>;
  collateralMint: Address<TAccountCollateralMint>;
  tokenProgram?: Address<TAccountTokenProgram>;
};

export function getPublicClaimCreditInstruction<
  TAccountCaller extends string,
  TAccountArena extends string,
  TAccountPlayer extends string,
  TAccountCredit extends string,
  TAccountCustody extends string,
  TAccountSeat extends string,
  TAccountPlayerToken extends string,
  TAccountCollateralMint extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof AGARI_ARENA_PROGRAM_ADDRESS,
>(
  input: PublicClaimCreditInput<
    TAccountCaller,
    TAccountArena,
    TAccountPlayer,
    TAccountCredit,
    TAccountCustody,
    TAccountSeat,
    TAccountPlayerToken,
    TAccountCollateralMint,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): PublicClaimCreditInstruction<
  TProgramAddress,
  TAccountCaller,
  TAccountArena,
  TAccountPlayer,
  TAccountCredit,
  TAccountCustody,
  TAccountSeat,
  TAccountPlayerToken,
  TAccountCollateralMint,
  TAccountTokenProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? AGARI_ARENA_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: false },
    arena: { value: input.arena ?? null, isWritable: true },
    player: { value: input.player ?? null, isWritable: false },
    credit: { value: input.credit ?? null, isWritable: true },
    custody: { value: input.custody ?? null, isWritable: true },
    seat: { value: input.seat ?? null, isWritable: false },
    playerToken: { value: input.playerToken ?? null, isWritable: true },
    collateralMint: { value: input.collateralMint ?? null, isWritable: false },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("caller", accounts.caller),
      getAccountMeta("arena", accounts.arena),
      getAccountMeta("player", accounts.player),
      getAccountMeta("credit", accounts.credit),
      getAccountMeta("custody", accounts.custody),
      getAccountMeta("seat", accounts.seat),
      getAccountMeta("playerToken", accounts.playerToken),
      getAccountMeta("collateralMint", accounts.collateralMint),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
    ],
    data: getPublicClaimCreditInstructionDataEncoder().encode({}),
    programAddress,
  } as PublicClaimCreditInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountArena,
    TAccountPlayer,
    TAccountCredit,
    TAccountCustody,
    TAccountSeat,
    TAccountPlayerToken,
    TAccountCollateralMint,
    TAccountTokenProgram
  >);
}

export type ParsedPublicClaimCreditInstruction<
  TProgram extends string = typeof AGARI_ARENA_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Permissionless: a relayer or a friend can crank it without becoming the beneficiary. */
    caller: TAccountMetas[0];
    arena: TAccountMetas[1];
    player: TAccountMetas[2];
    credit: TAccountMetas[3];
    custody: TAccountMetas[4];
    seat: TAccountMetas[5];
    /** The player's own token account, and nobody else's (AD-5). */
    playerToken: TAccountMetas[6];
    collateralMint: TAccountMetas[7];
    tokenProgram: TAccountMetas[8];
  };
  data: PublicClaimCreditInstructionData;
};

export function parsePublicClaimCreditInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedPublicClaimCreditInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 9) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 9,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      caller: getNextAccount(),
      arena: getNextAccount(),
      player: getNextAccount(),
      credit: getNextAccount(),
      custody: getNextAccount(),
      seat: getNextAccount(),
      playerToken: getNextAccount(),
      collateralMint: getNextAccount(),
      tokenProgram: getNextAccount(),
    },
    data: getPublicClaimCreditInstructionDataDecoder().decode(instruction.data),
  };
}
