/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findArenaPda, findGamePda } from "../pdas";
import { AGARI_ARENA_PROGRAM_ADDRESS } from "../programs";

export const PUBLIC_REVEAL_DECK_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([88, 175, 147, 23, 220, 55, 3, 139]);

export function getPublicRevealDeckDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PUBLIC_REVEAL_DECK_DISCRIMINATOR,
  );
}

export type PublicRevealDeckInstruction<
  TProgram extends string = typeof AGARI_ARENA_PROGRAM_ADDRESS,
  TAccountCaller extends string | AccountMeta<string> = string,
  TAccountArena extends string | AccountMeta<string> = string,
  TAccountGame extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCaller extends string
        ? ReadonlySignerAccount<TAccountCaller> &
            AccountSignerMeta<TAccountCaller>
        : TAccountCaller,
      TAccountArena extends string
        ? ReadonlyAccount<TAccountArena>
        : TAccountArena,
      TAccountGame extends string
        ? WritableAccount<TAccountGame>
        : TAccountGame,
      ...TRemainingAccounts,
    ]
  >;

export type PublicRevealDeckInstructionData = {
  discriminator: ReadonlyUint8Array;
  matchId: ReadonlyUint8Array;
  serverSeed: ReadonlyUint8Array;
  clientSeeds: Array<ReadonlyUint8Array>;
};

export type PublicRevealDeckInstructionDataArgs = {
  matchId: ReadonlyUint8Array;
  serverSeed: ReadonlyUint8Array;
  clientSeeds: Array<ReadonlyUint8Array>;
};

export function getPublicRevealDeckInstructionDataEncoder(): Encoder<PublicRevealDeckInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["matchId", fixEncoderSize(getBytesEncoder(), 32)],
      ["serverSeed", fixEncoderSize(getBytesEncoder(), 32)],
      ["clientSeeds", getArrayEncoder(fixEncoderSize(getBytesEncoder(), 32))],
    ]),
    (value) => ({ ...value, discriminator: PUBLIC_REVEAL_DECK_DISCRIMINATOR }),
  );
}

export function getPublicRevealDeckInstructionDataDecoder(): Decoder<PublicRevealDeckInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["matchId", fixDecoderSize(getBytesDecoder(), 32)],
    ["serverSeed", fixDecoderSize(getBytesDecoder(), 32)],
    ["clientSeeds", getArrayDecoder(fixDecoderSize(getBytesDecoder(), 32))],
  ]);
}

export function getPublicRevealDeckInstructionDataCodec(): Codec<
  PublicRevealDeckInstructionDataArgs,
  PublicRevealDeckInstructionData
> {
  return combineCodec(
    getPublicRevealDeckInstructionDataEncoder(),
    getPublicRevealDeckInstructionDataDecoder(),
  );
}

export type PublicRevealDeckAsyncInput<
  TAccountCaller extends string = string,
  TAccountArena extends string = string,
  TAccountGame extends string = string,
> = {
  /**
   * Permissionless: whoever holds the preimage may publish it, because the commitment, not a key, is what proves
   * the deck was fixed before either player saw it.
   */
  caller: TransactionSigner<TAccountCaller>;
  arena?: Address<TAccountArena>;
  game?: Address<TAccountGame>;
  matchId: PublicRevealDeckInstructionDataArgs["matchId"];
  serverSeed: PublicRevealDeckInstructionDataArgs["serverSeed"];
  clientSeeds: PublicRevealDeckInstructionDataArgs["clientSeeds"];
};

export async function getPublicRevealDeckInstructionAsync<
  TAccountCaller extends string,
  TAccountArena extends string,
  TAccountGame extends string,
  TProgramAddress extends Address = typeof AGARI_ARENA_PROGRAM_ADDRESS,
>(
  input: PublicRevealDeckAsyncInput<
    TAccountCaller,
    TAccountArena,
    TAccountGame
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  PublicRevealDeckInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountArena,
    TAccountGame
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? AGARI_ARENA_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: false },
    arena: { value: input.arena ?? null, isWritable: false },
    game: { value: input.game ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.arena.value) {
    accounts.arena.value = await findArenaPda({ programAddress });
  }
  if (!accounts.game.value) {
    accounts.game.value = await findGamePda(
      { matchId: getNonNullResolvedInstructionInput("matchId", args.matchId) },
      { programAddress },
    );
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("caller", accounts.caller),
      getAccountMeta("arena", accounts.arena),
      getAccountMeta("game", accounts.game),
    ],
    data: getPublicRevealDeckInstructionDataEncoder().encode(
      args as PublicRevealDeckInstructionDataArgs,
    ),
    programAddress,
  } as PublicRevealDeckInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountArena,
    TAccountGame
  >);
}

export type PublicRevealDeckInput<
  TAccountCaller extends string = string,
  TAccountArena extends string = string,
  TAccountGame extends string = string,
> = {
  /**
   * Permissionless: whoever holds the preimage may publish it, because the commitment, not a key, is what proves
   * the deck was fixed before either player saw it.
   */
  caller: TransactionSigner<TAccountCaller>;
  arena: Address<TAccountArena>;
  game: Address<TAccountGame>;
  matchId: PublicRevealDeckInstructionDataArgs["matchId"];
  serverSeed: PublicRevealDeckInstructionDataArgs["serverSeed"];
  clientSeeds: PublicRevealDeckInstructionDataArgs["clientSeeds"];
};

export function getPublicRevealDeckInstruction<
  TAccountCaller extends string,
  TAccountArena extends string,
  TAccountGame extends string,
  TProgramAddress extends Address = typeof AGARI_ARENA_PROGRAM_ADDRESS,
>(
  input: PublicRevealDeckInput<TAccountCaller, TAccountArena, TAccountGame>,
  config?: { programAddress?: TProgramAddress },
): PublicRevealDeckInstruction<
  TProgramAddress,
  TAccountCaller,
  TAccountArena,
  TAccountGame
> {
  // Program address.
  const programAddress = config?.programAddress ?? AGARI_ARENA_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: false },
    arena: { value: input.arena ?? null, isWritable: false },
    game: { value: input.game ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("caller", accounts.caller),
      getAccountMeta("arena", accounts.arena),
      getAccountMeta("game", accounts.game),
    ],
    data: getPublicRevealDeckInstructionDataEncoder().encode(
      args as PublicRevealDeckInstructionDataArgs,
    ),
    programAddress,
  } as PublicRevealDeckInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountArena,
    TAccountGame
  >);
}

export type ParsedPublicRevealDeckInstruction<
  TProgram extends string = typeof AGARI_ARENA_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * Permissionless: whoever holds the preimage may publish it, because the commitment, not a key, is what proves
     * the deck was fixed before either player saw it.
     */
    caller: TAccountMetas[0];
    arena: TAccountMetas[1];
    game: TAccountMetas[2];
  };
  data: PublicRevealDeckInstructionData;
};

export function parsePublicRevealDeckInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedPublicRevealDeckInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      caller: getNextAccount(),
      arena: getNextAccount(),
      game: getNextAccount(),
    },
    data: getPublicRevealDeckInstructionDataDecoder().decode(instruction.data),
  };
}
