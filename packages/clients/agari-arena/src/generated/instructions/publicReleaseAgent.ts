/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import {
  findAgentAccountPda,
  findArenaPda,
  findCreditPda,
  findGamePda,
} from "../pdas";
import { AGARI_ARENA_PROGRAM_ADDRESS } from "../programs";

export const PUBLIC_RELEASE_AGENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([111, 164, 43, 83, 59, 57, 173, 15]);

export function getPublicReleaseAgentDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PUBLIC_RELEASE_AGENT_DISCRIMINATOR,
  );
}

export type PublicReleaseAgentInstruction<
  TProgram extends string = typeof AGARI_ARENA_PROGRAM_ADDRESS,
  TAccountCaller extends string | AccountMeta<string> = string,
  TAccountArena extends string | AccountMeta<string> = string,
  TAccountGame extends string | AccountMeta<string> = string,
  TAccountPlayer extends string | AccountMeta<string> = string,
  TAccountAgentAccount extends string | AccountMeta<string> = string,
  TAccountCredit extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCaller extends string
        ? WritableSignerAccount<TAccountCaller> &
            AccountSignerMeta<TAccountCaller>
        : TAccountCaller,
      TAccountArena extends string
        ? WritableAccount<TAccountArena>
        : TAccountArena,
      TAccountGame extends string
        ? ReadonlyAccount<TAccountGame>
        : TAccountGame,
      TAccountPlayer extends string
        ? ReadonlyAccount<TAccountPlayer>
        : TAccountPlayer,
      TAccountAgentAccount extends string
        ? WritableAccount<TAccountAgentAccount>
        : TAccountAgentAccount,
      TAccountCredit extends string
        ? WritableAccount<TAccountCredit>
        : TAccountCredit,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type PublicReleaseAgentInstructionData = {
  discriminator: ReadonlyUint8Array;
  matchId: ReadonlyUint8Array;
};

export type PublicReleaseAgentInstructionDataArgs = {
  matchId: ReadonlyUint8Array;
};

export function getPublicReleaseAgentInstructionDataEncoder(): FixedSizeEncoder<PublicReleaseAgentInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["matchId", fixEncoderSize(getBytesEncoder(), 32)],
    ]),
    (value) => ({
      ...value,
      discriminator: PUBLIC_RELEASE_AGENT_DISCRIMINATOR,
    }),
  );
}

export function getPublicReleaseAgentInstructionDataDecoder(): FixedSizeDecoder<PublicReleaseAgentInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["matchId", fixDecoderSize(getBytesDecoder(), 32)],
  ]);
}

export function getPublicReleaseAgentInstructionDataCodec(): FixedSizeCodec<
  PublicReleaseAgentInstructionDataArgs,
  PublicReleaseAgentInstructionData
> {
  return combineCodec(
    getPublicReleaseAgentInstructionDataEncoder(),
    getPublicReleaseAgentInstructionDataDecoder(),
  );
}

export type PublicReleaseAgentAsyncInput<
  TAccountCaller extends string = string,
  TAccountArena extends string = string,
  TAccountGame extends string = string,
  TAccountPlayer extends string = string,
  TAccountAgentAccount extends string = string,
  TAccountCredit extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** The player to revoke their own key at any time; anyone once the pick phase is over or the key has expired. */
  caller: TransactionSigner<TAccountCaller>;
  arena?: Address<TAccountArena>;
  game?: Address<TAccountGame>;
  player: Address<TAccountPlayer>;
  agentAccount?: Address<TAccountAgentAccount>;
  credit?: Address<TAccountCredit>;
  systemProgram?: Address<TAccountSystemProgram>;
  matchId: PublicReleaseAgentInstructionDataArgs["matchId"];
};

export async function getPublicReleaseAgentInstructionAsync<
  TAccountCaller extends string,
  TAccountArena extends string,
  TAccountGame extends string,
  TAccountPlayer extends string,
  TAccountAgentAccount extends string,
  TAccountCredit extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGARI_ARENA_PROGRAM_ADDRESS,
>(
  input: PublicReleaseAgentAsyncInput<
    TAccountCaller,
    TAccountArena,
    TAccountGame,
    TAccountPlayer,
    TAccountAgentAccount,
    TAccountCredit,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  PublicReleaseAgentInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountArena,
    TAccountGame,
    TAccountPlayer,
    TAccountAgentAccount,
    TAccountCredit,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? AGARI_ARENA_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: true },
    arena: { value: input.arena ?? null, isWritable: true },
    game: { value: input.game ?? null, isWritable: false },
    player: { value: input.player ?? null, isWritable: false },
    agentAccount: { value: input.agentAccount ?? null, isWritable: true },
    credit: { value: input.credit ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.arena.value) {
    accounts.arena.value = await findArenaPda({ programAddress });
  }
  if (!accounts.game.value) {
    accounts.game.value = await findGamePda(
      { matchId: getNonNullResolvedInstructionInput("matchId", args.matchId) },
      { programAddress },
    );
  }
  if (!accounts.agentAccount.value) {
    accounts.agentAccount.value = await findAgentAccountPda(
      {
        matchId: getNonNullResolvedInstructionInput("matchId", args.matchId),
        player: getAddressFromResolvedInstructionAccount(
          "player",
          accounts.player.value,
        ),
      },
      { programAddress },
    );
  }
  if (!accounts.credit.value) {
    accounts.credit.value = await findCreditPda(
      {
        player: getAddressFromResolvedInstructionAccount(
          "player",
          accounts.player.value,
        ),
      },
      { programAddress },
    );
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("caller", accounts.caller),
      getAccountMeta("arena", accounts.arena),
      getAccountMeta("game", accounts.game),
      getAccountMeta("player", accounts.player),
      getAccountMeta("agentAccount", accounts.agentAccount),
      getAccountMeta("credit", accounts.credit),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getPublicReleaseAgentInstructionDataEncoder().encode(
      args as PublicReleaseAgentInstructionDataArgs,
    ),
    programAddress,
  } as PublicReleaseAgentInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountArena,
    TAccountGame,
    TAccountPlayer,
    TAccountAgentAccount,
    TAccountCredit,
    TAccountSystemProgram
  >);
}

export type PublicReleaseAgentInput<
  TAccountCaller extends string = string,
  TAccountArena extends string = string,
  TAccountGame extends string = string,
  TAccountPlayer extends string = string,
  TAccountAgentAccount extends string = string,
  TAccountCredit extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** The player to revoke their own key at any time; anyone once the pick phase is over or the key has expired. */
  caller: TransactionSigner<TAccountCaller>;
  arena: Address<TAccountArena>;
  game: Address<TAccountGame>;
  player: Address<TAccountPlayer>;
  agentAccount: Address<TAccountAgentAccount>;
  credit: Address<TAccountCredit>;
  systemProgram?: Address<TAccountSystemProgram>;
  matchId: PublicReleaseAgentInstructionDataArgs["matchId"];
};

export function getPublicReleaseAgentInstruction<
  TAccountCaller extends string,
  TAccountArena extends string,
  TAccountGame extends string,
  TAccountPlayer extends string,
  TAccountAgentAccount extends string,
  TAccountCredit extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGARI_ARENA_PROGRAM_ADDRESS,
>(
  input: PublicReleaseAgentInput<
    TAccountCaller,
    TAccountArena,
    TAccountGame,
    TAccountPlayer,
    TAccountAgentAccount,
    TAccountCredit,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): PublicReleaseAgentInstruction<
  TProgramAddress,
  TAccountCaller,
  TAccountArena,
  TAccountGame,
  TAccountPlayer,
  TAccountAgentAccount,
  TAccountCredit,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? AGARI_ARENA_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: true },
    arena: { value: input.arena ?? null, isWritable: true },
    game: { value: input.game ?? null, isWritable: false },
    player: { value: input.player ?? null, isWritable: false },
    agentAccount: { value: input.agentAccount ?? null, isWritable: true },
    credit: { value: input.credit ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("caller", accounts.caller),
      getAccountMeta("arena", accounts.arena),
      getAccountMeta("game", accounts.game),
      getAccountMeta("player", accounts.player),
      getAccountMeta("agentAccount", accounts.agentAccount),
      getAccountMeta("credit", accounts.credit),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getPublicReleaseAgentInstructionDataEncoder().encode(
      args as PublicReleaseAgentInstructionDataArgs,
    ),
    programAddress,
  } as PublicReleaseAgentInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountArena,
    TAccountGame,
    TAccountPlayer,
    TAccountAgentAccount,
    TAccountCredit,
    TAccountSystemProgram
  >);
}

export type ParsedPublicReleaseAgentInstruction<
  TProgram extends string = typeof AGARI_ARENA_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** The player to revoke their own key at any time; anyone once the pick phase is over or the key has expired. */
    caller: TAccountMetas[0];
    arena: TAccountMetas[1];
    game: TAccountMetas[2];
    player: TAccountMetas[3];
    agentAccount: TAccountMetas[4];
    credit: TAccountMetas[5];
    systemProgram: TAccountMetas[6];
  };
  data: PublicReleaseAgentInstructionData;
};

export function parsePublicReleaseAgentInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedPublicReleaseAgentInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 7) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 7,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      caller: getNextAccount(),
      arena: getNextAccount(),
      game: getNextAccount(),
      player: getNextAccount(),
      agentAccount: getNextAccount(),
      credit: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getPublicReleaseAgentInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
