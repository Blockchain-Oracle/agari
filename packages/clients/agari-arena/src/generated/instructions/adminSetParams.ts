/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findArenaPda } from "../pdas";
import { AGARI_ARENA_PROGRAM_ADDRESS } from "../programs";
import {
  getArenaParamsDecoder,
  getArenaParamsEncoder,
  type ArenaParams,
  type ArenaParamsArgs,
} from "../types";

export const ADMIN_SET_PARAMS_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([194, 243, 1, 30, 162, 133, 247, 72]);

export function getAdminSetParamsDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ADMIN_SET_PARAMS_DISCRIMINATOR,
  );
}

export type AdminSetParamsInstruction<
  TProgram extends string = typeof AGARI_ARENA_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountArena extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? ReadonlySignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountArena extends string
        ? WritableAccount<TAccountArena>
        : TAccountArena,
      ...TRemainingAccounts,
    ]
  >;

export type AdminSetParamsInstructionData = {
  discriminator: ReadonlyUint8Array;
  params: ArenaParams;
};

export type AdminSetParamsInstructionDataArgs = { params: ArenaParamsArgs };

export function getAdminSetParamsInstructionDataEncoder(): FixedSizeEncoder<AdminSetParamsInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["params", getArenaParamsEncoder()],
    ]),
    (value) => ({ ...value, discriminator: ADMIN_SET_PARAMS_DISCRIMINATOR }),
  );
}

export function getAdminSetParamsInstructionDataDecoder(): FixedSizeDecoder<AdminSetParamsInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["params", getArenaParamsDecoder()],
  ]);
}

export function getAdminSetParamsInstructionDataCodec(): FixedSizeCodec<
  AdminSetParamsInstructionDataArgs,
  AdminSetParamsInstructionData
> {
  return combineCodec(
    getAdminSetParamsInstructionDataEncoder(),
    getAdminSetParamsInstructionDataDecoder(),
  );
}

export type AdminSetParamsAsyncInput<
  TAccountAdmin extends string = string,
  TAccountArena extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  arena?: Address<TAccountArena>;
  params: AdminSetParamsInstructionDataArgs["params"];
};

export async function getAdminSetParamsInstructionAsync<
  TAccountAdmin extends string,
  TAccountArena extends string,
  TProgramAddress extends Address = typeof AGARI_ARENA_PROGRAM_ADDRESS,
>(
  input: AdminSetParamsAsyncInput<TAccountAdmin, TAccountArena>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  AdminSetParamsInstruction<TProgramAddress, TAccountAdmin, TAccountArena>
> {
  // Program address.
  const programAddress = config?.programAddress ?? AGARI_ARENA_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    arena: { value: input.arena ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.arena.value) {
    accounts.arena.value = await findArenaPda({ programAddress });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("arena", accounts.arena),
    ],
    data: getAdminSetParamsInstructionDataEncoder().encode(
      args as AdminSetParamsInstructionDataArgs,
    ),
    programAddress,
  } as AdminSetParamsInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountArena
  >);
}

export type AdminSetParamsInput<
  TAccountAdmin extends string = string,
  TAccountArena extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  arena: Address<TAccountArena>;
  params: AdminSetParamsInstructionDataArgs["params"];
};

export function getAdminSetParamsInstruction<
  TAccountAdmin extends string,
  TAccountArena extends string,
  TProgramAddress extends Address = typeof AGARI_ARENA_PROGRAM_ADDRESS,
>(
  input: AdminSetParamsInput<TAccountAdmin, TAccountArena>,
  config?: { programAddress?: TProgramAddress },
): AdminSetParamsInstruction<TProgramAddress, TAccountAdmin, TAccountArena> {
  // Program address.
  const programAddress = config?.programAddress ?? AGARI_ARENA_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    arena: { value: input.arena ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("arena", accounts.arena),
    ],
    data: getAdminSetParamsInstructionDataEncoder().encode(
      args as AdminSetParamsInstructionDataArgs,
    ),
    programAddress,
  } as AdminSetParamsInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountArena
  >);
}

export type ParsedAdminSetParamsInstruction<
  TProgram extends string = typeof AGARI_ARENA_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    arena: TAccountMetas[1];
  };
  data: AdminSetParamsInstructionData;
};

export function parseAdminSetParamsInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAdminSetParamsInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { admin: getNextAccount(), arena: getNextAccount() },
    data: getAdminSetParamsInstructionDataDecoder().decode(instruction.data),
  };
}
