/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findArenaPda } from "../pdas";
import { AGARI_ARENA_PROGRAM_ADDRESS } from "../programs";
import {
  getTierDecoder,
  getTierEncoder,
  type Tier,
  type TierArgs,
} from "../types";

export const ADMIN_SET_TIER_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  110, 42, 183, 137, 242, 72, 237, 81,
]);

export function getAdminSetTierDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ADMIN_SET_TIER_DISCRIMINATOR,
  );
}

export type AdminSetTierInstruction<
  TProgram extends string = typeof AGARI_ARENA_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountArena extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? ReadonlySignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountArena extends string
        ? WritableAccount<TAccountArena>
        : TAccountArena,
      ...TRemainingAccounts,
    ]
  >;

export type AdminSetTierInstructionData = {
  discriminator: ReadonlyUint8Array;
  tier: number;
  value: Tier;
};

export type AdminSetTierInstructionDataArgs = { tier: number; value: TierArgs };

export function getAdminSetTierInstructionDataEncoder(): FixedSizeEncoder<AdminSetTierInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["tier", getU8Encoder()],
      ["value", getTierEncoder()],
    ]),
    (value) => ({ ...value, discriminator: ADMIN_SET_TIER_DISCRIMINATOR }),
  );
}

export function getAdminSetTierInstructionDataDecoder(): FixedSizeDecoder<AdminSetTierInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["tier", getU8Decoder()],
    ["value", getTierDecoder()],
  ]);
}

export function getAdminSetTierInstructionDataCodec(): FixedSizeCodec<
  AdminSetTierInstructionDataArgs,
  AdminSetTierInstructionData
> {
  return combineCodec(
    getAdminSetTierInstructionDataEncoder(),
    getAdminSetTierInstructionDataDecoder(),
  );
}

export type AdminSetTierAsyncInput<
  TAccountAdmin extends string = string,
  TAccountArena extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  arena?: Address<TAccountArena>;
  tier: AdminSetTierInstructionDataArgs["tier"];
  value: AdminSetTierInstructionDataArgs["value"];
};

export async function getAdminSetTierInstructionAsync<
  TAccountAdmin extends string,
  TAccountArena extends string,
  TProgramAddress extends Address = typeof AGARI_ARENA_PROGRAM_ADDRESS,
>(
  input: AdminSetTierAsyncInput<TAccountAdmin, TAccountArena>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  AdminSetTierInstruction<TProgramAddress, TAccountAdmin, TAccountArena>
> {
  // Program address.
  const programAddress = config?.programAddress ?? AGARI_ARENA_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    arena: { value: input.arena ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.arena.value) {
    accounts.arena.value = await findArenaPda({ programAddress });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("arena", accounts.arena),
    ],
    data: getAdminSetTierInstructionDataEncoder().encode(
      args as AdminSetTierInstructionDataArgs,
    ),
    programAddress,
  } as AdminSetTierInstruction<TProgramAddress, TAccountAdmin, TAccountArena>);
}

export type AdminSetTierInput<
  TAccountAdmin extends string = string,
  TAccountArena extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  arena: Address<TAccountArena>;
  tier: AdminSetTierInstructionDataArgs["tier"];
  value: AdminSetTierInstructionDataArgs["value"];
};

export function getAdminSetTierInstruction<
  TAccountAdmin extends string,
  TAccountArena extends string,
  TProgramAddress extends Address = typeof AGARI_ARENA_PROGRAM_ADDRESS,
>(
  input: AdminSetTierInput<TAccountAdmin, TAccountArena>,
  config?: { programAddress?: TProgramAddress },
): AdminSetTierInstruction<TProgramAddress, TAccountAdmin, TAccountArena> {
  // Program address.
  const programAddress = config?.programAddress ?? AGARI_ARENA_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    arena: { value: input.arena ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("arena", accounts.arena),
    ],
    data: getAdminSetTierInstructionDataEncoder().encode(
      args as AdminSetTierInstructionDataArgs,
    ),
    programAddress,
  } as AdminSetTierInstruction<TProgramAddress, TAccountAdmin, TAccountArena>);
}

export type ParsedAdminSetTierInstruction<
  TProgram extends string = typeof AGARI_ARENA_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    arena: TAccountMetas[1];
  };
  data: AdminSetTierInstructionData;
};

export function parseAdminSetTierInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAdminSetTierInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { admin: getNextAccount(), arena: getNextAccount() },
    data: getAdminSetTierInstructionDataDecoder().decode(instruction.data),
  };
}
