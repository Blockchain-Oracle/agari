/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getBooleanDecoder,
  getBooleanEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
} from "@solana/kit";

/**
 * One entry price. `pot_base` is what each player escrows; `per_card_cap_base` is the most one card's order may
 * spend. Free is a tier with a zero pot: its picks are still real orders.
 */
export type Tier = {
  potBase: bigint;
  perCardCapBase: bigint;
  enabled: boolean;
};

export type TierArgs = {
  potBase: number | bigint;
  perCardCapBase: number | bigint;
  enabled: boolean;
};

export function getTierEncoder(): FixedSizeEncoder<TierArgs> {
  return getStructEncoder([
    ["potBase", getU64Encoder()],
    ["perCardCapBase", getU64Encoder()],
    ["enabled", getBooleanEncoder()],
  ]);
}

export function getTierDecoder(): FixedSizeDecoder<Tier> {
  return getStructDecoder([
    ["potBase", getU64Decoder()],
    ["perCardCapBase", getU64Decoder()],
    ["enabled", getBooleanDecoder()],
  ]);
}

export function getTierCodec(): FixedSizeCodec<TierArgs, Tier> {
  return combineCodec(getTierEncoder(), getTierDecoder());
}
