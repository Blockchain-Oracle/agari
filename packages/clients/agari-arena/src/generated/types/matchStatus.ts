/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getEnumDecoder,
  getEnumEncoder,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
} from "@solana/kit";

/**
 * WAITING until the challenger joins; ACTIVE_UNREVEALED until the deck is opened; PICKING while both players still
 * owe cards; then SETTLING (both complete), FORFEITED (one absent) or REFUNDED (nobody's fault). FINALIZED is the
 * pot's terminal state. Cards may settle in any of the last four, because a match that lost its pot still owns real
 * positions on the book. The order is `packages/core/src/games/types.ts`'s `ARENA_STATUSES`.
 */
export enum MatchStatus {
  Waiting,
  ActiveUnrevealed,
  Picking,
  Settling,
  Finalized,
  Refunded,
  Forfeited,
}

export type MatchStatusArgs = MatchStatus;

export function getMatchStatusEncoder(): FixedSizeEncoder<MatchStatusArgs> {
  return getEnumEncoder(MatchStatus);
}

export function getMatchStatusDecoder(): FixedSizeDecoder<MatchStatus> {
  return getEnumDecoder(MatchStatus);
}

export function getMatchStatusCodec(): FixedSizeCodec<
  MatchStatusArgs,
  MatchStatus
> {
  return combineCodec(getMatchStatusEncoder(), getMatchStatusDecoder());
}
