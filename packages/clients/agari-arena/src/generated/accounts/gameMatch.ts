/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getMatchStatusDecoder,
  getMatchStatusEncoder,
  getPickRecordDecoder,
  getPickRecordEncoder,
  type MatchStatus,
  type MatchStatusArgs,
  type PickRecord,
  type PickRecordArgs,
} from "../types";

export const GAME_MATCH_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  87, 187, 102, 98, 236, 52, 127, 39,
]);

export function getGameMatchDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(GAME_MATCH_DISCRIMINATOR);
}

export type GameMatch = {
  discriminator: ReadonlyUint8Array;
  matchId: ReadonlyUint8Array;
  creator: Address;
  challenger: Address;
  tier: number;
  status: MatchStatus;
  deckSize: number;
  /** One bit per card, per seat: set when that seat's pick is on the book. */
  pickedMask0: number;
  pickedMask1: number;
  /** One bit per card: set when both seats' picks on it have been redeemed. */
  settledMask: number;
  /** Bumped whenever the deck-selection rules change; part of the commitment preimage. */
  policyVersion: number;
  deckHash: ReadonlyUint8Array;
  createdAtSec: bigint;
  joinedAtSec: bigint;
  revealedAtSec: bigint;
  pickDeadlineSec: bigint;
  potBase: bigint;
  perCardCapBase: bigint;
  cards: Array<Address>;
  /** Keyed `card_index × 2 + seat`, so one card's two picks sit beside each other. */
  picks: Array<PickRecord>;
  /** `payout − cost` summed over settled cards, per seat. */
  pnlBase: Array<bigint>;
  bump: number;
};

export type GameMatchArgs = {
  matchId: ReadonlyUint8Array;
  creator: Address;
  challenger: Address;
  tier: number;
  status: MatchStatusArgs;
  deckSize: number;
  /** One bit per card, per seat: set when that seat's pick is on the book. */
  pickedMask0: number;
  pickedMask1: number;
  /** One bit per card: set when both seats' picks on it have been redeemed. */
  settledMask: number;
  /** Bumped whenever the deck-selection rules change; part of the commitment preimage. */
  policyVersion: number;
  deckHash: ReadonlyUint8Array;
  createdAtSec: number | bigint;
  joinedAtSec: number | bigint;
  revealedAtSec: number | bigint;
  pickDeadlineSec: number | bigint;
  potBase: number | bigint;
  perCardCapBase: number | bigint;
  cards: Array<Address>;
  /** Keyed `card_index × 2 + seat`, so one card's two picks sit beside each other. */
  picks: Array<PickRecordArgs>;
  /** `payout − cost` summed over settled cards, per seat. */
  pnlBase: Array<number | bigint>;
  bump: number;
};

/** Gets the encoder for {@link GameMatchArgs} account data. */
export function getGameMatchEncoder(): FixedSizeEncoder<GameMatchArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["matchId", fixEncoderSize(getBytesEncoder(), 32)],
      ["creator", getAddressEncoder()],
      ["challenger", getAddressEncoder()],
      ["tier", getU8Encoder()],
      ["status", getMatchStatusEncoder()],
      ["deckSize", getU8Encoder()],
      ["pickedMask0", getU8Encoder()],
      ["pickedMask1", getU8Encoder()],
      ["settledMask", getU8Encoder()],
      ["policyVersion", getU32Encoder()],
      ["deckHash", fixEncoderSize(getBytesEncoder(), 32)],
      ["createdAtSec", getI64Encoder()],
      ["joinedAtSec", getI64Encoder()],
      ["revealedAtSec", getI64Encoder()],
      ["pickDeadlineSec", getI64Encoder()],
      ["potBase", getU64Encoder()],
      ["perCardCapBase", getU64Encoder()],
      ["cards", getArrayEncoder(getAddressEncoder(), { size: 8 })],
      ["picks", getArrayEncoder(getPickRecordEncoder(), { size: 16 })],
      ["pnlBase", getArrayEncoder(getI64Encoder(), { size: 2 })],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: GAME_MATCH_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link GameMatch} account data. */
export function getGameMatchDecoder(): FixedSizeDecoder<GameMatch> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["matchId", fixDecoderSize(getBytesDecoder(), 32)],
    ["creator", getAddressDecoder()],
    ["challenger", getAddressDecoder()],
    ["tier", getU8Decoder()],
    ["status", getMatchStatusDecoder()],
    ["deckSize", getU8Decoder()],
    ["pickedMask0", getU8Decoder()],
    ["pickedMask1", getU8Decoder()],
    ["settledMask", getU8Decoder()],
    ["policyVersion", getU32Decoder()],
    ["deckHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["createdAtSec", getI64Decoder()],
    ["joinedAtSec", getI64Decoder()],
    ["revealedAtSec", getI64Decoder()],
    ["pickDeadlineSec", getI64Decoder()],
    ["potBase", getU64Decoder()],
    ["perCardCapBase", getU64Decoder()],
    ["cards", getArrayDecoder(getAddressDecoder(), { size: 8 })],
    ["picks", getArrayDecoder(getPickRecordDecoder(), { size: 16 })],
    ["pnlBase", getArrayDecoder(getI64Decoder(), { size: 2 })],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link GameMatch} account data. */
export function getGameMatchCodec(): FixedSizeCodec<GameMatchArgs, GameMatch> {
  return combineCodec(getGameMatchEncoder(), getGameMatchDecoder());
}

export function decodeGameMatch<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<GameMatch, TAddress>;
export function decodeGameMatch<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<GameMatch, TAddress>;
export function decodeGameMatch<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<GameMatch, TAddress> | MaybeAccount<GameMatch, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getGameMatchDecoder(),
  );
}

export async function fetchGameMatch<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<GameMatch, TAddress>> {
  const maybeAccount = await fetchMaybeGameMatch(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeGameMatch<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<GameMatch, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeGameMatch(maybeAccount);
}

export async function fetchAllGameMatch(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<GameMatch>[]> {
  const maybeAccounts = await fetchAllMaybeGameMatch(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeGameMatch(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<GameMatch>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeGameMatch(maybeAccount));
}

export function getGameMatchSize(): number {
  return 1027;
}
