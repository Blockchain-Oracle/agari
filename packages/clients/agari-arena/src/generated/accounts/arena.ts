/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getArenaParamsDecoder,
  getArenaParamsEncoder,
  getTierDecoder,
  getTierEncoder,
  type ArenaParams,
  type ArenaParamsArgs,
  type Tier,
  type TierArgs,
} from "../types";

export const ARENA_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  243, 215, 44, 44, 231, 211, 232, 168,
]);

export function getArenaDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(ARENA_DISCRIMINATOR);
}

export type Arena = {
  discriminator: ReadonlyUint8Array;
  admin: Address;
  collateralMint: Address;
  /** The cluster id the deck commitment carries, fixed at init, so a deck committed for one cluster opens nothing on another. */
  chainId: bigint;
  params: ArenaParams;
  tiers: Array<Tier>;
  /** Pots held for undecided matches. */
  escrowedBase: bigint;
  /** Credits owed and unclaimed: payouts, pots won, pots refunded, a key's unspent escrow. */
  creditedBase: bigint;
  /** Stakes players have set aside for their seats' keys. */
  agentEscrowBase: bigint;
  paused: boolean;
  bump: number;
  seatBump: number;
  custodyBump: number;
};

export type ArenaArgs = {
  admin: Address;
  collateralMint: Address;
  /** The cluster id the deck commitment carries, fixed at init, so a deck committed for one cluster opens nothing on another. */
  chainId: number | bigint;
  params: ArenaParamsArgs;
  tiers: Array<TierArgs>;
  /** Pots held for undecided matches. */
  escrowedBase: number | bigint;
  /** Credits owed and unclaimed: payouts, pots won, pots refunded, a key's unspent escrow. */
  creditedBase: number | bigint;
  /** Stakes players have set aside for their seats' keys. */
  agentEscrowBase: number | bigint;
  paused: boolean;
  bump: number;
  seatBump: number;
  custodyBump: number;
};

/** Gets the encoder for {@link ArenaArgs} account data. */
export function getArenaEncoder(): FixedSizeEncoder<ArenaArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["admin", getAddressEncoder()],
      ["collateralMint", getAddressEncoder()],
      ["chainId", getU64Encoder()],
      ["params", getArenaParamsEncoder()],
      ["tiers", getArrayEncoder(getTierEncoder(), { size: 4 })],
      ["escrowedBase", getU64Encoder()],
      ["creditedBase", getU64Encoder()],
      ["agentEscrowBase", getU64Encoder()],
      ["paused", getBooleanEncoder()],
      ["bump", getU8Encoder()],
      ["seatBump", getU8Encoder()],
      ["custodyBump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: ARENA_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link Arena} account data. */
export function getArenaDecoder(): FixedSizeDecoder<Arena> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["admin", getAddressDecoder()],
    ["collateralMint", getAddressDecoder()],
    ["chainId", getU64Decoder()],
    ["params", getArenaParamsDecoder()],
    ["tiers", getArrayDecoder(getTierDecoder(), { size: 4 })],
    ["escrowedBase", getU64Decoder()],
    ["creditedBase", getU64Decoder()],
    ["agentEscrowBase", getU64Decoder()],
    ["paused", getBooleanDecoder()],
    ["bump", getU8Decoder()],
    ["seatBump", getU8Decoder()],
    ["custodyBump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link Arena} account data. */
export function getArenaCodec(): FixedSizeCodec<ArenaArgs, Arena> {
  return combineCodec(getArenaEncoder(), getArenaDecoder());
}

export function decodeArena<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<Arena, TAddress>;
export function decodeArena<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<Arena, TAddress>;
export function decodeArena<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<Arena, TAddress> | MaybeAccount<Arena, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getArenaDecoder(),
  );
}

export async function fetchArena<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Arena, TAddress>> {
  const maybeAccount = await fetchMaybeArena(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeArena<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Arena, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeArena(maybeAccount);
}

export async function fetchAllArena(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Arena>[]> {
  const maybeAccounts = await fetchAllMaybeArena(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeArena(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Arena>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeArena(maybeAccount));
}

export function getArenaSize(): number {
  return 194;
}
