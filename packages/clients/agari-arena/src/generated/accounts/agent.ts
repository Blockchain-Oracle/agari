/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const AGENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  47, 166, 112, 147, 155, 197, 86, 7,
]);

export function getAgentDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(AGENT_DISCRIMINATOR);
}

export type Agent = {
  discriminator: ReadonlyUint8Array;
  agent: Address;
  expiresAtSec: bigint;
  budgetBase: bigint;
  /** Gross: the stakes handed over, not the costs after refund, so the ceiling is the deck's and never a guess about fills. */
  spentBase: bigint;
  /** What is still set aside for this key's picks. */
  escrowBase: bigint;
  bump: number;
};

export type AgentArgs = {
  agent: Address;
  expiresAtSec: number | bigint;
  budgetBase: number | bigint;
  /** Gross: the stakes handed over, not the costs after refund, so the ceiling is the deck's and never a guess about fills. */
  spentBase: number | bigint;
  /** What is still set aside for this key's picks. */
  escrowBase: number | bigint;
  bump: number;
};

/** Gets the encoder for {@link AgentArgs} account data. */
export function getAgentEncoder(): FixedSizeEncoder<AgentArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["agent", getAddressEncoder()],
      ["expiresAtSec", getI64Encoder()],
      ["budgetBase", getU64Encoder()],
      ["spentBase", getU64Encoder()],
      ["escrowBase", getU64Encoder()],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: AGENT_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link Agent} account data. */
export function getAgentDecoder(): FixedSizeDecoder<Agent> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["agent", getAddressDecoder()],
    ["expiresAtSec", getI64Decoder()],
    ["budgetBase", getU64Decoder()],
    ["spentBase", getU64Decoder()],
    ["escrowBase", getU64Decoder()],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link Agent} account data. */
export function getAgentCodec(): FixedSizeCodec<AgentArgs, Agent> {
  return combineCodec(getAgentEncoder(), getAgentDecoder());
}

export function decodeAgent<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<Agent, TAddress>;
export function decodeAgent<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<Agent, TAddress>;
export function decodeAgent<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<Agent, TAddress> | MaybeAccount<Agent, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getAgentDecoder(),
  );
}

export async function fetchAgent<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Agent, TAddress>> {
  const maybeAccount = await fetchMaybeAgent(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeAgent<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Agent, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeAgent(maybeAccount);
}

export async function fetchAllAgent(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Agent>[]> {
  const maybeAccounts = await fetchAllMaybeAgent(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeAgent(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Agent>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeAgent(maybeAccount));
}

export function getAgentSize(): number {
  return 73;
}
