/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const SEASON_POOL_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  236, 98, 219, 250, 20, 50, 140, 239,
]);

export function getSeasonPoolDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(SEASON_POOL_DISCRIMINATOR);
}

export type SeasonPool = {
  discriminator: ReadonlyUint8Array;
  admin: Address;
  collateralMint: Address;
  seasonId: string;
  endsAtSec: bigint;
  /** Set once `admin_distribute_season` runs; blocks a second distribution. */
  distributed: boolean;
  /** Everything ever deposited, so a page can say how the pool was built and not only what is left. */
  depositedBase: bigint;
  bump: number;
  vaultBump: number;
};

export type SeasonPoolArgs = {
  admin: Address;
  collateralMint: Address;
  seasonId: string;
  endsAtSec: number | bigint;
  /** Set once `admin_distribute_season` runs; blocks a second distribution. */
  distributed: boolean;
  /** Everything ever deposited, so a page can say how the pool was built and not only what is left. */
  depositedBase: number | bigint;
  bump: number;
  vaultBump: number;
};

/** Gets the encoder for {@link SeasonPoolArgs} account data. */
export function getSeasonPoolEncoder(): Encoder<SeasonPoolArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["admin", getAddressEncoder()],
      ["collateralMint", getAddressEncoder()],
      ["seasonId", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["endsAtSec", getI64Encoder()],
      ["distributed", getBooleanEncoder()],
      ["depositedBase", getU64Encoder()],
      ["bump", getU8Encoder()],
      ["vaultBump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SEASON_POOL_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link SeasonPool} account data. */
export function getSeasonPoolDecoder(): Decoder<SeasonPool> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["admin", getAddressDecoder()],
    ["collateralMint", getAddressDecoder()],
    ["seasonId", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["endsAtSec", getI64Decoder()],
    ["distributed", getBooleanDecoder()],
    ["depositedBase", getU64Decoder()],
    ["bump", getU8Decoder()],
    ["vaultBump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link SeasonPool} account data. */
export function getSeasonPoolCodec(): Codec<SeasonPoolArgs, SeasonPool> {
  return combineCodec(getSeasonPoolEncoder(), getSeasonPoolDecoder());
}

export function decodeSeasonPool<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<SeasonPool, TAddress>;
export function decodeSeasonPool<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<SeasonPool, TAddress>;
export function decodeSeasonPool<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<SeasonPool, TAddress> | MaybeAccount<SeasonPool, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getSeasonPoolDecoder(),
  );
}

export async function fetchSeasonPool<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<SeasonPool, TAddress>> {
  const maybeAccount = await fetchMaybeSeasonPool(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeSeasonPool<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<SeasonPool, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeSeasonPool(maybeAccount);
}

export async function fetchAllSeasonPool(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<SeasonPool>[]> {
  const maybeAccounts = await fetchAllMaybeSeasonPool(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeSeasonPool(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<SeasonPool>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeSeasonPool(maybeAccount));
}
