/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const CREDIT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  6, 79, 161, 136, 156, 172, 190, 149,
]);

export function getCreditDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(CREDIT_DISCRIMINATOR);
}

export type Credit = {
  discriminator: ReadonlyUint8Array;
  player: Address;
  amountBase: bigint;
  bump: number;
};

export type CreditArgs = {
  player: Address;
  amountBase: number | bigint;
  bump: number;
};

/** Gets the encoder for {@link CreditArgs} account data. */
export function getCreditEncoder(): FixedSizeEncoder<CreditArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["player", getAddressEncoder()],
      ["amountBase", getU64Encoder()],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: CREDIT_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link Credit} account data. */
export function getCreditDecoder(): FixedSizeDecoder<Credit> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["player", getAddressDecoder()],
    ["amountBase", getU64Decoder()],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link Credit} account data. */
export function getCreditCodec(): FixedSizeCodec<CreditArgs, Credit> {
  return combineCodec(getCreditEncoder(), getCreditDecoder());
}

export function decodeCredit<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<Credit, TAddress>;
export function decodeCredit<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<Credit, TAddress>;
export function decodeCredit<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<Credit, TAddress> | MaybeAccount<Credit, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getCreditDecoder(),
  );
}

export async function fetchCredit<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Credit, TAddress>> {
  const maybeAccount = await fetchMaybeCredit(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeCredit<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Credit, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeCredit(maybeAccount);
}

export async function fetchAllCredit(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Credit>[]> {
  const maybeAccounts = await fetchAllMaybeCredit(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeCredit(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Credit>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeCredit(maybeAccount));
}

export function getCreditSize(): number {
  return 49;
}
