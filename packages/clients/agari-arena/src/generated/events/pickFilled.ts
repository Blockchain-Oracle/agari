/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const PICK_FILLED_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([180, 128, 140, 233, 52, 28, 88, 101]);

export function getPickFilledEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PICK_FILLED_EVENT_DISCRIMINATOR,
  );
}

export type PickFilledEvent = {
  matchId: ReadonlyUint8Array;
  player: Address;
  market: Address;
  cardIndex: number;
  outcome: number;
  /** Contracts, in raw units (`lots × lot_base`). */
  quantityRaw: bigint;
  costBase: bigint;
  refundBase: bigint;
};

export type PickFilledEventArgs = {
  matchId: ReadonlyUint8Array;
  player: Address;
  market: Address;
  cardIndex: number;
  outcome: number;
  /** Contracts, in raw units (`lots × lot_base`). */
  quantityRaw: number | bigint;
  costBase: number | bigint;
  refundBase: number | bigint;
};

/** Gets the encoder for {@link PickFilledEventArgs} event data. */
export function getPickFilledEventEncoder(): FixedSizeEncoder<PickFilledEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["matchId", fixEncoderSize(getBytesEncoder(), 32)],
      ["player", getAddressEncoder()],
      ["market", getAddressEncoder()],
      ["cardIndex", getU8Encoder()],
      ["outcome", getU8Encoder()],
      ["quantityRaw", getU64Encoder()],
      ["costBase", getU64Encoder()],
      ["refundBase", getU64Encoder()],
    ]),
    [getConstantEncoder(PICK_FILLED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link PickFilledEvent} event data. */
export function getPickFilledEventDecoder(): FixedSizeDecoder<PickFilledEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["matchId", fixDecoderSize(getBytesDecoder(), 32)],
      ["player", getAddressDecoder()],
      ["market", getAddressDecoder()],
      ["cardIndex", getU8Decoder()],
      ["outcome", getU8Decoder()],
      ["quantityRaw", getU64Decoder()],
      ["costBase", getU64Decoder()],
      ["refundBase", getU64Decoder()],
    ]),
    [getConstantDecoder(PICK_FILLED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link PickFilledEvent} event data. */
export function getPickFilledEventCodec(): FixedSizeCodec<
  PickFilledEventArgs,
  PickFilledEvent
> {
  return combineCodec(getPickFilledEventEncoder(), getPickFilledEventDecoder());
}

/** Parses the provided bytes into {@link PickFilledEvent} event data. */
export function parsePickFilledEvent(
  data: ReadonlyUint8Array | Uint8Array,
): PickFilledEvent {
  if (containsBytes(data, PICK_FILLED_EVENT_DISCRIMINATOR, 0)) {
    return getPickFilledEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "PickFilledEvent" event discriminators.',
  );
}
