/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const DECK_REVEALED_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([23, 87, 10, 63, 115, 132, 214, 102]);

export function getDeckRevealedEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    DECK_REVEALED_EVENT_DISCRIMINATOR,
  );
}

export type DeckRevealedEvent = {
  matchId: ReadonlyUint8Array;
  policyVersion: number;
  cards: Array<Address>;
  pickDeadlineSec: bigint;
};

export type DeckRevealedEventArgs = {
  matchId: ReadonlyUint8Array;
  policyVersion: number;
  cards: Array<Address>;
  pickDeadlineSec: number | bigint;
};

/** Gets the encoder for {@link DeckRevealedEventArgs} event data. */
export function getDeckRevealedEventEncoder(): Encoder<DeckRevealedEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["matchId", fixEncoderSize(getBytesEncoder(), 32)],
      ["policyVersion", getU32Encoder()],
      ["cards", getArrayEncoder(getAddressEncoder())],
      ["pickDeadlineSec", getI64Encoder()],
    ]),
    [getConstantEncoder(DECK_REVEALED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link DeckRevealedEvent} event data. */
export function getDeckRevealedEventDecoder(): Decoder<DeckRevealedEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["matchId", fixDecoderSize(getBytesDecoder(), 32)],
      ["policyVersion", getU32Decoder()],
      ["cards", getArrayDecoder(getAddressDecoder())],
      ["pickDeadlineSec", getI64Decoder()],
    ]),
    [getConstantDecoder(DECK_REVEALED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link DeckRevealedEvent} event data. */
export function getDeckRevealedEventCodec(): Codec<
  DeckRevealedEventArgs,
  DeckRevealedEvent
> {
  return combineCodec(
    getDeckRevealedEventEncoder(),
    getDeckRevealedEventDecoder(),
  );
}

/** Parses the provided bytes into {@link DeckRevealedEvent} event data. */
export function parseDeckRevealedEvent(
  data: ReadonlyUint8Array | Uint8Array,
): DeckRevealedEvent {
  if (containsBytes(data, DECK_REVEALED_EVENT_DISCRIMINATOR, 0)) {
    return getDeckRevealedEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "DeckRevealedEvent" event discriminators.',
  );
}
