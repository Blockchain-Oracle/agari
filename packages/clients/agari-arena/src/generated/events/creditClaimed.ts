/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const CREDIT_CLAIMED_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([173, 2, 172, 90, 249, 178, 171, 194]);

export function getCreditClaimedEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CREDIT_CLAIMED_EVENT_DISCRIMINATOR,
  );
}

export type CreditClaimedEvent = {
  player: Address;
  amountBase: bigint;
  by: Address;
};

export type CreditClaimedEventArgs = {
  player: Address;
  amountBase: number | bigint;
  by: Address;
};

/** Gets the encoder for {@link CreditClaimedEventArgs} event data. */
export function getCreditClaimedEventEncoder(): FixedSizeEncoder<CreditClaimedEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["player", getAddressEncoder()],
      ["amountBase", getU64Encoder()],
      ["by", getAddressEncoder()],
    ]),
    [getConstantEncoder(CREDIT_CLAIMED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link CreditClaimedEvent} event data. */
export function getCreditClaimedEventDecoder(): FixedSizeDecoder<CreditClaimedEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["player", getAddressDecoder()],
      ["amountBase", getU64Decoder()],
      ["by", getAddressDecoder()],
    ]),
    [getConstantDecoder(CREDIT_CLAIMED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link CreditClaimedEvent} event data. */
export function getCreditClaimedEventCodec(): FixedSizeCodec<
  CreditClaimedEventArgs,
  CreditClaimedEvent
> {
  return combineCodec(
    getCreditClaimedEventEncoder(),
    getCreditClaimedEventDecoder(),
  );
}

/** Parses the provided bytes into {@link CreditClaimedEvent} event data. */
export function parseCreditClaimedEvent(
  data: ReadonlyUint8Array | Uint8Array,
): CreditClaimedEvent {
  if (containsBytes(data, CREDIT_CLAIMED_EVENT_DISCRIMINATOR, 0)) {
    return getCreditClaimedEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "CreditClaimedEvent" event discriminators.',
  );
}
