/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const AGENT_RELEASED_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([73, 46, 234, 47, 212, 74, 70, 217]);

export function getAgentReleasedEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    AGENT_RELEASED_EVENT_DISCRIMINATOR,
  );
}

export type AgentReleasedEvent = {
  matchId: ReadonlyUint8Array;
  player: Address;
  amountBase: bigint;
};

export type AgentReleasedEventArgs = {
  matchId: ReadonlyUint8Array;
  player: Address;
  amountBase: number | bigint;
};

/** Gets the encoder for {@link AgentReleasedEventArgs} event data. */
export function getAgentReleasedEventEncoder(): FixedSizeEncoder<AgentReleasedEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["matchId", fixEncoderSize(getBytesEncoder(), 32)],
      ["player", getAddressEncoder()],
      ["amountBase", getU64Encoder()],
    ]),
    [getConstantEncoder(AGENT_RELEASED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link AgentReleasedEvent} event data. */
export function getAgentReleasedEventDecoder(): FixedSizeDecoder<AgentReleasedEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["matchId", fixDecoderSize(getBytesDecoder(), 32)],
      ["player", getAddressDecoder()],
      ["amountBase", getU64Decoder()],
    ]),
    [getConstantDecoder(AGENT_RELEASED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link AgentReleasedEvent} event data. */
export function getAgentReleasedEventCodec(): FixedSizeCodec<
  AgentReleasedEventArgs,
  AgentReleasedEvent
> {
  return combineCodec(
    getAgentReleasedEventEncoder(),
    getAgentReleasedEventDecoder(),
  );
}

/** Parses the provided bytes into {@link AgentReleasedEvent} event data. */
export function parseAgentReleasedEvent(
  data: ReadonlyUint8Array | Uint8Array,
): AgentReleasedEvent {
  if (containsBytes(data, AGENT_RELEASED_EVENT_DISCRIMINATOR, 0)) {
    return getAgentReleasedEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "AgentReleasedEvent" event discriminators.',
  );
}
