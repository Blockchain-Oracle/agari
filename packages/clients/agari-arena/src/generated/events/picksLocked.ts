/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getMatchStatusDecoder,
  getMatchStatusEncoder,
  type MatchStatus,
  type MatchStatusArgs,
} from "../types";

export const PICKS_LOCKED_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([196, 173, 1, 142, 140, 106, 236, 226]);

export function getPicksLockedEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PICKS_LOCKED_EVENT_DISCRIMINATOR,
  );
}

export type PicksLockedEvent = {
  matchId: ReadonlyUint8Array;
  status: MatchStatus;
  /** The seat that never finished; the default key when both did. */
  forfeitedBy: Address;
};

export type PicksLockedEventArgs = {
  matchId: ReadonlyUint8Array;
  status: MatchStatusArgs;
  /** The seat that never finished; the default key when both did. */
  forfeitedBy: Address;
};

/** Gets the encoder for {@link PicksLockedEventArgs} event data. */
export function getPicksLockedEventEncoder(): FixedSizeEncoder<PicksLockedEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["matchId", fixEncoderSize(getBytesEncoder(), 32)],
      ["status", getMatchStatusEncoder()],
      ["forfeitedBy", getAddressEncoder()],
    ]),
    [getConstantEncoder(PICKS_LOCKED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link PicksLockedEvent} event data. */
export function getPicksLockedEventDecoder(): FixedSizeDecoder<PicksLockedEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["matchId", fixDecoderSize(getBytesDecoder(), 32)],
      ["status", getMatchStatusDecoder()],
      ["forfeitedBy", getAddressDecoder()],
    ]),
    [getConstantDecoder(PICKS_LOCKED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link PicksLockedEvent} event data. */
export function getPicksLockedEventCodec(): FixedSizeCodec<
  PicksLockedEventArgs,
  PicksLockedEvent
> {
  return combineCodec(
    getPicksLockedEventEncoder(),
    getPicksLockedEventDecoder(),
  );
}

/** Parses the provided bytes into {@link PicksLockedEvent} event data. */
export function parsePicksLockedEvent(
  data: ReadonlyUint8Array | Uint8Array,
): PicksLockedEvent {
  if (containsBytes(data, PICKS_LOCKED_EVENT_DISCRIMINATOR, 0)) {
    return getPicksLockedEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "PicksLockedEvent" event discriminators.',
  );
}
