/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const CARD_SETTLED_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([34, 206, 169, 204, 178, 170, 227, 94]);

export function getCardSettledEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CARD_SETTLED_EVENT_DISCRIMINATOR,
  );
}

export type CardSettledEvent = {
  matchId: ReadonlyUint8Array;
  player: Address;
  market: Address;
  cardIndex: number;
  payoutBase: bigint;
  pnlBase: bigint;
};

export type CardSettledEventArgs = {
  matchId: ReadonlyUint8Array;
  player: Address;
  market: Address;
  cardIndex: number;
  payoutBase: number | bigint;
  pnlBase: number | bigint;
};

/** Gets the encoder for {@link CardSettledEventArgs} event data. */
export function getCardSettledEventEncoder(): FixedSizeEncoder<CardSettledEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["matchId", fixEncoderSize(getBytesEncoder(), 32)],
      ["player", getAddressEncoder()],
      ["market", getAddressEncoder()],
      ["cardIndex", getU8Encoder()],
      ["payoutBase", getU64Encoder()],
      ["pnlBase", getI64Encoder()],
    ]),
    [getConstantEncoder(CARD_SETTLED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link CardSettledEvent} event data. */
export function getCardSettledEventDecoder(): FixedSizeDecoder<CardSettledEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["matchId", fixDecoderSize(getBytesDecoder(), 32)],
      ["player", getAddressDecoder()],
      ["market", getAddressDecoder()],
      ["cardIndex", getU8Decoder()],
      ["payoutBase", getU64Decoder()],
      ["pnlBase", getI64Decoder()],
    ]),
    [getConstantDecoder(CARD_SETTLED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link CardSettledEvent} event data. */
export function getCardSettledEventCodec(): FixedSizeCodec<
  CardSettledEventArgs,
  CardSettledEvent
> {
  return combineCodec(
    getCardSettledEventEncoder(),
    getCardSettledEventDecoder(),
  );
}

/** Parses the provided bytes into {@link CardSettledEvent} event data. */
export function parseCardSettledEvent(
  data: ReadonlyUint8Array | Uint8Array,
): CardSettledEvent {
  if (containsBytes(data, CARD_SETTLED_EVENT_DISCRIMINATOR, 0)) {
    return getCardSettledEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "CardSettledEvent" event discriminators.',
  );
}
