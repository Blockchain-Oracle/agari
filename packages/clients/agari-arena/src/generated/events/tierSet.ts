/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const TIER_SET_EVENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  195, 162, 76, 118, 218, 200, 225, 238,
]);

export function getTierSetEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    TIER_SET_EVENT_DISCRIMINATOR,
  );
}

export type TierSetEvent = {
  tier: number;
  potBase: bigint;
  perCardCapBase: bigint;
  enabled: boolean;
};

export type TierSetEventArgs = {
  tier: number;
  potBase: number | bigint;
  perCardCapBase: number | bigint;
  enabled: boolean;
};

/** Gets the encoder for {@link TierSetEventArgs} event data. */
export function getTierSetEventEncoder(): FixedSizeEncoder<TierSetEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["tier", getU8Encoder()],
      ["potBase", getU64Encoder()],
      ["perCardCapBase", getU64Encoder()],
      ["enabled", getBooleanEncoder()],
    ]),
    [getConstantEncoder(TIER_SET_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link TierSetEvent} event data. */
export function getTierSetEventDecoder(): FixedSizeDecoder<TierSetEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["tier", getU8Decoder()],
      ["potBase", getU64Decoder()],
      ["perCardCapBase", getU64Decoder()],
      ["enabled", getBooleanDecoder()],
    ]),
    [getConstantDecoder(TIER_SET_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link TierSetEvent} event data. */
export function getTierSetEventCodec(): FixedSizeCodec<
  TierSetEventArgs,
  TierSetEvent
> {
  return combineCodec(getTierSetEventEncoder(), getTierSetEventDecoder());
}

/** Parses the provided bytes into {@link TierSetEvent} event data. */
export function parseTierSetEvent(
  data: ReadonlyUint8Array | Uint8Array,
): TierSetEvent {
  if (containsBytes(data, TIER_SET_EVENT_DISCRIMINATOR, 0)) {
    return getTierSetEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "TierSetEvent" event discriminators.',
  );
}
