/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getLegStatusDecoder,
  getLegStatusEncoder,
  type LegStatus,
  type LegStatusArgs,
} from "../types";

export const PARLAY_SETTLED_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([143, 27, 37, 15, 205, 154, 47, 216]);

export function getParlaySettledEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PARLAY_SETTLED_EVENT_DISCRIMINATOR,
  );
}

export type ParlaySettledEvent = {
  reserve: Address;
  ticket: Address;
  owner: Address;
  parlayId: bigint;
  outcome: LegStatus;
  owedBase: bigint;
};

export type ParlaySettledEventArgs = {
  reserve: Address;
  ticket: Address;
  owner: Address;
  parlayId: number | bigint;
  outcome: LegStatusArgs;
  owedBase: number | bigint;
};

/** Gets the encoder for {@link ParlaySettledEventArgs} event data. */
export function getParlaySettledEventEncoder(): FixedSizeEncoder<ParlaySettledEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["reserve", getAddressEncoder()],
      ["ticket", getAddressEncoder()],
      ["owner", getAddressEncoder()],
      ["parlayId", getU64Encoder()],
      ["outcome", getLegStatusEncoder()],
      ["owedBase", getU64Encoder()],
    ]),
    [getConstantEncoder(PARLAY_SETTLED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link ParlaySettledEvent} event data. */
export function getParlaySettledEventDecoder(): FixedSizeDecoder<ParlaySettledEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["reserve", getAddressDecoder()],
      ["ticket", getAddressDecoder()],
      ["owner", getAddressDecoder()],
      ["parlayId", getU64Decoder()],
      ["outcome", getLegStatusDecoder()],
      ["owedBase", getU64Decoder()],
    ]),
    [getConstantDecoder(PARLAY_SETTLED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link ParlaySettledEvent} event data. */
export function getParlaySettledEventCodec(): FixedSizeCodec<
  ParlaySettledEventArgs,
  ParlaySettledEvent
> {
  return combineCodec(
    getParlaySettledEventEncoder(),
    getParlaySettledEventDecoder(),
  );
}

/** Parses the provided bytes into {@link ParlaySettledEvent} event data. */
export function parseParlaySettledEvent(
  data: ReadonlyUint8Array | Uint8Array,
): ParlaySettledEvent {
  if (containsBytes(data, PARLAY_SETTLED_EVENT_DISCRIMINATOR, 0)) {
    return getParlaySettledEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "ParlaySettledEvent" event discriminators.',
  );
}
