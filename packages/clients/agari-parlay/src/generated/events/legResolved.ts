/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getLegStatusDecoder,
  getLegStatusEncoder,
  type LegStatus,
  type LegStatusArgs,
} from "../types";

export const LEG_RESOLVED_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([83, 94, 20, 246, 252, 125, 188, 166]);

export function getLegResolvedEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    LEG_RESOLVED_EVENT_DISCRIMINATOR,
  );
}

export type LegResolvedEvent = {
  ticket: Address;
  parlayId: bigint;
  legIdx: number;
  market: Address;
  outcome: LegStatus;
  cranker: Address;
};

export type LegResolvedEventArgs = {
  ticket: Address;
  parlayId: number | bigint;
  legIdx: number;
  market: Address;
  outcome: LegStatusArgs;
  cranker: Address;
};

/** Gets the encoder for {@link LegResolvedEventArgs} event data. */
export function getLegResolvedEventEncoder(): FixedSizeEncoder<LegResolvedEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["ticket", getAddressEncoder()],
      ["parlayId", getU64Encoder()],
      ["legIdx", getU8Encoder()],
      ["market", getAddressEncoder()],
      ["outcome", getLegStatusEncoder()],
      ["cranker", getAddressEncoder()],
    ]),
    [getConstantEncoder(LEG_RESOLVED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link LegResolvedEvent} event data. */
export function getLegResolvedEventDecoder(): FixedSizeDecoder<LegResolvedEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["ticket", getAddressDecoder()],
      ["parlayId", getU64Decoder()],
      ["legIdx", getU8Decoder()],
      ["market", getAddressDecoder()],
      ["outcome", getLegStatusDecoder()],
      ["cranker", getAddressDecoder()],
    ]),
    [getConstantDecoder(LEG_RESOLVED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link LegResolvedEvent} event data. */
export function getLegResolvedEventCodec(): FixedSizeCodec<
  LegResolvedEventArgs,
  LegResolvedEvent
> {
  return combineCodec(
    getLegResolvedEventEncoder(),
    getLegResolvedEventDecoder(),
  );
}

/** Parses the provided bytes into {@link LegResolvedEvent} event data. */
export function parseLegResolvedEvent(
  data: ReadonlyUint8Array | Uint8Array,
): LegResolvedEvent {
  if (containsBytes(data, LEG_RESOLVED_EVENT_DISCRIMINATOR, 0)) {
    return getLegResolvedEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "LegResolvedEvent" event discriminators.',
  );
}
