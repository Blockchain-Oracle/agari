/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findReservePda } from "../pdas";
import { AGARI_PARLAY_PROGRAM_ADDRESS } from "../programs";

export const PUBLIC_RESOLVE_LEG_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([77, 22, 172, 215, 92, 95, 229, 168]);

export function getPublicResolveLegDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PUBLIC_RESOLVE_LEG_DISCRIMINATOR,
  );
}

export type PublicResolveLegInstruction<
  TProgram extends string = typeof AGARI_PARLAY_PROGRAM_ADDRESS,
  TAccountCranker extends string | AccountMeta<string> = string,
  TAccountReserve extends string | AccountMeta<string> = string,
  TAccountTicket extends string | AccountMeta<string> = string,
  TAccountMarket extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCranker extends string
        ? ReadonlySignerAccount<TAccountCranker> &
            AccountSignerMeta<TAccountCranker>
        : TAccountCranker,
      TAccountReserve extends string
        ? WritableAccount<TAccountReserve>
        : TAccountReserve,
      TAccountTicket extends string
        ? WritableAccount<TAccountTicket>
        : TAccountTicket,
      TAccountMarket extends string
        ? ReadonlyAccount<TAccountMarket>
        : TAccountMarket,
      ...TRemainingAccounts,
    ]
  >;

export type PublicResolveLegInstructionData = {
  discriminator: ReadonlyUint8Array;
  legIdx: number;
};

export type PublicResolveLegInstructionDataArgs = { legIdx: number };

export function getPublicResolveLegInstructionDataEncoder(): FixedSizeEncoder<PublicResolveLegInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["legIdx", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: PUBLIC_RESOLVE_LEG_DISCRIMINATOR }),
  );
}

export function getPublicResolveLegInstructionDataDecoder(): FixedSizeDecoder<PublicResolveLegInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["legIdx", getU8Decoder()],
  ]);
}

export function getPublicResolveLegInstructionDataCodec(): FixedSizeCodec<
  PublicResolveLegInstructionDataArgs,
  PublicResolveLegInstructionData
> {
  return combineCodec(
    getPublicResolveLegInstructionDataEncoder(),
    getPublicResolveLegInstructionDataDecoder(),
  );
}

export type PublicResolveLegAsyncInput<
  TAccountCranker extends string = string,
  TAccountReserve extends string = string,
  TAccountTicket extends string = string,
  TAccountMarket extends string = string,
> = {
  /** Permissionless: deciding a ticket never pays the caller. */
  cranker: TransactionSigner<TAccountCranker>;
  reserve?: Address<TAccountReserve>;
  ticket: Address<TAccountTicket>;
  market: Address<TAccountMarket>;
  legIdx: PublicResolveLegInstructionDataArgs["legIdx"];
};

export async function getPublicResolveLegInstructionAsync<
  TAccountCranker extends string,
  TAccountReserve extends string,
  TAccountTicket extends string,
  TAccountMarket extends string,
  TProgramAddress extends Address = typeof AGARI_PARLAY_PROGRAM_ADDRESS,
>(
  input: PublicResolveLegAsyncInput<
    TAccountCranker,
    TAccountReserve,
    TAccountTicket,
    TAccountMarket
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  PublicResolveLegInstruction<
    TProgramAddress,
    TAccountCranker,
    TAccountReserve,
    TAccountTicket,
    TAccountMarket
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? AGARI_PARLAY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    cranker: { value: input.cranker ?? null, isWritable: false },
    reserve: { value: input.reserve ?? null, isWritable: true },
    ticket: { value: input.ticket ?? null, isWritable: true },
    market: { value: input.market ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.reserve.value) {
    accounts.reserve.value = await findReservePda({ programAddress });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("cranker", accounts.cranker),
      getAccountMeta("reserve", accounts.reserve),
      getAccountMeta("ticket", accounts.ticket),
      getAccountMeta("market", accounts.market),
    ],
    data: getPublicResolveLegInstructionDataEncoder().encode(
      args as PublicResolveLegInstructionDataArgs,
    ),
    programAddress,
  } as PublicResolveLegInstruction<
    TProgramAddress,
    TAccountCranker,
    TAccountReserve,
    TAccountTicket,
    TAccountMarket
  >);
}

export type PublicResolveLegInput<
  TAccountCranker extends string = string,
  TAccountReserve extends string = string,
  TAccountTicket extends string = string,
  TAccountMarket extends string = string,
> = {
  /** Permissionless: deciding a ticket never pays the caller. */
  cranker: TransactionSigner<TAccountCranker>;
  reserve: Address<TAccountReserve>;
  ticket: Address<TAccountTicket>;
  market: Address<TAccountMarket>;
  legIdx: PublicResolveLegInstructionDataArgs["legIdx"];
};

export function getPublicResolveLegInstruction<
  TAccountCranker extends string,
  TAccountReserve extends string,
  TAccountTicket extends string,
  TAccountMarket extends string,
  TProgramAddress extends Address = typeof AGARI_PARLAY_PROGRAM_ADDRESS,
>(
  input: PublicResolveLegInput<
    TAccountCranker,
    TAccountReserve,
    TAccountTicket,
    TAccountMarket
  >,
  config?: { programAddress?: TProgramAddress },
): PublicResolveLegInstruction<
  TProgramAddress,
  TAccountCranker,
  TAccountReserve,
  TAccountTicket,
  TAccountMarket
> {
  // Program address.
  const programAddress = config?.programAddress ?? AGARI_PARLAY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    cranker: { value: input.cranker ?? null, isWritable: false },
    reserve: { value: input.reserve ?? null, isWritable: true },
    ticket: { value: input.ticket ?? null, isWritable: true },
    market: { value: input.market ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("cranker", accounts.cranker),
      getAccountMeta("reserve", accounts.reserve),
      getAccountMeta("ticket", accounts.ticket),
      getAccountMeta("market", accounts.market),
    ],
    data: getPublicResolveLegInstructionDataEncoder().encode(
      args as PublicResolveLegInstructionDataArgs,
    ),
    programAddress,
  } as PublicResolveLegInstruction<
    TProgramAddress,
    TAccountCranker,
    TAccountReserve,
    TAccountTicket,
    TAccountMarket
  >);
}

export type ParsedPublicResolveLegInstruction<
  TProgram extends string = typeof AGARI_PARLAY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Permissionless: deciding a ticket never pays the caller. */
    cranker: TAccountMetas[0];
    reserve: TAccountMetas[1];
    ticket: TAccountMetas[2];
    market: TAccountMetas[3];
  };
  data: PublicResolveLegInstructionData;
};

export function parsePublicResolveLegInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedPublicResolveLegInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      cranker: getNextAccount(),
      reserve: getNextAccount(),
      ticket: getNextAccount(),
      market: getNextAccount(),
    },
    data: getPublicResolveLegInstructionDataDecoder().decode(instruction.data),
  };
}
