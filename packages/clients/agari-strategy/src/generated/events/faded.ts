/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const FADED_EVENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  123, 40, 254, 62, 39, 141, 83, 104,
]);

export function getFadedEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(FADED_EVENT_DISCRIMINATOR);
}

export type FadedEvent = {
  strategyId: bigint;
  subscriber: Address;
  grantId: bigint;
  feeBase: bigint;
  subscribers: number;
};

export type FadedEventArgs = {
  strategyId: number | bigint;
  subscriber: Address;
  grantId: number | bigint;
  feeBase: number | bigint;
  subscribers: number;
};

/** Gets the encoder for {@link FadedEventArgs} event data. */
export function getFadedEventEncoder(): FixedSizeEncoder<FadedEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["strategyId", getU64Encoder()],
      ["subscriber", getAddressEncoder()],
      ["grantId", getU64Encoder()],
      ["feeBase", getU64Encoder()],
      ["subscribers", getU32Encoder()],
    ]),
    [getConstantEncoder(FADED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link FadedEvent} event data. */
export function getFadedEventDecoder(): FixedSizeDecoder<FadedEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["strategyId", getU64Decoder()],
      ["subscriber", getAddressDecoder()],
      ["grantId", getU64Decoder()],
      ["feeBase", getU64Decoder()],
      ["subscribers", getU32Decoder()],
    ]),
    [getConstantDecoder(FADED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link FadedEvent} event data. */
export function getFadedEventCodec(): FixedSizeCodec<
  FadedEventArgs,
  FadedEvent
> {
  return combineCodec(getFadedEventEncoder(), getFadedEventDecoder());
}

/** Parses the provided bytes into {@link FadedEvent} event data. */
export function parseFadedEvent(
  data: ReadonlyUint8Array | Uint8Array,
): FadedEvent {
  if (containsBytes(data, FADED_EVENT_DISCRIMINATOR, 0)) {
    return getFadedEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "FadedEvent" event discriminators.',
  );
}
