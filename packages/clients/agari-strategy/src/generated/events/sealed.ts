/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const SEALED_EVENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  82, 112, 101, 218, 79, 58, 49, 30,
]);

export function getSealedEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SEALED_EVENT_DISCRIMINATOR,
  );
}

export type SealedEvent = {
  strategyId: bigint;
  revision: number;
  metadataLen: number;
};

export type SealedEventArgs = {
  strategyId: number | bigint;
  revision: number;
  metadataLen: number;
};

/** Gets the encoder for {@link SealedEventArgs} event data. */
export function getSealedEventEncoder(): FixedSizeEncoder<SealedEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["strategyId", getU64Encoder()],
      ["revision", getU32Encoder()],
      ["metadataLen", getU32Encoder()],
    ]),
    [getConstantEncoder(SEALED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link SealedEvent} event data. */
export function getSealedEventDecoder(): FixedSizeDecoder<SealedEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["strategyId", getU64Decoder()],
      ["revision", getU32Decoder()],
      ["metadataLen", getU32Decoder()],
    ]),
    [getConstantDecoder(SEALED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link SealedEvent} event data. */
export function getSealedEventCodec(): FixedSizeCodec<
  SealedEventArgs,
  SealedEvent
> {
  return combineCodec(getSealedEventEncoder(), getSealedEventDecoder());
}

/** Parses the provided bytes into {@link SealedEvent} event data. */
export function parseSealedEvent(
  data: ReadonlyUint8Array | Uint8Array,
): SealedEvent {
  if (containsBytes(data, SEALED_EVENT_DISCRIMINATOR, 0)) {
    return getSealedEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "SealedEvent" event discriminators.',
  );
}
