/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getEnvelopeDecoder,
  getEnvelopeEncoder,
  type Envelope,
  type EnvelopeArgs,
} from "../types";

export const PUBLISHED_EVENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array(
  [209, 213, 222, 168, 97, 205, 86, 206],
);

export function getPublishedEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PUBLISHED_EVENT_DISCRIMINATOR,
  );
}

export type PublishedEvent = {
  strategy: Address;
  strategyId: bigint;
  creator: Address;
  runner: Address;
  specHash: ReadonlyUint8Array;
  metadataHash: ReadonlyUint8Array;
  envelope: Envelope;
  subscriptionFeeBase: bigint;
};

export type PublishedEventArgs = {
  strategy: Address;
  strategyId: number | bigint;
  creator: Address;
  runner: Address;
  specHash: ReadonlyUint8Array;
  metadataHash: ReadonlyUint8Array;
  envelope: EnvelopeArgs;
  subscriptionFeeBase: number | bigint;
};

/** Gets the encoder for {@link PublishedEventArgs} event data. */
export function getPublishedEventEncoder(): FixedSizeEncoder<PublishedEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["strategy", getAddressEncoder()],
      ["strategyId", getU64Encoder()],
      ["creator", getAddressEncoder()],
      ["runner", getAddressEncoder()],
      ["specHash", fixEncoderSize(getBytesEncoder(), 32)],
      ["metadataHash", fixEncoderSize(getBytesEncoder(), 32)],
      ["envelope", getEnvelopeEncoder()],
      ["subscriptionFeeBase", getU64Encoder()],
    ]),
    [getConstantEncoder(PUBLISHED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link PublishedEvent} event data. */
export function getPublishedEventDecoder(): FixedSizeDecoder<PublishedEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["strategy", getAddressDecoder()],
      ["strategyId", getU64Decoder()],
      ["creator", getAddressDecoder()],
      ["runner", getAddressDecoder()],
      ["specHash", fixDecoderSize(getBytesDecoder(), 32)],
      ["metadataHash", fixDecoderSize(getBytesDecoder(), 32)],
      ["envelope", getEnvelopeDecoder()],
      ["subscriptionFeeBase", getU64Decoder()],
    ]),
    [getConstantDecoder(PUBLISHED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link PublishedEvent} event data. */
export function getPublishedEventCodec(): FixedSizeCodec<
  PublishedEventArgs,
  PublishedEvent
> {
  return combineCodec(getPublishedEventEncoder(), getPublishedEventDecoder());
}

/** Parses the provided bytes into {@link PublishedEvent} event data. */
export function parsePublishedEvent(
  data: ReadonlyUint8Array | Uint8Array,
): PublishedEvent {
  if (containsBytes(data, PUBLISHED_EVENT_DISCRIMINATOR, 0)) {
    return getPublishedEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "PublishedEvent" event discriminators.',
  );
}
