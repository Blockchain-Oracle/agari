/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const UNFADED_EVENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  73, 160, 109, 211, 96, 147, 220, 172,
]);

export function getUnfadedEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UNFADED_EVENT_DISCRIMINATOR,
  );
}

export type UnfadedEvent = {
  strategyId: bigint;
  subscriber: Address;
  subscribers: number;
};

export type UnfadedEventArgs = {
  strategyId: number | bigint;
  subscriber: Address;
  subscribers: number;
};

/** Gets the encoder for {@link UnfadedEventArgs} event data. */
export function getUnfadedEventEncoder(): FixedSizeEncoder<UnfadedEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["strategyId", getU64Encoder()],
      ["subscriber", getAddressEncoder()],
      ["subscribers", getU32Encoder()],
    ]),
    [getConstantEncoder(UNFADED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link UnfadedEvent} event data. */
export function getUnfadedEventDecoder(): FixedSizeDecoder<UnfadedEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["strategyId", getU64Decoder()],
      ["subscriber", getAddressDecoder()],
      ["subscribers", getU32Decoder()],
    ]),
    [getConstantDecoder(UNFADED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link UnfadedEvent} event data. */
export function getUnfadedEventCodec(): FixedSizeCodec<
  UnfadedEventArgs,
  UnfadedEvent
> {
  return combineCodec(getUnfadedEventEncoder(), getUnfadedEventDecoder());
}

/** Parses the provided bytes into {@link UnfadedEvent} event data. */
export function parseUnfadedEvent(
  data: ReadonlyUint8Array | Uint8Array,
): UnfadedEvent {
  if (containsBytes(data, UNFADED_EVENT_DISCRIMINATOR, 0)) {
    return getUnfadedEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "UnfadedEvent" event discriminators.',
  );
}
