/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  type Codec,
  type Decoder,
  type Encoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

/**
 * What a creator publishes or revises. The metadata itself is too long for one transaction, so its hash and
 * length are declared here with whatever first piece fits, and the rest follows through `creator_write_metadata`.
 */
export type Revision = {
  specHash: ReadonlyUint8Array;
  metadataHash: ReadonlyUint8Array;
  metadataLen: number;
  firstChunk: ReadonlyUint8Array;
  subscriptionFeeBase: bigint;
};

export type RevisionArgs = {
  specHash: ReadonlyUint8Array;
  metadataHash: ReadonlyUint8Array;
  metadataLen: number;
  firstChunk: ReadonlyUint8Array;
  subscriptionFeeBase: number | bigint;
};

export function getRevisionEncoder(): Encoder<RevisionArgs> {
  return getStructEncoder([
    ["specHash", fixEncoderSize(getBytesEncoder(), 32)],
    ["metadataHash", fixEncoderSize(getBytesEncoder(), 32)],
    ["metadataLen", getU16Encoder()],
    ["firstChunk", addEncoderSizePrefix(getBytesEncoder(), getU32Encoder())],
    ["subscriptionFeeBase", getU64Encoder()],
  ]);
}

export function getRevisionDecoder(): Decoder<Revision> {
  return getStructDecoder([
    ["specHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["metadataHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["metadataLen", getU16Decoder()],
    ["firstChunk", addDecoderSizePrefix(getBytesDecoder(), getU32Decoder())],
    ["subscriptionFeeBase", getU64Decoder()],
  ]);
}

export function getRevisionCodec(): Codec<RevisionArgs, Revision> {
  return combineCodec(getRevisionEncoder(), getRevisionDecoder());
}
