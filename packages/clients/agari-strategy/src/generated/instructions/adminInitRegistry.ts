/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findRegistryPda } from "../pdas";
import { AGARI_STRATEGY_PROGRAM_ADDRESS } from "../programs";

export const ADMIN_INIT_REGISTRY_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([82, 90, 132, 136, 44, 59, 215, 250]);

export function getAdminInitRegistryDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ADMIN_INIT_REGISTRY_DISCRIMINATOR,
  );
}

export type AdminInitRegistryInstruction<
  TProgram extends string = typeof AGARI_STRATEGY_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountRegistry extends string | AccountMeta<string> = string,
  TAccountCollateralMint extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? WritableSignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountRegistry extends string
        ? WritableAccount<TAccountRegistry>
        : TAccountRegistry,
      TAccountCollateralMint extends string
        ? ReadonlyAccount<TAccountCollateralMint>
        : TAccountCollateralMint,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type AdminInitRegistryInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type AdminInitRegistryInstructionDataArgs = {};

export function getAdminInitRegistryInstructionDataEncoder(): FixedSizeEncoder<AdminInitRegistryInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: ADMIN_INIT_REGISTRY_DISCRIMINATOR }),
  );
}

export function getAdminInitRegistryInstructionDataDecoder(): FixedSizeDecoder<AdminInitRegistryInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getAdminInitRegistryInstructionDataCodec(): FixedSizeCodec<
  AdminInitRegistryInstructionDataArgs,
  AdminInitRegistryInstructionData
> {
  return combineCodec(
    getAdminInitRegistryInstructionDataEncoder(),
    getAdminInitRegistryInstructionDataDecoder(),
  );
}

export type AdminInitRegistryAsyncInput<
  TAccountAdmin extends string = string,
  TAccountRegistry extends string = string,
  TAccountCollateralMint extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  registry?: Address<TAccountRegistry>;
  /** The one collateral subscription fees are paid in: the venue's. */
  collateralMint: Address<TAccountCollateralMint>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getAdminInitRegistryInstructionAsync<
  TAccountAdmin extends string,
  TAccountRegistry extends string,
  TAccountCollateralMint extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGARI_STRATEGY_PROGRAM_ADDRESS,
>(
  input: AdminInitRegistryAsyncInput<
    TAccountAdmin,
    TAccountRegistry,
    TAccountCollateralMint,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  AdminInitRegistryInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountRegistry,
    TAccountCollateralMint,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGARI_STRATEGY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    registry: { value: input.registry ?? null, isWritable: true },
    collateralMint: { value: input.collateralMint ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.registry.value) {
    accounts.registry.value = await findRegistryPda({ programAddress });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("registry", accounts.registry),
      getAccountMeta("collateralMint", accounts.collateralMint),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getAdminInitRegistryInstructionDataEncoder().encode({}),
    programAddress,
  } as AdminInitRegistryInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountRegistry,
    TAccountCollateralMint,
    TAccountSystemProgram
  >);
}

export type AdminInitRegistryInput<
  TAccountAdmin extends string = string,
  TAccountRegistry extends string = string,
  TAccountCollateralMint extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  registry: Address<TAccountRegistry>;
  /** The one collateral subscription fees are paid in: the venue's. */
  collateralMint: Address<TAccountCollateralMint>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getAdminInitRegistryInstruction<
  TAccountAdmin extends string,
  TAccountRegistry extends string,
  TAccountCollateralMint extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGARI_STRATEGY_PROGRAM_ADDRESS,
>(
  input: AdminInitRegistryInput<
    TAccountAdmin,
    TAccountRegistry,
    TAccountCollateralMint,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): AdminInitRegistryInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountRegistry,
  TAccountCollateralMint,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGARI_STRATEGY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    registry: { value: input.registry ?? null, isWritable: true },
    collateralMint: { value: input.collateralMint ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("registry", accounts.registry),
      getAccountMeta("collateralMint", accounts.collateralMint),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getAdminInitRegistryInstructionDataEncoder().encode({}),
    programAddress,
  } as AdminInitRegistryInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountRegistry,
    TAccountCollateralMint,
    TAccountSystemProgram
  >);
}

export type ParsedAdminInitRegistryInstruction<
  TProgram extends string = typeof AGARI_STRATEGY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    registry: TAccountMetas[1];
    /** The one collateral subscription fees are paid in: the venue's. */
    collateralMint: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: AdminInitRegistryInstructionData;
};

export function parseAdminInitRegistryInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAdminInitRegistryInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      admin: getNextAccount(),
      registry: getNextAccount(),
      collateralMint: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getAdminInitRegistryInstructionDataDecoder().decode(instruction.data),
  };
}
