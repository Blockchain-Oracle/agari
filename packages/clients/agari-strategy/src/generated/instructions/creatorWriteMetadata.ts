/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU32Decoder,
  getU32Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { AGARI_STRATEGY_PROGRAM_ADDRESS } from "../programs";

export const CREATOR_WRITE_METADATA_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([212, 14, 239, 249, 240, 20, 171, 143]);

export function getCreatorWriteMetadataDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CREATOR_WRITE_METADATA_DISCRIMINATOR,
  );
}

export type CreatorWriteMetadataInstruction<
  TProgram extends string = typeof AGARI_STRATEGY_PROGRAM_ADDRESS,
  TAccountCreator extends string | AccountMeta<string> = string,
  TAccountStrategy extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCreator extends string
        ? ReadonlySignerAccount<TAccountCreator> &
            AccountSignerMeta<TAccountCreator>
        : TAccountCreator,
      TAccountStrategy extends string
        ? WritableAccount<TAccountStrategy>
        : TAccountStrategy,
      ...TRemainingAccounts,
    ]
  >;

export type CreatorWriteMetadataInstructionData = {
  discriminator: ReadonlyUint8Array;
  offset: number;
  chunk: ReadonlyUint8Array;
};

export type CreatorWriteMetadataInstructionDataArgs = {
  offset: number;
  chunk: ReadonlyUint8Array;
};

export function getCreatorWriteMetadataInstructionDataEncoder(): Encoder<CreatorWriteMetadataInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["offset", getU16Encoder()],
      ["chunk", addEncoderSizePrefix(getBytesEncoder(), getU32Encoder())],
    ]),
    (value) => ({
      ...value,
      discriminator: CREATOR_WRITE_METADATA_DISCRIMINATOR,
    }),
  );
}

export function getCreatorWriteMetadataInstructionDataDecoder(): Decoder<CreatorWriteMetadataInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["offset", getU16Decoder()],
    ["chunk", addDecoderSizePrefix(getBytesDecoder(), getU32Decoder())],
  ]);
}

export function getCreatorWriteMetadataInstructionDataCodec(): Codec<
  CreatorWriteMetadataInstructionDataArgs,
  CreatorWriteMetadataInstructionData
> {
  return combineCodec(
    getCreatorWriteMetadataInstructionDataEncoder(),
    getCreatorWriteMetadataInstructionDataDecoder(),
  );
}

export type CreatorWriteMetadataInput<
  TAccountCreator extends string = string,
  TAccountStrategy extends string = string,
> = {
  creator: TransactionSigner<TAccountCreator>;
  strategy: Address<TAccountStrategy>;
  offset: CreatorWriteMetadataInstructionDataArgs["offset"];
  chunk: CreatorWriteMetadataInstructionDataArgs["chunk"];
};

export function getCreatorWriteMetadataInstruction<
  TAccountCreator extends string,
  TAccountStrategy extends string,
  TProgramAddress extends Address = typeof AGARI_STRATEGY_PROGRAM_ADDRESS,
>(
  input: CreatorWriteMetadataInput<TAccountCreator, TAccountStrategy>,
  config?: { programAddress?: TProgramAddress },
): CreatorWriteMetadataInstruction<
  TProgramAddress,
  TAccountCreator,
  TAccountStrategy
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGARI_STRATEGY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    creator: { value: input.creator ?? null, isWritable: false },
    strategy: { value: input.strategy ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("creator", accounts.creator),
      getAccountMeta("strategy", accounts.strategy),
    ],
    data: getCreatorWriteMetadataInstructionDataEncoder().encode(
      args as CreatorWriteMetadataInstructionDataArgs,
    ),
    programAddress,
  } as CreatorWriteMetadataInstruction<
    TProgramAddress,
    TAccountCreator,
    TAccountStrategy
  >);
}

export type ParsedCreatorWriteMetadataInstruction<
  TProgram extends string = typeof AGARI_STRATEGY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    creator: TAccountMetas[0];
    strategy: TAccountMetas[1];
  };
  data: CreatorWriteMetadataInstructionData;
};

export function parseCreatorWriteMetadataInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCreatorWriteMetadataInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { creator: getNextAccount(), strategy: getNextAccount() },
    data: getCreatorWriteMetadataInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
