/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { AGARI_STRATEGY_PROGRAM_ADDRESS } from "../programs";

export const CREATOR_SET_RUNNER_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([178, 227, 53, 12, 147, 0, 238, 126]);

export function getCreatorSetRunnerDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CREATOR_SET_RUNNER_DISCRIMINATOR,
  );
}

export type CreatorSetRunnerInstruction<
  TProgram extends string = typeof AGARI_STRATEGY_PROGRAM_ADDRESS,
  TAccountCreator extends string | AccountMeta<string> = string,
  TAccountStrategy extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCreator extends string
        ? ReadonlySignerAccount<TAccountCreator> &
            AccountSignerMeta<TAccountCreator>
        : TAccountCreator,
      TAccountStrategy extends string
        ? WritableAccount<TAccountStrategy>
        : TAccountStrategy,
      ...TRemainingAccounts,
    ]
  >;

export type CreatorSetRunnerInstructionData = {
  discriminator: ReadonlyUint8Array;
  runner: Address;
};

export type CreatorSetRunnerInstructionDataArgs = { runner: Address };

export function getCreatorSetRunnerInstructionDataEncoder(): FixedSizeEncoder<CreatorSetRunnerInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["runner", getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: CREATOR_SET_RUNNER_DISCRIMINATOR }),
  );
}

export function getCreatorSetRunnerInstructionDataDecoder(): FixedSizeDecoder<CreatorSetRunnerInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["runner", getAddressDecoder()],
  ]);
}

export function getCreatorSetRunnerInstructionDataCodec(): FixedSizeCodec<
  CreatorSetRunnerInstructionDataArgs,
  CreatorSetRunnerInstructionData
> {
  return combineCodec(
    getCreatorSetRunnerInstructionDataEncoder(),
    getCreatorSetRunnerInstructionDataDecoder(),
  );
}

export type CreatorSetRunnerInput<
  TAccountCreator extends string = string,
  TAccountStrategy extends string = string,
> = {
  creator: TransactionSigner<TAccountCreator>;
  strategy: Address<TAccountStrategy>;
  runner: CreatorSetRunnerInstructionDataArgs["runner"];
};

export function getCreatorSetRunnerInstruction<
  TAccountCreator extends string,
  TAccountStrategy extends string,
  TProgramAddress extends Address = typeof AGARI_STRATEGY_PROGRAM_ADDRESS,
>(
  input: CreatorSetRunnerInput<TAccountCreator, TAccountStrategy>,
  config?: { programAddress?: TProgramAddress },
): CreatorSetRunnerInstruction<
  TProgramAddress,
  TAccountCreator,
  TAccountStrategy
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGARI_STRATEGY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    creator: { value: input.creator ?? null, isWritable: false },
    strategy: { value: input.strategy ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("creator", accounts.creator),
      getAccountMeta("strategy", accounts.strategy),
    ],
    data: getCreatorSetRunnerInstructionDataEncoder().encode(
      args as CreatorSetRunnerInstructionDataArgs,
    ),
    programAddress,
  } as CreatorSetRunnerInstruction<
    TProgramAddress,
    TAccountCreator,
    TAccountStrategy
  >);
}

export type ParsedCreatorSetRunnerInstruction<
  TProgram extends string = typeof AGARI_STRATEGY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    creator: TAccountMetas[0];
    strategy: TAccountMetas[1];
  };
  data: CreatorSetRunnerInstructionData;
};

export function parseCreatorSetRunnerInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCreatorSetRunnerInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { creator: getNextAccount(), strategy: getNextAccount() },
    data: getCreatorSetRunnerInstructionDataDecoder().decode(instruction.data),
  };
}
