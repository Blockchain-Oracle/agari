/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findRegistryPda } from "../pdas";
import { AGARI_STRATEGY_PROGRAM_ADDRESS } from "../programs";
import {
  getEnvelopeDecoder,
  getEnvelopeEncoder,
  getRevisionDecoder,
  getRevisionEncoder,
  type Envelope,
  type EnvelopeArgs,
  type Revision,
  type RevisionArgs,
} from "../types";

export const CREATOR_PUBLISH_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array(
  [28, 10, 239, 151, 56, 125, 220, 148],
);

export function getCreatorPublishDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CREATOR_PUBLISH_DISCRIMINATOR,
  );
}

export type CreatorPublishInstruction<
  TProgram extends string = typeof AGARI_STRATEGY_PROGRAM_ADDRESS,
  TAccountCreator extends string | AccountMeta<string> = string,
  TAccountRegistry extends string | AccountMeta<string> = string,
  TAccountStrategy extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCreator extends string
        ? WritableSignerAccount<TAccountCreator> &
            AccountSignerMeta<TAccountCreator>
        : TAccountCreator,
      TAccountRegistry extends string
        ? WritableAccount<TAccountRegistry>
        : TAccountRegistry,
      TAccountStrategy extends string
        ? WritableAccount<TAccountStrategy>
        : TAccountStrategy,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CreatorPublishInstructionData = {
  discriminator: ReadonlyUint8Array;
  runner: Address;
  envelope: Envelope;
  revision: Revision;
};

export type CreatorPublishInstructionDataArgs = {
  runner: Address;
  envelope: EnvelopeArgs;
  revision: RevisionArgs;
};

export function getCreatorPublishInstructionDataEncoder(): Encoder<CreatorPublishInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["runner", getAddressEncoder()],
      ["envelope", getEnvelopeEncoder()],
      ["revision", getRevisionEncoder()],
    ]),
    (value) => ({ ...value, discriminator: CREATOR_PUBLISH_DISCRIMINATOR }),
  );
}

export function getCreatorPublishInstructionDataDecoder(): Decoder<CreatorPublishInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["runner", getAddressDecoder()],
    ["envelope", getEnvelopeDecoder()],
    ["revision", getRevisionDecoder()],
  ]);
}

export function getCreatorPublishInstructionDataCodec(): Codec<
  CreatorPublishInstructionDataArgs,
  CreatorPublishInstructionData
> {
  return combineCodec(
    getCreatorPublishInstructionDataEncoder(),
    getCreatorPublishInstructionDataDecoder(),
  );
}

export type CreatorPublishAsyncInput<
  TAccountCreator extends string = string,
  TAccountRegistry extends string = string,
  TAccountStrategy extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  creator: TransactionSigner<TAccountCreator>;
  registry?: Address<TAccountRegistry>;
  strategy: Address<TAccountStrategy>;
  systemProgram?: Address<TAccountSystemProgram>;
  runner: CreatorPublishInstructionDataArgs["runner"];
  envelope: CreatorPublishInstructionDataArgs["envelope"];
  revision: CreatorPublishInstructionDataArgs["revision"];
};

export async function getCreatorPublishInstructionAsync<
  TAccountCreator extends string,
  TAccountRegistry extends string,
  TAccountStrategy extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGARI_STRATEGY_PROGRAM_ADDRESS,
>(
  input: CreatorPublishAsyncInput<
    TAccountCreator,
    TAccountRegistry,
    TAccountStrategy,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CreatorPublishInstruction<
    TProgramAddress,
    TAccountCreator,
    TAccountRegistry,
    TAccountStrategy,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGARI_STRATEGY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    creator: { value: input.creator ?? null, isWritable: true },
    registry: { value: input.registry ?? null, isWritable: true },
    strategy: { value: input.strategy ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.registry.value) {
    accounts.registry.value = await findRegistryPda({ programAddress });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("creator", accounts.creator),
      getAccountMeta("registry", accounts.registry),
      getAccountMeta("strategy", accounts.strategy),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getCreatorPublishInstructionDataEncoder().encode(
      args as CreatorPublishInstructionDataArgs,
    ),
    programAddress,
  } as CreatorPublishInstruction<
    TProgramAddress,
    TAccountCreator,
    TAccountRegistry,
    TAccountStrategy,
    TAccountSystemProgram
  >);
}

export type CreatorPublishInput<
  TAccountCreator extends string = string,
  TAccountRegistry extends string = string,
  TAccountStrategy extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  creator: TransactionSigner<TAccountCreator>;
  registry: Address<TAccountRegistry>;
  strategy: Address<TAccountStrategy>;
  systemProgram?: Address<TAccountSystemProgram>;
  runner: CreatorPublishInstructionDataArgs["runner"];
  envelope: CreatorPublishInstructionDataArgs["envelope"];
  revision: CreatorPublishInstructionDataArgs["revision"];
};

export function getCreatorPublishInstruction<
  TAccountCreator extends string,
  TAccountRegistry extends string,
  TAccountStrategy extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGARI_STRATEGY_PROGRAM_ADDRESS,
>(
  input: CreatorPublishInput<
    TAccountCreator,
    TAccountRegistry,
    TAccountStrategy,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CreatorPublishInstruction<
  TProgramAddress,
  TAccountCreator,
  TAccountRegistry,
  TAccountStrategy,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGARI_STRATEGY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    creator: { value: input.creator ?? null, isWritable: true },
    registry: { value: input.registry ?? null, isWritable: true },
    strategy: { value: input.strategy ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("creator", accounts.creator),
      getAccountMeta("registry", accounts.registry),
      getAccountMeta("strategy", accounts.strategy),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getCreatorPublishInstructionDataEncoder().encode(
      args as CreatorPublishInstructionDataArgs,
    ),
    programAddress,
  } as CreatorPublishInstruction<
    TProgramAddress,
    TAccountCreator,
    TAccountRegistry,
    TAccountStrategy,
    TAccountSystemProgram
  >);
}

export type ParsedCreatorPublishInstruction<
  TProgram extends string = typeof AGARI_STRATEGY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    creator: TAccountMetas[0];
    registry: TAccountMetas[1];
    strategy: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: CreatorPublishInstructionData;
};

export function parseCreatorPublishInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCreatorPublishInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      creator: getNextAccount(),
      registry: getNextAccount(),
      strategy: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCreatorPublishInstructionDataDecoder().decode(instruction.data),
  };
}
