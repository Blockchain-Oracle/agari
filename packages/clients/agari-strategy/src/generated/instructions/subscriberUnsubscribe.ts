/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { AGARI_STRATEGY_PROGRAM_ADDRESS } from "../programs";

export const SUBSCRIBER_UNSUBSCRIBE_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([192, 237, 135, 20, 6, 192, 171, 11]);

export function getSubscriberUnsubscribeDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SUBSCRIBER_UNSUBSCRIBE_DISCRIMINATOR,
  );
}

export type SubscriberUnsubscribeInstruction<
  TProgram extends string = typeof AGARI_STRATEGY_PROGRAM_ADDRESS,
  TAccountSubscriber extends string | AccountMeta<string> = string,
  TAccountStrategy extends string | AccountMeta<string> = string,
  TAccountSubscription extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSubscriber extends string
        ? ReadonlySignerAccount<TAccountSubscriber> &
            AccountSignerMeta<TAccountSubscriber>
        : TAccountSubscriber,
      TAccountStrategy extends string
        ? WritableAccount<TAccountStrategy>
        : TAccountStrategy,
      TAccountSubscription extends string
        ? WritableAccount<TAccountSubscription>
        : TAccountSubscription,
      ...TRemainingAccounts,
    ]
  >;

export type SubscriberUnsubscribeInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type SubscriberUnsubscribeInstructionDataArgs = {};

export function getSubscriberUnsubscribeInstructionDataEncoder(): FixedSizeEncoder<SubscriberUnsubscribeInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: SUBSCRIBER_UNSUBSCRIBE_DISCRIMINATOR,
    }),
  );
}

export function getSubscriberUnsubscribeInstructionDataDecoder(): FixedSizeDecoder<SubscriberUnsubscribeInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getSubscriberUnsubscribeInstructionDataCodec(): FixedSizeCodec<
  SubscriberUnsubscribeInstructionDataArgs,
  SubscriberUnsubscribeInstructionData
> {
  return combineCodec(
    getSubscriberUnsubscribeInstructionDataEncoder(),
    getSubscriberUnsubscribeInstructionDataDecoder(),
  );
}

export type SubscriberUnsubscribeInput<
  TAccountSubscriber extends string = string,
  TAccountStrategy extends string = string,
  TAccountSubscription extends string = string,
> = {
  subscriber: TransactionSigner<TAccountSubscriber>;
  strategy: Address<TAccountStrategy>;
  subscription: Address<TAccountSubscription>;
};

export function getSubscriberUnsubscribeInstruction<
  TAccountSubscriber extends string,
  TAccountStrategy extends string,
  TAccountSubscription extends string,
  TProgramAddress extends Address = typeof AGARI_STRATEGY_PROGRAM_ADDRESS,
>(
  input: SubscriberUnsubscribeInput<
    TAccountSubscriber,
    TAccountStrategy,
    TAccountSubscription
  >,
  config?: { programAddress?: TProgramAddress },
): SubscriberUnsubscribeInstruction<
  TProgramAddress,
  TAccountSubscriber,
  TAccountStrategy,
  TAccountSubscription
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGARI_STRATEGY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    subscriber: { value: input.subscriber ?? null, isWritable: false },
    strategy: { value: input.strategy ?? null, isWritable: true },
    subscription: { value: input.subscription ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("subscriber", accounts.subscriber),
      getAccountMeta("strategy", accounts.strategy),
      getAccountMeta("subscription", accounts.subscription),
    ],
    data: getSubscriberUnsubscribeInstructionDataEncoder().encode({}),
    programAddress,
  } as SubscriberUnsubscribeInstruction<
    TProgramAddress,
    TAccountSubscriber,
    TAccountStrategy,
    TAccountSubscription
  >);
}

export type ParsedSubscriberUnsubscribeInstruction<
  TProgram extends string = typeof AGARI_STRATEGY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    subscriber: TAccountMetas[0];
    strategy: TAccountMetas[1];
    subscription: TAccountMetas[2];
  };
  data: SubscriberUnsubscribeInstructionData;
};

export function parseSubscriberUnsubscribeInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSubscriberUnsubscribeInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      subscriber: getNextAccount(),
      strategy: getNextAccount(),
      subscription: getNextAccount(),
    },
    data: getSubscriberUnsubscribeInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
