/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { AGARI_STRATEGY_PROGRAM_ADDRESS } from "../programs";

export const SUBSCRIBER_UNFADE_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([8, 183, 191, 29, 6, 152, 174, 46]);

export function getSubscriberUnfadeDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SUBSCRIBER_UNFADE_DISCRIMINATOR,
  );
}

export type SubscriberUnfadeInstruction<
  TProgram extends string = typeof AGARI_STRATEGY_PROGRAM_ADDRESS,
  TAccountSubscriber extends string | AccountMeta<string> = string,
  TAccountStrategy extends string | AccountMeta<string> = string,
  TAccountFade extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSubscriber extends string
        ? ReadonlySignerAccount<TAccountSubscriber> &
            AccountSignerMeta<TAccountSubscriber>
        : TAccountSubscriber,
      TAccountStrategy extends string
        ? WritableAccount<TAccountStrategy>
        : TAccountStrategy,
      TAccountFade extends string
        ? WritableAccount<TAccountFade>
        : TAccountFade,
      ...TRemainingAccounts,
    ]
  >;

export type SubscriberUnfadeInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type SubscriberUnfadeInstructionDataArgs = {};

export function getSubscriberUnfadeInstructionDataEncoder(): FixedSizeEncoder<SubscriberUnfadeInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: SUBSCRIBER_UNFADE_DISCRIMINATOR }),
  );
}

export function getSubscriberUnfadeInstructionDataDecoder(): FixedSizeDecoder<SubscriberUnfadeInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getSubscriberUnfadeInstructionDataCodec(): FixedSizeCodec<
  SubscriberUnfadeInstructionDataArgs,
  SubscriberUnfadeInstructionData
> {
  return combineCodec(
    getSubscriberUnfadeInstructionDataEncoder(),
    getSubscriberUnfadeInstructionDataDecoder(),
  );
}

export type SubscriberUnfadeInput<
  TAccountSubscriber extends string = string,
  TAccountStrategy extends string = string,
  TAccountFade extends string = string,
> = {
  subscriber: TransactionSigner<TAccountSubscriber>;
  strategy: Address<TAccountStrategy>;
  fade: Address<TAccountFade>;
};

export function getSubscriberUnfadeInstruction<
  TAccountSubscriber extends string,
  TAccountStrategy extends string,
  TAccountFade extends string,
  TProgramAddress extends Address = typeof AGARI_STRATEGY_PROGRAM_ADDRESS,
>(
  input: SubscriberUnfadeInput<
    TAccountSubscriber,
    TAccountStrategy,
    TAccountFade
  >,
  config?: { programAddress?: TProgramAddress },
): SubscriberUnfadeInstruction<
  TProgramAddress,
  TAccountSubscriber,
  TAccountStrategy,
  TAccountFade
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGARI_STRATEGY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    subscriber: { value: input.subscriber ?? null, isWritable: false },
    strategy: { value: input.strategy ?? null, isWritable: true },
    fade: { value: input.fade ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("subscriber", accounts.subscriber),
      getAccountMeta("strategy", accounts.strategy),
      getAccountMeta("fade", accounts.fade),
    ],
    data: getSubscriberUnfadeInstructionDataEncoder().encode({}),
    programAddress,
  } as SubscriberUnfadeInstruction<
    TProgramAddress,
    TAccountSubscriber,
    TAccountStrategy,
    TAccountFade
  >);
}

export type ParsedSubscriberUnfadeInstruction<
  TProgram extends string = typeof AGARI_STRATEGY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    subscriber: TAccountMetas[0];
    strategy: TAccountMetas[1];
    fade: TAccountMetas[2];
  };
  data: SubscriberUnfadeInstructionData;
};

export function parseSubscriberUnfadeInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSubscriberUnfadeInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      subscriber: getNextAccount(),
      strategy: getNextAccount(),
      fade: getNextAccount(),
    },
    data: getSubscriberUnfadeInstructionDataDecoder().decode(instruction.data),
  };
}
