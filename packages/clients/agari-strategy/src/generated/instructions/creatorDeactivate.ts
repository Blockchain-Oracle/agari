/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { AGARI_STRATEGY_PROGRAM_ADDRESS } from "../programs";

export const CREATOR_DEACTIVATE_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([16, 151, 107, 120, 217, 185, 83, 154]);

export function getCreatorDeactivateDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CREATOR_DEACTIVATE_DISCRIMINATOR,
  );
}

export type CreatorDeactivateInstruction<
  TProgram extends string = typeof AGARI_STRATEGY_PROGRAM_ADDRESS,
  TAccountCreator extends string | AccountMeta<string> = string,
  TAccountStrategy extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCreator extends string
        ? ReadonlySignerAccount<TAccountCreator> &
            AccountSignerMeta<TAccountCreator>
        : TAccountCreator,
      TAccountStrategy extends string
        ? WritableAccount<TAccountStrategy>
        : TAccountStrategy,
      ...TRemainingAccounts,
    ]
  >;

export type CreatorDeactivateInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type CreatorDeactivateInstructionDataArgs = {};

export function getCreatorDeactivateInstructionDataEncoder(): FixedSizeEncoder<CreatorDeactivateInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CREATOR_DEACTIVATE_DISCRIMINATOR }),
  );
}

export function getCreatorDeactivateInstructionDataDecoder(): FixedSizeDecoder<CreatorDeactivateInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCreatorDeactivateInstructionDataCodec(): FixedSizeCodec<
  CreatorDeactivateInstructionDataArgs,
  CreatorDeactivateInstructionData
> {
  return combineCodec(
    getCreatorDeactivateInstructionDataEncoder(),
    getCreatorDeactivateInstructionDataDecoder(),
  );
}

export type CreatorDeactivateInput<
  TAccountCreator extends string = string,
  TAccountStrategy extends string = string,
> = {
  creator: TransactionSigner<TAccountCreator>;
  strategy: Address<TAccountStrategy>;
};

export function getCreatorDeactivateInstruction<
  TAccountCreator extends string,
  TAccountStrategy extends string,
  TProgramAddress extends Address = typeof AGARI_STRATEGY_PROGRAM_ADDRESS,
>(
  input: CreatorDeactivateInput<TAccountCreator, TAccountStrategy>,
  config?: { programAddress?: TProgramAddress },
): CreatorDeactivateInstruction<
  TProgramAddress,
  TAccountCreator,
  TAccountStrategy
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGARI_STRATEGY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    creator: { value: input.creator ?? null, isWritable: false },
    strategy: { value: input.strategy ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("creator", accounts.creator),
      getAccountMeta("strategy", accounts.strategy),
    ],
    data: getCreatorDeactivateInstructionDataEncoder().encode({}),
    programAddress,
  } as CreatorDeactivateInstruction<
    TProgramAddress,
    TAccountCreator,
    TAccountStrategy
  >);
}

export type ParsedCreatorDeactivateInstruction<
  TProgram extends string = typeof AGARI_STRATEGY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    creator: TAccountMetas[0];
    strategy: TAccountMetas[1];
  };
  data: CreatorDeactivateInstructionData;
};

export function parseCreatorDeactivateInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCreatorDeactivateInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { creator: getNextAccount(), strategy: getNextAccount() },
    data: getCreatorDeactivateInstructionDataDecoder().decode(instruction.data),
  };
}
