/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { AGARI_STRATEGY_PROGRAM_ADDRESS } from "../programs";
import {
  getRevisionDecoder,
  getRevisionEncoder,
  type Revision,
  type RevisionArgs,
} from "../types";

export const CREATOR_UPDATE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  41, 196, 139, 177, 91, 215, 131, 137,
]);

export function getCreatorUpdateDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CREATOR_UPDATE_DISCRIMINATOR,
  );
}

export type CreatorUpdateInstruction<
  TProgram extends string = typeof AGARI_STRATEGY_PROGRAM_ADDRESS,
  TAccountCreator extends string | AccountMeta<string> = string,
  TAccountStrategy extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCreator extends string
        ? ReadonlySignerAccount<TAccountCreator> &
            AccountSignerMeta<TAccountCreator>
        : TAccountCreator,
      TAccountStrategy extends string
        ? WritableAccount<TAccountStrategy>
        : TAccountStrategy,
      ...TRemainingAccounts,
    ]
  >;

export type CreatorUpdateInstructionData = {
  discriminator: ReadonlyUint8Array;
  revision: Revision;
};

export type CreatorUpdateInstructionDataArgs = { revision: RevisionArgs };

export function getCreatorUpdateInstructionDataEncoder(): Encoder<CreatorUpdateInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["revision", getRevisionEncoder()],
    ]),
    (value) => ({ ...value, discriminator: CREATOR_UPDATE_DISCRIMINATOR }),
  );
}

export function getCreatorUpdateInstructionDataDecoder(): Decoder<CreatorUpdateInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["revision", getRevisionDecoder()],
  ]);
}

export function getCreatorUpdateInstructionDataCodec(): Codec<
  CreatorUpdateInstructionDataArgs,
  CreatorUpdateInstructionData
> {
  return combineCodec(
    getCreatorUpdateInstructionDataEncoder(),
    getCreatorUpdateInstructionDataDecoder(),
  );
}

export type CreatorUpdateInput<
  TAccountCreator extends string = string,
  TAccountStrategy extends string = string,
> = {
  creator: TransactionSigner<TAccountCreator>;
  strategy: Address<TAccountStrategy>;
  revision: CreatorUpdateInstructionDataArgs["revision"];
};

export function getCreatorUpdateInstruction<
  TAccountCreator extends string,
  TAccountStrategy extends string,
  TProgramAddress extends Address = typeof AGARI_STRATEGY_PROGRAM_ADDRESS,
>(
  input: CreatorUpdateInput<TAccountCreator, TAccountStrategy>,
  config?: { programAddress?: TProgramAddress },
): CreatorUpdateInstruction<
  TProgramAddress,
  TAccountCreator,
  TAccountStrategy
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? AGARI_STRATEGY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    creator: { value: input.creator ?? null, isWritable: false },
    strategy: { value: input.strategy ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("creator", accounts.creator),
      getAccountMeta("strategy", accounts.strategy),
    ],
    data: getCreatorUpdateInstructionDataEncoder().encode(
      args as CreatorUpdateInstructionDataArgs,
    ),
    programAddress,
  } as CreatorUpdateInstruction<
    TProgramAddress,
    TAccountCreator,
    TAccountStrategy
  >);
}

export type ParsedCreatorUpdateInstruction<
  TProgram extends string = typeof AGARI_STRATEGY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    creator: TAccountMetas[0];
    strategy: TAccountMetas[1];
  };
  data: CreatorUpdateInstructionData;
};

export function parseCreatorUpdateInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCreatorUpdateInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { creator: getNextAccount(), strategy: getNextAccount() },
    data: getCreatorUpdateInstructionDataDecoder().decode(instruction.data),
  };
}
