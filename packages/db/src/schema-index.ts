/**
 * The chain indexer's projections and cursors (plan §4 indexer; venue-ops.md §9). Owned by S3 lane 3d, which
 * replaces this placeholder. Projections are rebuildable from chain (truncate and replay from the deploy slot).
 */
export const INDEX_SCHEMA_SQL = ``;
