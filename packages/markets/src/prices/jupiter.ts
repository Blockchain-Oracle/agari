/**
 * Jupiter Price v3 for verified xStock mints (session-lanes.md §2.4–2.5): the chart spot and the attested fallback's
 * median of three samples, scaled ×10⁸ exactly. Lane 6b owns this file; `ops/prints/index.ts` re-exports all of it.
 */
export {};
